# Decisions and gates

Status: authoritative human decisions and gate state
Read when: a requirement seems ambiguous or a model/architecture change is proposed.

## Approved decisions

Every row below is `APPROVED` by the user and recorded in this planning session on 2026-08-08 unless the row notes a later date or an explicit superseding row is added. Primary normative detail lives in the domain file named by `16_CONTEXT_MAINTENANCE.md`; this ledger owns decision/status, while MANIFEST mirrors the IDs for loading.

| ID | Decision | Why |
|---|---|---|
| `DEC_SCOPE_001` | MVP uses AI still images, not AI B-roll video | Much simpler, faster, cheaper; user explicitly chose image-only now |
| `DEC_OUTPUT_001` | Only full avatar, full image, and avatar-left/image-right split | Matches target style without effects |
| `DEC_OUTPUT_002` | Hard cuts; no motion graphics/text/decorative transitions | Absolute repeated user requirement |
| `DEC_OUTPUT_003` | Every AI image has a centered, jitter-free zoom with immediate linear movement from the first frame interval. Worker 0.1.31 keeps the v3 cubic subpixel transform, increases fullscreen zoom amounts by 25% and then another 12.5% after the user preview, to 1.00→1.03515625/1.0421875/1.04921875, and retains split-right 1.00→1.025. Convert to 30 fps before the transform. | User update on 2026-09-25 supersedes the 2026-08-10 eased progression: fullscreen should move a little faster, split amount should stay, and neither layout should appear still before zooming. Existing encoded videos remain immutable; execution bundle identity records renderer code. |
| `DEC_LLM_001` | Runware DeepSeek V4 Flash 0731 | User finalized provider/model after price research |
| `DEC_LLM_002` | DeepSeek writes production image prompts only | AI timeline-layout intelligence adds calls without valued quality; a separate vision model is allowed only for explicit analysis of a new draft style version |
| `DEC_STYLE_001` | Image Styles use immutable published versions; user-created styles are workspace-private and only explicit built-ins are system-visible | Reuse, privacy, and version pinning prevent cross-account exposure or silent project drift. |
| `DEC_STYLE_002` | Built-in `documentary_stock_v1` is the default | Preserves the approved realistic Ranga/stock-footage look without setup |
| `DEC_STYLE_003` | Runware Gemini 3.5 Flash performs one-time reference analysis | Seven-set Runware qualification closed `GATE_STYLE_001` on 2026-08-11 with strict exact-shape output, canonical local semantic validation, content separation, bounded retry, latency, privacy, and cost evidence |
| `DEC_STYLE_004` | Every project revision pins one published style version/hash | Reproducibility; later edits/archive cannot change prior output |
| `DEC_STYLE_005` | Optional extra image keywords require an explicit toggle, off by default; the toggle is the only persistent applied-state indicator | Gives user control without silently changing every prompt or adding an LLM call; the user removed redundant applied/not-applied confirmation UI on 2026-08-09 |
| `DEC_STYLE_006` | Analyzer extracts shared visual traits, not reference content/identity/logo/text | Prevents accidental copying and keeps styles reusable across topics |
| `DEC_STYLE_007` | Manual edits use preserve-and-detach provenance: accepted analyzer bytes/evidence remain immutable historical source truth, while every pre-publication edit creates a new immutable derived profile artifact in the same open version; the derived profile marks analyzer evidence inapplicable and publication pins only its exact current bytes | User explicitly selected this policy on 2026-08-11; it prevents analyzer confidence/evidence from being misrepresented as describing user-edited creative bytes while retaining full audit history |
| `DEC_IMAGE_001` | **SUPERSEDED for fresh ordinary generation by `DEC_API_GENERATION_001` on 2026-09-24.** The former Mage profile remains the immutable identity of historical RunPod attempts. | Preserve old receipts and provenance without using GPU availability as the new-project gate. |
| `DEC_IMAGE_002` | **HISTORICAL Mage decision; superseded for fresh ordinary generation by `DEC_API_GENERATION_001`.** Preserve the accepted Mage terms-risk record only for historical RunPod attempts. | The 2026-08-11 terms decision remains provenance for consumed Mage attempts; it does not select the fresh image provider. |
| `DEC_IMAGE_003` | Preserve the no-visible-text, logo, watermark, anatomy, and object-defect review rule for generated images. The Mage-specific profile/retry portion is historical after `DEC_API_GENERATION_001`. | Negative prompting does not guarantee clean media; human review remains mandatory. |
| `DEC_AVATAR_004` | The same accepted avatar clip serves both layouts. Fresh Fal square output uses the versioned `fal-flashhead-512x512p25-wide-v2` source-aligned CPU composite before full or split framing; uncertain alignment fails closed. Historical provider profiles retain their pinned geometry. | Restores the wide Avatar Hub framing without a second paid generation or alteration of the accepted clip. The exact Helen short-clip source and Fal output passed local alignment review on 2026-09-25; hosted render acceptance remains to be verified. |
| `DEC_AVATAR_005` | The workspace-private Avatar Hub is the only ordinary source of project avatars; projects select and pin an exact ready version, with no inline avatar upload | Create-once reuse, privacy, and immutable version pinning prevent cross-account exposure and project drift. |
| `DEC_AVATAR_010` | **SUPERSEDED for fresh ordinary generation by `DEC_API_GENERATION_001` on 2026-09-24.** The exact SoulX Pro profile remains historical RunPod provenance. | Existing attempts keep their pinned identity and are never replayed through another provider. |
| `DEC_API_GENERATION_001` | Fresh ordinary image jobs use Kie Market `z-image`; avatar jobs use Fal `fal-ai/flashhead/audio-to-video` with the revision-pinned Avatar Hub source and the selected voiceover span. Each project immutably pins `KIE_FAL` or historical `RUNPOD`. Postgres claims each API job before submission, persists provider task identity, verifies private media output, and never automatically resubmits an uncertain paid call. Existing RunPod attempts remain on their original reconciliation path. Kie image regeneration uses its own durable job and receipt, changes the Review gallery still only, and never replays an uncertain paid submission. | The user explicitly replaced GPU-dependent generation with ready-made APIs on 2026-09-24. The audio route preserves uploaded voiceover timing. Pilot6 proved short hosted generation, render, Chrome playback, approval, download, and regeneration. The 2026-09-25 source-aligned Fal crop, timing, and planned-cost improvements passed local proof; their new release and a full 18-minute hosted video remain open acceptance gates. |
| `DEC_TIMING_001` | **SUPERSEDED by `DEC_PERSONAL_WORKER_001` on 2026-08-17.** The former target was pinned `whisper.cpp base.en` in scale-to-zero Cloud Run Jobs. | Preserved as decision history; it is no longer production authority. |
| `DEC_CONTRACT_001` | TypeScript is the sole RFC 8785/JCS authority; Python workers validate schemas and exact byte hashes but treat canonical JSON hashes as opaque | Avoids cross-language number-serialization drift while keeping one deterministic canonical hash authority; recorded for Phase 0C on 2026-08-09 |
| `DEC_SCHEDULER_001` | Seeded deterministic bounded variation | Fast, cheap, reproducible; no AI layout calls |
| `DEC_SCHEDULER_002` | Target ~22% avatar, half full/half split, 2–6 sec appearances | Derived from both audited references |
| `DEC_RENDER_001` | **SUPERSEDED by `DEC_PERSONAL_WORKER_001` on 2026-08-17.** The former target was direct FFmpeg in scale-to-zero Cloud Run Jobs. | Preserved as decision history; it is no longer production authority. |
| `DEC_RENDER_002` | No Remotion/HyperFrames in MVP | No relevant quality benefit; extra runtime/cost |
| `DEC_HOSTING_001` | **SUPERSEDED by `DEC_PERSONAL_WORKER_001` on 2026-08-17.** The former target combined Cloudflare/Neon/private R2 with Cloud Run CPU jobs. | Preserved as decision history; Cloudflare/Neon/private R2 remain, but Cloud Run is no longer an active production dependency. |
| `DEC_BUILD_001` | Run heavyweight release builds and storage/RAM-intensive verification on standard GitHub-hosted runners, not on the user's development Mac. The repository may remain public to use public-repository standard-runner allowances. Build-only qualification must not publish images, model bytes, outputs, caches, or retained artifacts; larger paid runners require separate authority. The hosted control plane must not depend on the operator's development Mac. | The 2026-08-17 personal-worker decision supersedes only the former blanket production-device-independence clause: each account's CPU stages now intentionally depend on that account's paired device. |
| `DEC_PERSONAL_WORKER_001` | Production ASR and final render/probe run on an account-owned Windows x64 or macOS universal personal media worker. The hosted Cloudflare/Neon/private-R2 control plane admits and durably leases work only to a device paired to the same account/workspace. Installation offers the detected OS first and both OS links always, requires one browser confirmation and no copied code or configuration, stores a device credential in Windows Credential Manager or macOS Keychain, starts at login, and communicates through outbound HTTPS only. The worker receives only heartbeat/claim/renew/cancel/fresh artifact-port/result capabilities for its exact tenant and lease. Successful final videos remain in R2 until an explicit user Delete; cancellation/failure scratch is removed. Release distribution matches ImageForge by default: Windows is an explicitly unsigned beta and macOS is an explicitly ad-hoc sealed, non-notarized universal app in a verified DMG. Every immutable manifest records the exact trust mode, URL, size, SHA-256, protocol, and execution-bundle identity; clean-download install/launch/update/remove proof is mandatory. Authenticode and Developer ID/notarization remain optional future upgrades, not checkpoint blockers. | On 2026-08-17 the user explicitly selected free local compute and requested ordinary installer UX, then explicitly chose the already-accepted ImageForge beta distribution model after confirming ImageForge downloads and installs without trouble on the target Mac. This removes Cloud Run CPU charges and paid signing requirements without claiming warning-free behavior on every OS/device. User electricity/device time, R2, Runware, and RunPod remain separate. |
| `DEC_AUTH_001` | MVP has no public signup and uses Google sign-in only because the user selected no email provider on 2026-08-17. Each invite is a unique single-use code bound to one intended email; Google must return that same verified email. One atomic transaction binds identity/admission and consumes the code; raw codes are never stored or exposed. Replays, races, expiry, revocation, and identity collisions fail closed. Later Google login never requests the invite code again. Email/password and email delivery remain disabled unless a later explicit decision selects a provider. | Preserves the approved invite-only verified-email boundary without adding a paid or configured email-delivery service the user declined. |
| `DEC_TENANCY_002` | Each authenticated account owns one default workspace. Projects, revisions, user-created Avatar Profiles/Image Styles, assets, queue rows, attempts, costs, and results are private to that account/workspace and enforced by database-side composite ownership. Only explicit immutable built-in presets are system-visible. No global user-created catalog, cross-account result space, client-supplied owner authority, or cross-account queue control exists. | This is the smallest safe tenant model for 5–10 invited accounts: private by construction without adding organization, role, or shared-workspace complexity. |
| `DEC_DB_001` | The durable foundation uses committed additive PostgreSQL SQL migrations plus query-library-neutral repository contracts. PGlite is pinned only for network-free migration/constraint/adapter contract tests; it is not production persistence. V2-01 owns the additive account/default-workspace cutover and Neon remains production truth. | This boundary tests real PostgreSQL behavior without Docker, secrets, provider/network dependence, or an early ORM lock while preserving a production-hosted database target. Official compatibility sources are recorded in `17_SOURCE_INDEX.md`. |
| `DEC_QUEUE_001` | Postgres remains authoritative for admission, fairness, state, and recovery; fresh generation uses Kie and Fal, while existing RunPod attempts keep their original transport | Durable multi-user recovery and audit |
| `DEC_QUEUE_003` | Postgres performs durable fair admission: at most one active provider workload per account and at most two workloads globally from different accounts. For ordinary video work this enforces at most one active video/account and two active videos globally. FIFO and deterministic round-robin/last-served fairness apply across eligible account heads. Explicit `preset_preview` work uses the same slots/account lock, is lower priority than every eligible video, and never bypasses video fairness. Users see/reorder/cancel only their own waiting work. Waiting work creates no provider task; historical RunPod endpoint queues were transport backpressure only, never admission, fairness, or reorder truth. No singleton global queue or cross-account control path is active. | Supports 5–10 private users with two concurrent videos while safely admitting explicit preset tests without starvation, over-admission, provider work before admission, or cross-account queue control. A preview may intentionally wait while any video head remains eligible. |
| `DEC_RUNPOD_003` | **SUPERSEDED for fresh ordinary generation by `DEC_API_GENERATION_001` on 2026-09-24.** Existing RunPod attempts and retained-resource records remain historical and require their original reconciliation/cleanup rules. | GPU availability no longer gates the new API path. |
| `DEC_RUNTIME_001` | Production is hosted-control-plane and personal-worker based. Ordinary users and operators never create, select, start, stop, or delete Pods/GPUs for a video. Postgres fairly admits fresh Kie/Fal API jobs and durably tracks provider identities and outputs; ASR and final render/probe run on the tenant-owned paired worker. Existing RunPod attempts retain their pinned transport and cleanup. Every account/workspace owns durable isolated project, revision, media, preset, queue, stage, retry, cost, result, and paired-device state. Browser state is only a projection of durable server truth; no singleton global user context is active. | The user selected ready-made Kie/Fal APIs for fresh generation on 2026-09-24 to remove GPU-availability gating, while retaining the private hosted control plane and account-owned ASR/render worker. |
| `DEC_DISPATCH_001` | Every fresh Kie/Fal submission is claimed and durably identified before output acceptance; uncertain paid calls are never automatically resubmitted. Existing Serverless RunPod calls retain their persisted predispatch authority/outbox, exact provider-job binding, status reconciliation, at-most-one accepted output, and duplicate-cost visibility. No provider promises exactly-once execution or billing. | The historical RunPod `/run` contract does not guarantee client-controlled exactly-once execution; the API path has its own persisted task identity and no-replay rule. |
| `DEC_CONTEXT_001` | The working tree contains only the active V2 task briefs (`VF-10-00`, `VF-10-00A`, and `VF-10-01` through `VF-10-13`). Git history is the archive for retired checkpoints and planning. Retain in-tree evidence only when an active V2 foundation, decision, gate, artifact identity, or acceptance contract still depends on it. | The user explicitly replaced the former checkpoint system on 2026-08-15 and directed that obsolete active planning and unneeded historical evidence be removed instead of carried forward as a parallel authority surface. |
| `DEC_COST_001` | Hosted projects have no user-configured maximum-spend input and persist `NULL` as their project cost limit. Exact per-project/lane/provider cost accounting, reservation conservation, cancellation, timeouts, concurrency bounds, and no-redispatch controls remain mandatory. Cost targets are product/economics measurements, not project execution ceilings. Every paid release or deployment checkpoint still requires its own separate finite source-bound action cap under the authority protocol; the two retained model-volume charges remain separately reported fixed infrastructure costs. | On 2026-09-07 the user explicitly removed maximum-spend caps from every project so project creation cannot be blocked by a user budget field. This supersedes the former project-level `$2.00` hard ceiling without authorizing unbounded release operations or weakening cost observability and operational safety. |
| `DEC_DELIVERY_001` | Current API release work uses bounded visible checkpoints, focused tests for changed surfaces, one narrow observed-failure repair cycle, and real Chrome for visible changes. Paid proof centers on persisted Kie/Fal identities, private receipts, settled cost, render acceptance, and provider-workload release. Historical RunPod work retains its volume-integrity and zero-worker checks. Broad synthetic/provider-fault/browser matrices remain deferred unless a defect requires them. | The user prioritizes fast MVP progress, minimal relevant checks, and real-sample feedback. Tenant isolation, finite paid authority, artifact lineage, no-replay handling, and historical GPU cleanup remain mandatory. |
| `DEC_DELIVERY_002` | The former parallel RunPod certification process is historical. Fresh Kie/Fal API generation proceeds through source-bound deployment, persisted provider identities, accepted private receipts, personal-worker rendering, Chrome playback/download, regeneration, and settled cost. Reuse live evidence only when its immutable source/configuration and provider task identities match. Existing RunPod attempts keep their original authority and cleanup. | The 2026-09-24 provider decision removed RunPod GPU availability from fresh generation; historical qualifications remain evidence only for their exact old runtime. |
| `DEC_UX_001` | Fixture-first UI visible in real Chrome throughout development | User can identify breakage and guide UX live |
| `DEC_UX_002` | Use a compact minimal production-console UI whose real Chrome 100% appearance matches the user's preferred 80% reference through component geometry, never CSS `zoom` or a transformed shell. The root is 15 px, normal actionable controls keep a 44 px floor, the content canvas is 1184 px, and the active-project command bar remains full-width and internally inset. The fixed-base floating dock uses 76×62 px desktop items, 38×35 px icon tiles, 24 px glyphs, and scale-only fine-pointer proximity magnification above 820 px. The target peaks at 1.75×, tapers through neighbors across 240 px, and is exactly 1× outside that radius. Icon bottom edges, item layout boxes, and active-route backing geometry never move; reduced-motion, coarse-pointer, and compact/mobile modes remain neutral. Ordinary copy is 14–16 px; 12–13 px is limited to short secondary status/provenance labels. | User rejected the first shell as too small/zoomed-out, then rejected 20 px/60 px as oversized, corrected the narrow top bar, and refined the dock against macOS screenshots on 2026-08-09. On 2026-08-10 the user explicitly superseded the accepted 18 px/52 px density after comparing the same Create screen at Chrome 100% and 80%, asking for the 80% appearance at real 100%. This later request also proportionally compacts the dock while preserving the already approved scale-only, bottom-anchored behavior. |
| `DEC_UX_003` | Avatar and Image Style surfaces are visual-first and share a two-column/equal-media Hub layout; healthy cards keep only image/name/on-demand details, Create Project uses app-native integrated visual dropdowns, and authorized galleries plus dense technical detail remain progressively disclosed | User required seeing every avatar/style image without spreading presets across the form, removed repeated ready/passed/version copy, and rejected detached/browser-native dropdown surfaces on 2026-08-09; custom uploaded references remain distinct from built-in owned/generated examples |
| `DEC_UX_004` | Major sibling sections use one consistent 20 px desktop / 16 px compact/mobile vertical rhythm. Expanded generic disclosures keep at least 12 px between the trigger and first visible child and between sibling fact cards. Structural panels, cards, metrics, workflow rows, and summaries use dedicated, clearly visible translucent-lavender boundary tokens plus a dark depth shadow and restrained cobalt/violet halo; nested controls and incidental dividers keep a lighter treatment. Lists and grids always declare a nonzero gap, and no border, glow, or shadow may substitute for actual spacing. | On 2026-08-09 the user rejected touching Usage layouts and application-wide faint card/section boundaries, and explicitly requested clearer futuristic, clean, minimal division via a stronger border, drop shadow, or glow. On 2026-08-10 a Settings screenshot showed expanded disclosure content touching its trigger and sibling fact card, so the same rule now explicitly covers disclosure interiors. Evidence: `evidence/gates/GATE_UI_001/2026-08-09-surface-separation-refinement/` plus the 2026-08-10 real-Chrome refinement. |
| `DEC_UX_005` | Preserve the accepted UI, routes, visible product features, and visual grammar while replacing manual GPU/Pod selection/start/stop with one-click Generate, tenant-private queue/capacity/progress, cancellation, recovery, and result playback. | The user said the UI is good and wants infrastructure automation to feel like a top production product; users must not operate compute lifecycle or see another tenant's state. |

## Active avatar path

1. Fresh ordinary avatar generation uses Fal FlashHead audio-to-video with the revision-pinned Avatar Profile source and selected-span audio. Historical SoulX attempts retain their original qualification and reconciliation rules.
2. Deterministic checks establish only technical validity and `READY_FOR_USER_REVIEW`.
3. User reviews identity, lips/teeth, beard/hair/hat, eyes, motion, background, jitter, blur, and crop potential.
4. Poor output stops. No retry, repair, fallback, model substitution, tuning, or promotion without new user authority.

## Gate status

| ID | Evidence needed | Consequence |
|---|---|---|
| `GATE_TENANCY_001` | **CLOSED:** local contracts plus V2-06 two-account auth, forced-RLS runtime, and R2/device/lease/output isolation | Accepted foundation; later provider/integration gates still apply |
| `GATE_STORAGE_001` | **CLOSED:** private artifact contracts plus real-R2 isolation, hash read-back, durable delete, baseline restoration, and scratch cleanup | Accepted foundation; provider-lane gates still apply |
| `GATE_QUEUE_001` | **CLOSED:** durable fair admission, account/global slots, recovery/contention, Chrome, and V2-05 cutover are green | Accepted foundation; lane gates govern live capacity |
| `GATE_SERVERLESS_CONTRACT_001` | **CLOSED — HISTORICAL ADAPTER FOUNDATION:** v3 Serverless contracts remain for exact reconciliation of consumed RunPod attempts. They do not qualify or gate fresh Kie/Fal API generation. | No provider authority is inherited |
| `GATE_HOSTING_001` | **CLOSED:** final live evidence and a fresh independent audit cover auth/Neon/R2/Workflow, migration/restore, isolation, devices/leases, recovery/delete, rollback, spend, and immutable worker releases | Accepted V2-06 foundation; no V2-07 authority is inherited |
| `GATE_SERVERLESS_MAGE_001` | **HISTORICAL OPEN RUNPOD GATE:** Attempt64 completed 96 durable units but stopped at `RUNPOD_ZERO_NOT_CONFIRMED`; cleanup/reconciliation prove zero compute/disposables and both retained volumes unchanged, and repair `1283a23` passed independent audit. Its authority is consumed. This gate and the Mage image qualification remain only for historical RunPod work. | Does not gate fresh Kie generation; no old attempt/image/authority reuse or GPU fallback |
| `GATE_SERVERLESS_SOULX_001` | **HISTORICAL OPEN RUNPOD GATE:** SoulX runtime, sealed volume, Pod samples, timings/cost, cleanup, and crop approval remain provenance for historical SoulX work. No fresh SoulX endpoint qualification is planned under `DEC_API_GENERATION_001`. | Does not gate fresh Fal generation; historical attempts retain identity and cleanup |
| `GATE_SOULX_LICENSE_001` | **HISTORICAL OPEN GATE:** retain exact SoulX code/weight license evidence and unresolved-risk record for historical attempts. | Does not gate fresh Fal avatar generation; make no unambiguous commercial-permission claim for historical SoulX |
| `GATE_E2E_001` | **OPEN:** one owned short project through auth, tenant R2, fair admission, tenant-owned personal-worker ASR/render, Kie image and Fal audio-to-video jobs, durable receipts, provider-free resume/cancel, Chrome playback/download, settled cost, and provider-workload release | No integrated production claim |
| `GATE_RANGA_001` | **OPEN:** real 3–5-minute then 30-minute human review passes pinned cadence/compositions, evidence relevance, avatar quality/crops, no forbidden graphics, and playable final | No Ranga-close quality or full-length acceptance claim |
| `GATE_IMAGE_001` | **OPEN — ACTIVE QUALITY GATE:** review real Kie `z-image` outputs from representative projects for defects, crop, checksum, style lineage, and user acceptance; retain historical Mage review evidence with its original runtime identity | No production image-quality claim beyond verified samples |
| `GATE_IMAGE_002` | **HISTORICAL OPEN GATE:** preserve exact Mage code/weight terms evidence and unresolved-risk decision for historical Mage attempts only | Does not gate fresh Kie images; make no unambiguous commercial-permission claim for historical Mage |
| `GATE_STYLE_002` | **OPEN — ACTIVE V2-12 RELEASE BLOCKER:** every style version enabled for invited production needs one exact real accepted sample; untested versions remain draft/not-release-qualified | No production claim for an untested style version |
| `GATE_CONCURRENCY_001` | **OPEN — LOCAL ADMISSION CONTENTION GREEN:** 10-account deterministic rotation, two distinct winners/no third slot, same-account and preview locking, and two-account private Chrome pass; live Kie/Fal workload caps, failure isolation, bounded services, and provider-workload release remain | No live 5–10-user readiness claim |
| `GATE_ECONOMICS_001` | **OPEN:** representative projects plus accumulated API generation/render evidence must prove timing, settled Kie/Fal and worker cost, duplicate exposure, <=`$1` target/<=`$2` hard ceiling; a short run cannot close production-length quality or economics, and ten organic beta jobs later establish p50/p90 confidence | No initial production unit-economics claim |
| `GATE_SECURITY_001` | **OPEN:** independent auth/tenant/R2/callback/worker/supply-chain/threat review has no unresolved critical/high issue and runbooks/key rotation exist | No production release |

## Closed gates

| ID | Result | Evidence |
|---|---|---|
| `GATE_UI_001` | **PASS** — the user explicitly accepted the final medium/minimal fixture shell on 2026-08-09; the approved visual system is frozen unless later user feedback or a verified regression reopens it | `evidence/gates/GATE_UI_001/2026-08-09-stabilization-audit/` |
| `GATE_LLM_001` | **PASS** — live Runware model search resolved canonical AIR `deepseek:v4@flash` to `DeepSeek-V4-Flash-0731`; 40 strict-schema scenes across five styles passed exact IDs, roles, literal anchors, forbidden-output checks, and the cost target | `evidence/gates/GATE_LLM_001/2026-08-10-runware-deepseek-qualification/` |
| `GATE_STYLE_001` | **PASS** — canonical AIR `google:gemini@3.5-flash` passed seven owned/synthetic multi-reference sets for strict exact-shape output, canonical semantic validation, content separation, uncertainty/outliers, alias binding, crop guidance, bounded retry, latency, privacy disclosure, and cost | `evidence/gates/GATE_STYLE_001/2026-08-10-runware-gemini-style-qualification/` |

## Proposed values and approved targets awaiting evidence

- Fresh Kie/Fal generation uses no GPU endpoint. Historical RunPod lanes retain their exact GPU
  qualifications and never serve as automatic fallback for API jobs.
- The 30-minute all-variable per-video target is ≤$1 and the hard MVP ceiling is ≤$2 under
  `DEC_COST_001`; use representative Serverless cold/warm/concurrent measurements before production.
- RunPod endpoint cold-start, TTL, idle, and `RUNPOD_INIT_TIMEOUT` limits apply only to historical
  attempts; they do not describe fresh Kie/Fal API timing.
- Storage retention: intermediates 3–7 days, final 30 days.
- Mage's 1280×720 output rule is historical under `DEC_IMAGE_001`; fresh Kie output quality is gated by
  `GATE_IMAGE_001` and observed samples.
- Style analyzer first-attempt target: <$0.08; total with one approved retry <$0.15.
- SoulX compatibility evidence and previews apply only to historical SoulX attempts; fresh avatars use
  Fal with the revision-pinned Avatar Hub source and voiceover span.

## Deferred decisions

- AI B-roll video model/provider and percentage.
- Reusable performance-bank dubbing.
- Commercial customer accounts/billing.
- Music/SFX.
- Automatic vision QA model.
- Automatic Style LoRA training/reference-conditioned image generation.
- Public deployment domain/branding name.

## Change authority

Only an explicit user decision changes an approved item. Benchmark evidence may recommend a change, but the implementation pauses at the relevant gate and presents the tradeoff. Follow the authority/consistency protocol in `16_CONTEXT_MAINTENANCE.md`; never treat the MANIFEST mirror or an older summary as a separate authority.
