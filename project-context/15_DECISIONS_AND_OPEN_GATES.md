# Decisions and gates

Status: authoritative human decisions and gate state
Read when: a requirement seems ambiguous or a model/architecture change is proposed.

## Approved decisions

Every row below is `APPROVED` by the user and recorded in this planning session on 2026-08-08 unless the row notes a later date or an explicit superseding row is added. Primary normative detail lives in the domain file named by `16_CONTEXT_MAINTENANCE.md`; this ledger owns decision/status, while MANIFEST mirrors the IDs for loading.

| ID | Decision | Why |
|---|---|---|
| `DEC_SCOPE_001` | MVP uses AI still images, not AI B-roll video | Much simpler, faster, cheaper; user explicitly chose image-only now |
| `DEC_OUTPUT_001` | Only full avatar, full image, and avatar-left/image-right split | Matches target style without effects |
| `DEC_OUTPUT_002` | Hard cuts; no motion graphics/text/decorative transitions | Absolute repeated user requirement |
| `DEC_OUTPUT_003` | Every AI image has a subtle, centered, jitter-free eased zoom-in. The current `ffmpeg-render-v3` envelope is 1.00→1.025/1.03/1.035 for full images and 1.00→1.025 for split-right images, sampled with a continuous cubic subpixel transform. | Avoid static-feeling image clips without calling attention to the effect. After reviewing v2, the user requested exactly one percentage point more endpoint zoom and even smoother movement, then accepted the exact v3 replacement as “good enough” in installed Chrome on 2026-08-10; v1/v2 remain replay-only. Evidence: `evidence/acceptance/VF-0C-08/2026-08-10-continuous-zoom-v3/`. |
| `DEC_LLM_001` | Runware DeepSeek V4 Flash 0731 | User finalized provider/model after price research |
| `DEC_LLM_002` | DeepSeek writes production image prompts only | AI timeline-layout intelligence adds calls without valued quality; a separate vision model is allowed only for explicit analysis of a new draft style version |
| `DEC_STYLE_001` | **ACTIVE CREATIVE RULE; scope superseded by `DEC_TENANCY_002`:** Image Styles use immutable published versions | Reuse/version pinning remains approved. The former globally shared user catalog is historical; user-created styles are workspace-private and only explicit built-ins are system-visible. |
| `DEC_STYLE_002` | Built-in `documentary_stock_v1` is the default | Preserves the approved realistic Ranga/stock-footage look without setup |
| `DEC_STYLE_003` | Runware Gemini 3.5 Flash performs one-time reference analysis | Seven-set Runware qualification closed `GATE_STYLE_001` on 2026-08-11 with strict exact-shape output, canonical local semantic validation, content separation, bounded retry, latency, privacy, and cost evidence |
| `DEC_STYLE_004` | Every project revision pins one published style version/hash | Reproducibility; later edits/archive cannot change prior output |
| `DEC_STYLE_005` | Optional extra image keywords require an explicit toggle, off by default; the toggle is the only persistent applied-state indicator | Gives user control without silently changing every prompt or adding an LLM call; the user removed redundant applied/not-applied confirmation UI on 2026-08-09 |
| `DEC_STYLE_006` | Analyzer extracts shared visual traits, not reference content/identity/logo/text | Prevents accidental copying and keeps styles reusable across topics |
| `DEC_STYLE_007` | Manual edits use preserve-and-detach provenance: accepted analyzer bytes/evidence remain immutable historical source truth, while every pre-publication edit creates a new immutable derived profile artifact in the same open version; the derived profile marks analyzer evidence inapplicable and publication pins only its exact current bytes | User explicitly selected this policy on 2026-08-11; it prevents analyzer confidence/evidence from being misrepresented as describing user-edited creative bytes while retaining full audit history |
| `DEC_IMAGE_001` | Use the exact current ImageForge Mage profile: `Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6` through pinned stock headless ComfyUI, the public `mage_flow_turbo_int8_convrot.safetensors` checkpoint, 4 steps, guidance 1.0, and 1280×720 output | On 2026-08-13 the user explicitly replaced the earlier VideoForge BF16 candidate with the already-used ImageForge runtime profile. The prior BF16 RunPod attempts remain immutable historical evidence; the exact VideoForge volume manifest and Pod/runtime compatibility still require gate evidence. |
| `DEC_IMAGE_002` | Proceed with locked Mage-Flow-Turbo despite unresolved terms metadata; do not substitute another image model | The exact public Comfy-Org runtime revision is accessible. On 2026-08-11 the user explicitly accepted the remaining open-weight terms risk for their YouTube use and directed implementation not to treat licensing ambiguity as an execution blocker. Evidence must retain the ambiguity and must not claim commercially clear permission. |
| `DEC_IMAGE_003` | Keep the exact Mage-Flow-Turbo profile selected by `DEC_IMAGE_001`; use exact negative prompting, exclude scenes whose core meaning requires readable generated text/branding, reject visible text/logo/watermark and material anatomy/object defects, and permit at most one same-scene candidate retry after rejection | Historical BF16 1280×720 RTX 4090 outputs showed variable strict quality and remain evidence for the review policy, not qualification of the new INT8 profile. Negative prompting improved facade/crowd composition but did not guarantee clean plate text or objects. Non-Turbo is not assumed to fix these defects and is not selected. Real source media handles text-critical branded scenes; human review remains mandatory before promotion. |
| `DEC_AVATAR_001` | **SUPERSEDED by `DEC_AVATAR_007`:** AvatarForcing was the primary path | Preserved for historical replay only; `VF-9-21` spent `$0.4496891390` and produced no reviewable output across the durable boundaries. No new AvatarForcing dispatch is allowed. |
| `DEC_AVATAR_002` | **SUPERSEDED in active effect by `DEC_AVATAR_007`:** MuseTalk was the lip-only repair | Historical replay only; no repair model is active for the sample-first Echo path. |
| `DEC_AVATAR_003` | **SUPERSEDED in active effect by `DEC_AVATAR_007`:** SkyReels V3 was the whole-frame fallback | Historical replay only; no fallback model is active for the sample-first Echo path. |
| `DEC_AVATAR_004` | Same centered avatar clip serves both layouts | Saves inference; deterministic crop is sufficient |
| `DEC_AVATAR_005` | **ACTIVE CREATIVE RULE; scope superseded by `DEC_TENANCY_002`:** the Avatar Hub is the only ordinary source of project avatars; projects select and pin an exact ready version, with no inline avatar upload | Create-once reuse and immutable version pinning remain approved. The former globally shared user catalog is historical; user avatars are workspace-private and only explicit built-ins are system-visible. |
| `DEC_AVATAR_006` | **SUPERSEDED by `DEC_AVATAR_007`:** proceed with the locked AvatarForcing fallback ladder despite unresolved terms | Preserved as the exact historical authority for its completed attempts; it authorizes no new dispatch. |
| `DEC_AVATAR_007` | **HISTORICAL; superseded by `DEC_AVATAR_010`:** EchoMimicV3-Flash native output was once the sole proposed avatar path, with no repair/fallback | User selected the sample-first replacement on 2026-08-12. Its BF16 single-batch attempt history remains immutable; it grants no current dispatch. Source/license evidence: `evidence/gates/GATE_AVATAR_004/2026-08-12-echomimic-v3-flash-preflight/`. |
| `DEC_AVATAR_008` | **HISTORICAL; superseded by `DEC_AVATAR_010`:** EchoMimicV3-Flash Turbo FP8 was the owned prepared short-span runtime; it never authorized Long Video CFG | On 2026-08-13/14 the user selected the Turbo FP8 profile. The exact historical lineage/evidence remains immutable, but the model and deleted volume are non-dispatchable. |
| `DEC_AVATAR_009` | **HISTORICAL; superseded by `DEC_AVATAR_010`:** the accepted Echo renderer source profile was `echomimic-v3-flash-turbo-fp8-centered-1024x560p25-v1`: full crop `992:558:16:0` scaled to 1920x1080; split-avatar crop `496:558:280:0` scaled to 960x1080 | The user reviewed previews from the qualified native 6-second Echo output and accepted both crops as good enough on 2026-08-15. One native clip served both layouts; the deleted Echo volume/runtime is non-dispatchable and the profile remains historical evidence only. |
| `DEC_AVATAR_010` | **SUPERSEDES `DEC_AVATAR_007` through `DEC_AVATAR_009` for active dispatch:** remove EchoMimic from operational storage and qualify exact SoulX-FlashHead Pro as the only proposed avatar runtime; no repair or fallback | On 2026-08-15 the user rejected Echo economics, authorized removal of its exact retained volume/runtime, selected the latest official SoulX-FlashHead Pro with best official Pro settings, and authorized one owned 10.12-second sample under a `$4.00` finite cap. Echo evidence and crops remain immutable historical truth but cannot dispatch. SoulX remains blocked from production and renderer crop activation until its exact sample is reviewed. |
| `DEC_TIMING_001` | Pinned `whisper.cpp base.en` runs in scale-to-zero Cloud Run Jobs over private R2 in production; the same container/entrypoint runs on the Mac only for development parity | Reuses the proven free-ASR model while making the 5–10-user hosted app independent of the user's Mac; user accepted the recommended hosted CPU boundary on 2026-08-13. |
| `DEC_CONTRACT_001` | TypeScript is the sole RFC 8785/JCS authority; Python workers validate schemas and exact byte hashes but treat canonical JSON hashes as opaque | Avoids cross-language number-serialization drift while keeping one deterministic canonical hash authority; recorded for Phase 0C on 2026-08-09 |
| `DEC_SCHEDULER_001` | Seeded deterministic bounded variation | Fast, cheap, reproducible; no AI layout calls |
| `DEC_SCHEDULER_002` | Target ~22% avatar, half full/half split, 2–6 sec appearances | Derived from both audited references |
| `DEC_RENDER_001` | Direct FFmpeg runs in scale-to-zero Cloud Run Jobs over private R2 in production; the same container/entrypoint runs on the Mac only for development parity | Leanest correct renderer for simple grammar and reliable shared hosted operation. |
| `DEC_RENDER_002` | No Remotion/HyperFrames in MVP | No relevant quality benefit; extra runtime/cost |
| `DEC_HOSTING_001` | Scale-to-zero Cloudflare/Neon/private-R2 control plane plus Cloud Run Jobs for production whisper.cpp and FFmpeg | Avoid an always-on media server; current free allowances and exact Cloud Run sizing/pricing remain deployment-time and benchmark gates. |
| `DEC_BUILD_001` | Run heavyweight x86 container builds and storage/RAM-intensive verification on standard GitHub-hosted Linux runners, never on the user's Mac. The VideoForge repository may remain public to use the public-repository standard-runner allowance if private-repository Actions billing would otherwise block work. Build-only qualification must not publish images, weights, outputs, caches, or retained artifacts; larger paid runners require separate authority. Production must never serve from or depend on the user's Mac. | On 2026-08-14 the user explicitly prioritized a shareable hosted app, their Mac's 16 GB RAM and limited storage, and continuity over repository privacy. The repository was already public when this decision was recorded. |
| `DEC_AUTH_001` | MVP has no public signup. Each invite is a unique single-use code bound to one intended email. First registration uses email/password or Google plus that code. Email/password access requires verified email; Google must return the same verified email. One atomic transaction binds the identity/admission and consumes the code; raw codes are never stored or exposed. Replays, races, expiry, revocation, and Google/email collisions fail closed. Later login uses the bound method normally and never requests the invite code again. | The user explicitly selected this simple invited-team access model on 2026-08-13 and accepted the recommended single-use, email-bound, verified-email policy to ship safely and quickly. |
| `DEC_TENANCY_001` | **SUPERSEDED by `DEC_TENANCY_002`:** one global shared catalog and result space for all admitted users | Preserved as historical architecture truth only; it must not authorize cross-account reads or writes in V2. |
| `DEC_TENANCY_002` | Each authenticated account owns one default workspace. Projects, revisions, user-created Avatar Profiles/Image Styles, assets, queue rows, attempts, costs, and results are private to that account/workspace and enforced by database-side composite ownership. Only explicit immutable built-in presets are system-visible. | On 2026-08-15 the user requested a production-ready 5–10-user application. Private tenant data is required for safe multi-user production without adding unnecessary organization/role complexity. |
| `DEC_DB_001` | The durable foundation uses committed additive PostgreSQL SQL migrations plus query-library-neutral repository contracts. PGlite is pinned only for network-free migration/constraint/adapter contract tests; it is not production persistence. Neon connectivity and the production query implementation wait for `VF-1-02`/`VF-1-05`. | The user asked for a fast, trouble-free fresh-chat plan on 2026-08-10. This boundary tests real PostgreSQL behavior without Docker, secrets, provider/network dependence, or an early ORM lock while preserving Neon as production truth. Official compatibility sources are recorded in `17_SOURCE_INDEX.md`; exact task scope is `tasks/VF-1-01.md`. |
| `DEC_QUEUE_001` | Postgres is authoritative; RunPod is execution transport. `DEC_QUEUE_003` supersedes the old concurrency/fairness interpretation while preserving this durability boundary. | Durable multi-user recovery and audit |
| `DEC_QUEUE_002` | **SUPERSEDED by `DEC_QUEUE_003`:** one globally shared, strictly sequential queue with cross-user reorder rights | Preserved for replay of the manual-Pod architecture only. Its cross-user visibility, no-fairness rule, and one-global-video limit are inactive. |
| `DEC_QUEUE_003` | Postgres performs durable fair admission: at most one active provider workload per account and at most two workloads globally from different accounts. For ordinary video work this enforces at most one active video/account and two active videos globally. FIFO and deterministic round-robin/last-served fairness apply across eligible account heads. Explicit `preset_preview` work uses the same slots/account lock, is lower priority than every eligible video, and never bypasses video fairness. Users see/reorder/cancel only their own waiting work. Waiting work creates no provider task; RunPod endpoint queues are execution backpressure, not fairness. | Supports 5–10 private users with two concurrent videos while safely admitting explicit preset tests without video-account starvation, over-admission, or cross-account queue control. A preview may intentionally wait while any video head remains eligible. |
| `DEC_RUNPOD_001` | **SUPERSEDED by `DEC_RUNPOD_002`:** scale-to-zero Serverless endpoints first | Preserved only as the architecture used by historical qualification attempts; it is not the production lifecycle. |
| `DEC_RUNPOD_002` | **SUPERSEDED by `DEC_RUNPOD_003`:** two isolated model volumes served by disposable API-created Pods | The exact Mage/SoulX Pod qualifications and volume evidence remain valid historical evidence. Manual Pod create/delete is not the V2 production transport. |
| `DEC_RUNPOD_003` | Use two queue-based RunPod Serverless endpoints in `EU-RO-1`: one Mage endpoint with the existing isolated 50 GB Mage volume, and one SoulX endpoint with the existing isolated 50 GB SoulX volume. Each uses `/runpod-volume`, `workersMin=0`, `workersMax=2`, one GPU/worker, and RTX 4090 only until a lane-specific RTX 5090 qualification. Ordinary runtime treats its volume read-only, uses per-job scratch, and performs no model download/preparation or cross-mount. | Removes manual start/stop from users, preserves already prepared model storage, scales compute to demand, and supports the two-video admission ceiling. Serverless compatibility is a new gate; Pod evidence is not silently promoted. |
| `DEC_DISPATCH_001` | Every Serverless call uses persisted predispatch authority/outbox plus a post-assignment binding to the exact provider job ID. VideoForge guarantees at most one accepted output, not provider exactly-once execution or billing; ambiguous responses are reconciled and possible duplicate compute/cost remains visible. Poll status because async results expire after 30 minutes; webhooks are not sole truth. | RunPod `/run` has no documented client exactly-once guarantee. Durable two-phase authority prevents stale/foreign result acceptance without making a false billing promise. |
| `DEC_COST_001` | For a representative 30-minute output, total variable per-video cost targets ≤$1.00 and has a hard MVP ceiling of ≤$2.00. The two retained model-volume charges are accepted fixed infrastructure costs, reported separately, and excluded from this per-video target/ceiling. | The user explicitly set this envelope on 2026-08-13. Shared-session boot/deletion cost still requires deterministic per-project attribution and measured acceptance evidence. |
| `DEC_UX_001` | Fixture-first UI visible in real Chrome throughout development | User can identify breakage and guide UX live |
| `DEC_UX_002` | Use a compact minimal production-console UI whose real Chrome 100% appearance matches the user's preferred 80% reference through component geometry, never CSS `zoom` or a transformed shell. The root is 15 px, normal actionable controls keep a 44 px floor, the content canvas is 1184 px, and the active-project command bar remains full-width and internally inset. The fixed-base floating dock uses 76×62 px desktop items, 38×35 px icon tiles, 24 px glyphs, and scale-only fine-pointer proximity magnification above 820 px. The target peaks at 1.75×, tapers through neighbors across 240 px, and is exactly 1× outside that radius. Icon bottom edges, item layout boxes, and active-route backing geometry never move; reduced-motion, coarse-pointer, and compact/mobile modes remain neutral. Ordinary copy is 14–16 px; 12–13 px is limited to short secondary status/provenance labels. | User rejected the first shell as too small/zoomed-out, then rejected 20 px/60 px as oversized, corrected the narrow top bar, and refined the dock against macOS screenshots on 2026-08-09. On 2026-08-10 the user explicitly superseded the accepted 18 px/52 px density after comparing the same Create screen at Chrome 100% and 80%, asking for the 80% appearance at real 100%. This later request also proportionally compacts the dock while preserving the already approved scale-only, bottom-anchored behavior. |
| `DEC_UX_003` | Avatar and Image Style surfaces are visual-first and share a two-column/equal-media Hub layout; healthy cards keep only image/name/on-demand details, Create Project uses app-native integrated visual dropdowns, and authorized galleries plus dense technical detail remain progressively disclosed | User required seeing every avatar/style image without spreading presets across the form, removed repeated ready/passed/version copy, and rejected detached/browser-native dropdown surfaces on 2026-08-09; custom uploaded references remain distinct from built-in owned/generated examples |
| `DEC_UX_004` | Major sibling sections use one consistent 20 px desktop / 16 px compact/mobile vertical rhythm. Expanded generic disclosures keep at least 12 px between the trigger and first visible child and between sibling fact cards. Structural panels, cards, metrics, workflow rows, and summaries use dedicated, clearly visible translucent-lavender boundary tokens plus a dark depth shadow and restrained cobalt/violet halo; nested controls and incidental dividers keep a lighter treatment. Lists and grids always declare a nonzero gap, and no border, glow, or shadow may substitute for actual spacing. | On 2026-08-09 the user rejected touching Usage layouts and application-wide faint card/section boundaries, and explicitly requested clearer futuristic, clean, minimal division via a stronger border, drop shadow, or glow. On 2026-08-10 a Settings screenshot showed expanded disclosure content touching its trigger and sibling fact card, so the same rule now explicitly covers disclosure interiors. Evidence: `evidence/gates/GATE_UI_001/2026-08-09-surface-separation-refinement/` plus the 2026-08-10 real-Chrome refinement. |
| `DEC_UX_005` | Preserve the accepted UI, routes, visible product features, and visual grammar while replacing manual GPU/Pod selection/start/stop with one-click Generate, tenant-private queue/capacity/progress, cancellation, recovery, and result playback. | The user said the UI is good and wants infrastructure automation to feel like a top production product; users must not operate compute lifecycle or see another tenant's state. |

## Active avatar path

1. SoulX-FlashHead Pro replacement qualification from the revision-pinned canonical Avatar Profile runtime source and selected span audio; Echo is historical and non-dispatchable.
2. Deterministic checks establish only technical validity and `READY_FOR_USER_REVIEW`.
3. User reviews identity, lips/teeth, beard/hair/hat, eyes, motion, background, jitter, blur, and crop potential.
4. Poor output stops. No retry, repair, fallback, model substitution, tuning, or promotion without new user authority.

## Open gates

| ID | Evidence needed | Consequence |
|---|---|---|
| `GATE_TENANCY_001` | **OPEN:** additive account/default-workspace migration, composite DB ownership, private repositories/APIs/events, built-in/system split, and two-account negative matrix | No hosted multi-user or provider dispatch |
| `GATE_STORAGE_001` | **OPEN:** tenant-private R2 keys, least-privilege signed URLs, durable object verification, lifecycle/redaction, and isolated job scratch | No production media upload/download or worker artifact path |
| `GATE_QUEUE_001` | **OPEN:** serializable one-active-provider-workload/account and two-workload/global admission, ordinary video caps, video-over-preview priority, fair cross-account rotation, private reorder/cancel, crash/race recovery, and 1/2/5/10-user simulation | No production dispatch or concurrency claim |
| `GATE_SERVERLESS_CONTRACT_001` | **OPEN:** v3 Serverless schemas/fixtures, old-Pod firewall, predispatch/outbox/post-assignment authority, ambiguity/status/webhook/result-expiry recovery, at-most-one accepted output, timeout and no-purge proof | No endpoint publication or `/run` request |
| `GATE_HOSTING_001` | **OPEN:** isolated staging Better Auth/Neon/private R2/Cloudflare Workflow/Cloud Run Jobs, migration/restore, security, cost, and installed-Chrome acceptance | No production deployment |
| `GATE_SERVERLESS_MAGE_001` | **OPEN — POD FOUNDATION PRESERVED:** CP-06 proved exact Mage artifact, sealed 50 GB volume, offline Pod boot, outputs, and cleanup; exact Serverless image/template/endpoint, concurrent read-only volume use, RTX 4090 cold/warm batches, receipts/cost, and zero-worker drain remain | Mage Serverless production dispatch disabled |
| `GATE_SERVERLESS_SOULX_001` | **OPEN — POD FOUNDATION PRESERVED:** SoulX exact runtime, sealed 50 GB volume, owned 10-second-class Pod samples, timings/cost, and cleanup passed technically; user quality decision plus exact Serverless handler/endpoint, init-timeout, concurrent read-only volume use, RTX 4090 cold/warm batches, receipts/cost, and zero-worker drain remain | SoulX Serverless production dispatch and crop/profile activation disabled |
| `GATE_SOULX_LICENSE_001` | **OPEN:** exact code/weight license and hosted/commercial-use evidence plus explicit unresolved-risk record | No claim of unambiguous SoulX commercial permission; production activation blocked until decision is explicit |
| `GATE_E2E_001` | **OPEN:** one owned short project through auth, tenant R2, fair admission, hosted CPU jobs, both Serverless endpoints, durable receipts, Cloud Run render, Chrome playback, restart recovery, settled cost, zero endpoint jobs, and zero total workers (`Active + Flex`) | No integrated production claim |
| `GATE_RANGA_001` | **OPEN:** real 3–5-minute then 20–30-minute human review passes pinned cadence/compositions, evidence relevance, avatar quality/crops, no forbidden graphics, and playable final | No Ranga-close quality or full-length acceptance claim |
| `GATE_IMAGE_001` | **OPEN — ACTIVE V2-12 RELEASE BLOCKER:** complete the representative Mage quality suite, including the required prompt/image counts, rejection/retry policy, defect-free accepted outputs, and exact style/runtime lineage | No production image-quality claim or release |
| `GATE_STYLE_002` | **OPEN — ACTIVE V2-12 RELEASE BLOCKER:** pass same-content adherence across all five production Image Styles with accepted technical and human evidence | No production multi-style adherence claim or release |
| `GATE_CONCURRENCY_001` | **OPEN:** 1/2/5/10-account load proves privacy, provider-workload and ordinary-video limits, video-account fairness/no starvation, preview priority, endpoint max-two workers, failure isolation, bounded services, and zero-worker drain | No 5–10-user readiness claim |
| `GATE_ECONOMICS_001` | **OPEN:** at least 10 representative cold/warm/concurrent accepted runs prove timing, settled variable cost, possible duplicate exposure, <=`$1` target/<=`$2` hard ceiling, and fixed-volume disclosure | No production unit-economics claim |
| `GATE_SECURITY_001` | **OPEN:** independent auth/tenant/R2/callback/worker/supply-chain/threat review has no unresolved critical/high issue and runbooks/key rotation exist | No production release |

### Historical gate records

The following IDs remain immutable evidence references but do not replace an active V2 gate:

| ID | Historical state |
|---|---|
| `GATE_AVATAR_001` / `GATE_AVATAR_002` | Echo review/demotion research; Echo is now non-dispatchable |
| `GATE_AVATAR_003` | AvatarForcing license conflict; historical/nonblocking because AvatarForcing is inactive |
| `GATE_AVATAR_005` | SoulX Pod-era technical sample ready for user review; feeds `GATE_SERVERLESS_SOULX_001` |
| `GATE_IMAGE_002` | Mage terms ambiguity and the user's historical risk decision remain explicit; this gate blocks only a claim of unambiguous permission, not the separately active V2-12 quality gates |
| `GATE_FALLBACK_001` | Failed SkyReels history; no fallback is active |
| `GATE_GPU_001` / `GATE_RUNPOD_001` | Manual-Pod lifecycle evidence only; Serverless gates supersede active effect |
| `GATE_COST_001` | Historical cost target evidence; `GATE_ECONOMICS_001` owns V2 release economics |

## Closed gates

| ID | Result | Evidence |
|---|---|---|
| `GATE_UI_001` | **PASS** — the user explicitly accepted the final medium/minimal fixture shell on 2026-08-09; the approved visual system is frozen unless later user feedback or a verified regression reopens it | `evidence/gates/GATE_UI_001/2026-08-09-stabilization-audit/` |
| `GATE_LLM_001` | **PASS** — live Runware model search resolved canonical AIR `deepseek:v4@flash` to `DeepSeek-V4-Flash-0731`; 40 strict-schema scenes across five styles passed exact IDs, roles, literal anchors, forbidden-output checks, and the cost target | `evidence/gates/GATE_LLM_001/2026-08-10-runware-deepseek-qualification/` |
| `GATE_STYLE_001` | **PASS** — canonical AIR `google:gemini@3.5-flash` passed seven owned/synthetic multi-reference sets for strict exact-shape output, canonical semantic validation, content separation, uncertainty/outliers, alias binding, crop guidance, bounded retry, latency, privacy disclosure, and cost | `evidence/gates/GATE_STYLE_001/2026-08-10-runware-gemini-style-qualification/` |
| `GATE_AVATAR_004` | **PASS** — exact pinned Echo Flash/Wan/audio lineage was prepared into the owned FP8 artifact, sealed on the isolated 50 GB Echo volume, loaded offline with no runtime download or quantization, passed real warm-up, and produced native 2/4/6-second clips | `evidence/acceptance/VF-9-24R/cp07-paid-execution/final-qualification.json` |

## Proposed values and approved targets awaiting evidence

- Initial Serverless endpoints use RTX 4090 only. RTX 5090 requires separate exact Mage and SoulX
  compatibility/quality/speed/cost evidence before either lane adds it; no automatic fallback is active.
- The 30-minute all-variable per-video target is ≤$1 and the hard MVP ceiling is ≤$2 under
  `DEC_COST_001`; replace Pod-era extrapolations with representative Serverless cold/warm/concurrent
  measurements before production.
- Endpoint cold start must remain below RunPod's documented seven-minute unhealthy threshold. Exact
  request TTL, execution timeout, idle timeout, scaling policy, and `RUNPOD_INIT_TIMEOUT` remain
  measured checkpoint outputs, not inherited defaults.
- Storage retention: intermediates 3–7 days, final 30 days.
- Mage output resolution is fixed at 1280×720 by `DEC_IMAGE_001`; visible 1080p delivery quality remains an open acceptance measurement.
- Style analyzer first-attempt target: <$0.08; total with one approved retry <$0.15.
- Per-profile SoulX compatibility preview remains optional until production evidence changes the
  policy. A ready untested/stale profile shows a warning; it never triggers an automatic provider job.

## Deferred decisions

- AI B-roll video model/provider and percentage.
- Reusable performance-bank dubbing.
- Commercial customer accounts/billing.
- Music/SFX.
- Automatic vision QA model.
- Automatic Style LoRA training/reference-conditioned image generation.
- Public deployment domain/branding name.

## Superseded research

- The historical complex architecture screenshot is not the implementation blueprint.
- The unattributed Hallo3/Hallo2 ranking screenshot is not the current avatar ladder.
- LongCat is not the default because its diffusion runtime/cost conflicts with the budget; the user
  explicitly reaffirmed this exclusion on 2026-08-11.
- DeepSeek via other providers, Gemini, GPT Luna, and OpenRouter were researched but not selected after the user fixed Runware.
- Gemini is now used through Runware only for one-time Image Style analysis; this does not supersede DeepSeek as the fixed production prompt writer.
- AI video model comparisons are retained only as historical research; AI B-roll video is deferred.

## Change authority

Only an explicit user decision changes an approved item. Benchmark evidence may recommend a change, but the implementation pauses at the relevant gate and presents the tradeoff. Follow the authority/consistency protocol in `16_CONTEXT_MAINTENANCE.md`; never treat the MANIFEST mirror or an older summary as a separate authority.
