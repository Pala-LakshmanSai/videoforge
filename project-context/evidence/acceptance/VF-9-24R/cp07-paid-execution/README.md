# CP-07 technical qualification

Exact `EchoMimicV3-Flash Turbo FP8` preparation, offline ordinary boot, real warm-up, and owned
2/4/6-second short-span samples passed on fresh RTX 5090 Pods. Each output is H.264/AAC,
1024x560 at 25 fps, with exact requested A/V duration and zero measured A/V delta. The final live
audit proves zero Pods, templates, endpoints, and active Serverless workers while retaining only the
intended isolated 50 GB Mage and Echo volumes.

Turbo remains the VideoForge name for the pinned official Flash/flash-pro 8-step lineage plus the
owned FP8 preparation; no separate upstream Turbo checkpoint is claimed. Ordinary boot loaded the
sealed manifest from the Echo-only volume with zero downloaded model bytes, no material
quantization, and registry access disabled.

The proposed renderer crops are `992:558:16:0` for full 16:9 and `496:558:280:0` for the 8:9 split
avatar panel. They are not activated. CP-07 remains at `READY_FOR_USER_CROP_REVIEW` until the user
reviews the playable MP4s and explicitly approves or changes both crops.
