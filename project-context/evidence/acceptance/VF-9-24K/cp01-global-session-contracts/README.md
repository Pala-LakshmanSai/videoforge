# CP-01 global-session contracts acceptance

Status: complete provider-free checkpoint after independent re-audit at fix commit
`0e90f3b637949711aecda6acc5b9e0bd51ae3202`.

The checkpoint added three vNext machine contracts, additive migration `0014`, the singleton
session/queue repository, exact Pod lifecycle barriers, metadata restore coverage, and a production
dispatch firewall. The synthetic session has one concurrent idle-start winner, inert/reorderable
waiters, exact immutable Mage/Echo bindings, fail-closed ambiguity, next-activation-only recreation,
two independently proven Pod absences, and two retained model volumes.

`acceptance.json` preserves the original acceptance. `reaudit-acceptance.json` records the audit
findings, fixes, revised hashes, adversarial dispatch proof, exact commands, updated test counts, and
the `$0` authority boundary. No provider API, credential, remote resource, model download, GPU, or
spend was used. No live provider inventory refresh was performed, so historical remote-zero evidence
remains timestamped history rather than a new current-state claim.

All pre-existing migrations `0001`–`0013`, v1 worker-envelope bytes, and historical provider
evidence remain unchanged. Production gates remain open. CP-02 was not started.
