# CP-03 production-grade word transcript acceptance

Provider-free acceptance for `VF-9-24N`; external spend `$0`.

- `real-owned-fixtures.json`: exact pinned Mac and local Linux-container tools/model, owned
  short/noisy/silence/malformed and 30-minute-class results, exact successful transcript/semantic
  receipt parity, monotonic/phrase coverage, replay, and restart recovery. The private artifact root
  is ignored evidence context, not a tracked media asset.
- `container-contract-parity.json`: real Linux image identity, executable hashes, byte-identical
  Mac/container chunk/transcript contract, and actual-media evidence. Runtime network was disabled;
  the pinned model was mounted read-only and was not embedded.
- `manual-spot-check.json`: manual timing review and honest lexical limits from pinned base.en.

No input audio, normalized audio, chunk audio, model bytes, credentials, or provider output is
tracked. Original voiceover is final-render truth; analysis files are transient and removed.
This is local provider-free proof, not Cloud Run/private production R2 proof; CP-08 owns that gate.
