# VideoForge v2 checkpoint chat prompts

Use one implementation prompt in a new chat. After its handoff commit, open another new chat with
the paired audit prompt. Do not paste this whole file. V2-00 through V2-06 are complete and have no
successor implementation prompts here.

All prompts assume `/Users/lakshmansai/Documents/videoforge`. A prompt authorizes provider-free
repository work and narrow read-only inventory/rate preflight only. It never reuses an old approval
or cap. Before external mutation, deployment, publication, paid request, or GPU use, the agent must
stop once with an exact combined proposal and obtain a fresh user-supplied finite numeric cap.

## V2-07 implementation — close Mage Serverless

```text
Complete VideoForge checkpoint V2-07 only: close the exact current Mage Serverless qualification as fast and safely as possible.

Read AGENTS.md and the three startup files. Read only the Closed Attempt53 handoff section of project-context/tasks/VF-10-07.md, V2-07 in project-context/22_PROJECT_COMPLETION_CHECKPOINTS.md, and the selected profile files needed for the current boundary. Do not preload the historical attempt ledger. Resolve git HEAD/status and reconcile any stale current-state mirror before action.

Current expected boundary: V2-00..V2-06 are accepted. Attempt53 consumed its exact authority after the repaired terminal-history boundary passed and 64 durable units were accepted across probe, resume, and cold batches. Warm status reconciliation failed with V207_QUALIFICATION_FAILED. Exact RunPod cleanup and three stable reads prove zero compute/disposable resources, both volumes retained, and USD 0 immediate observed increment subject to billing lag. Cloudflare rollback is unresolved because the later hosted route is stably 503 HOSTED_ROUTE_NOT_COMPOSED instead of required 404 V207_ROUTE_DISABLED. No authority or cap remains; V2-08 is forbidden.

Use only Mage-Flow INT8 ConvRot, RTX 4090, EU-RO-1, the existing sealed Mage 50 GB volume at /runpod-volume, and the already-published immutable image. Do not rebuild, redownload, mutate/cross-mount/delete volumes, change model/GPU/region, add fallback, or start successor work.

Do provider-free Cloudflare rollback fingerprint diagnosis only. Do not mutate Cloudflare or RunPod, dispatch, spend, retry Attempt53, create a successor paid proposal unless explicitly requested, or begin V2-08. Stop with the exact blocker and smallest next action.

Any remote rollback repair or paid retry requires a new exact proposal and fresh approval. Continued USD 7/month retention of the two existing volumes remains approved separately. Keep V2-08 forbidden.

Use concise progress updates. Run focused validation, relevant typecheck, context/schema validation only if changed, and git diff --check. Update evidence/GATES/CURRENT_STATE only if stale, commit one green V2-07 handoff, and stop. Report exact commit, commands/exits, remaining gate, consumed authority/observed spend, zero jobs/workers/disposable resources, retained volumes, and successor safety.
```

## V2-07 independent audit

```text
Independently audit VideoForge V2-07 at current HEAD. Read AGENTS.md, startup truth, only the current V2-07 handoff/Attempt53 section, V2-07 acceptance, diff, immutable paid evidence, and cleanup. Read-only only: do not edit, commit, access credential values, call providers, mutate resources, or spend.

Verify exact current-lineage Mage image/model/manifest/volume/RTX4090/EU-RO-1 identity and the entire immutable paid sequence: max1 probe, exact terminal identity/count boundary, unresolved-only process-replacement resume, separate cold and warm complete batches, duplicate delivery/cost, cancellation, timeout, two simultaneous max2 complete read-only readers, exact max1 restore, disposable cleanup, and three-read reconciliation. Verify durable 1280x720 outputs/private readbacks before expiry; provider status reconciliation; valid signed v3 receipts; accepted-unit/dispatch truth; timings/VRAM; settled cost versus the exact cap; unchanged pre/post manifest; terminal endpoint jobs; zero total workers; and only the two intended retained volumes. Check that no old cap/authority or fallback was used and no broad test claim substitutes for live proof.

Return PASS or FAIL with P0/P1/P2 findings and exact file/evidence references, commands/exits, unproven claims, CURRENT_STATE/provider/spend/worker truth, and whether V2-08 is safe. Do not fix anything.
```

## V2-08 implementation — SoulX Serverless and visual activation

```text
Complete VideoForge checkpoint V2-08 only: exact SoulX-FlashHead Pro Serverless and visual activation.

Read AGENTS.md, startup truth, the selected V2-08 brief/profile, and V2-08 in the checkpoint file. Verify V2-07 is committed and independently PASS. Use only the locked SoulX Pro BF16 runtime, RTX 4090, EU-RO-1, existing sealed SoulX 50 GB volume at /runpod-volume, and current exact source/profile lineage. No alternate model, repair, enhancement, fallback, full-voiceover dispatch, download, or volume mutation.

First locate and show the existing owned native/full/split SoulX samples at $0 and obtain my explicit crop/visual decision. Record exact first-party code/weight terms and my explicit risk decision without overstating permission. Then implement only the missing Serverless-v3 whole-span-batch handler/image/config: scoped private artifact ports, job-local scratch, sealed-manifest check, durable per-span resume, exact pad/trim/A-V contract, one native clip reused for full/split rendering, signed receipts, reconciliation, and terminal cleanup.

Use focused handler/contract tests, one synthetic span batch, relevant Python/TypeScript checks, and git diff --check. Do not run a broad matrix. Set `RUNPOD_INIT_TIMEOUT` deliberately, require cold `MODEL_READY` below the documented seven-minute unhealthy threshold, and keep initial endpoint max1; max2 live proof belongs to V2-11.

Finish provider-free/read-only preflight, then ask once before publication/config mutation/GPU/spend with exact operations, identities, current rate, estimate, recurring volume cost separately, rollback/cleanup/stop rules, and a fresh numeric finite cap. After exact approval, qualify one small representative 2/4/6/10-second span batch with cold and warm max1 execution; require playable native/full/split outputs, hashes/ffprobe/A-V, receipts/readbacks, init/load/inference/upload timings, VRAM, settled cost, unchanged volume, terminal jobs, and workers=0. Reuse the accepted Pod samples for avatar/phoneme/head-motion/hair/hat/background breadth, and add only focused request/status/reconcile, cancel, invalid-output, and timeout proofs.

One narrow repair cycle maximum. Update evidence/GATES/CURRENT_STATE, commit one green handoff, and stop before V2-09. Report exact validation exits, user visual decision, terms/risk state, spend, jobs/workers/volumes, and successor safety.
```

## V2-08 independent audit

```text
Independently audit VideoForge V2-08 at current HEAD. Read startup truth, the exact V2-08 records, current SoulX lineage, terms/risk record, user visual decision, diff, paid evidence, and cleanup. Read-only only: no fixes, credentials, provider calls, mutations, or spend.

Verify exact SoulX Pro bytes/profile/image, existing SoulX-only volume at /runpod-volume, RTX4090 EU-RO-1 max1 scale-to-zero config, deliberate init timeout, cold `MODEL_READY` below seven minutes, cold and warm max1 execution, no download/write/fallback, isolated scratch, whole-span batching/resume, exact pad/trim/A-V, one native clip reused for full/split, private durable receipts/readbacks, playable sample outputs, explicit crop approval, honest deployability language, focused request/status/reconcile/cancel/invalid-output/timeout proof, timings/VRAM/cost/cap, terminal jobs, unchanged volumes, and zero total workers. A PASS permits bounded max1 V2-09/V2-10 dispatch; max2 is owned by V2-11.

Return PASS/FAIL with exact P0/P1/P2 evidence, commands/exits, missing proof, CURRENT_STATE/provider/spend/worker truth, and V2-09 safety. Do not repair.
```

## V2-09 implementation — hosted real-GPU bridge and short E2E

```text
Complete VideoForge checkpoint V2-09 only: connect the hosted production app to the qualified autoscaling GPU lanes and finish one short real Generate-to-MP4 project.

Read AGENTS.md, startup truth, the V2-09 brief/profile, V2-09 in the checkpoint file, and accepted V2-07/V2-08 endpoint identities. Verify both predecessors are independent PASS. Preserve the current UI and exact Mage/SoulX models.

Fix the production build-mode inconsistency so the hosted client and Worker share factual staging/production mode; extend the emitted-bundle firewall against fixture execution profiles and old runtime controls. Compose the hosted tenant repositories, Postgres fair admission, durable account/workspace-owned tenant project/stage state, Serverless outbox/runtime, real Mage/SoulX adapters, private R2 artifact barrier, and account-owned personal-worker render-plan materialization.

Reuse the already-green word-level whisper transcript, scheduler-v2 voiceover splitting, three compositions, prompt/style path, and FFmpeg renderer. Do not reimplement them. Keep FakeServerlessEndpoint only in fixture/test entrypoints. Remove old public/root production exports only after transitive-import proof, the provider-free bridge, and the short hosted E2E are green; never rewrite migrations.

Provider-free first: one complete fixture journey plus one resume/cancel and a focused foreign-tenant negative matrix over only the newly composed assignment, callback, signed-R2-port, artifact-barrier, render-plan, and result paths. Run only touched-package tests/typecheck/build, production-bundle scan, one canonical provider-free aggregate for this integration boundary, context/schema checks if changed, and git diff --check.

Freeze one owned 30-60-second project and exact work counts/lineage/cost/cleanup. Before deploy or any paid/live call, ask once with exact operations, deployment/endpoint/config/image/model/volume/GPU/rates, estimate and recurring charges, rollback/stop rules, and a fresh numeric cap. After approval, run once through actual hosted Generate. Require private Chrome play/seek/download, hashes/ffprobe, exact lineage, itemized settled cost, the focused changed-surface tenant-negative matrix, terminal jobs, and workers=0. No manual media edit or fixture GPU asset.

One narrow repair cycle maximum. Update evidence/GATES/CURRENT_STATE, commit one green handoff, and stop before V2-10. Clearly state whether the functional single-video private-beta milestone is achieved.
```

## V2-09 independent audit

```text
Independently audit VideoForge V2-09 at current HEAD. Read startup truth, exact V2-09 records, accepted endpoint identities, implementation diff/build graph, immutable run evidence, output, and cleanup. Read-only only: no edits, credentials, live calls, mutations, or spend.

Verify the production client/Worker mode is consistent; emitted production bundle has no fixture GPU profile/manual Pod controls/legacy dispatch path; hosted Postgres admission and durable account/workspace-owned tenant state connect real transcript -> scheduler -> prompts -> Mage/SoulX -> private R2 barrier -> personal-worker render; and one owned 30-60-second project completed without fake assets or manual edit. Verify foreign-tenant rejection at the newly composed assignment, callback, signed-R2-port, artifact-barrier, render-plan, and result boundaries; exact lineage; playable Chrome output; hashes/ffprobe; settled cost/cap; terminal jobs; and workers=0.

Return PASS/FAIL with exact P0/P1/P2 findings, commands/exits, missing proof, CURRENT_STATE/provider/spend/worker truth, and V2-10 safety. Do not fix.
```

## V2-10 implementation — automatic 3–5-minute quality pilot

```text
Complete VideoForge checkpoint V2-10 only: one automatic 3-5-minute Ranga-style pilot.

Read AGENTS.md, startup truth, the V2-10 brief/profile, exact V2-10 checkpoint section, and only the pinned Ranga/reference files named by the profile. Verify V2-09 is independent PASS. Preserve scheduler-v2, the exact three compositions, Mage/SoulX, hard cuts, image zoom, and prohibited-graphics rules.

Freeze one owned final voiceover, exact tenant/revision/Avatar Profile crop/Image Style versions, work counts, cost estimate, review rubric, and cleanup. Provider-free checks should reuse existing scheduler metrics; do not run the 300-image suite. Before paid generation, ask once with exact operations/identities/rates/estimate/recurring costs/cleanup/stop rules and a fresh numeric cap.

After approval, generate once with no manual timeline edit or model/asset substitution. Review every cut for literal evidence, documentary realism, crop/lips/background, cadence, three-composition geometry, hard cuts, slow zoom, prohibited graphics, and A/V. Show the final in real Chrome with metric report, contact sheet only if it materially helps review, hashes/probes, retries, settled itemized cost, user visual decision, terminal jobs, and workers=0.

Fix only observed defects, with one reproduction test per fix and one narrow repair cycle. Run focused tests/typecheck, context checks if changed, and git diff --check. Update evidence/GATES/CURRENT_STATE, commit, and stop before V2-11.
```

## V2-10 independent audit

```text
Independently audit VideoForge V2-10 at current HEAD. Read startup truth, exact pilot records, pinned reference metrics, authority, final output/review evidence, and handoff. Read-only only: no edits, credentials, provider calls, mutations, or spend.

Verify a real owned 3-5-minute project completed automatically with no manual timeline/asset substitution; exact profile/style/model lineage; three-composition/cadence metrics; literal relevance and defect review; no prohibited graphics; playable A/V; user visual decision; actual cost versus cap; tenant privacy; terminal jobs; and workers=0. Do not require a 300-image suite or claim identical natural motion to real footage.

Return PASS/FAIL with exact findings, commands/exits, missing proof, CURRENT_STATE/provider/spend/worker truth, and V2-11 safety. Do not repair.
```

## V2-11 implementation — live two-user autoscaling and queue

```text
Complete VideoForge checkpoint V2-11 only: prove live two-user autoscaling, 5-10-user fair admission, and essential recovery.

Read AGENTS.md, startup truth, V2-11 brief/profile and checkpoint section, and accepted lane/E2E evidence. Verify V2-10 is independent PASS. Keep Postgres as admission/fairness truth, one active workload/account, two different accounts globally, REQUEST_COUNT=1, handler concurrency 1, exact Mage/SoulX, RTX4090, and autoscaling only. No manual Pod lifecycle or RunPod queue fairness claim.

Reuse the accepted provider-free 10-account proof; rerun its one focused contention test only if queue code changed. Prepare exact max2 configs only after each lane max1 is accepted. Freeze two short owned projects from different accounts, a same-account second request, a third-account waiter, one essential fault/recovery point, cancellation/reconciliation, cost estimate, and cleanup. For each lane, pin the pre-run sealed-volume manifest and the exact max1/max2 configurations.

Before config mutation/live calls/GPU/spend, ask once with exact accounts-safe scope, operations, endpoint/config/image/model/volume/GPU/rates, project counts, estimate/recurring costs, rollback/cleanup/stop rules, and a fresh numeric cap. After approval, deliberately prove two simultaneous read-only workers on Mage and two simultaneous read-only workers on SoulX against each lane's exact shared volume; prove two accounts run concurrently, same-account second waits, third account waits then promotes fairly, all UI/state/artifacts/costs remain private, and both endpoints scale 0 -> demand/max2 -> 0. Exercise the one named fault plus cancel/reconcile; do not run the exhaustive historical matrix.

Require at most two active videos/four total GPU workers, durable receipts, at-most-one accepted output with duplicate cost visible, valid results, unchanged pre/post Mage and SoulX volume manifests, exact config readback/restoration, terminal jobs, zero total workers, and intended volumes. Run focused tests/typecheck, one Chrome private-status journey, context checks if changed, and diff check. One repair cycle maximum. Update evidence/GATES/CURRENT_STATE, commit, and stop before V2-12.
```

## V2-11 independent audit

```text
Independently audit VideoForge V2-11 at current HEAD. Read startup truth, exact V2-11 configs/authority/raw events/cost/output/cleanup evidence and focused contention proof. Read-only only: no edits, credentials, live calls, mutations, or spend.

Verify two different accounts truly ran concurrently; one workload/account and two accounts globally; same-account second and third account waited; fair promotion; private state/artifacts/costs; exact max2 lane configs with REQUEST_COUNT=1; two simultaneous read-only workers on each exact lane volume; unchanged pre/post Mage and SoulX manifests; exact config readback/restoration; no more than four total GPU workers; named recovery/cancel/reconciliation; canonical output/duplicate-cost truth; valid results; terminal jobs; zero endpoint jobs; and both endpoints at zero workers after drain. Do not demand the deferred exhaustive fault matrix.

Return PASS/FAIL with exact P0/P1/P2 findings, commands/exits, missing proof, CURRENT_STATE/provider/spend/worker truth, and V2-12 safety. Do not repair.
```

## V2-12 implementation — production-length quality and economics

```text
Complete VideoForge checkpoint V2-12 only: one representative 30-minute automatic quality, speed, and economics qualification.

Read AGENTS.md, startup truth, V2-12 brief/profile and checkpoint section, accepted live evidence, and pinned Ranga/cost contracts. Verify V2-11 is independent PASS. Do not change Mage, SoulX, RTX4090, resolution, scheduler, compositions, or quality to force a cost result.

Freeze one owned voiceover whose final automatic output must be between 29 and 31 minutes, exact versions/work counts/configs, conservative cost forecast, $1 target/$2 hard ceiling evaluation, quality rubric, cleanup, and stop threshold. A shorter run cannot close production-length quality or economics. Use accumulated accepted checkpoint outputs for image/style/avatar evidence. Do not run 300 images, five styles, or ten artificial jobs; ten organic beta jobs are a later confidence sample.

Before any paid call, ask once with every exact operation/identity/rate, estimate, recurring charges separately, rollback/cleanup/stop rules, and a fresh numeric cap. Stop before dispatch if the conservative estimate exceeds the approved cap or $2 hard ceiling. After approval, run once and report queue/cold/warm/init/inference/upload/render timings, retries/duplicate cost, full output quality, settled variable cost, fixed volume/storage costs, local device occupancy, terminal jobs, and workers=0. Obtain my explicit quality/economics decision.

Run only focused measurement/manifest tests, ffprobe/hash/receipt checks, one Chrome playback, context checks if changed, and git diff --check. One narrow repair cycle maximum. If quality or cost misses, present the measured dominant cause and bounded choices; do not silently alter architecture. Update evidence/GATES/CURRENT_STATE, commit, and stop before V2-13.
```

## V2-12 independent audit

```text
Independently audit VideoForge V2-12 at current HEAD. Read startup truth, exact production-length input/manifests/authority/raw timing and billing/output/review evidence, and handoff. Read-only only: no edits, credentials, live calls, mutations, or spend.

Verify one real automatic run with final duration between 29 and 31 minutes; exact work counts and model/config lineage; actual Serverless billing; timings and queue separation; output/Ranga/avatar/image quality; retries/duplicate compute; fixed versus variable cost; honest $1 target/$2 ceiling result; user decision; terminal jobs; and workers=0. A shorter run is FAIL. Accept accumulated checkpoint evidence; do not require 300-image/five-style/ten-artificial-job suites.

Return PASS/FAIL with exact findings, commands/exits, missing proof, CURRENT_STATE/provider/spend/worker truth, and V2-13 safety. Do not repair.
```

## V2-13 implementation — invited-team production release

```text
Complete VideoForge checkpoint V2-13 only: release the accepted autoscaling, tenant-private system for 5-10 invited accounts.

Read AGENTS.md, startup truth, V2-13 brief/profile and checkpoint section, every accepted predecessor gate, current deployment/build graph, and release pins. Verify V2-12 is independent PASS. Do not add features or change models/architecture.

Close essential P0/P1 auth/session/origin/rate-limit, tenant/R2/callback/SSRF/path/upload, secrets/logging, cost-amplification, and legacy-runtime risks. Add per-project/global spend stops, quotas, queue/endpoint/worker/drain alerts, concise stuck-job/provider-outage/billing/rollback runbooks, backup readback, and exact rollback pins. Production client/API mode must be factual and its emitted bundle must contain no fixture GPU profile, fake production transport, manual Pod/GPU controls, or legacy public dispatch export.

Run focused security/build tests, canonical provider-free aggregate once, context/schema validators, secret/dependency/container scans, git diff --check, one backup/rollback readback, one release-current hosted control-plane restart/reconcile proving no duplicate dispatch or foreign result, and one production Chrome smoke. Perform a disposable restore only if V2-09 through V2-13 changed schemas/migrations; otherwise cite the accepted V2-06 restore proof. Do not run broad nonessential cross-browser/accessibility or exhaustive fault matrices now.

Before deploy/domain/OAuth/secret/config/live smoke or paid mutation, ask once with exact operations/resources/identities/rates, finite estimate plus recurring charges/retention consent, rollback/cleanup/stop rules, and a fresh numeric cap. After approval, deploy only pinned artifacts and prove invited auth, private queue/state, one bounded Generate-to-download smoke, spend guards, alerts, rollback readiness, terminal jobs, zero total GPU workers, and intended retained resources.

No P0/P1 may remain. Record nonblocking P2s for real-sample follow-up. Update final evidence/GATES/CURRENT_STATE, commit one green release handoff, and report production URL/commit/images/endpoints/configs, invitations/operator steps, cost, recurring charges, jobs/workers/volumes, rollback, and honest remaining risks. Do not push unless I explicitly ask.
```

## V2-13 independent audit

```text
Independently audit VideoForge V2-13 at current HEAD and immutable production evidence. Read startup truth, exact release/security/backup/rollback/build artifacts, authority, Chrome smoke, cost and cleanup. Read-only only: no edits, credentials, live calls, mutations, emails, or spend.

Verify every predecessor is accepted; tenant isolation and durable account/workspace-owned tenant state; autoscaling-only Mage/SoulX production paths; one/account two-global admission; no fixture/fake/manual-Pod/legacy dispatch production surface; essential auth/security/rate/cost guards; secrets; backup readback and rollback; one release-current hosted control-plane restart/reconcile with no duplicate dispatch or foreign result; a disposable restore if V2-09 through V2-13 changed schemas/migrations, otherwise the accepted V2-06 restore evidence; alerts/runbooks; pinned deployment; invited Chrome smoke; output lineage/cost; recurring-charge disclosure; terminal jobs; zero total GPU workers; and intended retained resources. Any P0/P1 or missing production proof is FAIL. Deferred broad matrices/statistical samples may be P2 only when they do not invalidate invited-team operation.

Return final PASS/FAIL with exact P0/P1/P2 references, commands/exits, unproven claims, CURRENT_STATE/provider/spend/worker truth, rollback readiness, and whether VideoForge is ready for 5-10 invited accounts. Do not repair or praise.
```
