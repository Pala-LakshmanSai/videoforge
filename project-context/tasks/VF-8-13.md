# VF-8-13 — SkyReels V3 A100-80GB bounded qualification

Status: exact paid successor after VF-8-12 pinned worker build

## Goal

Run one five-second SkyReels whole-frame fallback qualification from the original pinned square
Avatar runtime source and selected audio, retain exact evidence, and return RunPod to absolute zero.

## Context read profile

Use `phase8_skyreels_qualification` from `MANIFEST.yaml`.

## In scope

- Immutable image
  `ghcr.io/pala-lakshmansai/videoforge-avatar-quality@sha256:eed0778157bda9c28ebdb54bbab407d64205063b0467897ee9dcb9438b930497`.
- Locked SkyReels source/model revisions, one scale-zero endpoint, one A100 80GB worker maximum,
  at least 160 GB ephemeral disk, one owned five-second inline dispatch, 30-minute job timeout.
- Native expected output is 960x960/25 from the square source; preserve exact hash/probe/lineage.

## Authority and limits

- Existing secure RunPod credential and exact pinned model download only; maximum new spend `$2.00`,
  hard stop at `$1.80` observed. Combined sprint provider spend remains below `$10.00`.
- No second dispatch, alternate GPU, standalone Pod, persistent volume, low-VRAM/USP mode, model
  substitution, MuseTalk, Mage work, app/schema/UI change, promotion, or deployment.
- Cancel/drain/delete immediately on terminal result, timeout, cap risk, or ambiguity. Never leave a
  worker, endpoint, template, Pod, or volume running.

## Acceptance

- One valid MP4 has exact output/profile/source/model/checksum/probe/timing/cost evidence, or one exact
  safe terminal failure is retained without redispatch.
- Human quality judgment remains separate; no candidate promotion from technical evidence alone.
- Final and independent RunPod inventories show zero Pods, endpoints, templates, volumes, and workers.
