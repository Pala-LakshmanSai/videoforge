# VF-9-24T — SoulX Pro second sample, crop previews, and avatar economics

Status: provider-free/read-only preflight complete; awaiting paid authority with user-supplied cap

## Goal

Use the retained exact SoulX-FlashHead Pro runtime and the same owned Elias avatar source to generate
one different, natural-pause-bounded 10.00-second voiceover sample. Produce the native 512x512/25
fps result plus the two renderer-preview crops required by VideoForge, measure exact Pod-start to
model-ready and generation timings, project the avatar cost for the accepted 30-minute work-plan
fixture, delete the temporary Pod/template, and return all playable MP4s.

## Fixed inputs and lineage

- Avatar image: `elias yoder(sampe avatar).png`, SHA-256
  `34ca7513d6e34af3fa37d2e05afa8d2f81cab9ff587c2a12d047002abc7c75e3`.
- Original owned voiceover: `sampe voiceover.mp3`, SHA-256
  `4b5c404ea6a042ca5e82ef64f238d7866f2fb770967b953e593abdadcf6c7fc4`.
- Selected different sentence: “It came from a man who spent his whole life fighting one thistle
  in one fence row and finally taught me how it is actually done.”
- Deterministic source interval: `118.600–128.100` seconds, followed by `0.500` seconds of zero
  padding; exact 10.000-second 16 kHz mono PCM WAV SHA-256
  `7409fb7411829f0e2313d70346d477c86f5255733ea39a8afdc0668be86592c8`.
- Runtime/model/image/manifest/volume identities remain exactly those proven by VF-9-24S. No
  download, preparation, volume write, image publication, repair, enhancement, model change, or
  fallback is permitted.

## Proposed crop previews

One native SoulX result must serve both compositions. These are review-only proposals and do not
become active renderer contracts until the user approves them:

- Full: native `crop=512:288:0:112`, scale to 1920x1080, then `fps=30:round=near`.
- Split avatar panel: native `crop=448:504:32:4`, scale to 960x1080, then
  `fps=30:round=near`.
- Both previews use H.264 `yuv420p`, AAC 48 kHz stereo, fast-start MP4, no interpolation, overlay,
  border, label, repair, or enhancement.

## Provider-free preflight

- Extend the bounded controller to record provider `startedAt`, model-ready observation time,
  their exact delta, runtime phase timings, generation/encode/retrieval/delete timing, Pod cost,
  output hashes/probes, and final resource absence.
- Require the exact retained SoulX volume hash and corrected immutable image digest; fail closed on
  any account, region, GPU, rate, manifest, volume, input, output, cap, or cleanup mismatch.
- Use the exact CP-04 30-minute economics fixture: 103 avatar spans, 481.320 seconds of padded model
  input, and 378.820 seconds visible after trim. Report measured-sample projection separately from
  settled provider billing; do not present it as a full production settlement.

## Paid boundary

No paid operation is authorized yet. After the `$0` local/read-only preflight, request one combined
approval for the exact GPU/rate, one temporary template, one fresh offline runtime Pod, one native
sample, two local crop renders, private retrieval, deletion/absence verification, and a user-supplied
numeric cumulative finite spend cap. The retained 50 GB SoulX volume continues at `$3.50/month`
under prior retention consent and is excluded from the finite cap.

## Required handoff

- Playable native, full-crop, and split-panel MP4s with SHA-256, bytes, probe, frame count, and A/V
  duration.
- Exact GPU/rate/VRAM; provider Pod start, model-ready observation, manifest verification, load,
  compile warm-up, generation, encoding, retrieval, deletion, and full lifecycle timings.
- Conservative and settled finite cost, retained-volume rate, zero-Pod/template/endpoint/worker
  proof, and exactly the Mage and SoulX volumes retained.
- A transparent 30-minute avatar-cost projection from the exact 481.320-second padded workload,
  with uncertainty from 103 separate requests identified.
- User review before crop activation or production-profile promotion.
