# VF-9-24G — A100 SXM capacity sample

Status: selected; one same-memory capacity alternative authorized by primary output goal

## Goal and boundary

Run the unchanged exact Elias BF16 sample once on `NVIDIA A100-SXM4-80GB`, return its native MP4,
and prove RunPod absolute zero. This is only a capacity-class substitution after A100 PCIe acquired
no worker for ten minutes at `$0`; model, inputs, 253 frames, resolution, dtype, parameters, worker
image, transport, and output acceptance remain unchanged.

- Cumulative ceiling remains `$2.00`; live balance delta before this attempt is `$0.1313548907`.
- Operational breaker remains `$0.50` hard / `$0.45` active. One job only.
- No network volume, tuning, fallback model, second job, or successor.
- Output: `.videoforge/review/VF-9-24G/echomimic-v3-flash-elias-10.12s-native.mp4`.
- Evidence: `evidence/acceptance/VF-9-24G/elias-echomimic-v3-flash-a100-sxm-sample/`.
- Record queue, activation, bootstrap, model load/generation/encode, total wall, peak VRAM, cost,
  output hash/probe, then delete all RunPod resources and prove zero three times.
