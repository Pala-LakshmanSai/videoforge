# VF-10-07 — V2-07 Mage Serverless qualification

Status: NOT_QUALIFIED; Attempt25 is consumed and closed after one owned job reached `COMPLETED`/
`SUCCEEDED` but failed closed at `output_finalization` before any accepted batch, durable output, or
accepted receipt. Exact cleanup removed only its disposable endpoint/template, three stable reads
proved zero compute and `$0` settled incremental endpoint spend, and both intended 50 GB EU-RO-1
volumes remain retained. Closure: `evidence/acceptance/VF-10-07/2026-08-21-live-qualification/failed-attempt-25.json`
(`sha256:4b1d8b14f24b3e38a672cbe15b772590646bf35fe4e92f7a1046f23f13e5daf2`). Its proposal
`sha256:c8baa8a45b8e3e108904cac5f04f472ad22da2936dad75daa2a59d23476a8946` and authority
`sha256:2fc6072b88ca5069eef5510e6f0699faad977102565455495f89b56b02444b7c` are non-reusable. A fresh
exact proposal and fresh positive numeric cap are required before retry; V2-08 remains forbidden.

Attempt24 exact authority was consumed and closed after the pre-dispatch
quiescence guard raised `RUNPOD_QUIESCENT_NOT_CONFIRMED`. Exact template/endpoint identity work
reached the safety boundary, but `/run/job` was never called: zero jobs and zero batches were
submitted. Closure evidence records no guessed provider response detail, exact cleanup, final
zero-compute reconciliation, both retained volumes, and the settled USD 0.03767574962694198
increment. Control `63517e6` retains only structurally branded verification-stage diagnostics for
any future completed-job non-success, then stops without retry.

Source `79f123268b6ade640c02dd20616a89d16b43a5e6` and exact immutable image
`ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:bc662a182b2a874c6aeffb05f65cc3ffbdff6b5130c6a75c214618e86cf208b5`
remain published evidence, not qualification. The exact Worker version hash is restored and the
temporary signer is absent, and rollback proved 16 consecutive exact 404 `V207_ROUTE_DISABLED`
fingerprints over 30 seconds. Narrow exact cleanup deleted the disposable endpoint/template after
two stable terminal snapshots. Three later reconciliation reads at `2026-08-21T10:20:15.123Z` prove
zero Pods/endpoints/templates/workers, both intended 50 GB EU-RO-1 volumes retained by identity/size/
region, and billing from `$0.18311072164215147` to `$0.22078647126909345`, so Attempt24 settled at
`$0.03767574962694198`. Proposal
`sha256:be17430ce61a48a823a1ac87a128e83e44cfb88b01163331c285280e95274137` and authority
`sha256:fccd60a68ee93f522d9e378012c5ccbefb182f6b03e26fde1b5940506ab9c412` are consumed, closed,
and non-transferable. Evidence:
`evidence/acceptance/VF-10-07/2026-08-21-live-qualification/failed-attempt-24.json`
(`sha256:12ca4be38d063f761537cc4184b387ae83feeaebc6e9bb102260feff6c347bcb`). Any retry requires a fresh exact proposal and fresh positive
numeric cap. No image republication, model or retained-volume mutation, fallback GPU/region, public
sample publication, V2-08, or successor work is authorized.

## Attempt25 startup-terminal-inventory candidate and closure — 2026-08-21

- Provider-free control commit `bb9abc03f286cae56bf874fe47dc1d7ebddb1fe9` adds a narrowly scoped
  startup-only fallback for the Attempt24 `RUNPOD_QUIESCENT_NOT_CONFIRMED` boundary. After exact
  template/endpoint identity work and a warm-idle failure, the fallback may proceed only before any
  owned job or initial qualification when fresh endpoint-scoped worker and Pod inventories show only
  `EXITED`/`TERMINATED` records, `health.jobs` is present with `inQueue=0` and `inProgress=0`, and two
  stable inventory snapshots have matching signatures. It cannot be used after dispatch, cancellation,
  concurrent-reader proof, or drain; those paths remain health-first.
- Candidate proposal:
  `evidence/acceptance/VF-10-07/2026-08-21-attempt25-startup-terminal-inventory-candidate/combined-live-proposal.json`
  at `sha256:c8baa8a45b8e3e108904cac5f04f472ad22da2936dad75daa2a59d23476a8946`. Max-one is
  `sha256:d7a5791c80fa96f997994c70486208af5faea93989a1cc3fe5033a0a911ddacd`; max-two is
  `sha256:e1edf2d61b188428ce16e6f5597ceadc6ce7d58aa50dda4f8a7ea09e96bd0e38`.
- It binds the unchanged published image/source, exact Mage manifest and sealed 50 GB EU-RO-1
  volume at `/runpod-volume`, FlashBoot=true, LOW EU-RO-1, RTX 4090 only, and Attempt24 closure
  `failed-attempt-24.json` at `sha256:12ca4be38d063f761537cc4184b387ae83feeaebc6e9bb102260feff6c347bcb`.
  The user approved this exact proposal with FlashBoot=true, LOW EU-RO-1, and a fresh maximum
  cumulative finite spend of `$4`. The append-only authority is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt25-startup-terminal-inventory-candidate/approved-authority.json`
  at `sha256:2fc6072b88ca5069eef5510e6f0699faad977102565455495f89b56b02444b7c`. Provider
  mutation and GPU use were authorized only for this exact proposal and cap. They are now consumed;
  V2-07 remains NOT_QUALIFIED and any retry requires a fresh exact proposal and fresh cap.

## Historical Attempt25 startup-terminal-inventory authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:c8baa8a45b8e3e108904cac5f04f472ad22da2936dad75daa2a59d23476a8946` with FlashBoot=true,
  LOW EU-RO-1 availability, and a fresh maximum cumulative finite spend of `$4`.
- The append-only authority is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt25-startup-terminal-inventory-candidate/approved-authority.json`
  at `sha256:2fc6072b88ca5069eef5510e6f0699faad977102565455495f89b56b02444b7c`. It binds the
  unchanged immutable image/source/control, exact Mage volume and manifest, RTX 4090, max-one
  then separately hashed max-two reader configurations, startup-only exact terminal inventory
  fallback, health-first post-dispatch/cancellation/reader/drain proof, exact operations,
  cleanup/rollback, retention, and V2-08 prohibition. The proposal bytes remain unchanged.
- Provider mutation, scoped Cloudflare activation, endpoint/template creation, and GPU use were
  authorized only for this exact proposal and `$4` cap. This authority is now consumed. Image
  republication, model download/quantization, retained-volume mutation, fallback GPU/region,
  public sample publication, V2-08, and successor work remain forbidden. V2-07 remains
  `NOT_QUALIFIED` until all live output, receipt, integrity, timing/VRAM, two-reader, cost,
  cleanup, and independent drain gates pass.

## Attempt26 FINALIZE transport-repair candidate — 2026-08-21

- Provider-free control `b8666dd8b8bc12578ffae8925f6ce73dbf53a841` is the full-resolution FINALIZE
  transport repair. It retries only idempotent `FINALIZE` transport loss or invalid responses three
  times with the same reservation/callback tuple; PUT and non-FINALIZE POST retries remain forbidden.
- Candidate proposal:
  `evidence/acceptance/VF-10-07/2026-08-21-attempt26-finalize-transport-repair-candidate/combined-live-proposal.json`
  at `sha256:0112b0b72254ef286643fc63bee0176fce327edc401ce40de4a3a860a5e68632`. Max-one is
  `sha256:b64d008bac42fb13ec342028675a1bb498836981c553e884529ad846d6cdf964`; max-two is
  `sha256:10f887ba47e8a7cac952374eb236fed08cb67962171769b65d96a4f0d3a7acf7`.
- It binds the unchanged published image, exact Mage model/manifest, sealed 50 GB EU-RO-1 volume at
  `/runpod-volume`, FlashBoot=true, LOW EU-RO-1, RTX 4090 only, and corrected Attempt25 closure
  `failed-attempt-25.json` at `sha256:4b1d8b14f24b3e38a672cbe15b772590646bf35fe4e92f7a1046f23f13e5daf2`.
  The candidate has no authority and a null cap. No provider calls, endpoint/template mutation, GPU
  use, publication, model/volume mutation, or spend is authorized. Fresh exact approval and a fresh
  positive numeric cap are required before any provider action; V2-08 remains forbidden.

## Attempt 24 closure — 2026-08-21

- Attempt24 used the exact approved proposal under FlashBoot=true, LOW EU-RO-1, and a fresh USD 4
  cap. The immutable image, source, exact Mage manifest, and sealed 50 GB EU-RO-1 volume remained
  pinned; no image republication or model-volume mutation occurred.
- Exact template and endpoint identity work reached the pre-dispatch safety guard. The guard raised
  `RUNPOD_QUIESCENT_NOT_CONFIRMED`; `/run/job` was never called, so jobs submitted and batches
  submitted are both zero. The exact provider response detail is not retained and is not guessed.
- Generated-output rollback is `CONFIRMED`. Initial cleanup was uncertain; narrow exact cleanup then
  deleted only the disposable endpoint/template after two stable terminal snapshots covering one
  terminal worker and one terminal Pod record. Final reconciliation at `2026-08-21T10:20:15.123Z`
  proves zero Pods/endpoints/templates/workers/running Pods and both intended 50 GB EU-RO-1 volumes.
- Billing reconciled across three stable reads from `$0.18311072164215147` to
  `$0.22078647126909345`, a settled Attempt24 increment of `$0.03767574962694198`, within the
  approved `$4` cap. The signer is deleted, the captured Worker version is restored, and the route
  returned to 16 consecutive exact 404 `V207_ROUTE_DISABLED` fingerprints over 30 seconds.
- Authority is consumed and non-reusable; V2-07 is `NOT_QUALIFIED`; the complete cold/warm output
  suite, durable receipts, duplicate/cancel/timeout, two-reader, volume-integrity, timing/VRAM,
  and successful endpoint-retention gates remain open. A fresh exact proposal and fresh positive
  numeric cap are required for any retry. V2-08 remains forbidden.

## Attempt 23 closure — 2026-08-21

- Attempt23 used the exact approved proposal under FlashBoot=true, LOW EU-RO-1, and a fresh USD 4
  cap. The immutable image, source, exact Mage manifest, and sealed 50 GB EU-RO-1 volume remained
  pinned; no image republication or model-volume mutation occurred.
- One owned job reached `COMPLETED` after 32711 ms delay and 112694 ms execution. The application
  accepted zero batches, zero durable outputs, and no v3 inference receipt. The persisted error is
  `V207_QUALIFICATION_FAILED`; output status, failure code, and shape are `UNPROVEN` in the
  redaction-safe closure record and must not be guessed. The diagnostic stop prevented warm,
  duplicate, cancel/timeout, or two-reader follow-on work.
- Generated-output rollback is `CONFIRMED`. Initial cleanup was uncertain; narrow exact cleanup
  then deleted only the disposable endpoint/template after two stable terminal snapshots covering
  three worker and three terminal Pod records. Final reconciliation at `2026-08-21T09:14:21.303Z`
  proves zero disposable compute, two intended retained 50 GB EU-RO-1 volumes, three stable reads,
  and `$0` settled increment. Cloudflare signer deletion, Worker restore, and stable
  `404 V207_ROUTE_DISABLED` are confirmed.
- Authority is consumed and non-reusable; cap is closed at `$0` actual settled increment. V2-07 is
  `NOT_QUALIFIED`; the complete cold/warm output suite, durable receipts, duplicate/cancel/timeout,
  two-reader, volume-integrity, timing/VRAM, and successful endpoint-retention gates remain open.
  A fresh exact proposal and fresh positive numeric cap are required for any retry. V2-08 remains
  forbidden.

## Attempt 22 closure — 2026-08-21

- Exact template identity/environment GET proof, endpoint identity binding, and initial workers=0
  proof passed before one owned dispatch. Provider delay was 9887 ms and execution was 180546 ms.
- Provider status reached `COMPLETED`, but the application accepted zero batches, zero outputs, and
  no provenance receipt. The exact rejected output subfield cannot be recovered from the retained
  redaction-safe evidence and must not be guessed.
- Scoped generated-output rollback is confirmed. Exact cleanup deleted only the disposable endpoint
  and template after two stable terminal snapshots covering two worker and two Pod records.
- Independent reconciliation proves zero Pods/endpoints/templates/active workers/running Pods,
  both intended 50 GB EU-RO-1 volumes retained, baseline/final billing
  `$0.12480033212341368`, `$0` settled increment, and three stable reads. The existing two-volume
  charge remains `$7/month` separately.
- The signer is deleted, the captured Worker version is restored, and the route returned to 16
  consecutive exact 404 `V207_ROUTE_DISABLED` fingerprints over 30 seconds. Attempt22 authority is
  consumed; provider-free output-contract diagnosis is next.

## Attempt 21 provider-free diagnostic candidate — 2026-08-21

- Control commit `8d62be71b9b10585ea99d0583a4a4267ed9a5a79` classifies strict endpoint GET readback
  mismatches into only ten bounded categories: `identity`, `environment`, `flashboot`, `region`,
  `cuda`, `volume`, `gpu`, `workers`, `timing`, or `scaler`. The qualification evidence persists
  only `error_category`; provider response values and bodies are not retained. Any mismatch stops
  before `/run` dispatch. The live qualification tests cover missing/extra/drifted environment,
  omitted FlashBoot, string region, empty CUDA, volume, GPU, worker, scaler, and timing drift.
- The exact Attempt21 proposal is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt21-diagnostic-readback-candidate/combined-live-proposal.json`
  at `sha256:13acabaed3c21b3a15fcca203072c211f9002057453d4cd9b0fb5a765444d2d4`. It binds the
  published immutable image, the existing 50 GB EU-RO-1 Mage volume at `/runpod-volume`, FlashBoot
  true, RTX 4090 only, max-one then separately hashed max-two readers, and Attempt20 closure evidence
  `sha256:82aae2abf02041620c18d6a016719bab0f92ef41ed77430c2239ebfab005a37d`.
- This was a provider-free candidate with a null cap. It is now approved for bounded execution under
  the append-only authority below. Existing two-volume billing is `$7/month` separately; finite
  retry estimate is `$0.90–$1.65` incremental and `$1.024800332–$1.774800332` cumulative. V2-08
  remains forbidden.

## Fresh Attempt21 diagnostic authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:13acabaed3c21b3a15fcca203072c211f9002057453d4cd9b0fb5a765444d2d4` with FlashBoot=true,
  LOW EU-RO-1 availability, and a fresh maximum cumulative finite spend of `$4`.
- The append-only authority is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt21-diagnostic-readback-candidate/approved-authority.json`
  at `sha256:bc7580ad3f4782504587904115abb76738da72e3f2a048314a959475ef7316ec`. It binds the
  exact published image, control commit `8d62be71b9b10585ea99d0583a4a4267ed9a5a79`, Mage model
  manifest, sealed 50 GB EU-RO-1 volume at `/runpod-volume`, RTX 4090, FlashBoot=true, max-one
  then separately hashed max-two reader configs, operations, cleanup, retention, and V2-08
  prohibition. Provider calls begin only after this authority commit.
- Execution remains `NOT_QUALIFIED` until the exact GET readback, complete cold/warm batches,
  durable receipts, duplicate/cancel/timeout behavior, two-reader proof, sealed-volume integrity,
  settled cost, independent worker drain, and rollback/cleanup evidence pass. Stop on any mismatch,
  cap risk, failed output, uncertain cleanup, or unplanned duplicate compute.

## Goal

Convert and qualify the exact existing Mage-Flow INT8 ConvRot runtime as an immutable EU-RO-1
queue worker using only the retained sealed Mage 50 GB volume.

## Scope

- Complete local handler/image definition and read-only inventory first; no model download or
  volume mutation.
- Use `/runpod-volume` only for verified model reads and local job scratch for mutable bytes.
- Qualify RTX 4090, min 0, max 1 then 2, request-count 1, handler concurrency 1, cold/warm,
  two-reader manifest stability, resume/cancel/timeout/unknown-ack, output durability, cost, and
  scale-down.
- Diagnose the exact GET mismatch provider-free. Any later retry requires a new exact proposal and
  fresh numeric finite cap.

## Pinned V2-07 identity

- Mage: `Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6#int8-convrot`,
  ComfyUI `26d7f8556822d9d08c2d3e1878636ac3b4969af9`, 4 steps, CFG 1.0, 1280x720.
- Existing sealed Mage volume: hash
  `sha256:eae4e1ecee86be5d8bed2f6814e06332bc8a97e9f35767771d28c10cfdecd619`, 50 GB, EU-RO-1;
  manifest `sha256:cebcd5c6233c2eae32f26ced7510acef8192f0d92d7ec3e9dd3ee881d66d205b`,
  13,379,919,280 bytes. It is retained, Mage-only, and never writable/deletable in V2-07.
- Prior Mage foundation source image is immutable evidence only:
  `ghcr.io/pala-lakshmansai/videoforge-mage-cp06@sha256:0bd33cc8c41c7dc81964652b68e8f902e3521b931ade330c089f7999eb9c9f69`.
  V2-07 must publish a new immutable Serverless-handler digest before it can create an endpoint.
- Initial configuration is `EU-RO-1`, the exact Mage volume, one RTX 4090 GPU, Flex only,
  `workersMin=0`, `workersMax=1`, request-count scaler value 1, handler concurrency 1. A second,
  separately hashed `workersMax=2` configuration is temporary concurrent-reader proof only.

## Provider-free exit criteria

The handler must fail closed for malformed v3 authority, wrong image/manifest/volume/region/GPU,
model-volume write/cache escape, absent or mismatched scoped ports, duplicate delivery, cancellation,
timeout, and two-reader simulation. Do not call a provider while these are incomplete. The evidence
record at `evidence/acceptance/VF-10-07/2026-08-19-provider-free-preflight/acceptance.json` owns the
earlier $0 inventory/source snapshot; it is not qualification evidence. The repaired dynamic
authority/finalize contract is recorded in the committed control-plane, hosted R2, and Mage
handler tests, but a real durable R2 signer is still required before dispatch.

RTX 5090 and the SoulX endpoint are out of scope.

## Current V2-07 handoff — 2026-08-20

- The prior corrected Serverless image is published and smoke-tested at
  `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:ab5043715f422c20ad1190f063c4f9e66f0d73907738c1ff185ab4d37a57af4e`.
  Source commit `39541d5` normalizes the full immutable image environment value to the envelope's
  `sha256:` container digest while preserving the runtime identity fence.
- Exact RTX 4090 enforcement, malformed-authority/port/path/volume/region guards, duplicate
  delivery, receipt hardening, terminal scratch cleanup, dynamic generated-output authority
  validation/finalization, image identity normalization, and health-based worker drain are locally
  green. A nonce-scoped hosted R2 signer route was deployed for qualification probes and disabled
  afterward. Repeated disposable exact-image EU-RO-1 endpoint activations reported initializing or
  throttled workers before a bounded ready/warm-idle baseline; one ready-idle probe hung before
  dispatch. Every disposable endpoint/template was deleted and the two retained 50 GB volumes
  remain unchanged.
- Multiple submissions of that prior corrected image reached provider workers and terminated with output
  status `FAILED`; the returned output object exposed only `status`, no provider error code,
  durable output, signed receipt, or accepted sealed-volume write. A redacted provider stream read
  returned only terminal `COMPLETED` status. A later exact attempt reached `throttled` before
  dispatch. The read-only RunPod GPU catalog now reports RTX 4090/CUDA 13.0 available globally
  and LOW in EU-RO-1. The exact CUDA-13 image/runtime fence and scoped signer remain intact;
  relaxing them would invalidate this checkpoint.
- Provider diagnosis is now pinned to RunPod Python SDK `1.7.0`: its SLS-Core path moves a
  handler-returned `error` outside `output` and then posts only `output`, explaining the exact
  status-only failures. Commit `1623fa1` adds a bounded `failure_code` field to every explicit
  handler failure and teaches the live verifier to prefer it. The published image digest remains
  unchanged; republish and any further GPU dispatch require a fresh exact-image authority.
- Evidence: `evidence/acceptance/VF-10-07/2026-08-19-provider-activation-blocked/acceptance.json`.
- Provider-free canonical verification was rerun after the repair and formatting/lint cleanup:
  `CI=1 TURBO_FORCE=true pnpm verify` passed with 28 turbo tasks and installed Chrome 44/44;
  formatting/lint commit `1c9b0de` and evidence commit `f63569b` are local verification only.
  The prior `$4` authority is closed for the old immutable image and must not be reused for the
  repaired digest; no current finite spend cap or publication/GPU authority exists.
- The live runner now fails closed unless `V207_IMAGE` is a full immutable digest,
  `V207_IMAGE_SOURCE_COMMIT` is the complete repaired source `d1d704c2f39581e745ba90151c7388673107de41`,
  and `V207_FINITE_CAP_USD` is explicitly supplied; the old hard-coded cap/image were removed in
  `024547b`. Its template environment pins `RUNPOD_INIT_TIMEOUT=800` and the new guard has focused
  4/4 coverage.
- Commit `dadef6a` closes a build-lineage gap in the hosted image workflow: before Docker build or
  optional publication, it verifies the exact SHA-256 bytes of all four repaired overlay modules
  against the complete repaired source lineage. The image-definition suite is 20/20 and Python
  lint passes. This remains provider-free; no workflow was triggered and no repaired digest exists.
- Commit `c53e773` adds a settled-spend check immediately after the endpoint's warm-idle or drained
  baseline. If endpoint creation briefly starts a billed worker and crosses the supplied cap, the
  harness fails closed and deletes only the disposable endpoint/template. The new negative test is
  covered in the 11-test V2-07 harness suite; no provider state changed.
- The post-fence regression is green: 43 focused web tests, all six worker suites, the 10-test
  artifact-plane receipt/readback suite, TypeScript, lint, and formatting pass. No provider state
  changed and the repaired image remains unpublished.
- Commit `e147aff` extends the same cap fence after the temporary max-two policy update and after
  each single `/run` acknowledgement. The focused web suite is now 45/45; no provider state or
  spend changed.
- The exact provider-free combined proposal is now recorded at
  `evidence/acceptance/VF-10-07/2026-08-20-provider-free-repair-handoff/combined-live-proposal.json`.
  It pins both staged configs, lineage, operations, rates, a non-authorizing finite estimate,
  cleanup, and stop conditions; its user cap remains explicitly null.

## Provider-free repair handoff — 2026-08-20

- Commit `b40af353a3e8630bc7f4bc2776ca9879e23ad542` binds generated-output authorities to the
  exact accepted batch `output_prefix`, rejects any `max_uses` other than `1`, and aligns the v3
  job envelope `init_timeout_seconds` with the endpoint's pinned `RUNPOD_INIT_TIMEOUT=800`; the
  request authority TTL is explicit at 7,200 seconds.
  Commit `4311983` further requires the exact Mage batch prefix and one artifact identifier in
  every generated-output path, rejecting nested or cross-lane paths before runtime startup.
  The earlier `b40af353a3e8630bc7f4bc2776ca9879e23ad542` is only its repair ancestor; the complete
  repaired image source including runtime hygiene is `d1d704c2f39581e745ba90151c7388673107de41`.
  The worker suite is 15/15, focused web authority/harness tests are 5/5, and canonical
  `CI=1 TURBO_FORCE=true pnpm verify` is green with 28 turbo tasks and installed Chrome 44/44.
- This is provider-free evidence only. The published digest remains the old executable source
  `39541d5`; the repaired branch source is not published. The old `$4` authority is closed and
  no new numeric cap exists. No publication, endpoint mutation, `/run`, GPU allocation, or spend
  is authorized until a fresh exact-image authority and one combined bounded proposal are approved.
- Latest handoff evidence: `evidence/acceptance/VF-10-07/2026-08-20-provider-free-repair-handoff/acceptance.json`.

## Fresh live authority — 2026-08-20

- The user approved the exact combined proposal and supplied a fresh `$4` maximum cumulative
  finite spend cap at `2026-08-20T03:56:06Z`. This is new authority for the repaired source and
  does not reuse the historical cap.
- Authority record:
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/authority.json`; exact proposal
  digest: `sha256:e09c62846c9dd1a11577297016e156b2c739d6002b2c94bc36e214ccdf352c8b`.
- Hosted workflow run `32330272028` published and smoke-tested the exact repaired image:
  `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:8a5b8f453c694b2eeee097e3d958b08c5e47c15290b5cdc17a4fb7e5e3e4f497`.
  Independent registry inspection proves Linux/amd64, repaired source `d1d704c`, the pinned base
  digest, offline runtime flags, `/runpod-volume`, and the inherited queue entrypoint. RunPod
  remained at zero mutation, zero workers, and zero new spend through publication.
- The authorized sequence is image publication, exact max-one endpoint qualification, bounded
  duplicate/cancel proof, separately hashed max-two read-only proof, independent drain to zero,
  then retain the endpoint/template and both intended volumes on success. Stop on any identity,
  output, manifest, cleanup, rate, or cap mismatch and never mutate either retained volume.

## Pre-dispatch safety repair — 2026-08-20

- Commits `742835d`, `257c77f`, and `ab50ab2` close the final local gaps before endpoint mutation:
  exact-name ambiguous-create recovery, cancel-to-terminal polling, max-two primary fencing,
  phase-aware ownership cleanup, durable R2 reservations, full PNG decode validation, idempotent
  `artifact-commit-receipt/v3`, exact rollback, unique live attempt lineage, complete 32-item
  verification, stable billing settlement, and account-wide final inventory.
- The qualification port is compiled only into `worker/staging-index.ts`; the normal production
  Worker remains Serverless-free and passes its bundle quarantine. The orchestrator captures the
  current Worker version, activates an ephemeral nonce only over stdin, runs the exact qualification,
  deletes the secret, restores the captured version, and proves the route disabled on every path.
- Focused V2-07 tests pass 83/83. TypeScript, Python worker/lint, context/authority validation,
  production and staging builds, and canonical `CI=1 TURBO_FORCE=true pnpm verify` pass; canonical
  evidence includes web 407 passed/1 skipped and installed Chrome 44/44. No RunPod or Cloudflare
  mutation, GPU worker, new spend, or retained-volume mutation occurred during this repair.
- Evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-pre-dispatch-safety-repair/acceptance.json`.

## Complete runtime overlay repair — 2026-08-20

- Commit `b0f696c` corrects the image-definition boundary: the digest-pinned overlay now copies
  every changed runtime module (`mage_serverless.py`, `mage_runtime.py`, `mage_bootstrap.py`, and
  `src/videoforge_image_media/mage_production.py`) over the prior immutable base. This keeps the
  scratch/input/output hygiene inside the candidate image; copying only the handler would have
  left the old base runtime in place.
- Commit `e80640b` updates the activation guard and image labels to the complete source lineage
  `d1d704c2f39581e745ba90151c7388673107de41`. Image-definition tests are 18/18, focused authority
  and rollback tests are 9/9, web typecheck/lint pass, and no image build, publication, endpoint
  mutation, GPU allocation, or spend occurred.

## Provider-free hardening follow-up — 2026-08-20

- Commit `ee69c95` makes the live harness prove the exact retained Mage volume from the redacted
  inventory before any template or endpoint mutation: the configured volume hash must be present
  at exactly 50 GB with `dataCenterId=EU-RO-1`; missing or mismatched identity fails closed. The
  inventory mapper now retains only this non-secret placement field alongside the volume hash and
  size.
- The separately hashed `workersMax=2` configuration now includes the endpoint/template/image
  lineage and exact volume size/region. Concurrent policy updates, both reader dispatch preflights,
  post-dispatch reconciliation, and scale-down are finite-cap fenced. Focused authority/harness
  tests are 12/12; harness/control tests are 28/28; TypeScript typecheck passes. No provider call,
  publication, GPU allocation, retained-volume mutation, or spend occurred.
- V2-07 remains `NOT_QUALIFIED`: the repaired image is still unpublished, the historical `$4`
  authority is closed, and a new numeric cumulative cap plus one exact combined proposal are still
  required before publication, endpoint mutation, `/run`, or GPU allocation.
- Canonical `CI=1 TURBO_FORCE=true pnpm verify` is green after `ee69c95`: 28 turbo tasks,
  Cloudflare parity 1/1, web 52 files (373 passed, 1 skipped), six worker suites, context/secret
  checks, and installed Chrome 44/44.
- The latest `$0` inventory refresh at `2026-08-19T23:30:44.600Z` still shows zero Pods,
  endpoints, templates, and active workers, with exactly the two retained 50 GB volumes in
  `EU-RO-1`; no provider mutation or spend occurred.

## Fresh FlashBoot=true authority — 2026-08-20

- The user explicitly approved the exact FlashBoot=true requalification proposal
  `sha256:2338ff8d596284408080c94970d0c2a5e8a8ae58f62b92d590e880e72079d605` under LOW
  EU-RO-1 availability and supplied a fresh maximum cumulative finite spend cap of `$2`.
  The immutable authority record is
  `evidence/acceptance/VF-10-07/2026-08-20-flashboot-true-requalification-candidate/approved-authority.json`.
  Its authority bytes are `sha256:4deb86bd503eb51e452ce7b59a9a2214faa050ebe72daf63128bd97d9728e998`.
- Provider execution is pending. The approved sequence is limited to the exact ordered
  operations in that authority record: read-only account/inventory/rate refresh; immutable
  image/config/registry read-back without republication; scoped Cloudflare route activation;
  exact private template and FlashBoot=true max-one endpoint; full identity read-back; owned
  diagnostic/sample and complete 32-image cold/warm batches; durable receipt/status reconciliation;
  duplicate/cancel/timeout proof; separately hashed max-two concurrent read-only proof; manifest
  and model-volume write checks; max-one restore and independent zero-worker drain; signer/route
  rollback; and success/failure cleanup with both intended volumes retained.
- The exact image remains
  `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:bc662a182b2a874c6aeffb05f65cc3ffbdff6b5130c6a75c214618e86cf208b5`.
  Initial max-one definition is `sha256:5952e7f8d0f1512301d9863e52d399d724c2081c5ab6be52c4458c89cc2566f2`;
  bounded max-two reader definition is `sha256:34af811c7489dba9a3a8ec81f36325d547b140dea4d7af30b7591bd44415c6f2`.
  The exact Mage volume remains 50 GB in EU-RO-1 at `/runpod-volume`, application read-only;
  the separate SoulX volume remains retained. Existing two-volume billing is `$7/month`,
  outside the finite cap. Serverless Flex RTX 4090 is `$1.10/GPU-hour`; the secure Pod reference
  is `$0.74/hour`; network-volume pricing is `$0.07/GB-month`. The last observed checkpoint billing
  is `$0.12480033212341368`; the proposal estimates `$0.90–$1.65` incremental and
  `$1.0248003321234138–$1.7748003321234138` cumulative.
- The prior proposal `sha256:8c11e156df6544b2023eb843f3961ca948b755b4f3bf8a4b75e7c03df4bf2774`,
  its authority `sha256:afa5a4ded8eb25cd6df6105d3e3f7813e01bfa7a13cd1d7eb3d4b3ba35b1bed2`,
  and its `$4` cap are closed and not reused. Image republication/tag mutation, model download,
  quantization, cross-mounting, retained-volume mutation, GPU/region fallback, public sample
  publication, and V2-08/successor work remain forbidden. Stop on any identity/FlashBoot/output/
  receipt/manifest/write/cache/duplicate/cancel/cleanup/route/worker/rate/capacity/cap mismatch
  or any current/projected cumulative spend risk. V2-08 remains unauthorized.

## Hosted-runner repaired-image definition — 2026-08-20

- Historical initial commit `5f075b2` added `workers/image-media/Dockerfile.mage.repair`, pinned to the prior
  immutable image digest and copying only the repaired `mage_serverless.py` for source commit
  `431198333a22d66ad1929244bf92662c53be5a6b`. The prior entrypoint, sealed-volume environment,
  and offline dependency/runtime policy are inherited; the overlay has no download, volume
  mutation, or extra runtime instruction. It was superseded by the complete runtime overlay in
  `b0f696c`; the historical handler-only definition is not the active image candidate.
- Commit `57bd915` updates `.github/workflows/mage-image.yml` to build and smoke-check this
  overlay on an `ubuntu-24.04` hosted runner, explicitly builds `linux/amd64`, and smoke-checks
  the resulting image architecture, exact source/base labels, Mage/ComfyUI imports, handler
  import, and compile checks. Publication remains an explicit workflow input and was not triggered.
- Focused image-contract tests pass 15/15 and `pnpm python:lint` passes. This removes the local
  Mac heavy-build path but does not produce a final repaired digest or authorize publication.
- The initial handler-only overlay was superseded provider-free: `b0f696c` overlays all changed
  runtime-hygiene modules, and `e80640b` pins the image/activation source lineage to
  `d1d704c2f39581e745ba90151c7388673107de41`; image-definition tests are now 18/18.
- V2-07 remains `NOT_QUALIFIED` until a fresh exact image authority and a new numeric finite cap
  are supplied; the prior `$4` authority remains closed.
- Requirement-by-requirement provider-free audit: `evidence/acceptance/VF-10-07/2026-08-20-provider-free-repair-handoff/requirement-matrix.json`.
  It records the local handler/image/config/negative-contract passes separately from the live-only
  digest, publication, GPU, output, timing, cost, and drain proof that remains intentionally open.

## Provider-free runtime hygiene repair — 2026-08-20

- The Mage handler now downloads each declared v3 GET input through a bounded HTTPS URL, checks
  response length and SHA-256 against the scoped port, and stores it only below the attempt's
  scratch tree. Input reservation IDs are included in the accepted authority when inputs exist.
- Runtime cache/config/temp/output/lock roots are validated outside `/runpod-volume`; Comfy output
  is rooted in a worker-local directory and the exact fetched output file is removed in a `finally`
  path after read. Bootstrap evidence defaults outside the model volume and rejects an explicit
  model-volume path.
- Provider-free tests now cover tampered model bytes, wrong volume identity, handler-level volume
  mutation detection, timeout/cancellation scratch cleanup, exact input length/hash, exact Comfy
  output cleanup, and two independent reader scratch trees. The web harness carries distinct
  attempt IDs, reservation IDs, and output prefixes for the concurrent-reader proof.
- `pnpm test:workers` passes all six worker suites (85 image-media tests in the first suite),
  `pnpm python:lint` passes, and the web package typecheck plus V2-07 harness suite pass. This is
  provider-free evidence only; no repaired image digest exists, and no publication, endpoint
  mutation, `/run`, GPU allocation, or spend is authorized.
- Fresh authorized read-only RunPod refresh at `2026-08-20T00:41:40.146Z` shows zero Pods,
  endpoints, private templates, and active Serverless workers, plus exactly the two retained
  50 GB EU-RO-1 volumes. Official rate refresh at `2026-08-20T00:42:09.839Z` records Serverless
  Flex RTX 4090 at `$1.10/hour`, secure RTX 4090 at `$0.74/hour` with HIGH EU-RO-1 availability,
  and network-volume storage at `$0.07/GB-month`; retained Mage billing remains `$3.50/month`
  separately from any finite compute cap.
- Commit `767a27b` adds a nonce-fenced exact-key generated-output DELETE route and wires failed
  qualification rollback to delete every object created by that run. Successful outputs remain
  durable; rollback is marked `CONFIRMED` only after every delete response is acknowledged. This
  is locally tested and still needs live deployment proof.
- Commit `270cf95` tightens that rollback seam: DELETE reuses the exact single-artifact R2 key
  grammar, requires a per-object HMAC token derived from the ephemeral activation nonce, rejects
  cross-object token reuse, and returns success only after `head()` proves the object absent. The
  route now has provider-free coverage for nested paths, cross-project/revision/job/artifact
  targets, malformed fields, missing bindings, and uncertain post-delete verification. Live R2
  deployment and durable `artifact-commit-receipt/v3` finalization remain open.
- Read-only preflight refreshed at `2026-08-20T01:07:22.089Z`: zero Pods/endpoints/private
  templates/active workers and exactly the two untouched 50 GB EU-RO-1 volumes; selected secure
  RTX 4090 remains HIGH at `$0.74/hour`. Official RunPod pricing remains Serverless Flex 4090
  `$1.10/hour` and network volume `$0.07/GB-month`; no mutation or spend occurred.

## Strict staged endpoint identity repair — 2026-08-20

- Commit `d67e83e` closes the remaining provider-free configuration ambiguity. The V2-07 harness
  now accepts only the exact Mage GHCR repository, a 120 GB container disk, the measured
  `RUNPOD_INIT_TIMEOUT=800`/`LOG_LEVEL=INFO` environment, EU-RO-1, GPU compute, RTX 4090, CUDA
  13.0, `flashboot=false`, `REQUEST_COUNT=1`, `workersMin=0`, and the exact 5-second idle and
  2,400,000 ms execution limits. Endpoint mutation responses must echo the exact template ID,
  volume binding, region, CUDA, timing, and placement identity; a mismatch fails closed.
- Initial and separately hashed max-two configuration records now include container disk,
  handler concurrency, init timeout, request-authority TTL, template environment, template
  identity, image, endpoint, volume, and region lineage. Focused web coverage is 37/37, all
  worker suites pass, and the provider-free artifact-plane receipt/readback suite is 10/10.
- This remains provider-free definition evidence. The repaired image has no hosted digest, no
  publication or endpoint mutation has occurred, the old `$4` authority remains closed, and a
  fresh exact-image authority plus a new numeric finite cap are still required before any GPU
  or spend boundary.

## Provider-control negative hardening — 2026-08-20

- Commit `2f566f7` adds strict endpoint-response coverage for template identity drift. Commit
  `d1b75ab` includes compute type, `flashboot`, volume mount, and the complete runtime controls in
  both staged endpoint configuration hashes.
- Commit `6db83c8` makes the transport fail closed when one request key is reused with changed
  bytes, `/status` or `/cancel` returns a different job ID, a response reports contradictory
  `networkVolumeId`/`networkVolumeIds`, or a concurrent-reader drain cannot independently prove
  zero workers. Focused V2-07 coverage is now 42/42 with typecheck and lint green.
- These are local negative/identity proofs only. No endpoint, template, image, volume, worker, or
  provider billing state changed; the repaired image digest and fresh finite cap remain open.
- Commits `2b90d7e` and `bd461f7` remove live-runner timing/region/GPU/mount constant drift by
  importing the same authoritative values used by endpoint control. Typecheck and lint remain
  green; this does not mint an image digest or authorize a provider call.

## Attempt 12 and exact inline-wire candidate — 2026-08-20

- Attempt 12 dispatched one exact 32-item owned probe to the historical published image. RunPod
  reported terminal `COMPLETED`, while the signed worker payload reported
  `FAILED:MAGE_INLINE_SCOPE_INVALID`. Queue delay was 293,644 ms and provider execution was
  55,558 ms. No output, cold/warm batch, or concurrent-reader proof was accepted.
- The failure was local wire conversion: the handler passed dataclass/tuple internals back through
  a parser that accepts raw JSON mappings. Commit `a52e7e4` passes the already validated raw
  `INLINE_QUALIFICATION_V1` mapping directly. Focused Mage Serverless/production tests pass 31/31.
- Cleanup is exact: generated-output rollback confirmed, two stable terminal drain snapshots,
  zero Pods/endpoints/private templates/active workers, both 50 GB EU-RO-1 volumes retained, and
  the staging signer route restored to `404 V207_ROUTE_DISABLED`. Attempt evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-12.json`.
- Commit `df9b312` adds a deterministic one-file OCI overlay and exact-byte publisher. Independent
  reproduction over the raw parent manifest/config yields config
  `sha256:38b7633f199017ea66d39cc5b10d4d5a86ae34885f9e23e20fc20ea0be90cf5e`, layer
  `sha256:7dc5be30ec2116ff0729b524b1e5bea5e54c38f7e86132a95518fcda0e53470e`, and candidate manifest
  `sha256:6318edbc73b59d1a495566a765515831b3ff28302a4dc33c5e09ba52352215e3`.
  Publication remains false. Definition and separately hashed max-one/max-two proposal records are
  under `evidence/acceptance/VF-10-07/2026-08-20-inline-wire-image-candidate/`.
- The exact candidate evidence is independently checked by
  `node project-context/scripts/validate-v207-amended-proposal.mjs`, which rejects malformed
  digests, source/lineage drift, staged-definition hash drift, and stale proposal pointers.
- Fresh authenticated read-only truth at `2026-08-20T12:46:39.077Z` remains zero compute and two
  unchanged intended volumes. Cumulative checkpoint billing through `2026-08-20T12:47:06.801Z`
  reads `$0.0606543055` and remains provisional, leaving `$3.9393456945` below the approved cap.
  The `2026-08-20T12:46:52.790Z` regional catalog reports exact RTX 4090 availability `LOW` at the
  `$0.74/hour` secure Pod reference; official Serverless Flex is `$1.10/hour`. The signer remains
  disabled, and local disk was restored above the 2 GiB dispatch floor. Evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-inline-wire-image-candidate/pre-execution-preflight.json`.
- LOW availability and the `$4` cumulative cap are user-confirmed. The prior exact image authority
  cannot authorize the changed digest. Amended proposal
  `sha256:56f82ee2c32df36e1db3693c12002b008e17b34fed1998863a0ec020be6aac55` is approved for the
  recorded bounded operations. The exact candidate publication was then completed by workflow
  `32373571521` at commit `bcf1b6b`; independent local tag/manifest readback matches the approved
  candidate, and the workflow validated the exact config/layer bytes and descriptors before upload.
  Endpoint/template creation, `/run`, GPU use, and all live qualification proof remain pending under
  the recorded authority. Evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-inline-wire-image-candidate/image-publication.json`.

## Exact candidate publication — 2026-08-20

- The first amended publication workflow `32371796242` reached all deterministic candidate and
  image smoke checks, then failed on the first GHCR blob-completion PUT with HTTP 404 because the
  publisher omitted `Content-Type: application/octet-stream`. It performed no RunPod mutation,
  GPU allocation, or paid compute. The repair is committed at `bcf1b6b` and retains exact config,
  layer, manifest, tag, source, and parent lineage.
- Repaired workflow `32373571521` completed successfully at commit `bcf1b6b`. The immutable tag
  `v2-07-inline-wire-a52e7e4` resolves to
  `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:6318edbc73b59d1a495566a765515831b3ff28302a4dc33c5e09ba52352215e3`; independent local
  readback matches the exact manifest, while the workflow validated config
  `sha256:38b7633f199017ea66d39cc5b10d4d5a86ae34885f9e23e20fc20ea0be90cf5e` and layer
  `sha256:7dc5be30ec2116ff0729b524b1e5bea5e54c38f7e86132a95518fcda0e53470e` bytes and descriptors
  before upload.
- This closes only the image-publication gate. `GATE_SERVERLESS_MAGE_001` remains open for the
  exact EU-RO-1 endpoint/template, max-one cold/warm complete-batch proof, timeout/cancel/duplicate
  behavior, separately hashed max-two reader proof, sealed-volume immutability, timings/VRAM,
  settled cost, and independent zero-worker drain. Latest publication evidence states zero RunPod
  mutation, zero GPU allocation, zero finite spend, and two retained unchanged volumes.

## Attempt 13 — 2026-08-20 final failed-output boundary

- The exact amended proposal `sha256:56f82ee2c32df36e1db3693c12002b008e17b34fed1998863a0ec020be6aac55`
  was used only for this bounded attempt and is now closed. The approved finite cap was `$4` with
  LOW EU-RO-1 availability; observed cumulative endpoint billing was `$0.10264860291499645`,
  including an attempt-13 increment of `$0.041994297411292796` from the
  `$0.060654305503703654` baseline. The cap was not exceeded.
- One owned probe reached provider terminal `COMPLETED` with queue delay `202476 ms` and execution
  `190043 ms`, but the safe provider diagnostic was null and strict output verification accepted
  `0` batches before durable output/receipt acceptance. No cold/warm complete-batch or concurrent
  reader qualification is proven. Evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-13.json`.
- Identity-locked failed cleanup succeeded for the disposable RunPod endpoint/template, leaving
  final inventory at zero Pods/endpoints/private templates/active workers. The exact Worker
  version was restored and the temporary signer secret was absent. The final independent route
  probe returned HTTP 503 `HOSTED_ROUTE_NOT_COMPOSED` instead of the captured route fingerprint,
  so orchestrator cleanup remains `CLEANUP_UNCERTAIN`.
- Both intended 50 GB EU-RO-1 volumes were retained and observed by identity, size, and region.
  Their post-run content hash and absence of model-volume writes are deliberately unproven; no
  unchanged-volume claim is made. No retained-volume mutation, rebuild, delete, cross-mount,
  model download/preparation, or public output publication is authorized.
- V2-07 remains `NOT_QUALIFIED` and V2-08 remains forbidden. Any further paid qualification needs
  a fresh exact proposal and a fresh numeric finite cap; do not reuse the closed authority.

## Diagnostic endpoint-bound candidate — 2026-08-20

- Commits `080682a` and `79f1232` preserve bounded redacted root provider/worker failure codes,
  return stable unexpected-handler failures without exception text, and fail closed unless the
  allocated endpoint identity is bound as `sha256(raw endpoint id)` through a full exact environment
  PATCH and matching GET readback before baseline or dispatch. Binding is required in normal and
  ambiguous-adoption paths; binding failure deletes only the disposable endpoint/template.
- The exact handler bytes at source `79f123268b6ade640c02dd20616a89d16b43a5e6` hash to
  `sha256:be050e3c1db8eae65c32e68c1d70ef01aa9b9f74b6079f2386fd4dbce37efe68`.
  Deterministic validate-only reproduction over the unchanged parent emits layer
  `sha256:befafc2ec3d32a73b632f769069c9c02645d3fac049ebd2478fbf8ad3d5cdf38`, config
  `sha256:8e11a42cb91fa1d0d6a4e19fc6b4a6cfd5f77116c49a8516b6435813dfaab1de`, and manifest
  `sha256:bc662a182b2a874c6aeffb05f65cc3ffbdff6b5130c6a75c214618e86cf208b5`.
  Publication, provider mutation, GPU use, and external spend for this candidate remain zero.
- New immutable evidence lives under
  `evidence/acceptance/VF-10-07/2026-08-20-diagnostic-endpoint-bound-candidate/`. The separately
  hashed max-one and max-two configurations retain EU-RO-1, exact Mage volume, RTX 4090 only,
  min zero, request-count one, handler concurrency one, idle 5 s, init 800 s, execution 2400 s,
  authority TTL 7200 s, CUDA 13 only, offline model policy, and `/runpod-volume` application reads.
- Combined proposal
  `sha256:8c11e156df6544b2023eb843f3961ca948b755b4f3bf8a4b75e7c03df4bf2774` includes exact
  publish/template/endpoint/PATCH/GET/submit/status/cancel/timeout/max-two/scale-down/retain and
  failure-delete operations. Its cap is deliberately null and its authority state is pending.
  Existing volumes remain `$3.50/month` each (`$7/month` total) outside the finite run cap;
  last-observed Serverless Flex RTX 4090 is `$1.10/GPU-hour`, with a `$0.74/hour` EU-RO-1 secure
  Pod reference. A fresh user-supplied numeric maximum cumulative finite spend is required; the
  closed `$4` cap is not reusable.
- Validate with `node project-context/scripts/validate-v207-diagnostic-proposal.mjs`. It binds exact
  source, OCI identities, stage-file hashes, immutable proposal bytes, append-only authority, current
  state/task pointers, endpoint identity contract, and negative malformed/stale/cap/old-lineage
  invariants. This is provider-free proof only; the full V2-07 acceptance gate remains open.

## Diagnostic proposal authority — 2026-08-20

- The user explicitly approved exact proposal
  `sha256:8c11e156df6544b2023eb843f3961ca948b755b4f3bf8a4b75e7c03df4bf2774` with a fresh
  maximum cumulative finite spend of `$4`. The proposal bytes remain immutable with their null-cap
  preapproval state; append-only `approved-authority.json` records the approval and exact lineage.
- Authority covers only the proposal's read-only refresh, exact image publication/readback, private
  template and max-one endpoint, endpoint-identity PATCH plus GET readback, owned diagnostic/cold/warm
  32-image batches, duplicate/cancel/timeout proof, separately hashed max-two concurrent readers,
  max-one restoration, zero-worker drain, success retention, and failure cleanup. LOW EU-RO-1 and
  RTX 4090 only are accepted inside that exact boundary.
- The prior `$4` authority remains closed and is not reused; this is a fresh cap record even though
  the numeric value is the same. Existing retained volumes remain `$7/month` separately. Model
  download/preparation, volume mutation/deletion/cross-mount/write, public samples, fallback GPU or
  region, and V2-08 remain forbidden.
- Hosted workflow `32383853926` at authority commit `26d555b` independently derived, built, smoke-tested,
  published, and read back the exact Linux/amd64 diagnostic image
  `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:bc662a182b2a874c6aeffb05f65cc3ffbdff6b5130c6a75c214618e86cf208b5`.
  Exact tag/manifest bytes, config descriptor, and overlay layer descriptor match. Publication caused
  no RunPod mutation, GPU worker, retained-volume mutation, or external spend. Live qualification
  remains pending and the gate remains open.

## Attempt 14 — pre-dispatch FlashBoot mismatch — 2026-08-20

- The exact diagnostic image publication succeeded, and the authorized live preflight matched the
  Sujal account, Mage volume, EU-RO-1, RTX 4090-only identity, and current rate snapshot. Endpoint
  creation then returned the exact disposable identity with `flashboot=true`; RunPod did not confirm
  the staged `false` policy within the bounded update/readback window.
- Qualification stopped at `phase=initialized` with
  `RUNPOD_RESOURCE_RECONCILIATION_FLASHBOOT_NORMALIZATION_UNCONFIRMED`. `batches=[]`: no owned
  sample, complete image batch, `/run` job, durable output, cold/warm proof, concurrent reader, or
  inference result was dispatched or accepted. Generated-output rollback is `CONFIRMED`.
- The deletion-only cleanup seam accepted only boolean FlashBoot drift while retaining exact
  endpoint/template/image/GPU/CUDA/region/volume/scaler/timing identity and two stable terminal
  snapshots. The remote cleanup ran from the locally validated worktree bytes before their commit
  object existed; the exact bytes were immediately committed as `afd7e7f`. It deleted only the
  disposable endpoint and template. Independent inventory then observed zero Pods, endpoints,
  private templates, running Pods, and active Serverless workers; both intended volumes remained.
- The exact Worker version hash was restored and the temporary signer secret was absent. The route
  still returned HTTP 503 `HOSTED_ROUTE_NOT_COMPOSED` instead of captured 404
  `V207_ROUTE_DISABLED`, so route restoration is deliberately unconfirmed and remains a stop
  condition.
- Three post-cleanup billing reads, ten seconds apart, were stable at
  `$0.12480033212341368`. The Attempt 14 increment observed from its baseline is `$0`, but provider
  billing may lag. Existing retained-volume billing remains `$7/month` separately.
- Evidence:
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-14.json`.
  The approved proposal and append-only authority bytes remain unchanged; compiled activation is
  closed. V2-07 remains `NOT_QUALIFIED`, and V2-08 remains forbidden.

## FlashBoot=true provider-free requalification candidate — 2026-08-20

- Commits `148045c0ed73e10b874bf9ddd4044723248ee063`,
  `ad2e2e0ad31bc259b3010ebef8f70cf8f79d1d68`, and
  `8694f474f98bbcdd6d84a79614cda6ef907c7b9e` turn Attempt 14's provider observation into an
  explicit exact contract: endpoint create, response, full identity PATCH, GET readback, max-one,
  max-two, runtime configuration hashes, and terminal inventory all require `flashboot=true`.
  The previous `true`-to-`false` normalization path is deleted; `false` or malformed drift fails
  closed before dispatch. Failed-resource cleanup still accepts either boolean only for deletion,
  behind every other exact identity fence and two terminal snapshots that now bind FlashBoot
  stability. Both concurrent-reader inputs are fully validated before either `/run` can begin.
- Cloudflare route restoration now uses per-request timeouts and a bounded 120-second poll for the
  exact captured fingerprint. A temporary 404/503 or network failure cannot be called restored;
  exhaustion remains `V207_ROUTE_RESTORATION_UNCONFIRMED`.
- Focused control, harness, cleanup, orchestrator, and activation tests pass 141/141. Web TypeScript,
  ESLint, and targeted formatting pass. No image bytes changed; the exact published diagnostic image
  remains the only candidate, and no provider call, mutation, GPU allocation, or spend occurred.
- The new max-one definition is
  `sha256:5952e7f8d0f1512301d9863e52d399d724c2081c5ab6be52c4458c89cc2566f2`;
  max-two is
  `sha256:34af811c7489dba9a3a8ec81f36325d547b140dea4d7af30b7591bd44415c6f2`.
  Combined proposal
  `sha256:2338ff8d596284408080c94970d0c2a5e8a8ae58f62b92d590e880e72079d605`
  includes exact operations, lineage, staged configs, rates, finite estimate, cleanup/rollback,
  stop conditions, FlashBoot=true, LOW availability, and separately reports existing `$7/month`
  retained-volume billing.
- The proposal is unapproved with a null cap. The closed `$4` authority cannot be reused. A fresh
  explicit approval of this exact hash, FlashBoot=true, LOW EU-RO-1 availability, and a fresh
  positive numeric maximum cumulative finite spend are required before any provider call or GPU
  work. V2-08 remains forbidden.

## Attempt 15 — provider-free Cloudflare startup validation failure — 2026-08-20

- The signer-disabled staging orchestrator failed before signer activation, RunPod invocation,
  template or endpoint creation, job dispatch, GPU execution, volume attachment or mutation, and
  finite spend. The exact redacted orchestrator evidence is retained by SHA-256
  `sha256:18efc15fae184e7b08f7a31bd82fd486e1a4e8fa4ed585720b88241a8a0970c7` from
  `/private/tmp/videoforge-v207-live-orchestrator-48099.json`.
- Cloudflare code `10021` reported startup CPU validation failure during the signer-disabled
  staging deployment. The exact worker version hash
  `sha256:0ca150f857bbbbd63b474e220a008511805daf337ae7d0154489046e57419e53` was restored, and
  the captured pre-mutation and final route fingerprints both returned HTTP 404
  `V207_ROUTE_DISABLED`; route restoration is confirmed.
- Append-only evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-15.json`.
  It binds proposal `sha256:2338ff8d596284408080c94970d0c2a5e8a8ae58f62b92d590e880e72079d605`,
  the approved-authority record, exact FlashBoot=true image/source/volume/GPU/region identity,
  and the observed `$0` Attempt 15 increment under the approved `$2` cap. Existing retained
  volume billing remains `$7/month` separately outside the finite cap.
- Because no provider execution started and cleanup restored the exact route, the approved
  authority remains open for one exact retry only. Before retry, repair and test Cloudflare code
  10021 provider-free, preserve every authority-bound identity/rate/cap/operation, use a new
  ephemeral startup attempt if signer activation is reached, and stop before V2-08. V2-07 remains
  `NOT_QUALIFIED`; no batch, durable output, cancellation/duplicate proof, concurrent-reader proof,
  timing/VRAM proof, or volume write/hash proof was established.

## Attempt 16 — endpoint identity-binding mutation failure — 2026-08-20

- The exact retry cleared Cloudflare startup validation, activated the nonce-scoped route, and
  passed the live preflight. It created the exact RunPod private template and max-one endpoint, then
  failed with `RUNPOD_MUTATION_FAILED` before `endpoint_identity_bound`, endpoint config hashing,
  `/run`, job assignment, any image batch, durable R2 output, or inference.
- The fail-closed harness observed one terminal endpoint worker record and one terminal Pod record.
  Automatic endpoint/template deletion returned `RUNPOD_CLEANUP_UNCERTAIN`, but a later independent
  read-only inventory proved zero Pods, endpoints, private templates, and active Serverless workers,
  with only the two expected 50 GB EU-RO-1 volumes retained. Their content hashes and write absence
  remain unproven because no accepted worker receipt exists.
- Three later billing reads were stable at cumulative `$0.12480033212341368`, establishing a settled
  `$0` Attempt 16 increment under the consumed `$2` cap. Existing retained-volume billing remains
  `$7/month` separately.
- The exact Worker version hash is active and the temporary signer secret is absent. Route probes
  nevertheless alternated between 404 `V207_ROUTE_DISABLED` and 503
  `HOSTED_ROUTE_NOT_COMPOSED`, so route cleanup is not stable enough for qualification.
- Append-only evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-16.json`. The single retry
  is consumed; proposal `sha256:2338ff8d596284408080c94970d0c2a5e8a8ae58f62b92d590e880e72079d605`
  and its authority are closed and must not be reused. Provider-free endpoint/template update and
  route-stability repairs must pass before preparing a fresh exact proposal and requesting a fresh
  positive numeric cap. V2-07 remains `NOT_QUALIFIED`; V2-08 remains forbidden.

## Provider-free Attempt 16 repair and fresh proposal — 2026-08-20

- Commit `8d601af` replaces the unsupported endpoint `env` PATCH with the pinned RunPod REST
  sequence: update the exact private template environment, require the exact template response,
  PATCH the full endpoint configuration without `env`, then require exact effective-environment and
  configuration GET readback before dispatch. GPU, region, volume, FlashBoot, CUDA, timing, scaler,
  and identity drift still fail closed.
- The same commit requires 16 consecutive exact route fingerprints at the existing two-second
  cadence, a documented 30-second stability window inside the hard 120-second cleanup deadline.
  Any mismatch or probe error after the first match fails closed. Focused control/harness/orchestrator
  proof passes 106/106 with typecheck, lint, formatting, and diff checks green.
- Commit `8a5153a` closes the consumed compiled activation authority. Commit `68b41f8` binds the new
  pending proposal hash while keeping its compiled cap null, so repository state cannot run the old
  `$2` authority or the new proposal before fresh approval.
- Staged max-one definition is
  `sha256:d8c4da38c2f5d4516b7eb59cbfc69acdeb6834fcde9d934aa3141a4df89f3d44`; staged max-two is
  `sha256:70f36a5b5140b0c3e1063a775ab0cb5030d7024931a1f8fa57bbabb962a6208b`.
  The exact combined proposal is
  `sha256:6bc0cef713615f5bdd47b85a5903249644f514f7666956941d5435288d6bd99c` at
  `evidence/acceptance/VF-10-07/2026-08-20-template-identity-requalification-candidate/combined-live-proposal.json`.
  It keeps FlashBoot=true, LOW EU-RO-1, the exact already-published image, RTX 4090 only, the exact
  Mage volume, no republication, the bounded cold/warm/two-reader proof, identity-locked cleanup,
  retained endpoint/template on success, both intended volumes retained in all outcomes, current
  Flex `$1.10/GPU-hour`, and existing `$7/month` volume billing separately.
- The proposal estimate remains `$0.90–$1.65` incremental and
  `$1.0248003321234138–$1.7748003321234138` cumulative from the settled
  `$0.12480033212341368` baseline. Its cap is deliberately null. No provider call, mutation, GPU
  use, or spend is authorized until the user approves this exact proposal and supplies a fresh
  positive numeric maximum cumulative finite spend. V2-07 remains `NOT_QUALIFIED`; V2-08 is
  forbidden.

## Fresh template-identity requalification authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:6bc0cef713615f5bdd47b85a5903249644f514f7666956941d5435288d6bd99c`
  with FlashBoot=true and LOW EU-RO-1 availability, and supplied a fresh `$4` maximum cumulative
  finite spend cap. The append-only authority record is
  `evidence/acceptance/VF-10-07/2026-08-20-template-identity-requalification-candidate/approved-authority.json`.
  Its exact bytes are
  `sha256:7f36db5a22aa3c1b347d45e75199d3e758fdcdc5b4aff788e68e6e9875ee0462`.
- This authority covers only the proposal's ordered read-only refresh, exact image readback,
  scoped Cloudflare activation, template-env/endpoint-config identity binding, max-one cold/warm
  complete batches, duplicate/cancel/timeout behavior, separately hashed max-two concurrent-reader
  proof, durable outputs/receipts, manifest/write checks, scale-down, settlement, and exact cleanup.
  Image republication, model or volume mutation, fallback, public sample publication, and V2-08 are
  forbidden. Existing two-volume billing remains `$7/month` separately.
- Provider execution remains pending until the authority/context/compiled activation record is
  validated and committed. V2-07 remains `NOT_QUALIFIED` before live acceptance proof.

## Attempt 17 — endpoint PATCH schema failure and exact cleanup — 2026-08-21

- Exact proposal `sha256:6bc0cef713615f5bdd47b85a5903249644f514f7666956941d5435288d6bd99c`
  ran under its fresh `$4` cap. Cloudflare activation and preflight passed. RunPod created the exact
  template and endpoint and updated the template endpoint-identity environment, then endpoint
  `PATCH` returned `RUNPOD_MUTATION_FAILED` before `/run`, any GPU job, batch, output, or inference.
- The fail-closed transport did not retain provider status/body. Comparing the request to the pinned
  official PATCH schema identifies `computeType` as the remaining undocumented request field; this
  is a bounded inference, not a provider-confirmed error detail. Provider-free control removes it
  while keeping exact compute-type response and GET-readback verification.
- Narrow recovery validated the exact image, endpoint/template identity, endpoint hash binding,
  two stable terminal snapshots, GPU/region/volume/config fences, and both intended volumes before
  deleting only the disposable endpoint and template. Three later stable read-only inventory and
  billing reads prove zero Pods, endpoints, private templates, running Pods, and active workers;
  both intended 50 GB EU-RO-1 volumes remain, and cumulative endpoint billing stayed
  `$0.12480033212341368`, so Attempt 17 settled at `$0` under the cap. Existing volume billing is
  still `$7/month` separately.
- Cloudflare deleted the signer, restored the exact Worker version hash, and proved 16 consecutive
  `404 V207_ROUTE_DISABLED` fingerprints over 30 seconds. Append-only evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-17.json`.
- Attempt 17 consumed and closed the exact proposal and `$4` authority. V2-07 remains
  `NOT_QUALIFIED`; every cold/warm/complete-batch/durable-output/cancel/duplicate/two-reader/VRAM/
  timing/volume-write gate remains unproven. A fresh exact proposal and fresh positive numeric cap
  are required before retry. V2-08 remains forbidden.

## Provider-free Attempt 17 repair and fresh proposal — 2026-08-21

- Commits `0dbf8f3` and `253723c` remove `computeType` from the endpoint PATCH body because the pinned official
  RunPod PATCH request schema omits it. The same control still requires GPU compute type in the
  PATCH response and GET readback and preserves every image, model, volume, region, CUDA,
  FlashBoot, timing, scaler, authority, output, cancellation, cost, and cleanup fence.
- Cleanup now binds template endpoint-hash state across terminal snapshots, rejects post-delete
  binding drift, validates exactly both intended 50 GB EU-RO-1 volumes, and fails if any Pod,
  endpoint, private template, running Pod, or active worker remains. Failure cleanup now invokes
  three stable zero-compute and billing reads using the fresh attempt's captured baseline. Focused
  proof passes 167/167 plus typecheck, lint, context validation, secret scan, formatting, and diff
  checks.
- Fresh combined proposal
  `sha256:2752b61dfe4481eaa15ef349f859d91650160971a828d7d19af2638f7c8715be`
  is at
  `evidence/acceptance/VF-10-07/2026-08-21-patch-schema-requalification-candidate/combined-live-proposal.json`.
  Its fresh max-one/max-two definitions are `sha256:c75c3193a32047b5dc7fb47dd20c1bdcd81527be133d41504447a99d2335b865`
  and `sha256:b72472a69a7ae74202f40591c1e706afdf3c6be3a0cf0e52ebb4462196efeec5`,
  both binding control commit `253723c`. It keeps the already-published immutable image, exact Mage
  volume, RTX 4090-only LOW EU-RO-1 Flex policy, `$1.10/GPU-hour` rate, existing
  `$7/month` retained-volume charge separately, and `$0.90–$1.65` incremental estimate. Its cap is
  null; approval is recorded separately without changing proposal bytes. Authority
  `sha256:bd077b2ae63fcf60a6e9c7dca0b95c777f360f28c9c53a7e7cf1d2dcca60e11c`
  approved the exact proposal, FlashBoot=true, LOW EU-RO-1, and a fresh `$4` cumulative finite cap.
  Attempt 18 consumed and closed that authority; V2-08 remains forbidden.

## Attempt 18 — PATCH response binding mismatch and exact cleanup — 2026-08-21

- Exact proposal `sha256:2752b61dfe4481eaa15ef349f859d91650160971a828d7d19af2638f7c8715be`
  ran under its fresh `$4` cap. Cloudflare activation/preflight passed. RunPod created the exact
  private template and max-one endpoint, updated the exact template environment, and accepted the
  endpoint `PATCH`. The PATCH response failed the complete endpoint configuration matcher before
  GET readback or dispatch with `RUNPOD_ENDPOINT_ID_BINDING_UNCONFIRMED`.
- No `/run`, GPU job, batch, output, or inference occurred. The provider body was not retained, so
  the exact mismatched response field is unproven and no field-level root cause is claimed.
- Initial cleanup was uncertain. The separately hardened cleanup proved two stable terminal
  snapshots with one terminal worker record and one terminal Pod record, then deleted only the
  exact disposable endpoint/template. Three later stable reads prove zero Pods, endpoints, private
  templates, running Pods, and active workers and exactly both intended retained volumes.
- Cloudflare deleted the signer, restored Worker version hash
  `sha256:0ca150f857bbbbd63b474e220a008511805daf337ae7d0154489046e57419e53`,
  and proved the original 404 `V207_ROUTE_DISABLED` fingerprint continuously for 30 seconds.
  Cumulative endpoint billing remained `$0.12480033212341368`; Attempt 18 settled at `$0`.
- Append-only evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-18.json`. The proposal
  and authority are consumed and closed. Every complete-batch/output/timing/VRAM/two-reader and
  post-worker volume proof remains open. Provider-free repair and a fresh exact proposal are now
  complete; fresh approval and a fresh cap are required before retry. V2-08 remains forbidden.

## Partial PATCH acknowledgement repair proposal — 2026-08-21

- Commits `e09a2d0bf2cae873eb49ac545241baa427bbfa05` and
  `9331845d529fd54a8ec3afa5e2406e7c1ebb77bc` separate the PATCH response from the
  authoritative GET. PATCH must return the exact endpoint ID; every known field it does return must
  match the staged config, and omitted fields remain unconfirmed. Missing identity or any conflict
  stops before GET. The following GET must still exactly match the full template, GPU, CUDA,
  FlashBoot, volume, region, worker, scaler, timing, and endpoint-hash environment configuration.
- Focused control tests pass 47/47, including partial acknowledgement success, conflicting field,
  missing endpoint identity, and complete GET drift. TypeScript and lint pass. No provider call,
  mutation, GPU use, or spend occurred. Failure reconciliation now also requires the fresh approved
  maximum cumulative cap and fails closed if the baseline is already above it or three stable final
  billing reads settle above it.
- Fresh proposal is
  `evidence/acceptance/VF-10-07/2026-08-21-partial-patch-acknowledgement-candidate/combined-live-proposal.json`,
  exact hash `sha256:ce11e4efb3b97f47c9ca70f83451ce6535e8467ac506b682527466f9327dafde`.
  Max-one is `sha256:da2f5a1ee7f412014ecc7b63911131da02b4b313bca1079f9471fef1fb807347`;
  max-two is `sha256:889110673da3cfef09d8a534208acfc7bf8d980995ba1ad4ee1eec9c4ac95160`.
  Proposal bytes retain a null cap; approval was recorded separately without changing them.
  Authority `sha256:b824bea61e30c4ad1b5eda4bf8113c390c0ae0eff0a03c6fb279210e81d9e5c2`
  approved the exact proposal, FlashBoot=true, LOW EU-RO-1, and a fresh `$4` cumulative finite cap.
  Attempt 19 consumed and closed it; V2-08 remains forbidden.

## Attempt 19 — GET readback mismatch and exact cleanup — 2026-08-21

- Exact proposal `sha256:ce11e4efb3b97f47c9ca70f83451ce6535e8467ac506b682527466f9327dafde`
  ran under its fresh `$4` cap. The exact-ID, non-conflicting endpoint PATCH acknowledgement passed.
  The required complete GET readback then failed with
  `RUNPOD_ENDPOINT_ID_BINDING_READBACK_UNCONFIRMED` before dispatch.
- A late independent audit found the recorded authority claimed publication-evidence hash
  `sha256:0191b33d5852484ec5802abde21c02a8f3753085918c012710047532615e216e`,
  while the unchanged file actually hashes to
  `sha256:0191b33d692775f0877ac07cc126c6476d51cafaf37d8b8dac26f7da629e216e`.
  The orchestrator was stopped and its owned cleanup completed separately.
- No `/run`, GPU job, batch, output, or inference occurred. Two stable terminal snapshots allowed
  deletion of only the exact disposable endpoint/template. Three later reads prove zero
  Pods/endpoints/templates/workers, both intended 50 GB EU-RO-1 volumes, unchanged cumulative
  billing `$0.12480033212341368`, and `$0` Attempt 19 spend.
- The signer is absent, the captured Worker version hash is restored, and 16 consecutive exact 404
  `V207_ROUTE_DISABLED` probes passed over 30 seconds. Append-only evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-19.json`.
- Every batch/output/timing/VRAM/two-reader/post-worker-volume proof remains open. Provider-free
  diagnosis plus a fresh exact proposal and fresh numeric cap are required before retry.

## GET readback optional-fields repair proposal — 2026-08-21

- Commit `b35f4a60fe99d6b5649797c7aaaae7af4ef1368d` treats omitted GET `computeType`,
  `dataCenterIds`, and the duplicate `networkVolumeIds` alias as documented REST omissions. Any
  present alias value and the mandatory primary `networkVolumeId` must be exact; either compute or
  data-center field still fails if explicitly wrong;
  endpoint/template identity, workers, RTX 4090, CUDA, FlashBoot, volume, scaler, timing, and the
  complete environment remain exact. Focused control/authority/reconciliation tests pass 64/64.
- Fresh proposal is
  `evidence/acceptance/VF-10-07/2026-08-21-get-readback-optional-fields-candidate/combined-live-proposal.json`,
  exact hash `sha256:9e9675dcf6943dce35b4bf6155fdfc39f8dade5e9775bcc3ee9a427980d39e02`.
  Max-one is `sha256:76a2b5406115f1060cd72b1fccda9e02a2fdccb17450c8e4b1aae73cbea67f13`;
  max-two is `sha256:3d53d9d44540575d164cc33e2faa73587245812d9ad9002965f8fa50ad34aae8`.
  It binds exact publication evidence hash
  `sha256:0191b33d692775f0877ac07cc126c6476d51cafaf37d8b8dac26f7da629e216e`
  and Attempt 19 evidence `sha256:f5258f5d8f258dba27f966c19eda91e7652c00544d6c9290c802fb92eb6e2e27`.
  The cap is null and exact fresh approval is required. V2-08 remains forbidden.

## Fresh GET readback optional-fields authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:9e9675dcf6943dce35b4bf6155fdfc39f8dade5e9775bcc3ee9a427980d39e02` with
  FlashBoot=true and LOW EU-RO-1 availability, and supplied a fresh maximum cumulative finite
  spend cap of `$4`. The append-only authority record is
  `evidence/acceptance/VF-10-07/2026-08-21-get-readback-optional-fields-candidate/approved-authority.json`,
  whose exact bytes are `sha256:ac8f45bdb3d5429fa3b93e9624f62242f026ced07f19f28d740503dccfd8f56d`.
- This authority permits only the proposal's ordered read-only refresh, exact immutable-image
  verification, scoped Cloudflare activation, exact template/endpoint creation, partial PATCH
  acknowledgement plus documented optional-field GET readback, max-one cold/warm complete batches,
  duplicate/cancel/timeout behavior, separately hashed max-two concurrent readers, durable output
  receipts, manifest/write checks, scale-down, settlement, rollback, and exact cleanup. Image
  republication, model or retained-volume mutation, fallback, public sample publication, and V2-08
  remain forbidden. Existing two-volume billing remains `$7/month` separately outside the cap.
- Historical provider execution was authorized but later consumed by Attempt20. V2-07 remains `NOT_QUALIFIED` until
  the live output, timing, VRAM, volume-integrity, cost, and independent zero-worker drain evidence
  is complete. Stop on any authority, identity, output, cleanup, route, rate, capacity, or cap mismatch.

## Attempt 20 closure — 2026-08-21

- The exact authority was consumed. Cloudflare activation and preflight passed; RunPod created the
  exact template and endpoint, but the post-PATCH GET matcher returned
  `RUNPOD_ENDPOINT_ID_BINDING_READBACK_UNCONFIRMED` before `/run`. Zero GPU jobs, batches, outputs,
  and inference occurred.
- The initial runner cleanup was uncertain. The narrow exact cleanup then proved two stable terminal
  snapshots and deleted only the disposable endpoint/template. Independent reconciliation proved
  zero Pods/endpoints/private templates/workers/running Pods, both intended 50 GB EU-RO-1 volumes,
  and three stable billing reads at `$0.12480033212341368`; Attempt 20 increment is `$0`.
- The signer is absent, the original Worker version hash is restored, and the exact disabled-route
  fingerprint was stable for 16 consecutive probes over 30 seconds within the 120-second deadline.
  Attempt 20 evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-20.json` at
  `sha256:82aae2abf02041620c18d6a016719bab0f92ef41ed77430c2239ebfab005a37d`.
  V2-07 remains not qualified; the consumed proposal/cap cannot be reused; V2-08 remains forbidden.

## Attempt 21 closure — 2026-08-21

- The exact Attempt21 authority consumed proposal
  `sha256:13acabaed3c21b3a15fcca203072c211f9002057453d4cd9b0fb5a765444d2d4` under authority
  `sha256:bc7580ad3f4782504587904115abb76738da72e3f2a048314a959475ef7316ec`, FlashBoot=true,
  LOW EU-RO-1, and a fresh `$4` cumulative cap. Cloudflare activation/preflight passed. RunPod
  created the exact private template and endpoint, then failed the strict GET binding before
  `/run` with `RUNPOD_ENDPOINT_ID_BINDING_READBACK_UNCONFIRMED`; the persisted redaction-safe
  `error_category` is `environment`.
- No `/run`, GPU job, batch, output, or inference occurred. Measured spend and settled increment
  are `$0`. The initial cleanup was uncertain, but narrow exact cleanup proved two stable terminal
  snapshots, one terminal worker/pod record, endpoint/template deletion, and final disposable
  resource absence. Both intended 50 GB EU-RO-1 volumes remain retained and untouched.
- Reconciliation at `2026-08-21T07:27:54.866Z` proved zero Pods/endpoints/private templates/active
  workers/running Pods, both retained volumes, unchanged cumulative endpoint billing
  `$0.12480033212341368`, and three stable reads. The signer is absent, the Worker hash is restored,
  and 16 exact 404 `V207_ROUTE_DISABLED` fingerprints were stable over 30 seconds.
- Closure evidence is
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-21.json` at
  `sha256:cd7200aca5f532a3e9062b37c296cf412bce974605f44278156c23674710bd68`. V2-07 remains
  `NOT_QUALIFIED`; the proposal and authority are consumed and non-reusable. Every complete
  batch/output/timing/VRAM/two-reader/post-worker-volume gate remains unproven. Any retry requires
  provider-free diagnosis changes plus a new exact proposal and fresh positive numeric cap; V2-08
  remains forbidden.

## Attempt 22 template-environment readback candidate — 2026-08-21

- Provider-free control commit `54af72f1e9a29eed7f53e47ecdda9f6a34abb7df` now performs the exact
  template environment proof required before endpoint binding: POST the endpoint-hash environment
  update, GET the exact template identity and environment, then PATCH only documented endpoint
  fields and require strict endpoint GET configuration readback. Endpoint `env` omission is allowed
  only after the template proof; any present endpoint environment must match exactly. A mismatch
  persists only its bounded redaction-safe category and stops before `/run`.
- The fresh provider-free candidate is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt22-template-environment-readback-candidate/combined-live-proposal.json`
  at `sha256:96ead6591874229d93537af46a3159002e2fe86c93cc2905c42bbb1326ccece7`. It binds the
  unchanged published immutable image, Mage manifest, sealed 50 GB EU-RO-1 Mage volume at
  `/runpod-volume`, FlashBoot=true, RTX 4090 only, max-one then separately hashed max-two readers,
  and Attempt21 closure evidence `sha256:cd7200aca5f532a3e9062b37c296cf412bce974605f44278156c23674710bd68`.
  Staged definitions are max-one
  `sha256:cb3fb55861caefb70aea10670b7be3483cf69ea00b82030f5f64d0f4a8bbe22c` and max-two
  `sha256:9e8ceb943d257f66d336c64d5ffc2f3df349a76fc98d54c391435d0d8cea72bd`.
- The candidate was subsequently approved and consumed by Attempt22. It is retained as immutable
  historical proposal evidence and cannot be reused. Existing two-volume billing remains
  `$7/month` separately. V2-08 remains forbidden.

## Fresh Attempt22 template-environment readback authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:96ead6591874229d93537af46a3159002e2fe86c93cc2905c42bbb1326ccece7` with FlashBoot=true,
  LOW EU-RO-1 availability, and a fresh maximum cumulative finite spend of `$4`.
- The append-only authority is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt22-template-environment-readback-candidate/approved-authority.json`
  at `sha256:fecdfa6dee640d483a1787a726723bef08cdeaf455f5b7df0a2fbcdf3c3699f6`. It binds the
  exact published image, control commit `54af72f1e9a29eed7f53e47ecdda9f6a34abb7df`, Mage model
  manifest, sealed 50 GB EU-RO-1 volume at `/runpod-volume`, RTX 4090, FlashBoot=true, max-one
  then separately hashed max-two reader configs, operations, cleanup, retention, and V2-08
  prohibition. That authority is now consumed and non-reusable.
- Attempt22 passed template/endpoint GET readback and dispatched one owned job, but the completed
  provider job produced no accepted application batch/output receipt. Execution stopped on the
  failed-output condition, cleanup/reconciliation completed, and V2-07 remains `NOT_QUALIFIED`.

## Attempt23 output-contract diagnostic authority — 2026-08-21

- Provider-free control repair `9f5a15c3382c03af675392dacc487b96811674ed` retains only the bounded
  `output_contract` category plus safe output status, failure-code, shape-kind, and shape-key facts
  when the first completed provider job is non-success. Provider bodies, raw output, unsafe fields,
  and secrets are not retained. The run stops immediately with no retry, warm batch, concurrent
  reader, or duplicate dispatch after that failure.
- The approved candidate is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt23-output-contract-diagnostic-candidate/combined-live-proposal.json`
  at `sha256:386dd8330f8e626d9afe8c8de8bbd1385fd9664b9fefbc472c24722105f917f9`. It binds the
  unchanged published image/source, exact Mage manifest and sealed 50 GB EU-RO-1 volume at
  `/runpod-volume`, FlashBoot=true, LOW EU-RO-1, RTX 4090 only, and max-one then separately hashed
  max-two configurations. Max-one is
  `sha256:45f8d447829d63517b78807ce710af7fbd81a9ff06d67cafe1a5a6bf37a15959`; max-two is
  `sha256:6b02604fd7a58ee98c350429663c038bbc5c93ea2e0786e64ac3a6ef3f476e8b`.
- Attempt22 closure evidence remains the prior boundary:
  `evidence/acceptance/VF-10-07/2026-08-20-live-qualification/failed-attempt-22.json` at
  `sha256:43f9db51e67a39e4a837614be5af14299d91c4fbdd446b9d78ecc51260da517a`. The user approved
  FlashBoot=true, LOW EU-RO-1, and a fresh USD 4 cap; authority
  `sha256:c59bd74673263eeeafed828dade74fe36ae2f27ed7914d413e37bfd6722a3b35` is recorded. Provider
  execution remains pending; existing two-volume billing remains `$7/month` separately.

## Attempt 24 verification-stage diagnostic candidate — 2026-08-21

- Provider-free control commit `63517e605d441fa23020bea8bff2987cc4bc99c5` extends the strict
  output-contract boundary with a structural brand and a bounded `output_failure_stage` allowlist:
  top-level, item count, authority count, receipt presence/hash/signature/identity, output lineage,
  readback, PNG probe, finalization, finalization replay, or unknown. Unsafe, unbranded, or
  provider-body fields fail closed; no raw output or provider response body is retained.
- The new candidate is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt24-verification-stage-diagnostic-candidate/combined-live-proposal.json`
  at `sha256:be17430ce61a48a823a1ac87a128e83e44cfb88b01163331c285280e95274137`. It binds the
  unchanged published Mage image/source, exact model manifest, sealed 50 GB EU-RO-1 Mage volume
  at `/runpod-volume`, FlashBoot=true, LOW EU-RO-1, RTX 4090 only, and separately hashed
  max-one `sha256:345072150945c7dfa686c6b90b36565accd65ad5666f5c2917e160d5cf9f308a` and max-two
  `sha256:173e52dde1443d61f9a678e54ff859f2709797a3f4aa818f0402772887c2be8a` configurations.
- Attempt23 closure remains the exact prior boundary:
  `failed-attempt-23.json` at
  `sha256:0f48f3bc82b6d0b7fb48e723c4a3fc36a142129de578447acd30d77157e1ca1b`. The candidate has
  an exact recorded authority and fresh `$4` cap. Provider mutation and GPU use are authorized
  only for this proposal; V2-07 remains `NOT_QUALIFIED` until live gates pass and V2-08 remains
  forbidden.

## Fresh Attempt24 verification-stage diagnostic authority — 2026-08-21

- The user explicitly approved exact proposal
  `sha256:be17430ce61a48a823a1ac87a128e83e44cfb88b01163331c285280e95274137` with FlashBoot=true,
  LOW EU-RO-1 availability, and a fresh maximum cumulative finite spend of `$4`.
- The append-only authority is
  `evidence/acceptance/VF-10-07/2026-08-21-attempt24-verification-stage-diagnostic-candidate/approved-authority.json`
  at `sha256:fccd60a68ee93f522d9e378012c5ccbefb182f6b03e26fde1b5940506ab9c412`. It binds the
  exact immutable image/source/control, Mage manifest, sealed 50 GB EU-RO-1 volume at
  `/runpod-volume`, FlashBoot=true, RTX 4090, max-one then separately hashed max-two reader
  configurations, bounded output diagnostics, exact operations, cleanup/rollback, retention,
  and V2-08 prohibition. The proposal bytes remain unchanged.
- Provider mutation, scoped Cloudflare activation, endpoint/template creation, and GPU use are
  authorized only for this exact proposal and `$4` cap, after this authority commit. Image
  republication, model download/quantization, retained-volume mutation, fallback GPU/region,
  public sample publication, V2-08, and successor work remain forbidden. V2-07 remains
  `NOT_QUALIFIED` until all live output, receipt, integrity, timing/VRAM, two-reader, cost,
  cleanup, and independent drain gates pass.
