# VF-10-07 — V2-07 Mage Serverless qualification

Status: `NOT_QUALIFIED`; Attempt56 consumed and non-reusable after one queued probe; exact cleanup
and reconciliation prove zero compute/disposable resources, executable cap `$0`, V2-08 forbidden

## Reader contract

This brief contains only the active boundary. Historical attempt narration is archived by Git and
immutable evidence. Do not preload or restore it. Follow only the current pointers below; open one
historical artifact only if a current failure or audit explicitly reaches it.

## Closed Attempt56 live qualification — queued probe, clean terminal state

- Repair commit `5c2fbe06ba559543c122876d32ef41cb26fd688b` binds the executable orchestrator anchor
  constants, staged configs, proposal, and validator gate to the fresh active Cloudflare anchors.
- Exact proposal:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt56-anchor-constant-rebind-candidate/combined-live-proposal.json`
  at `sha256:d3c10f7af00591dea0afe73d2960b316a788235bb2585decab6ca479b4ce9ab9`.
- Candidate acceptance `sha256:3ddaf8f9fde45f93de0a9c4a770a09bc117168ff27dbd2ec312e7516cf9c094d`; read-only
  preflight `sha256:4a1f95ee496afabedc33ca148bee8543010600ea302966f4b66b8b23c2425373`; max1
  `sha256:6bba5f707e19352b2935129429665f1a488f241065c9c84fe814a2f8677dae7a`; max2
  `sha256:19d4824c205b5c3c17edc351d7762578b249caee497d60ec4d8ff762fd41b37b`.
- The exact `$4` authority was used once. One job remained `IN_QUEUE` for 34 status reads, then was
  cancelled. No execution/delay time, GPU use, accepted batch, durable output, readback, or receipt
  was recorded. Primary stop: `V207_QUALIFICATION_FAILED`.
- Closed authority is `sha256:6a82d8ae1bf7c3a987b941cd00153a7cc9a51c4d589aaa537aa88f854d5de5ac`
  (approved pre-execution hash `sha256:c708d07d31cb832658de0e9a3ac0e9fecceffcf3f7571d690bdfd8605617bdf2`).
  Executable authority, cap, and anchor-refresh bindings are null; Attempt56 cannot be retried.
- Closure `failed-attempt-56.json` is `sha256:9465f6a19ef02bc763900cb14fdeaa89c2dd9a36f46d4ffcbce1022ded6731dd`;
  cleanup is `sha256:b7ded64662bd0d1b20cb0da05347f0ec96ab4620f8f73c02612e7ea3f4f5bfc8`;
  reconciliation is `sha256:e1d71ed322f705c49f31c7be0fddd056974e0e919f19911b22528aadaa2cfd3d`.
- Three stable reads prove zero Pods, endpoints, private templates, workers, and running Pods. The
  exact signer is absent, protected config is restored, and the refreshed route is exact POST `404
  V207_ROUTE_DISABLED`. Both 50 GB EU-RO-1 volumes remain retained at `$7/month` total.
- Measured and three-read observed billing increment is `$0`, subject to provider billing lag;
  projected spend was `$0.997638888888889` and worst-case active liability was
  `$0.7531944444444445`, within the approved cap.
- Provider-free diagnosis
  `evidence/acceptance/VF-10-07/2026-08-21-live-qualification/attempt56-queue-stop-diagnosis.json`
  (`sha256:247c8c6b1bc1f5f3ce09978ca178d70e4d323a00448e42d97e766c483dabf922`) proves the
  run stopped after 34 of 180 allowed reads, so the reconciliation timeout was not reached. The
  exact immediate exception and provider-capacity root cause remain unproven.
- Repair `c2086e1a4d0f54adf50848fe5b0ddf9f75962b03` adds bounded retry and named fail-closed
  classification for billing reads plus named checkpoint-persistence failure while preserving
  owned-job cleanup and forbidding redispatch. Focused validation passed 129/129 tests and
  TypeScript with no provider call, mutation, GPU use, or spend.
- Provider-free work is complete. Any paid retry requires a fresh exact proposal, fresh approval,
  fresh positive cap, and re-confirmed provider truth. Do not create Attempt57 or start V2-08 under
  this closed boundary.

## Closed Attempt55 pre-execution boundary

- Exact proposal:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt55-config-lineage-rebind-candidate/combined-live-proposal.json`
  at `sha256:d3481231340fb7a4a22ae1047103024ca37e6b75c70c37d36c3bfb2a9baaff1f`.
- Exact single-use authority:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt55-config-lineage-rebind-candidate/approved-authority.json`
  at consumed closure hash `sha256:47e06a69ece45babed6e8c1c32544d13033680901995ba77cdfc70e4cbd70818`
  (pre-execution approval record `sha256:966159fc2afa2cf0a6365c5e3b017041effbf5b1f4f312aa4fde310f3d921c0d`);
  approved at `2026-08-24T15:03:31Z`, consumed at `2026-08-24T15:12:28Z`, and non-reusable.
- Fresh max1 `sha256:62167243073a7c35c9b7b0d2701971fae2da1366a56aa1b6a303b67c1f2fa368`
  and max2 `sha256:551b30c3e669d4127390bf5cb9b7c6c1a3ce9cb76a9bd9b400350a6f4e2d48ee`
  both bind repaired control `85391b1`, orchestrator `sha256:5298ec25…fc42c`, canonical
  activation `sha256:36a23948…a6b3`, and the exact version-bound route contract.
- Fresh read-only truth remains zero RunPod compute/disposable resources, two retained volumes, LOW
  RTX 4090 EU-RO-1 availability, `$1.10/GPU-hour` Flex, `$0.74/hour` Secure reference, billing
  baseline `1.7478178361488972`, and exact active-version-bound POST `404 V207_ROUTE_DISABLED`.
- Requested approval: FlashBoot=true, LOW-or-better EU-RO-1, `two-phase-v1` anchor refresh, `$4`
  maximum cumulative finite cap, and continued `$7/month` retention of both existing volumes. The
  exact approval was recorded, but pre-execution validation closed the authority at
  `V207_EXECUTABLE_ANCHOR_LINEAGE_MISMATCH`; no provider call, mutation, job, GPU use, or spend
  occurred. Attempt54/55 authority or cap reuse is forbidden.
- Executable finite cap after closure is `$0`; external spend is `$0`; both existing volumes remain
  retained at `$7/month`; V2-08 remains forbidden.
- Smallest next action: one provider-free micro-checkpoint to update executable anchor constants and
  fully bind the validator/config/proposal to them, then create a fresh exact proposal and request
  fresh approval. Do not run a provider qualification under Attempt55.

## Closed Attempt54 pre-execution boundary

- Exact proposal:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt54-version-bound-rollback-candidate/combined-live-proposal.json`
  at `sha256:5a61facbadeb460ee8dff2072a9c5c54abccd0267c60f1cefbb5aad2194a0e4b`.
- Candidate acceptance `sha256:db91d6143f3d4d551cf7b44b7c6f33100195bda36bfc78785ad175cd3394858c`;
  read-only preflight `sha256:94e84a0ee7abe1d880b723782e5129b341176ee7655d58a20391c5c2e88618f3`.
- Control commit `85391b130673200e2d1f74fea4ea2581d5d83c1a`; orchestrator
  `sha256:5298ec25000e42affb7e36b518b778fa692e65b72d868087d58fbd81e6cfc42c`.
  Every refresh pre-route, refreshed-disabled, post-promotion, and refresh-cleanup route read now
  requires the exact expected Worker version. Missing, stale, alternating, and mixed identities fail
  closed; non-refresh behavior remains compatible; evidence retains only hashes. Focused tests pass
  55/55 and web TypeScript passes.
- Fresh read-only truth: exact POST `404 V207_ROUTE_DISABLED` carries the active Worker version,
  RunPod has zero Pods/endpoints/templates/workers, both existing volumes remain, RTX 4090 EU-RO-1
  availability is `LOW`, Serverless Flex is `$1.10/GPU-hour`, Secure reference is `$0.74/hour`, and
  fresh cumulative endpoint billing is `1.7478178361488972` with zero increment across the two
  preflight reads. The late `$0.1023716396884992` since the prior preflight is absorbed into the new
  baseline and remains subject to provider billing lag.
- The exact approval was recorded, then pre-execution validation found that the proposal reused the
  Attempt53 max1/max2 files. Their embedded control commit `3b40fd3`, orchestrator
  `sha256:358a5b7c…f1d9c`, and canonical activation `sha256:ce890309…f63aa` do not match repaired
  control `85391b1`, orchestrator `sha256:5298ec25…fc42c`, and current canonical activation
  `sha256:36a23948…a6b3`. Stop code: `V207_STAGED_CONFIG_CONTROL_LINEAGE_MISMATCH`.
- Attempt54 authority is consumed and non-reusable. No provider call, mutation, job, GPU use, or
  spend occurred; activation authority/cap/refresh are null. Blocker evidence
  `blocked-attempt-54-staged-config-lineage-mismatch.json` is
  `sha256:b225757a6b5e9c7f6a76d6d1c0fcc9f10018b3a091caeba0a56088aa8c93903f`.
- Smallest next action: create new max1/max2 definitions bound to the repaired lineage, recompute a
  fresh exact proposal, and request fresh approval. Do not reuse Attempt54 authority or cap.

## Closed Attempt53 handoff

- Proposal:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt53-active-anchor-rebind-candidate/combined-live-proposal.json`
  at `sha256:5c2023d6451284b9ccdf64112e3bee6d8417ed6b0fa796520875ea080eead75f`.
- Candidate acceptance:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt53-active-anchor-rebind-candidate/acceptance.json`
  at `sha256:a04962398607efcd8350e14417f86c185f2c2e5b8146286edf8843712d433b99`.
- Read-only preflight `sha256:86befdc3eee48ec030b73848fb2133f1be9f7ed2f76023bbc9b7d920b2a4fb55`;
  max1 `sha256:2b9f6a9f5d3220491996c5b910205cdb18f21655735fcd1c8a1e40b14a032d1e`;
  max2 `sha256:f897321d10e9d4a839f9bc675c24351fbc8b73c98bfb93a64c261a5359db0ff4`.
- Control commit `3b40fd36767acde51ed979155a67ec4555a76400` binds the fresh retained active
  Worker anchor hashes while preserving the unique matching signed terminal Pod, queue-empty,
  two-identical-snapshot, all-terminal, zero-active-worker, zero-running-Pod, and no-redispatch
  fences.
- Exact runtime: Mage-Flow INT8 ConvRot
  `Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6`, manifest
  `sha256:cebcd5c6233c2eae32f26ced7510acef8192f0d92d7ec3e9dd3ee881d66d205b`, immutable
  image `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:79fe7e40b69c011c15cc31b2d84b356cd2c755ea338976172cd78cc581304d59`.
- Exact resource: existing sealed Mage-only 50 GB `EU-RO-1` volume at `/runpod-volume`,
  application-read-only, RTX 4090 Flex, FlashBoot true, no fallback.

Attempt53 was approved under authority `sha256:d8087eb316fd9d0f32d77db6343457a836a56f1d40c5d51cb2a2d89e98ceeefb`
binds proposal `sha256:5c2023d6451284b9ccdf64112e3bee6d8417ed6b0fa796520875ea080eead75f`,
FlashBoot true, LOW-or-better EU-RO-1, exact `two-phase-v1` anchor refresh, a `$4` maximum cumulative
finite cap, and continued `$7/month` retention of the two existing volumes. It is consumed and
non-reusable at `sha256:4f09ae611ffa2181ce8660a3d41c05862a082471a862821ec29fbdb18faea44a`.
The repaired unique signed terminal-Pod boundary passed. Probe, unresolved-only resume, and cold
batches accepted 64 durable units before the warm job failed at status reconciliation after its
first `IN_QUEUE` read with `V207_QUALIFICATION_FAILED`. The exact warm job was cancelled, generated
outputs rolled back, and the disposable endpoint/template deleted after two stable terminal
snapshots. Three RunPod reads prove zero Pods/endpoints/templates/workers, both volumes retained,
and `$0` immediate observed billing increment, subject to provider billing lag. Closure
`failed-attempt-53.json` is
`sha256:490c178e6b0c63d01a905f4382f39fbe00cf514d8f0bb01e825a2be52bffed87`.

Cloudflare cleanup is not accepted: the orchestrator captured immediate `404 V207_ROUTE_DISABLED`,
but the required later read-only handoff probe is stably `503 HOSTED_ROUTE_NOT_COMPOSED` across
three reads. Provider-free diagnosis `attempt53-rollback-fingerprint-diagnosis.json` at
`sha256:fcfadc8bd7202f3bcf7cba3403bc5935e8fbaa53a758b3002114b722c5cd3f80` proves the cleanup path
separately validates the control-plane rollback target and data-plane status/code, but does not
require the disabled/restored route responses to carry that exact Worker version. The later 503 has
the V2-06 runtime header and no Worker-version header, matching the uncomposed production adapter.
Stop external work. The smallest next action is one provider-free orchestrator repair micro-checkpoint
that binds refresh-disabled and cleanup reads to the exact rollback-target version and adds focused
missing/stale/alternating-version tests. Any remote repair or paid retry needs a fresh exact proposal
and approval.

Attempt52 authority
`sha256:3b15fca048ba4eb570a27b5a6e6d521a415e27c556c230757b6702e0c1017453` is recorded as
single-use pending execution with FlashBoot true, LOW-or-better EU-RO-1, exact two-phase-v1 anchor
refresh, a `$4` maximum cumulative finite cap, and continued `$7/month` retention of the two
existing volumes. It was consumed when execution stopped at
`V207_ROLLBACK_ANCHOR_REFRESH_OLD_ANCHOR_MISMATCH` before any Cloudflare/RunPod mutation, job, GPU
use, output, or observed spend. Closure `failed-attempt-52.json` is
`sha256:267918ea297d63e614107815a01f2a45192be85495752008c375c229db98dec0`; reconciliation
`sha256:83dbb0790059de5215ea857849b5897f1deee474ed5d4f02727b26f30cf32876` proves zero disposable
resources/workers/Pods, both retained volumes, unchanged billing, baseline config mode 0600, and
POST `404 V207_ROUTE_DISABLED`. Attempt51 authority
`sha256:61e00a70cb64c1e467a2d2877f67a20597b5a17e19a1ffa7a9a767ef06f52ea9` was consumed once and is
non-reusable. Its closure `evidence/acceptance/VF-10-07/2026-08-21-live-qualification/failed-attempt-51.json`
at `sha256:c2ac52ad7b5600c472be09be6d1ba5376194c9a4e1f192242ec281b686eab02a`
records one durable probe followed by `RUNPOD_PROCESS_REPLACEMENT_WORKER_IDENTITY_UNAVAILABLE`.
Exact cleanup and three-read reconciliation prove zero compute/disposable resources, both volumes
retained, restored route/config, and `$0` observed increment. Existing volume cost remains `$7/month`.

Fresh read-only truth at `2026-08-24T11:13:48.804Z` is zero Pods/endpoints/private templates/workers,
both exact retained volumes, RTX 4090 `LOW` availability, `$1.10/GPU-hour` Serverless Flex,
`$0.74/hour` Secure reference, and cumulative endpoint billing `1.645446196460398`. The active
Cloudflare anchor is retained at index 7 of 10 and rebound by exact redacted hashes in the candidate.
The exact POST probe returned `404 V207_ROUTE_DISABLED` and remains mandatory before mutation.

## Prior terminal boundary

Attempt50 is closed and non-reusable. Closure
`evidence/acceptance/VF-10-07/2026-08-21-live-qualification/failed-attempt-50.json` is
`sha256:341b8e07eec419133acf9e428da2309282be7a2482c440ce56ea10906182b32a`.
Its cleanup/reconciliation prove zero disposable resources/workers, both retained volumes, and `$0`
observed incremental spend at the recorded terminal read. Provider truth must be refreshed under the
new exact authority before mutation.

## Exact paid sequence — do not shorten or expand

1. Max1 probe.
2. Prove exactly one matching signed Pod among two stable endpoint-bound terminal-history snapshots,
   with queue empty, every record terminal, zero active workers, and zero running Pods.
3. Resume the 31 unresolved units without regenerating the accepted probe unit.
4. Complete a separate max1 cold 32-unit batch.
5. Complete a max1 warm 32-unit batch and duplicate-delivery/cost proof.
6. Complete max1 cancellation proof.
7. Complete max1 timeout proof.
8. Apply the separately hashed max2 config and run two simultaneous complete 32-unit read-only
   readers as the final GPU phase.
9. Drain, restore exact max1, clean disposable resources, and perform three-read reconciliation; no
   later GPU dispatch.

Every accepted batch requires durable private output readback before expiry, valid signed v3
receipts, provider status/output reconciliation, at most one acceptance/unit, visible duplicate
compute/cost, queue/init/load/warm/inference/upload timings, peak VRAM, exact pre/post manifest,
terminal jobs, zero total workers, and unchanged two-volume inventory.

## Execution and stop rules

- Run the current candidate validator and tests directly touched by a new repair. Reuse unchanged
  accepted proof; do not rerun historical attempt suites merely for counts.
- Attempt52 is consumed and non-reusable. Do not retry or rebind it.
- Do not rebuild or republish the image, redownload/quantize the model, mutate/cross-mount/delete a
  volume, change GPU/region/model, add fallback, expose a public sample, or begin V2-08.
- Stop on any proposal/authority/cap/code/anchor/route/resource/image/model/volume/GPU/region/rate/
  config mismatch; availability below the approved threshold; cap risk; partial output; missing
  receipt/readback; invisible duplicate cost; or uncertain restore/drain/cleanup/reconciliation.
- One narrow observed-failure repair cycle maximum. A second unrelated failure becomes a named
  micro-checkpoint.

## Handoff

Update exact evidence, `GATES.yaml`, and `CURRENT_STATE.yaml`; run focused validation, relevant
typecheck/build, context/schema checks when changed, and `git diff --check`; commit one green handoff.
The smallest next action is the exact provider-free version-bound rollback repair described above.
Do not repair remotely, retry Attempt53, or begin V2-08 under the consumed authority.
V2-08 is safe only after an independent V2-07 audit PASS.
