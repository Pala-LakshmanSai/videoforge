# VF-10-07 — V2-07 Mage Serverless qualification

Status: provider-free implementation/preflight in progress; V2-06 accepted. Read-only RunPod
inventory/rate lookup is authorized at $0, but every publication, endpoint/template mutation, GPU
request, volume action, model preparation, and spend remains blocked on one fresh bounded proposal
and user-supplied numeric cap.

## Goal

Convert and qualify the exact existing Mage-Flow INT8 ConvRot runtime as an immutable EU-RO-1
queue worker using only the retained sealed Mage 50 GB volume.

## Scope

- Complete local handler/image definition and read-only inventory first; no model download or
  volume mutation.
- Use `/runpod-volume` only for verified model reads and local job scratch for mutable bytes.
- Qualify RTX 4090, min 0, max 1 then 2, request-count 1, handler concurrency 1, cold/warm,
  two-reader manifest stability, resume/cancel/timeout/unknown-ack, output durability, cost, and
  scale-down.
- Publish/create/run only after one exact combined proposal and fresh numeric user cap.

## Pinned V2-07 identity

- Mage: `Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6#int8-convrot`,
  ComfyUI `26d7f8556822d9d08c2d3e1878636ac3b4969af9`, 4 steps, CFG 1.0, 1280x720.
- Existing sealed Mage volume: hash
  `sha256:eae4e1ecee86be5d8bed2f6814e06332bc8a97e9f35767771d28c10cfdecd619`, 50 GB, EU-RO-1;
  manifest `sha256:cebcd5c6233c2eae32f26ced7510acef8192f0d92d7ec3e9dd3ee881d66d205b`,
  13,379,919,280 bytes. It is retained, Mage-only, and never writable/deletable in V2-07.
- Prior Mage foundation source image is immutable evidence only:
  `ghcr.io/pala-lakshmansai/videoforge-mage-cp06@sha256:0bd33cc8c41c7dc81964652b68e8f902e3521b931ade330c089f7999eb9c9f69`.
  V2-07 must publish a new immutable Serverless-handler digest before it can create an endpoint.
- Initial configuration is `EU-RO-1`, the exact Mage volume, one RTX 4090 GPU, Flex only,
  `workersMin=0`, `workersMax=1`, request-count scaler value 1, handler concurrency 1. A second,
  separately hashed `workersMax=2` configuration is temporary concurrent-reader proof only.

## Provider-free exit criteria

The handler must fail closed for malformed v3 authority, wrong image/manifest/volume/region/GPU,
model-volume write/cache escape, absent or mismatched scoped ports, duplicate delivery, cancellation,
timeout, and two-reader simulation. Do not call a provider while these are incomplete. The evidence
record at `evidence/acceptance/VF-10-07/2026-08-19-provider-free-preflight/acceptance.json` owns the
current $0 inventory/source snapshot; it is not qualification evidence.

RTX 5090 and the SoulX endpoint are out of scope.
