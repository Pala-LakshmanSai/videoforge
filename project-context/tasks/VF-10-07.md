# VF-10-07 — V2-07 Mage Serverless qualification

Status: `NOT_QUALIFIED`; exact Attempt51 single-use authority committed, `$4` cumulative finite cap,
execution pending; 3–4-hour best-case timebox

## Reader contract

This brief contains only the active boundary. Historical attempt narration is archived by Git and
immutable evidence. Do not preload or restore it. Follow only the current pointers below; open one
historical artifact only if a current failure or audit explicitly reaches it.

## Exact current candidate

- Proposal:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt51-terminal-pod-namespace-repair-candidate/combined-live-proposal.json`
  at `sha256:739aa53d398c223a690758e66f03fed437c5eaf51526ea52a33283fa1918c3fe`.
- Candidate acceptance:
  `evidence/acceptance/VF-10-07/2026-08-24-attempt51-terminal-pod-namespace-repair-candidate/acceptance.json`
  at `sha256:be67953bc6189adcacff5c06b265f340cbbcdb045f19efbfc426bc4d695a856b`.
- Max1 config `sha256:c2c31282f991c08677d9b49c1b6a367ba945c3fa232b48354e8913e14889ba5e`;
  max2 config `sha256:e1b23717ccf3b4b03a281936602e3b0f36ef19345a4ba22db9afc78585cb7f8c`.
- Control repair `f4054ed4865ed8fac1af53bd766cf2c5153c7e29` separates provider worker-record
  IDs from signed runtime Pod IDs and requires the unique endpoint-bound terminal Pod plus exact
  terminal worker count before resume.
- Exact runtime: Mage-Flow INT8 ConvRot
  `Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6`, manifest
  `sha256:cebcd5c6233c2eae32f26ced7510acef8192f0d92d7ec3e9dd3ee881d66d205b`, immutable
  image `ghcr.io/pala-lakshmansai/videoforge-mage-v2-07@sha256:79fe7e40b69c011c15cc31b2d84b356cd2c755ea338976172cd78cc581304d59`.
- Exact resource: existing sealed Mage-only 50 GB `EU-RO-1` volume at `/runpod-volume`,
  application-read-only, RTX 4090 Flex, FlashBoot true, no fallback.

Authority `approved-authority.json` at
`sha256:61e00a70cb64c1e467a2d2877f67a20597b5a17e19a1ffa7a9a767ef06f52ea9` commits the exact proposal,
two-phase anchor refresh, RunPod/Cloudflare mutations, GPU use, and a `$4` cumulative finite cap.
It is single-use and unconsumed. Reconfirm the current rate, availability, route, resources, and cap
headroom immediately before mutation; invalidate it on drift. Existing retained-volume planning
cost is `$7/month` for both volumes, separate from the finite cap.

Fresh read-only truth at `2026-08-24T08:50:12Z` is zero Pods/endpoints/private templates/workers,
both exact retained volumes, RTX 4090 `LOW` availability, `$1.10/GPU-hour` Serverless Flex,
`$0.74/hour` Secure reference, and cumulative endpoint billing `1.645446196460398`. The active
Cloudflare anchor is retained at index 7 of 10 and rebound by exact hashes in the candidate. The
disabled-route probe hit local DNS `ENOTFOUND`; exact POST `404 V207_ROUTE_DISABLED` remains a
mandatory pre-mutation check.

## Prior terminal boundary

Attempt50 is closed and non-reusable. Closure
`evidence/acceptance/VF-10-07/2026-08-21-live-qualification/failed-attempt-50.json` is
`sha256:341b8e07eec419133acf9e428da2309282be7a2482c440ce56ea10906182b32a`.
Its cleanup/reconciliation prove zero disposable resources/workers, both retained volumes, and `$0`
observed incremental spend at the recorded terminal read. Provider truth must be refreshed under the
new exact authority before mutation.

## Exact paid sequence — do not shorten or expand

1. Max1 probe.
2. Prove exact terminal Pod identity and exact terminal worker-count boundary.
3. Resume the 31 unresolved units without regenerating the accepted probe unit.
4. Complete a separate max1 cold 32-unit batch.
5. Complete a max1 warm 32-unit batch and duplicate-delivery/cost proof.
6. Complete max1 cancellation proof.
7. Complete max1 timeout proof.
8. Apply the separately hashed max2 config and run two simultaneous complete 32-unit read-only
   readers as the final GPU phase.
9. Drain, restore exact max1, clean disposable resources, and perform three-read reconciliation; no
   later GPU dispatch.

Every accepted batch requires durable private output readback before expiry, valid signed v3
receipts, provider status/output reconciliation, at most one acceptance/unit, visible duplicate
compute/cost, queue/init/load/warm/inference/upload timings, peak VRAM, exact pre/post manifest,
terminal jobs, zero total workers, and unchanged two-volume inventory.

## Execution and stop rules

- Run the current candidate validator and tests directly touched by a new repair. Reuse unchanged
  accepted proof; do not rerun historical attempt suites merely for counts.
- Execute only under the committed exact authority and cap; never reuse it after any execution.
- Do not rebuild or republish the image, redownload/quantize the model, mutate/cross-mount/delete a
  volume, change GPU/region/model, add fallback, expose a public sample, or begin V2-08.
- Stop on any proposal/authority/cap/code/anchor/route/resource/image/model/volume/GPU/region/rate/
  config mismatch; availability below the approved threshold; cap risk; partial output; missing
  receipt/readback; invisible duplicate cost; or uncertain restore/drain/cleanup/reconciliation.
- One narrow observed-failure repair cycle maximum. A second unrelated failure becomes a named
  micro-checkpoint.

## Handoff

Update exact evidence, `GATES.yaml`, and `CURRENT_STATE.yaml`; run focused validation, relevant
typecheck/build, context/schema checks when changed, and `git diff --check`; commit one green handoff.
V2-08 is safe only after an independent V2-07 audit PASS.
