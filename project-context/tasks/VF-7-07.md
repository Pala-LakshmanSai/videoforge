# VF-7-07 — Durable Image Style derived-artifact edit service

Status: exact provider-free persistence continuation; selected after VF-4-01 completion

## Goal

Close VF-7-07 by persisting the existing preserve-and-detach edit service through migration `0010`,
a production PGlite repository/UoW, publication, and metadata restore. Preserve foundation commit
`4a4806d` and completed VF-4-01 commit `1fba04c`.

## Context read profile

Use `phase7_style_derived_edit_service` from `MANIFEST.yaml`.

## In scope

- Add additive migration `0010` for immutable profile artifacts and edit records.
- Backfill every existing accepted style profile as an immutable `VISION_ANALYSIS` root without
  changing canonical bytes, analyzer evidence, hashes, or accepted lineage.
- Implement the production PGlite repository/UoW used by the existing style edit service, including
  row locks, expected-revision concurrency, idempotency, server-computed changed pointers, review
  invalidation, current-artifact/revision movement, and append-only triggers.
- Keep full-candidate `image-style-profile/v1` validation and JCS canonicalization server-side.
- Update publication to pin the exact current derived artifact bytes while preserving the immutable
  root analyzer artifact and evidence.
- Extend metadata export/restore for every new table and pointer.
- Exact replay returns the original result. Conflicting replay, stale/partial/cross-workspace writes,
  post-publication edits, or any attempt to mutate accepted roots fail closed.

## Out of scope

- No Hono route, browser UI, upload/normalization, preview generation, analyzer/provider call,
  credential, cloud/deployment, model/GPU action, or spend.

## Ownership

- One integration owner owns the style repository, migration, publication adapter, metadata restore,
  tests, and context. Shared surfaces remain serial.

## Acceptance

- Focused fresh/`0009` upgrade/reopened/restored PGlite tests cover root backfill, source
  preservation, multiple edits, changed pointers, stale concurrency, exact/conflicting replay,
  rollback, publication of exact current bytes, post-publication rejection, reviewer identity, and
  truthful historical evidence.
- Control-plane gates and forced full `pnpm verify` pass with zero skips at `$0`.
- Evidence: `evidence/acceptance/VF-7-07/style-derived-edit-service`.
- On completion author exactly one successor brief: `VF-4-02`. Do not author later briefs.

## Safety and rollback

- No accepted analyzer or published artifact may be updated or deleted.
- Roll back additive local changes with normal Git revert; no external cleanup exists.
