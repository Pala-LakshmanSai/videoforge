# VF-7-06 — Manual Image Style provenance contract decision

Status: exact provider-free context/contract decision task; no application implementation

## Identity and dependencies

- Milestone: `phase_7_style_manual_edit_provenance_decision`.
- Depends on VF-7-04 canonical accepted analysis truth and VF-7-05 byte-identical reviewed
  publication being committed and green.
- `GATE_STYLE_001` is closed. `GATE_STYLE_002` remains open and is not changed by this task.
- Base: clean `main` descending from VF-7-05 implementation
  `f0e3e66a545634b3f145932156e56b7da6efe4da` and evidence
  `2502b62082bcd976daafa4273c8e68a48b4a947d`.

## Goal

Reconcile one deterministic normative contract for how a `MANUAL_EDIT` transforms analyzer-derived
confidence, trait evidence, outliers, leakage warnings, canonical profile/artifact lineage, review
identity, and immutable publication history before any edit implementation is authorized.

## Context read profile

Use `phase7_style_manual_edit_provenance_decision` only.

## In scope

- Inventory every active statement and machine field that describes Image Style manual edits,
  analyzer evidence, trusted profile bytes/hash, version lineage, review, and publication.
- Define whether each analyzer-derived fact is preserved as historical source evidence, reset,
  recomputed deterministically, or made inapplicable after each editable creative field changes.
- Define the exact immutable source-artifact and derived-artifact relationship, canonical document
  identity, reviewer attribution, optimistic-concurrency input, replay identity, and whether an edit
  occurs within the current unpublished version or requires a new version.
- Define fail-closed behavior for partial edits, stale review snapshots, edits after publication,
  incompatible analyzer/profile versions, and evidence that cannot honestly describe edited bytes.
- Update the decision ledger, primary Image Styles domain file, data/API contract, acceptance plan,
  manifest indexes, and current handoff together. Mark superseded or clarified prose explicitly.
- Author exactly one subsequent dependency-ready implementation brief only after the contract is
  internally consistent. If more than one product-valid policy remains, stop and present the
  smallest explicit user decision instead of choosing silently.

## Out of scope

- No application code, migration, schema file, generated binding, route, UI, edit service, upload,
  preview, analyzer invocation, provider call, credential access, model download, Mage, RunPod,
  cloud/staging mutation, deployment, GPU, or spend.
- No change to qualified Gemini identity/caps, built-in style behavior, ordinary-video
  zero-analysis behavior, accepted UI/output grammar, or any gate status.

## Files and ownership

- Own `15_DECISIONS_AND_OPEN_GATES.md`, `18_IMAGE_STYLES_HUB.md`, the minimum affected sections of
  `10_DATA_AND_API_CONTRACTS.md` and `14_TESTING_AND_ACCEPTANCE.md`, derived context indexes, one
  exact subsequent task brief, and `CURRENT_STATE.yaml`.
- Maximum lanes including integration: one. This is a serial normative decision task.

## Acceptance

- One field-by-field manual-edit provenance table is normative and has no contradictory active
  statement in the selected profile or directly affected context files.
- Canonical source/derived artifact identity, immutable version behavior, review attribution,
  optimistic concurrency, replay, and post-publication behavior are exact enough to implement and
  test without product inference.
- Existing VF-7-04 accepted analysis and VF-7-05 byte-identical publication remain truthful and
  unchanged.
- `project-context/scripts/validate-context.sh`,
  `project-context/scripts/validate-schemas.sh`, `pnpm context:validate`, `pnpm secret:scan`,
  Prettier, and `git diff --check` pass.
- One small context-only decision commit, then one clean handoff selecting exactly one
  dependency-ready provider-free brief. No application implementation commit is allowed.

## Safety and budget

- `provider_calls_authorized: false`; credential access and model downloads are forbidden.
- Maximum external spend: `$0`.
- Provider mode remains `fixture`; no GPU or RunPod lifecycle action is allowed.
- Do not invent analyzer evidence for user-authored bytes or silently discard historical evidence.

## Rollback

Revert only this task's context/decision commit. Do not reset or rewrite accepted Phase 0–2,
provider qualification, UI, public remote, or VF-7-01 through VF-7-05 history.

## Context update

On a green handoff, record the exact decision and commit, preserve gate/provider/spend truth, remove
the manual-edit ambiguity from active blockers only if every affected normative copy is reconciled,
and select exactly one dependency-ready implementation brief.
