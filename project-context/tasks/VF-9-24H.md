# VF-9-24H — persistent-cache A100 sample

Status: selected; primary-output recovery within existing cumulative cap

## Goal

Produce and return the exact Elias EchoMimicV3-Flash BF16 native MP4. Avoid another ephemeral 24 GB
redownload by warming one temporary network volume on a cheaper RTX 4090 bootstrap-only job, then run
the unchanged inference once on A100 SXM 80 GB from the verified cache.

## Exact authority and limits

- User's cumulative ceiling remains `$2.00`. Stable live balance delta before this task is
  `$0.802093288`; maximum remaining spend is `$1.197906712`.
- Create one 50 GB network volume in `EU-RO-1`; storage is about `$0.0049/day` and must be deleted
  immediately after the A100 result or terminal failure.
- Bootstrap-only job: one RTX 4090, cost stop `$0.35`, no inference.
- Inference job: one A100 SXM 80 GB, hard cap `$1.00`, active stop `$0.90`.
- One immutable corrected image. Exact input bytes, BF16, 253 frames, 768 square, 8 steps,
  `Flow_Unipc`, TeaCache `0.1`, seed `43`, prompt, and private transport remain unchanged.
- No FP8, chunking, tuning, fallback model, second inference job, or retained paid resource.

## Evidence and output

- Output: `.videoforge/review/VF-9-24H/echomimic-v3-flash-elias-10.12s-native.mp4`.
- Evidence: `evidence/acceptance/VF-9-24H/elias-echomimic-v3-flash-persistent-cache/`.
- Record volume identity hash, bootstrap cache miss/hit and time, queue/activation, model load plus
  generation/encode, total execution, peak VRAM, cost, output SHA-256/probe.
- Delete endpoint/template after each lane; delete volume after inference; prove global zero three
  times. Show MP4 here and end `READY_FOR_USER_REVIEW`.
