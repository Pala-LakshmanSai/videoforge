# VF-10-01 — Split PGlite adapter by capability

Status: authorized local seam; `$0`; no behavior change

## Goal

Split the oversized PGlite repository adapter into capability-owned modules while preserving its
public factory, transaction semantics, SQL, runtime behavior, and existing imports.

## Context read profile

Use `phase10_pglite_adapter_split` from `MANIFEST.yaml`.

## In scope

- Partition `packages/control-plane/src/adapters/pglite-repositories.ts` by existing repository
  capabilities. Keep `createPGliteControlPlaneRepositories` and public exports backward compatible.
- Move code mechanically first. Do not redesign contracts, queries, transactions, or error mapping.
- Extend import/factory/contract tests enough to prove each capability remains reachable and shares
  the same transaction boundary.
- Run focused tests, `pnpm verify:fast`, canonical `pnpm verify`, context/schema validation, secret
  scan, formatting, dependency audit, and diff checks.

## Authority and limits

- Local code/tests/context, Git push, and hosted CI only; `$0`.
- No cloud/account mutation, plugin install, credential access, provider call, deployment, database
  migration, production adapter, schema behavior change, or paid plan.
- Preserve fixture-default behavior and all existing API/domain semantics.

## Acceptance

- Oversized adapter is split by capability with no behavior change.
- Existing and added contract/replay/restore tests pass.
- Server is stopped, worktree clean, hosted CI green, and exactly one private R2 local-seam
  successor brief is authored. Stop before installing Cloudflare plugin or creating R2 resources.
