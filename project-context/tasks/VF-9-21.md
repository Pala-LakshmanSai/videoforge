# VF-9-21 — Elias Yoder AvatarForcing-only sample

Status: corrected worker ready; bounded job stopped on A100 capacity without execution or spend

## Goal

Generate one reviewable 10.12-second AvatarForcing-only sample from the user-provided Elias Yoder
image and first 10.12 seconds of the user-provided voiceover. Return the MP4 for user quality review.

## Context read profile

Use `phase9_avatarforcing_user_sample` from `MANIFEST.yaml`.

## Exact authority

- Private local input image SHA-256
  `sha256:34ca7513d6e34af3fa37d2e05afa8d2f81cab9ff587c2a12d047002abc7c75e3`.
- Private local voiceover SHA-256
  `sha256:4b5c404ea6a042ca5e82ef64f238d7866f2fb770967b953e593abdadcf6c7fc4`;
  use only first 10.12 seconds for this quality sample.
- Canonical private runtime derivatives: 832x480 JPEG
  `sha256:bcc1b20cf51c0a1f5d8aae834dced1dcecf62e78434470f014f1391939fe31f7`
  and 16 kHz mono PCM WAV
  `sha256:ee4f007c5c5770a404a1fe7eb008ed7c9964acb47c34677374131ab509f26076`.
- AvatarForcing source revision `63b73e6c0f7bb42180ca6d7e1bf11c1de1a80b39`, weights revision
  `e2448919a7b535c29f34e07892884ae1a43c6ace`, worker image
  `ghcr.io/pala-lakshmansai/videoforge-avatar-primary@sha256:42a14b44cd0ab42c85cd6de91a44009ba7897723ebef2ee18d358ee3c0e3a384`,
  one A100-80GB worker maximum and one job at a time.
- Remaining Avatar/Fallback task budget and authorized cap are `$1.0240051888`; hard stop is `$0.85`.
  VF-9-21 measured spend is `$0.4496891390` before this final corrected-worker attempt.

## Execution and acceptance

- Use the existing production signed-URL job shape through one temporary unlogged private tunnel.
  Persist output locally before deleting RunPod resources; retain checksum/size/deadline guards.
- Never commit or publish user image/audio. Never use LongCat, MuseTalk, or SkyReels in this task.
- Save exact MP4/hash/probe/runtime/GPU/cost evidence. Technical success remains
  `READY_FOR_USER_REVIEW`; only user decides whether AvatarForcing quality is good enough.
- Corrected worker build passed. The authorized persisted-output job remained `IN_QUEUE`, acquired no
  worker, ran no inference, and spent `$0`. New explicit authority is required for a capacity retry;
  never blindly redispatch.
- Cancel, drain, delete, and independently prove zero pods, workers, endpoints, templates, and
  volumes.
