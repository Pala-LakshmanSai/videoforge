# VF-3-00 — Phase 3 gate and authorization checkpoint

Status: exact planning-only checkpoint; no application implementation

## Goal and dependencies

Turn the green Phase 2 handoff into one concise, evidence-backed user decision for the next safe
activation. Phase 2 must be committed and green. This task does not itself authorize Phase 3 code,
credentials, model downloads, provider calls, spend, remote publication, or cloud/account mutation.

Read profile: `phase3_checkpoint`.

## Scope

1. Confirm clean local HEAD descends from the VF-2-05 implementation/evidence/context handoff. Do
   not rerun or redesign completed work.
2. Read the decision ledger, gate registry, provider/model plan, cost plan, the optimized sequence in
   `CURRENT_STATE.yaml`, and this brief. Classify every open gate by the first implementation lane it
   blocks.
3. Refresh only time-sensitive public facts from official sources if the fresh user request permits
   read-only web research. Record checked dates and distinguish official fact from inference. Do not
   log in, create an account/resource, retrieve a credential, call a generation endpoint, or spend.
4. Present one consolidated decision with separate opt-ins for:
   - read-only official-source research/context refresh;
   - bounded provider qualification using the existing non-transferable `$1/$3/$8/$8/$5` sub-caps;
   - private Git remote/hosted CI mutation;
   - isolated Cloudflare/Neon/R2/Google-OAuth staging mutation;
   - production remains deferred.
5. Recommend the smallest evidence-first sequence. Explain that Phase 3–8 implementation briefs
   are authored only after their required facts/gates are measured, then selected one at a time by
   a refreshed `CURRENT_STATE.yaml`.

## Optimized sequence after explicit authority

1. Run bounded provider qualification through the committed provider sandbox; close or fail exact
   LLM, image, Avatar, style, GPU/RunPod, fallback, and cost gates with reproducible evidence.
2. Author/select the deterministic prompt compiler and fixture adapter, then gated DeepSeek.
3. Run disjoint Mage image, AvatarForcing, and custom-style lifecycle lanes with one integration
   owner; no lane consumes another lane's uncommitted interface.
4. Converge accepted content-addressed assets through the preserved FFmpeg renderer and real Chrome.
5. Add fallback routing only where measured primary failures require it.
6. Complete fault/isolation/security/retention/backup/load hardening, then separately authorized
   staging and controlled release.

## Acceptance and stop

- The user receives one explicit authorization question with effects/caps separated.
- Every claimed current provider fact is either freshly cited from an official source or labeled
  stale/unverified.
- No application or context implementation is performed before the user selects authority.
- No secret, credential, model weight, provider call, spend, remote/cloud/account mutation,
  deployment, public tunnel, or LAN listener occurs.
- Stop on a missing source, ambiguous authority, unresolved gate, or any request that crosses the
  listed boundary.

This planning checkpoint produces no acceptance artifact and no code commit. After the user makes a
choice, update the context pack in that newly authorized task; do not preselect a Phase 3 brief here.
