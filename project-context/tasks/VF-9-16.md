# VF-9-16 — Provider-neutral durable image acceptance contract

Status: authorized `$0` schema-and-implementation correction

## Goal

Evolve durable image acceptance from a fixture-literal envelope into a strict provider-neutral union
that preserves existing fixture bytes and admits the exact Mage result lineage without false
provenance.

## Context read profile

Use `phase9_provider_neutral_image_acceptance` from `MANIFEST.yaml`.

## In scope

- Add versioned fixture and Mage result variants with exact discriminators, media profiles, model/
  image/source/GPU/runtime identity, positive/negative hashes, cost, and technical QA.
- Generalize durable acceptance/store metadata, telemetry, replay, backup/restore, and hostile-input
  validation while preserving existing fixture behavior byte-for-byte.
- Add any additive SQL/contract migration required; no destructive rewrite.
- Compose the existing exact Mage validator into the new durable Mage variant with fake transport.

## Authority and limits

- Local contracts/schema/migration/control-plane/server/tests/docs, Git push, hosted CI, `$0` only.
- No provider call, credential, GPU, model download, RunPod/Runware mutation, deployment, profile
  promotion, UI redesign, Avatar work, or spend.

## Acceptance

- Existing fixture acceptance/replay/restore remains green and byte-identical.
- One exact real-shaped Mage result atomically converges and replays; lineage/cost/QA drift fails
  before durable mutation; rejected visual QA cannot become accepted.
- Canonical verification green and one real-video composition successor authored.
