# VF-9-15 — Durable real Mage result integration

Status: authorized `$0` implementation

## Goal

Compose the corrected exact Mage result boundary into the durable image acceptance path behind a
fail-closed staging seam, using fake transport and canonical bytes only.

## Context read profile

Use `phase9_mage_durable_result_integration` from `MANIFEST.yaml`.

## In scope

- Map exact Mage result bytes/evidence into existing durable image asset, QA, task/attempt, lineage,
  and cost acceptance without weakening fixture behavior.
- Persist positive/negative prompt hashes, exact image/source/model/GPU/runtime identity, output hash,
  and rejection/retry disposition.
- Fake-transport tests for replay, restart, cancellation, workspace/lineage/cost drift, visible-review
  rejection, and one bounded same-scene retry.
- Fixture remains default; no profile promotion.

## Authority and limits

- Local application/server/control-plane tests/docs, Git push, hosted CI, `$0` only.
- No provider call, credential, GPU, model download, RunPod/Runware mutation, deployment, production
  promotion, UI redesign, Avatar work, or spend.

## Acceptance

- One exact real-shaped result can atomically converge and replay through durable acceptance; hostile
  drift and rejected QA cannot become accepted state.
- Canonical verification green and one real-video composition successor authored.
