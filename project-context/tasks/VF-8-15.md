# VF-8-15 — SkyReels corrected-image A100 resume

Status: exact paid successor after VF-8-14 full dependency-path proof

## Goal

Run one five-second SkyReels qualification with the corrected immutable image, retain exact evidence,
and return RunPod to absolute zero.

## Context read profile

Use `phase8_skyreels_corrected_resume` from `MANIFEST.yaml`.

## In scope

- Immutable image
  `ghcr.io/pala-lakshmansai/videoforge-avatar-quality@sha256:1e7f9100bef7759ffe527d083e1655c7bfda6c9192668c9a13d15e4c4e73e878`.
- Same locked source/model/input hashes as VF-8-13, one scale-zero endpoint, one A100 80GB worker,
  120 GB ephemeral container disk, one five-second inline dispatch, 30-minute job timeout.

## Authority and limits

- Existing secure RunPod credential and exact pinned model download only; maximum new spend `$2.00`,
  hard stop at `$1.80` observed. Combined sprint provider spend remains below `$10.00`.
- No second dispatch, alternate GPU, Pod/volume, low-VRAM/USP, substitution, MuseTalk, Mage work,
  app/schema/UI change, promotion, or deployment.
- Cancel/drain/delete on any terminal result, cap risk, or ambiguity. Zero idle cost is mandatory.

## Acceptance

- One valid 960x960/25 MP4 has exact hash/probe/lineage/timing/cost evidence, or one safe terminal
  failure is retained without redispatch.
- Quality review remains separate; no promotion from technical evidence alone.
- Final and independent RunPod inventories are absolute zero.
