# VF-8-04 — Locked AvatarForcing worker qualification

Status: exact successor selected after VF-8-03

## Goal

Build and qualify the pinned AvatarForcing primary worker on one bounded RunPod Serverless lane,
then prove callback/cancel/cost/drain and leave every paid worker at zero.

## Context read profile

Use `phase8_avatarforcing_worker` from `MANIFEST.yaml`.

## In scope

- Pin source `KlingAIResearch/AvatarForcing@63b73e6c0f7bb42180ca6d7e1bf11c1de1a80b39`
  and weights `lycui/AvatarForcing@e2448919a7b535c29f34e07892884ae1a43c6ace`.
- Build one immutable RunPod worker image with strict job/result contracts, exact source/audio/layout
  hashes, progress, cancellation, technical probes, and redacted evidence.
- Use one Serverless endpoint with `workersMin=0`, `workersMax=1`, one approved GPU, bounded job TTL,
  and the VF-8-03 control adapter.
- Run the smallest owned/synthetic short clips needed to measure boot/runtime/VRAM/cost and validate
  full/split renderer bindings. Do not run MuseTalk or SkyReels unless primary output reaches their
  already locked technical trigger; author those as later tasks.

## Authority and limits

- Existing Keychain RunPod credential only; public/pinned GitHub and Hugging Face artifacts only.
- Maximum new external spend: `$5.00`; stop earlier when sufficient evidence exists.
- Maximum one endpoint and one worker; `workersMin=0`, `workersMax=1`; no standalone Pod.
- Mandatory cancel/drain/delete and final live inventory proving zero running Pods/workers.
- User accepted unresolved open-weight terms risk; preserve ambiguity but do not block execution or
  claim unambiguous commercial permission.

## Acceptance

- Pinned image digest and source/weight revisions; no ambient `latest` identity.
- Owned/synthetic input produces technically valid short output with exact duration/FPS/audio/video,
  lineage, cost, and full/split compatibility evidence—or a truthful bounded failure.
- Ambiguous dispatch never retries blindly; cancellation and cleanup reconcile before further work.
- Full focused/fast verification, context/schema/secret/audit/diff checks pass; server stopped and
  RunPod inventory confirms zero paid compute.
- Evidence: `evidence/acceptance/VF-8-04/avatarforcing-worker`.
