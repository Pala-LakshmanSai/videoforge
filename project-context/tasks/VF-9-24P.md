# VF-9-24P — CP-05 provider-free complete MVP orchestration and legacy cutover

Status: complete provider-free; external spend `$0`; stopped before `CP-06`

## Identity and dependencies

- Task ID: `VF-9-24P`
- Milestone: `CP-05` provider-free complete MVP orchestration and legacy cutover
- Dependency: committed, re-audited green `CP-04` at local-render binding fix
  `cf7a843fea8535bbc4fb1dc6b516ac2dbe5e9690`
- Prerequisite evidence:
  `project-context/evidence/acceptance/VF-9-24O/cp04-three-composition-scheduler/reaudit-after-fixes.json`
- Base commit/branch: `9f91b26` on `main`
- Activation commit: `6c841b8c5ed4c7e11cdf786a0f43faf90e1914bc`
- Implementation commit: `426156b0c30a469fb2eb786de8adb4566b8d8b22`
- Workerd boundary fix: `55ee5caf5b742ab54f9e59198b1ab9be1a327e12`
- Chrome download proof: `8bfd0529e9cac4773d1f0b67629cc10f97aed313`
- Decision IDs: `DEC_QUEUE_001`, `DEC_QUEUE_002`, `DEC_RUNPOD_002`, `DEC_RENDER_001`,
  `DEC_RENDER_002`, `DEC_AVATAR_007`, and `DEC_AVATAR_008`
- All production/provider/model/GPU/cost gates remain open.

## Goal

Wire the accepted CP-03 transcript, CP-04 scheduler/work manifests, provider-free prompt fixtures,
two isolated synthetic Pod lanes, durable asset barriers, direct FFmpeg, global queue/session
contracts, cost, cancellation, and recovery into one complete automatic fixture-mode MVP. Cut the
active application to vNext while preserving provider-free legacy replay bytes and evidence.

## Context read profile

Use `cp05_complete_fixture_mvp` from `MANIFEST.yaml`. Read only the CP-05 section of
`22_PROJECT_COMPLETION_CHECKPOINTS.md`, this brief, and the orchestration/runtime/acceptance files
selected by that profile.

## In scope

- Fake-backed live inventory and exact paired GPU selection with one immutable singleton session.
- Three-project global queue with one active project; waiting reorder/remove; inert waiters.
- Truthful synthetic volume, container, model load, warm-up, `model_ready`, work, drain, delete,
  and independently proven absence states for isolated Mage and Echo lanes.
- Existing warm Pod retention only when a waiter exists. No work while waiting, no late recreation
  of an absent Pod, and recreation only after next-project activation and fixture revalidation.
- Runware prompt fixtures, synthetic image/avatar outputs, content-addressed durable barriers,
  exact cost ledger, resolved render manifest, direct FFmpeg, playable MP4, and download.
- Crash/restart, cancellation, stale callback, wrong Pod/GPU/volume, and independent lane-drain
  rejection/recovery proofs.
- Active vNext imports/config/UI vocabulary. Serverless, old Mage BF16, `Auto`, AvatarForcing,
  MuseTalk, SkyReels, repair/quality/fallback, and historical crop dispatch stay reachable only
  through explicit provider-free legacy replay.
- Preserve every old migration, historical evidence file, and v1 byte payload.

## Out of scope

- CP-06 or later work, real Mage/Echo execution, hosted deployment, production promotion, gate
  closure, image publication, or provider inventory refresh.
- Credentials, provider/cloud calls, remote mutation, model downloads, GPU use, or external spend.
- Parallel active videos, waiting-project work, model substitution, repair, fallback, or claims
  that fixture evidence proves hosted production.

## Acceptance

- Three synthetic projects queue under one session and GPU pair; only one project works at a time;
  waiting entries reorder/remove without orchestration side effects.
- Crash/restart preserves exact active/queue/lane/artifact/cost state and converges safely.
- Stale callbacks and wrong Pod/GPU/volume fail closed without durable output or cost corruption.
- Both lanes show truthful staged readiness. Each drains/deletes independently; absence is proven;
  session closes/unlocks only after both absences and no active/waiting work.
- Existing warm Pods may survive for a waiter. An absent Pod is not recreated until that project is
  atomically active and the same fixture GPU is freshly revalidated.
- Every finished project has complete durable prompt/image/avatar/render barriers, conserved cost,
  a playable 1920x1080 MP4, and a working download path.
- Negative active-runtime scan rejects Serverless, old Mage BF16, `Auto`, AvatarForcing, MuseTalk,
  SkyReels, repair/quality/fallback, and historical crop imports outside legacy replay.
- Focused orchestration/control-plane/web tests, canonical `CI=1 TURBO_FORCE=true pnpm verify`,
  real installed-Chrome multi-session acceptance, context/schema validation, secret scan,
  dependency audit, and `git diff --check` exit zero.
- Evidence lives under
  `project-context/evidence/acceptance/VF-9-24P/cp05-complete-fixture-mvp/`.
- Final handoff commits bounded CP-05 only, stops all local servers, and stops before CP-06.

## Safety and budget

- `provider_calls_authorized: false`
- `remote_or_cloud_mutations_authorized: false`
- `credential_access_authorized: false`
- `model_downloads_authorized: false`
- `application_code_changes_authorized: true` for CP-05 only
- Maximum external spend: `$0`
- No paid compute starts. No live provider inventory refresh is authorized or required.

## Context update

At handoff, update `CURRENT_STATE.yaml`, `MANIFEST.yaml`, `00_START_HERE.md`, this brief, and
immutable acceptance evidence. Record exact commits, validations, queue/session/lane/artifact/cost
results, remaining gates, provider/spend state, and paid-compute shutdown state. Select no CP-06
work without new authority.

## Completion result

CP-05 passed at audited code head `8bfd0529e9cac4773d1f0b67629cc10f97aed313`.
The complete fixture MVP processed three serial projects after waiting reorder/removal and restart,
rejected stale and wrong-identity callbacks, independently proved both synthetic Pod absences, and
returned byte-verified playable/downloadable 1920x1080 H.264/AAC MP4s. The active runtime is vNext;
the negative scan passed 62 files and historical dispatch remains provider-free replay only.

Canonical `CI=1 TURBO_FORCE=true pnpm verify` passed: Workerd `1/1`, package graph `28/28`, web
`224`, control plane `226`, pipeline `120`, and installed Chrome `42/42` across desktop and compact.
No provider call, credential, cloud mutation, model download, image publication, GPU, paid compute,
or external spend occurred. Evidence:
`project-context/evidence/acceptance/VF-9-24P/cp05-complete-fixture-mvp/acceptance.json`.
All production gates remain open. CP-06 is neither authorized nor started.
