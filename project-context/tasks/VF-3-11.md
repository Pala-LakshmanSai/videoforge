# VF-3-11 — Primary-avatar replacement-model decision

Status: exact read-only successor selected after blocked AvatarForcing commercial-license preflight

## Goal

Select one commercially usable primary talking-avatar candidate for a later bounded qualification,
or record that no inspected candidate is acceptable. Replace `DEC_AVATAR_001` only from current,
unambiguous first-party code-and-weights terms plus evidence that the candidate can plausibly serve
the existing full/split Avatar Profile contract.

## Context read profile

Use `phase3_avatar_replacement_decision` from `MANIFEST.yaml`.

## In scope

- Start from the existing avatar ladder and `GATE_AVATAR_003` evidence; avoid broad model research.
- Inspect a tightly bounded shortlist of primary-avatar candidates using only authoritative
  first-party code, weights-host, model-card, revision, license, and technical documentation.
- Record exact URLs, timestamps, revisions/hashes, access state, code license, weight license,
  commercial-use status, input/output contract, native resolution/FPS, and documented hardware
  requirements. Separate confirmed facts from inference.
- Recommend at most one candidate. Keep AvatarForcing blocked and preserve it as historical evidence;
  do not reinterpret or download around its license contradiction.

## Out of scope

- No weights, LFS/Xet objects, account creation, legal acceptance, credentials, provider contact,
  install, execution, inference, provider call, RunPod/GPU/cloud mutation, spend, deployment, push,
  application code, schema, UI, architecture, or paid qualification.
- No change to Avatar Hub pinning, one-clip/two-crop behavior, output grammar, fallback routing, or
  accepted provider-free work.

## Acceptance and handoff

- Evidence is compact, reproducible, first-party, timestamped, and revision/hash-pinned where
  possible; no secrets, signed URLs, weight bytes, or copyrighted bulk content enter Git.
- A recommended replacement must have unambiguous commercial code-and-weights permission and a
  plausible fit for image + selected-span audio input and realistic full/split output. Ambiguous or
  inaccessible terms are a blocker, never a pass.
- Update decision/model/source/gate/current-state truth and author exactly one successor: a tightly
  capped qualification brief for the selected candidate, or a user-decision/blocker brief when no
  candidate qualifies on paper.
- Run context/schema validation, contracts check, secret scan, dependency audit, Prettier, and
  `git diff --check` at `$0`. Keep server stopped and commit one context/evidence-only change.

## Safety and stop conditions

- Provider calls are unauthorized; maximum external spend is `$0`.
- Stop on credential exposure, legal acceptance, owner/license ambiguity, large-file transfer,
  external mutation, charge, context contradiction, or pressure to select an ambiguous candidate.
