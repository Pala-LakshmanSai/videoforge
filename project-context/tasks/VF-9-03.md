# VF-9-03 — Authenticated Mage revision admission

Status: selected read-only admission successor; no model bytes or spend

## Goal

Resolve and pin the exact official `microsoft/Mage-Flow-Turbo` repository revision and immutable file
inventory through an existing authenticated Hugging Face session, or retain one precise access
blocker without guessing.

## Context read profile

Use `phase9_mage_revision_admission` from `MANIFEST.yaml`.

## In scope

- Use only an existing authenticated Hugging Face browser/session; do not create, reveal, copy, or
  persist a token.
- Verify official repository identity, exact commit revision, model config, and immutable file/LFS
  identities through first-party Hugging Face surfaces.
- If exact identity is available, update the fail-closed worker constant and test the exact revision.
- If unavailable, record the exact authenticated status/blocker and leave execution disabled.

## Authority and limits

- Read-only Hugging Face inspection plus local code/tests/docs and Git push; `$0` external spend.
- No model/weight download, GHCR image, RunPod/Runware mutation, GPU, dispatch, provider inference,
  deployment, new credential/token, or secret persistence. RunPod remains absolute zero.

## Acceptance

- Exact official 40-hex model revision and immutable inventory are evidenced, or one exact blocker is
  retained without model readiness claims.
- Focused/full verification passes for any code change.
- Exactly one bounded worker-image successor is authored only if revision admission succeeds;
  otherwise exactly one user-access blocker successor is authored.
