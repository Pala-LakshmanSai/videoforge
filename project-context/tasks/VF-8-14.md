# VF-8-14 — SkyReels dependency-path correction

Status: exact provider-free successor after VF-8-13 safe dependency failure

## Goal

Close the pinned source's missing runtime dependency and publish an image whose full
`generate_video.py --help` import path succeeds before any paid resume.

## Context read profile

Use `phase8_skyreels_dependency_diagnostic` from `MANIFEST.yaml`.

## In scope

- Pin the missing `av` runtime package found by static import audit.
- Extend container smoke from shallow package import to the full official CLI import/parser path.
- Publish one immutable corrected GHCR image and retain exact workflow evidence.

## Authority and limits

- `$0` external provider spend; GitHub/GHCR build/publish only.
- No RunPod credential read/mutation, model-weight download, GPU, dispatch, second attempt, Mage,
  MuseTalk, model substitution, app/schema/UI change, promotion, or deployment.
- RunPod remains absolute zero.

## Acceptance

- Focused/full checks pass; full pinned SkyReels CLI import/parser smoke passes in linux/amd64.
- One immutable corrected image digest is retained.
- Exactly one capped paid-resume successor is authored; RunPod stays zero.
