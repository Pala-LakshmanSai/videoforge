# VF-9-24A — EchoMimic RunPod observability correction

Status: complete; corrected image published; waiting fresh sample spend cap

## Goal

Correct the worker-state interpretation and evidence loss that made the first EchoMimic sample
attempt unobservable, publish one corrected immutable worker image, then stop before paid RunPod use.

## Scope

- Count only `RUNNING` endpoint Pods as active serverless workers; retain exited/terminated records
  as historical churn evidence.
- Enforce one concurrent running worker, not one lifetime worker record.
- Persist a redacted local journal before cloud mutation and after each resource/job/status/cleanup
  transition.
- Preserve job progress, worker statuses, hashed resource identities, balance, and final inventory.
- Add entrypoint-first structured logging, stable bootstrap failure codes, and a real default-entrypoint
  container smoke that does not contact RunPod.
- Set the documented bounded worker initialization timeout and structured log level.
- Run focused/full verification, one corrected GHCR build, pin its digest, push, and confirm hosted CI.

## Authority

- Application/source/context/Git/GHCR changes are authorized.
- RunPod reads, creates, dispatches, GPUs, model downloads, and spend are not authorized in this task.
- A new sample requires a fresh explicit spend cap because the prior `$0.50` authority was
  non-transferable and consumed.

## Acceptance

- Exited history never increases active worker count.
- More than one concurrent `RUNNING` endpoint worker still fails closed.
- A forced stop cannot erase the preflight/resource/job/status journal.
- Exact container entrypoint and handler imports run in the hosted image smoke.
- Full local and hosted verification pass; private bytes remain untracked.
