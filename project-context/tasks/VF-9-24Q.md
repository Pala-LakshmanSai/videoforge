# VF-9-24Q — CP-06 exact Mage INT8 on a persistent RunPod volume

Status: selected next; not started; no authority at rest; maximum external spend `$0`

## Identity and dependencies

- Task ID: `VF-9-24Q`
- Milestone: `CP-06` exact Mage INT8 on a persistent RunPod volume
- Dependency: green provider-free `CP-05` at repository handoff
  `d2285117c17a3f6dd37b84e9dd441fd6fe7aa1d0`; audited code head
  `58395225d9f2e11ac556e6b67f186b1fb5502336`
- Prerequisite evidence:
  `project-context/evidence/acceptance/VF-9-24P/cp05-complete-fixture-mvp/reaudit-after-fixes.json`
- Base branch/commit: `main` at `d2285117c17a3f6dd37b84e9dd441fd6fe7aa1d0`
- Decision IDs: `DEC_IMAGE_001`, `DEC_IMAGE_002`, `DEC_IMAGE_003`, and `DEC_RUNPOD_002`
- Open gates: `GATE_IMAGE_001`, `GATE_STYLE_002`, `GATE_GPU_001`, `GATE_RUNPOD_001`, and
  `GATE_COST_001`

Selecting this brief does not authorize execution. At rest, provider calls, credential access,
cloud mutation, model downloads, worker/output publication, GPU use, volume retention, and spend
are all forbidden.

## Goal

Create a VideoForge-owned, immutable, exact Mage-Flow INT8 ConvRot worker; prepare one new
Mage-only `EU-RO-1` persistent volume once; prove normal offline boots and owned image generation
from two fresh disposable Pods; show the outputs; delete every Pod and independently prove zero
Pods while retaining only the explicitly approved Mage volume.

## Context read profile

Use `cp06_mage_int8_persistent_runpod` from `MANIFEST.yaml`. Read only the `CP-06` section of
`22_PROJECT_COMPLETION_CHECKPOINTS.md`, this brief, and the files selected by that profile.

## Source precedence and known drift

1. Current explicit user decisions and VideoForge `DEC_IMAGE_001` plus its normative model/RunPod
   documents are product authority.
2. Adapt executable Mage/runtime mechanics from the local ImageForge source exactly at commit
   `1a6204e7b9387a4d26b5fbbb176506d670949fba`; never copy its resource IDs, secrets, outputs,
   ownership model, or app queue policy.
3. The executable snapshot pins `Comfy-Org/Mage-Flow` revision
   `d8c99241f6fa80fbd453014234af2bf337ea21e6`, ComfyUI revision
   `26d7f8556822d9d08c2d3e1878636ac3b4969af9`, `int8-convrot`, four steps, guidance `1.0`, and
   `1280x720`.
4. ImageForge `worker/README.md` incorrectly says BF16 while executable `constants.py` and
   `model_profiles.py` select INT8 ConvRot. The README is known drift, not authority.
5. ImageForge `worker/scripts/prepare_mageflow_volume.py` defaults `--revision` to `None`. That is
   unacceptable for VideoForge: its adapted preparation path must require the exact Mage revision,
   verify the exact three-file byte manifest, and fail closed on any missing or extra expected file.

Approximate prose totals such as `13.4 GB` cannot size the volume. Phase A must obtain byte-exact
sizes and hashes without downloading model bytes, then derive and document explicit headroom.

## Two-phase authority protocol

### Phase A — local and read-only preflight

Phase A begins only when the user pastes the exact CP-06 implementation prompt that explicitly
authorizes `read_only_preflight`. That prompt authorizes, for `VF-9-24Q` only:

- local application/worker code, tests, container builds, and evidence changes;
- public read-only source/registry metadata needed for exact revisions, hashes, and sizes;
- read-only RunPod calls at `$0` for current `EU-RO-1` inventory, exact offering/rate candidates,
  and zero-Pod/zero-resource confirmation; and
- access to the existing RunPod credential only for those read-only calls, without printing,
  copying, rotating, or persisting it.

Before Phase A work, set top-level `implementation_authorized_in_current_task: true`,
`current_task: VF-9-24Q`, and `in_progress_checkpoint: CP-06`. Record
`task_stage: read_only_preflight`, `application_code_changes_authorized: true`,
`provider_calls_authorized: true`, and `maximum_external_spend_usd: 0`. Record
`provider_authority` with `mode: read_only`, provider
`runpod`, `cap_usd: 0`, the current prompt timestamp, `non_transferable: true`, and only
`inventory_lookup`, `rate_lookup`, `quota_lookup`, `resource_identity_lookup`, and
`resource_absence_lookup`. Keep `live_development.provider_mode: fixture`, its calls flag true and
spend `0`; set credential access true while cloud mutations, model-byte downloads,
container/output publication, GPU use, and retained-volume billing remain false.
Phase A may implement and verify the local worker and prepare an immutable publish/provision plan.
It must then present the exact compatible GPU offering/rate, byte-derived volume size/rate,
finite checkpoint-cap proposal, and ongoing retention charge, and stop for Phase B authority.

### Phase B — bounded external mutation

Phase B begins only after a later user reply explicitly supplies all of the following in one
authorization:

- a positive numeric maximum cumulative external spend through the CP-06 handoff;
- one exact live `EU-RO-1` GPU offering/identifier and maximum accepted hourly rate;
- one exact Mage volume size in GiB and its current storage rate/estimated recurring charge;
- explicit authorization to retain that volume and continue its stated billing after handoff; and
- explicit CP-06 authority for credential use, provider/cloud mutation, model download, immutable
  worker-image publication, private owned-sample publication/transfer, and GPU use.

There is no default `$3` or other implied cap. Accept an unambiguous current-chat amount written as
`3`, `3.00`, `$3`, or `USD 3.00`, then persist it as numeric `3.0`; an unresolved placeholder,
conflicting amounts, an old cap, or authorization missing any field above is invalid. Before the first mutation,
persist the exact reply as `task_stage: bounded_mutation` and `provider_authority.mode: paid`.
In `CURRENT_STATE.yaml`, use provider `runpod`, model ID
`Comfy-Org/Mage-Flow@d8c99241f6fa80fbd453014234af2bf337ea21e6#int8-convrot`, and exact
resource targets. Authorize only `publish_worker_image`, `create_mage_template`,
`create_mage_volume`, `download_prepare_mage_volume`, `create_mage_pod`,
`generate_owned_samples`, `publish_owned_sample_outputs`, `delete_mage_pod`,
`verify_zero_pods`, and `retain_mage_volume`. Record the
timestamp, `non_transferable: true`, numeric cap, and set application-code, credential, mutation,
model-download, worker-image-publication, sample-output-publication, GPU-use, and Mage-volume-
retention authority true. Set `live_development.provider_mode: sandbox` and
record `mage_gpu_offering_id`,
`mage_gpu_rate_usd_per_hour`, `mage_volume_size_gib`,
`mage_volume_rate_usd_per_gb_month`, and `ongoing_retention_charge_usd_per_month` before the first
mutation. Revalidate live rate/availability and stop if anything differs.

The finite cumulative cap includes all metered CP-06 work through handoff. The separately approved
Mage-volume retention rate is ongoing infrastructure billing after handoff and must be reported
separately; neither authorization silently substitutes for the other.

## Phase A scope and acceptance

- Adapt only the required ImageForge Mage worker mechanics into VideoForge-owned code and identity.
- Require exact model/ComfyUI revisions, three-file byte manifest, offline environment, GPU floor,
  real warm-up, truthful health, deterministic request identity, and local/R2 artifact contracts.
- Prove local unit/integration, manifest mismatch, missing/wrong/cross-lane volume, registry-disabled
  normal boot, stale/wrong callback, secret scan, canonical verification, and context validation.
- Build locally but do not publish. Do not create a template, volume, Pod, GPU job, or remote output.
- Produce the exact Phase B authorization request from fresh read-only evidence; do not guess GPU,
  rate, capacity, headroom, or cap.

## Phase B scope and acceptance

- Publish a new immutable VideoForge worker digest; never reuse an ImageForge resource.
- Create the one approved Mage-only volume, prepare it once, checksum every required byte, and make
  ordinary boot registry-disabled and download-free.
- Use only the approved exact GPU offering. Create/adopt by exact attempt, image, GPU, volume,
  region, model, and manifest identity; ambiguity fails closed.
- Run at least eight representative owned prompts across the required subject/style/crop categories,
  distributed across at least two fresh Pods. Retain PNG probes, hashes, contact sheet,
  model-ready/inference/upload/delete timings, peak VRAM, rate, and settled cost.
- Delete every Pod immediately when no longer needed and independently prove zero Pods. Retain only
  the exact approved Mage volume and report its ongoing billing. No Echo work starts.
- Show the real output images to the user. Technical success is `READY_FOR_USER_REVIEW`; only the
  user accepts visual quality.

## Stop conditions

Stop before mutation or further spend on missing/ambiguous authority; cap risk; changed offering,
rate, volume, image, revision, hash, GPU, VRAM, or region; bad output; failed upload/delete; uncertain
Pod absence; credential exposure; registry access during normal boot; fallback/BF16 substitution;
or any attempt to touch Echo/ImageForge resources. Reconcile/delete the exact Pod before stopping
whenever a Pod may exist.

## Required handoff

Record exact commits, image digest, manifest/revisions/hashes, selected and actual GPU/rate, volume
ID/size/rate, finite cap and settled spend, ongoing retained-volume charge, timings/VRAM, output
hashes/probes/contact sheet, validation results, gate status, and attempt lineage. Stop local
servers, delete all Pods, independently prove zero Pods, and distinguish the retained volume from
paid compute. CP-07 remains unselected and unauthorized.
