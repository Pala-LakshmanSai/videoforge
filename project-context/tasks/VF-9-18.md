# VF-9-18 — Mage 40-prompt quality qualification

Status: authorized bounded provider qualification; maximum `$1.50`

## Goal

Run the existing 40-prompt Mage acceptance matrix through the locked corrected BF16 path, retain
every output and exact execution lineage, and leave the visual result ready for user review.

## Context read profile

Use `phase9_mage_quality_matrix` from `MANIFEST.yaml`.

## Exact authority

- Existing Keychain RunPod credential; first prove zero pods, workers, endpoints, templates, and
  volumes. Then use only one transient template and one scale-zero endpoint with at most one
  RTX 4090 worker and one job active at a time.
- Exact image:
  `ghcr.io/pala-lakshmansai/videoforge-mage@sha256:ee844a242956a376466fa233f05e3bb6ffdcf71a645f5b27241331e5e295a89c`.
- Exact stock-ComfyUI Mage-Flow-Turbo BF16 graph and negative prompt already pinned by `VF-9-12`.
  No LongCat, model/image/graph substitution, diffusers, FlashAttention, upscaler, or source patch.
- Task cap `$1.50`; combined Tasks 2-4 cap remains `$5.00`. Unused budget cannot raise any task cap.

## Execution

- Run all 40 required prompts across content categories, split-safe framing, built-in
  `documentary_stock_v1`, and four published custom styles.
- Save every PNG and exact prompt hashes, seed, GPU, queue delay, pod/volume attach, bootstrap,
  model load, cold/warm generation, measurable VRAM, cost, checksum, and review result.
- Maximum one retry for a scene only after a recorded rejection. Never redispatch an ambiguous
  acknowledgement; reconcile the same job or stop.
- Reject gibberish text, logos, watermark-like artifacts, severe anatomy, repeated people, weak
  geometry, failed crop safety, or wrong lineage.
- Stop on cap, provider ambiguity, deadline, or unrecoverable quality/technical failure.

## Acceptance and cleanup

- At least 90% first-generation relevance and no more than 5% severe failures after bounded retry.
- Full/split crop safety, final 1080p detail, style distinction, technical validity, and checksum
  validation recorded. At least one image may become technically accepted; visual lock remains
  `READY_FOR_USER_REVIEW` and no execution/style profile is promoted.
- In `finally`, cancel/drain/delete all temporary resources. Independently prove absolute zero pods,
  workers, endpoints, templates, and volumes before closing the task.
- Refresh context, commit/push, and verify hosted CI. Author exactly one Avatar/fallback successor.
