# VF-8-08 — AvatarForcing audio-runtime correction and bounded resume

Status: exact successor selected after VF-8-07 observability image

## Goal

Restore upstream audio runtime parity, publish a pinned worker image, run one smallest corrected
qualification, retain exact safe evidence, and leave RunPod at absolute zero.

## Context read profile

Use `phase8_avatarforcing_audio_runtime_resume` from `MANIFEST.yaml`.

## In scope

- Add pinned `torchaudio==2.5.1`, matching upstream and base `torch==2.5.1`; qualification audio is
  44.1 kHz and upstream imports torchaudio when resampling to 16 kHz.
- Built-container import/handler/source-compile smoke and immutable GHCR digest.
- One Serverless endpoint, one concurrent worker, `workersMin=0`, exact L40S/A100-80GB allowlist,
  and one owned five-frame dispatch.
- Persist valid MP4 evidence or exact secret-safe failure code/hash; always drain/delete/confirm zero.

## Authority and limits

- Existing Keychain RunPod credential only; maximum new spend `$1.00`, hard stop at `$0.90` observed.
- Exact pinned AvatarForcing/Wan/wav2vec model downloads during this single worker job are authorized.
- Combined sprint provider spend remains below `$10.00`; settled spend before this task is
  `$0.6383969013`.
- Maximum one endpoint and one running worker; no standalone Pod or network volume.
- No blind redispatch, model substitution, MuseTalk/SkyReels/Mage execution, UI/schema redesign,
  deployment, or production profile promotion.

## Acceptance

- Pinned image imports torchaudio and matches locked AvatarForcing source/model lineage.
- One run returns checksum/probe/cost evidence or one exact classified terminal failure.
- Candidate remains unpromoted without valid technical and human quality evidence.
- RunPod ends with zero Pods, endpoints, templates, volumes, and active workers.
