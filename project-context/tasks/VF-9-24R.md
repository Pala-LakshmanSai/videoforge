# VF-9-24R — CP-07 exact EchoMimicV3-Flash Turbo FP8 persistent runtime and crop lock

Status: technical qualification passed; native crop pair awaiting user review and explicit approval

## Identity and dependency

- Task ID: `VF-9-24R`
- Milestone: `CP-07` exact EchoMimicV3-Flash Turbo FP8 persistent-volume runtime and renderer crop lock
- Dependency: green `CP-06` at `efee7a11bf1a0b07f551a7fcfcc4948fe54609dd`; later local
  documentation verification at `edea850cc739843a6e210d4c0aaeff9ba607461a`
- Prerequisite evidence:
  `project-context/evidence/acceptance/VF-9-24Q/cp06-phase-b/settlement-reaudit.json`
- Pinned Echo preflight:
  `project-context/evidence/gates/GATE_AVATAR_004/2026-08-12-echomimic-v3-flash-preflight/`
- Branch/base: `codex/cp07-echo-fp8` from `edea850cc739843a6e210d4c0aaeff9ba607461a`
- Decisions: `DEC_AVATAR_007`, `DEC_AVATAR_008`, `DEC_RUNPOD_002`
- Open gates: `GATE_AVATAR_001`, `GATE_GPU_001`, `GATE_RUNPOD_001`, `GATE_COST_001`.
  `GATE_AVATAR_004` is closed by this qualification.

## Goal

Define and qualify locally a VideoForge-owned prepared FP8 Echo runtime from the exact pinned
first-party source, Flash, Wan, and audio lineage. Enforce download-free ordinary boot from a
separate Echo-only `EU-RO-1` volume, real warm-up readiness, short-span padding/trim/output
contracts, project-isolated scratch, and fail-closed manifest/identity checks. After separate paid
authority, publish and prepare only the approved Echo resources, run owned 2/4/6-second samples,
delete every Pod, retain the Echo volume, and propose but do not activate measured crops until user
review.

## Context read profile

Use `cp07_echo_flash_turbo_fp8_persistent_runpod_crop_lock` from `MANIFEST.yaml`. Read only the `CP-07` section
of `22_PROJECT_COMPLETION_CHECKPOINTS.md`, this brief, the pinned Echo preflight, and files selected
by that profile.

## Exact artifact lineage and runtime contract

- Source: `antgroup/echomimic_v3@7e89489ca51c0d008fc1963ec6c03fc5bd0b9397`.
- Runtime profile: `EchoMimicV3-Flash Turbo FP8`
  (`videoforge_echo_v3_flash_turbo_fp8_v1`). First-party verification found no separately named
  Turbo checkpoint; Turbo is VideoForge's explicit label for the official accelerated 8-step
  `EchoMimicV3-Flash` / `echomimicv3-flash-pro` lineage plus the owned FP8 preparation. Upstream
  identifiers and bytes remain exact and unchanged.
- Flash weights: `BadToBest/EchoMimicV3@311e176905a8c4c24b240b530488fe636ce4d249`;
  safetensors SHA-256 `5ebdbb2fc709108bf2a1728fd92eb2874804e4bc0324e92a2cd55425968c85a4`.
- Wan base: `alibaba-pai/Wan2.1-Fun-V1.1-1.3B-InP@fc913c34361f4ec879e2f9c78b4f11ae50a937d1`.
- Audio encoder: `TencentGameMate/chinese-wav2vec2-base@3991242c806928916fff4a8c0e4f76acf661b743`.
- Exact selected source runtime bytes: `23,922,317,735` before small configs, source, prepared
  artifacts, and operational headroom.
- Preparation derives VideoForge-owned `float8_e4m3fn` dynamic activation-and-weight quantization
  for compatible transformer linears with a pinned TorchAO toolchain; remaining tensors stay BF16.
- No uncarded pickle, Long Video CFG, full-voiceover work, first-request quantization, repair,
  fallback, substitute model, Mage mount, cross-mount, or normal-boot download.
- Sampling remains 8 steps, `Flow_Unipc`, 25 fps, seed 43, TeaCache threshold 0.1, and empty
  negative prompt unless later authority changes the accepted profile.

## Current provider-free and read-only authority

The current CP-07 prompt authorizes local application/worker/context work, local builds/tests, and
read-only RunPod calls at a `$0` cap for Sujal-account identity plus current `EU-RO-1` Echo-compatible
GPU inventory/rates and network-volume pricing. Credential values may not be printed, copied, or
persisted. Allowed operations are `account_identity_lookup`, `inventory_lookup`, `rate_lookup`,
`volume_pricing_lookup`, and `resource_absence_lookup` only.

It does not authorize remote mutation, image/output publication, model download/preparation,
template/volume/Pod creation, GPU allocation, retained Echo-volume billing, or spend. CP-06's
consumed authority is not transferable. The approved Mage volume remains retained and untouched.

## Provider-free acceptance

- Immutable preparation manifest schema pins every source path/revision/size/hash, prepared path,
  TorchAO/runtime identity, quantization policy, and completion marker written last.
- Ordinary boot requires the exact Echo lane/volume/mount/manifest and registry-disabled mode,
  loads only prepared bytes, performs a real inference-path warm-up, and exposes `model_ready` only
  after successful GPU identity, finite-output, and memory checks.
- Requests accept only scheduler-selected 2–6-second speech spans (bounded opener exception remains
  outside these qualification samples), with exact context padding, EDL trim, A/V duration, media,
  lineage, and output contracts.
- Each attempt uses a canonical project/revision/task/attempt scratch root outside the model mount;
  collision, traversal, symlink escape, stale output, full-length input, and cross-project reuse fail.
- Negative tests cover missing/corrupt/extra/incomplete/wrong-lane/cross-mounted manifests,
  registry/network access, first-request quantization, false warm-up, wrong GPU/volume/image,
  invalid span/padding/trim, unsafe media/pickle, and ambiguous cleanup.
- The immutable worker image has a build definition and publish target, but local preflight neither
  publishes nor downloads model bytes.

## Paid phase boundary

After local validation and fresh read-only evidence, ask once for: one exact compatible GPU choice
and current rate; derived Echo volume size and current recurring rate; explicit consent to create,
prepare, retain, and continue billing that volume; exact publish/create/prepare/fresh-Pod/sample/
delete operations; and a user-supplied positive numeric maximum cumulative external spend through
handoff. The finite cap excludes continuing retained-volume billing. Do not invent a cap.

Before the first mutation, persist the exact approval, numeric cap, selected GPU/rate, approved
volume size/rate, immutable image target, provider/model/resource identities, and allow only:
`publish_echo_worker_image`, `create_echo_template`, `create_echo_volume`,
`download_prepare_echo_volume`, `create_echo_pod`, `generate_owned_2_4_6_second_samples`,
`publish_owned_sample_outputs`, `delete_echo_pod`, `delete_echo_template`, `verify_zero_pods`,
`verify_two_isolated_volumes`, and `retain_echo_volume`. Revalidate identity/rate/availability first.

## Recorded paid authority

At `2026-08-14T17:30:52Z`, the user reauthorized the exact paid CP-07 operation list and corrected
immutable image target. GPU selection now prefers `NVIDIA GeForce RTX 4090` in Secure `EU-RO-1` at
no more than `$0.74/hour`, with `NVIDIA GeForce RTX 5090` at no more than `$0.99/hour` permitted only
when the 4090 is unavailable. The user approved creation and continuing retention of one separate
50 GB Echo volume at `$3.50/month` and supplied a `$4.00` maximum cumulative finite external spend
through handoff. The retained volume charge continues outside the finite cap. Revalidate Sujal
identity, both approved offerings' exact rate/availability, zero compute, and one pre-existing
Mage-only volume before the first mutation. Any proposal/rate/capacity/cap drift stops execution.

The pre-mutation refresh at `2026-08-14T17:32:32.338Z` passed on Sujal: zero compute, exactly the
retained 50 GB Mage volume, and no Echo volume. RTX 4090 was available at the approved `$0.74/hour`
with 24 GB VRAM and is therefore selected; RTX 5090 remains an unused authorized alternate.
Evidence: `project-context/evidence/acceptance/VF-9-24R/cp07-paid-execution/pre-mutation-preflight.json`.

## Phase A result

- Implementation commit: `c89acef78d4a4bbbbac118ca9c658b6180d68fc8`.
- Byte-budget and native-FP8 GPU safety fix:
  `75c852bb891dbe205ee43d2b73e74765bc336373`.
- Flash Turbo FP8 runtime-profile implementation:
  `4a8c0c2d7f440dd2f649fc03d9cf86a6a3ab618b`.
- Evidence: `project-context/evidence/acceptance/VF-9-24R/cp07-read-only-preflight/preflight.json`.
- Local CP-07 tests, all worker suites, Python lint, full TypeScript/build/lint/test graph,
  active-runtime scan, vNext dispatch firewall, context validation, and secret scan pass.
- Canonical `CI=1 TURBO_FORCE=true pnpm verify` passes against current Turbo-profile context commit
  `5d38739a5f53f6b0e67d6cbf1950ba29c7645061`, including Cloudflare parity and `42/42`
  installed-Chrome tests.
- Read-only Sujal inventory at `2026-08-14T16:51:35.652Z` shows zero Pods, endpoints, private
  templates, and active Serverless workers; exactly one retained 50 GB Mage volume; and no Echo
  volume. Mutations and external spend remain zero.
- Current Secure `EU-RO-1` eligible choices are RTX 4090 at `$0.74/hour` (`MEDIUM`, 24 GB), RTX
  5090 at `$0.99/hour` (`LOW`, 32 GB), and RTX PRO 6000 Blackwell Server Edition at `$2.09/hour`
  (`LOW`, 96 GB). Live model fit remains unproven until the approved real warm-up.
- The proposed separate Echo volume is 50 GB: 23,922,317,735 exact pinned runtime-blob bytes, a
  fail-closed 50,000,000-byte ceiling for pinned small configs, up to 4,000,000,000
  prepared-artifact bytes, and at least 22,027,682,265 bytes of headroom. STANDARD
  network-volume pricing is `$0.07/GB/month`, or `$3.50/month` retained.
- The user permits a larger Echo volume if exact preparation proves 50 GB insufficient. Current
  derivation still selects 50 GB because it leaves at least 22,027,682,265 bytes. Any bound failure
  stops before mutation; the next exact GB size and recurring rate must replace the proposal and be
  explicitly approved because retained billing changes.
- No image was built or published, no model bytes were downloaded, no volume/template/Pod was
  created, no GPU ran, and no crop was activated.

## Phase B image-publication attempt 1

GitHub Actions run `31822490514` built approved source `4a8c0c2d7f440dd2f649fc03d9cf86a6a3ab618b`
but its smoke command failed before registry authentication with `ModuleNotFoundError: No module
named 'echo_api'`. The image was not published and RunPod remained untouched at `$0`. Root cause:
the Docker `PYTHONPATH` omitted `/opt/videoforge` even though worker modules are copied there.
Commit `237b108819432b4bea3d7b3cb5e6bff039749cc1` fixes the path and adds a regression assertion.
The user approved the corrected immutable image source target at `2026-08-14T17:30:52Z` together
with the revised GPU priority and `$4.00` cap. Publication may retry only after current pre-mutation
identity, inventory, rate, absence, and cap validation passes.

## Phase B image-publication attempt 2

GitHub Actions run `31824574378` smoke-tested and published corrected source
`237b108819432b4bea3d7b3cb5e6bff039749cc1` successfully. The public immutable image is
`ghcr.io/pala-lakshmansai/videoforge-echo-flash-turbo-cp07@sha256:e53842b5b9e8115e576f6d98337c228d584651fa50df0d15e086d417f60258e8`.
RunPod remained at zero Pods and no Echo volume through publication; external spend remained `$0`.

## Phase B preparation attempt 1

The exact RTX 4090 preparation Pod created the approved 50 GB Echo-only volume, then returned a
terminal health result rejected as `CP07_PREPARATION_RESULT_INVALID`. The first executor version did
not persist the exact worker terminal payload before rejecting it, so the precise worker error is
ambiguous. The stop condition fired: no retry, repair, substitute GPU/model, or sample Pod was
started. The Pod and temporary template were deleted. A read-only audit at
`2026-08-14T18:00:37.809Z` proves zero Pods, templates, endpoints, and active workers plus exactly
the isolated 50 GB Mage and 50 GB Echo volumes. The Echo volume is retained at `$3.50/month` but its
prepared manifest is unverified. Provider billing remains unsettled; the conservative reserved
finite upper bound is `$1.11` under the `$4` cap.

The pinned preflight JSON also omitted the final `b` from one Wan tokenizer SHA-256. The active
runtime definition already contained the correct 64-character hash, so that historical evidence
defect does not explain the terminal preparation failure. Correction evidence is recorded without
rewriting the historical file.

## Revised provider-free correction and paid boundary

Pinned TorchAO `0.11.0` requires CUDA compute capability 8.9 or newer for
`float8_dynamic_activation_float8_weight`. Preparation attempt 1 invoked that transform while the
loaded transformer remained on CPU. Correction commit `d51c0a1` now gates CUDA capability, moves
the BF16 transformer to the preparation GPU, synchronizes it, and only then invokes TorchAO. The
worker tests pass `15/15`; repository `verify:fast` passes all 28 tasks. The corrected immutable
image source target is
`ghcr.io/pala-lakshmansai/videoforge-echo-flash-turbo-cp07:d51c0a1` and is not published.

The retained Echo volume contains partial unverified bytes and cannot use the fail-closed
preparation path because preparation rejects a non-empty unsealed volume. Executor commit
`16cd30a` permits replacement only when inventory contains exactly the hash-locked Mage volume and
the exact known invalid Echo volume. It deletes only that invalid Echo volume, proves absence,
creates one fresh 50 GB Echo volume, and rejects any identity/count/size/region drift. This changed
delete/recreate operation and corrected image target require revised exact paid approval before any
mutation.

Cost-guard commit `1310b72` includes the prior `$1.11` conservative exposure in every Pod
reservation, adds a 120-second lifecycle reserve for each fresh Pod, and rejects any projected or
observed cumulative value above the user-supplied cap.

Read-only refresh at `2026-08-14T18:34:10.033Z` proves Sujal identity, zero compute, exactly both
50 GB volumes, and RTX 4090 Secure `EU-RO-1` availability `HIGH` at `$0.74/hour` with 24 GB VRAM.
Prior Pod billing still returns no settled row; its conservative upper bound remains `$1.11`. The
bounded retry adds at most `$2.812`, making the maximum cumulative finite estimate `$3.922` under
the latest user-supplied `$4.00` cap. Continuing retention of the recreated 50 GB Echo volume is
still `$3.50/month` outside the finite cap. Evidence:
`project-context/evidence/acceptance/VF-9-24R/cp07-paid-execution/revised-local-preflight.json` and
`project-context/evidence/acceptance/VF-9-24R/cp07-read-only-preflight/revised-refresh-2026-08-14T183410Z.json`.

## Stop conditions and handoff

Preparation attempt 2 used the corrected immutable image
`ghcr.io/pala-lakshmansai/videoforge-echo-flash-turbo-cp07@sha256:9bc4e5467efeadadae58771d1c75ab40982cbecf78fb27bb85043627f5d7d5d0`
on the exact RTX 4090 offering. It stopped before download because `huggingface_hub` retained the
image's import-time offline constant even though preparation changed the environment after importing
`snapshot_download`. The controller rejected the failed terminal as
`CP07_PREPARATION_RESULT_INVALID`; no repair, fallback, retry, or sample ran. The Pod and temporary
template were deleted, and the fresh incomplete 50 GB Echo volume remains retained at `$3.50/month`.
The attempt settled at `$0.05622458457946777`; cumulative exposure remains conservatively bounded at
`$1.1662245845794678` under the `$4.00` cap. Evidence:
`project-context/evidence/acceptance/VF-9-24R/cp07-paid-execution/preparation-attempt-2-failure.json`.

Provider-free commit `f6c952a` clears all three offline environment flags before the preparation
app imports `huggingface_hub`, scopes that state to exact preparation mode, and preserves mandatory
offline flags for ordinary runtime. Executor commit `d23fa10` binds deletion to only the fresh
invalid Echo volume hash and includes the new `$1.166225` prior conservative exposure. Canonical
provider-free validation is green. A read-only refresh at `2026-08-14T18:57:39.128Z` proves Sujal
identity, zero compute, exactly the hash-locked Mage and invalid Echo volumes, and RTX 4090 Secure
`EU-RO-1` availability `LOW` at `$0.74/hour` with 24 GB VRAM. A third bounded attempt adds at most
`$2.812`, making the maximum cumulative finite estimate `$3.978225` under the latest `$4.00` cap
with `$0.021775` margin. The changed image source and second invalid-volume deletion require new
exact approval. Evidence:
`project-context/evidence/acceptance/VF-9-24R/cp07-paid-execution/third-local-preflight.json`.

Stop on missing or ambiguous authority, identity/rate/cap/capacity/VRAM/region/artifact mismatch,
registry access during ordinary boot, output or upload failure, model substitution, repair/fallback,
unsafe serialization, cross-mount, or uncertain Pod cleanup. If a Pod might exist, reconcile and
delete that exact Pod before stopping.

Handoff records commits, validations, image digest, complete source/prepared manifest, exact
GPU/rate/VRAM, volume size/rate/identity, 2/4/6-second playable MP4 hashes and probes, A/V duration,
cold/warm ready/inference/upload/delete timings, finite settled cost, continuing retained-volume
rate, and independent zero-Pod/two-isolated-volume proof. Native geometry may produce proposed
Echo-only full/split crops, but no crop becomes active until the user explicitly approves it.

## Phase B technical qualification result

- Final immutable image:
  `ghcr.io/pala-lakshmansai/videoforge-echo-flash-turbo-cp07@sha256:2da621771387ca52a8110d0f94c86dda1d140ec584bea86ddecfda263f5406fe`
  from source commit `7b6647ab4bc0265ad6ae4c9bdc30a98259ce380e` and publication run
  `31846645317`.
- The isolated 50 GB Echo volume is sealed with manifest
  `sha256:c868241d1c6ebee2645c41161d00e046839eaaafa7b994c340d8d010202098c8`.
  Ordinary boot proved zero model download, zero material quantization, registry-disabled operation,
  exact prepared-state loading, and real inference-path warm-up.
- RTX 4090 and the unoffloaded RTX 5090 were rejected on measured VRAM fit. The exact final profile
  passed on Secure EU-RO-1 `NVIDIA GeForce RTX 5090` at `$0.99/hour`, 32 GB advertised and
  33,680,326,656 observed bytes, with standard pipeline stage offload and the FP8 transformer
  unchanged.
- Fresh-Pod 2/4/6-second clips pass H.264/AAC, 1024x560, 25 fps, exact requested A/V durations, and
  zero A/V delta. Evidence and hashes:
  `evidence/acceptance/VF-9-24R/cp07-paid-execution/final-qualification.json`.
- Final live audit at `2026-08-14T23:32:50.044Z` proves zero Pods/templates/endpoints/workers and
  exactly the isolated 50 GB Mage and Echo volumes. Echo retention remains `$3.50/month` outside the
  finite cap.
- Proposed but inactive crops from the exact native geometry are full `992:558:16:0` scaled to
  1920x1080 and split `496:558:280:0` scaled to 960x1080. Only explicit user approval can activate
  either crop.
- Technical qualification is complete. `GATE_AVATAR_004` is closed. `GATE_AVATAR_001` and CP-07
  remain at user review because crop approval and the later full exact-avatar suite are not agent-owned
  actions.
