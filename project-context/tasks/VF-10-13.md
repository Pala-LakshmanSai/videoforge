# VF-10-13 — V2-13 invited-team production release

Status: provider-free final certification ledger implemented and independently audited GO at
`bd0cede`; factual GPU readiness independently audited GO at `c516726`; guarded production-config
activation independently audited GO at `614464b`; guarded two-lane Workflow independently audited
through `4239be9` and migration 0044. Config, migrations, grants, roles, secrets, and Workflow remain
undeployed/unprovisioned and `DISABLED_UNQUALIFIED`; production release and final live smoke are
blocked on accepted V2-12 plus fresh qualifications for both changed lane images; no live acceptance
claimed; 3–5-hour best-case live timebox

## Goal

Release the exact tenant-private, autoscaling system for 5–10 invited accounts, with two concurrent
videos, durable account/workspace-owned tenant state, caps, recovery, and no user/developer Pod
lifecycle.

## Scope

Provider-free security hardening, scans, spend/queue guards, observability, runbooks, fixture
firewalls, rollback preparation, and focused tests may proceed now. Deployment, invitations, live
smoke, provider calls, mutations, and spend remain blocked pending every release gate.

## Provider-free progress — 2026-08-25

- Commit `bd0cede` adds a fail-closed ledger for 15 exact release gates. It binds current source,
  deployed executable/config/contracts, both immutable lane identities, and V2-09–V2-12 evidence to
  trusted evidence classes and strict freshness/lineage checks.
- Fresh bounded two-lane smoke must precede independent zero-job/zero-worker drain and settled
  billing. Missing, stale, misordered, fixture-backed, or identity-drifted evidence blocks release.
  The ledger always leaves `liveReleaseAuthorized=false` and requires separate explicit authority.
- Commit `c516726` exposes authenticated factual GPU readiness in the hosted catalog/UI: both lanes
  remain `NOT_QUALIFIED`, exact SoulX full/split visual approval is recorded, missing live gates are explicit, transport
  is `DISABLED_UNQUALIFIED`, and no dispatch control is exposed.
- Commit `614464b` adds dry-run-default production config rendering, exact binding validation, a
  separately guarded deployment entrypoint, and production bundle firewalls. Placeholder or drifted
  config fails closed; the committed runtime remains `DISABLED_UNQUALIFIED` and is not deployed.
- Commits `0f73d81`, `9ef6efa`, `120b6b8`, `941176f`, `3d1b439`, and `4239be9` complete audited
  provider-free source through migrations 0041–0044 and the guarded pair Workflow: durable lane
  batches, authenticated envelopes, atomic two-lane predispatch, fenced runtime/recovery, callback
  settlement, and durable zero-worker evidence. They neither deploy nor authorize the live path.
- The envelope verifier byte change invalidates both earlier image-bound lane qualifications. Fresh
  immutable Mage and SoulX images, sealed deployment snapshots, dual live qualification, explicit
  fresh exact paid/deployment authority are required before activation. The visual crop gate is closed.
- Focused tests pass 8/8 for the release ledger and 15/15 for readiness; both received independent
  GO audits, and the production-config slice also received independent provider-free GO. No config
  render, deployment, provider call, credential access, GPU use, invitation, or spend occurred.

- Close essential P0/P1 auth/session/origin/rate, tenant/R2/callback/SSRF/path/upload, secret/log,
  cost-amplification, and legacy production-bundle risks.
- Add per-project/global spend stops, quotas, essential alerts, backup readback, rollback pins, and
  concise incident/provider/billing/rollback runbooks.
- Remove/fail-close fixture GPU profiles, fake production transport, manual Pod/GPU controls, and
  legacy public dispatch exports from production builds.
- Run focused security/build checks, canonical provider-free aggregate once, scans, one backup/
  rollback readback, one release-current hosted control-plane restart/reconcile with no duplicate
  dispatch or foreign result, and one production Chrome smoke. Perform a disposable restore only if
  V2-09 through V2-13 changed schemas/migrations; otherwise cite the accepted V2-06 restore proof.
- Defer nonessential broad cross-browser/accessibility, exhaustive fault, and ten-job statistical
  matrices to real-sample hardening.

## Minimum proof

No P0/P1, exact production URL/commit/config/image/endpoint identities, invited private smoke,
spend/queue/drain guards, release-current restart/reconcile proof, rollback readiness, settled cost
and recurring-charge disclosure, terminal jobs, zero workers, intended retained resources, and final
independent audit.
