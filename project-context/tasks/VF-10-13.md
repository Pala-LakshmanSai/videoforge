# VF-10-13 — V2-13 invited-team production release

## New-chat handoff — 2026-08-27

- Reviewed provider-free predecessor:
  `dd5549509dcbe89d31f56517ebc96f50ee6c2a70` (fresh independent audit; default no-op). Its proposal
  `sha256:9357579a1adfa4cbb750599fc70c31d9234af6e74f1b00b3c4e7c3aa44de800a` and authority-record
  commit `21b770bc2fe86869443a4cead713101a2cb79776` are superseded unconsumed. A pre-execution seam
  audit found the TypeScript admission ceiling was `$1.10/GPU-hour`, below the sealed official
  `$1.116/GPU-hour` rate. The authority was never consumed; no credential access, provider call,
  mutation, GPU use, or spend occurred. The provider-free repair aligns the ceiling and adds exact
  cap acceptance and reject-above regression coverage. Fresh source audit, reseal, proposal-record
  audit, and new exact approval are mandatory before execution.
- Credential setup: complete. The final corrective result is
  `sha256:815258fce0b32ecd8afa6ad1dae0399615c26533c7fd1b1d60ecf4657d567ac6`; its secret-free receipt is
  `sha256:35caf042a18f6f4b42f264d96e52926856bcc387890c4925f512f2bf2c6c1eab`. Preserve the exact
  resources and protected files. The authority is consumed and cannot be reused.
- Historical validation repair `e31e275a767bd9a098f4b94c078b50136d5b998b` preserves the original
  reuse-result/incident/authority hashes while binding the exact completed rotation successor and
  current receipt; the mutable receipt-path alias no longer produces a false historical failure.
- Release truth: `DISABLED_UNQUALIFIED`; no current full-live authority. Migrations/roles, production
  Cloudflare activation, fresh immutable Mage/SoulX qualification, max-one endpoints, V2-09–V2-13
  live acceptance, final settled billing, and independent zero-worker proof remain open.
- Fresh read-only preflight proved zero temporary compute and bound the completed receipt, exact
  account-derived absent 404 HTML route, same two retained volumes, and official Serverless Flex
  `$0.00031/second` (`$1.116/GPU-hour`) separately from the `$0.74/hour` Secure Pod catalog rate.
  This snapshot is stale and must be refreshed only after the new proposal and authority validate,
  immediately before mutation. Only then execute the serial graph. Temporary RunPod compute must be
  drained immediately whenever not strictly required; any uncertainty enters cleanup-only and no
  redispatch.
- Planning estimate: roughly 75% overall, 92% source/product, 100% credentials, and 48% live
  qualification/deployment. This estimate is for handoff planning, not proof that any release gate passed.

Status: the credential rotation/normalization boundary is completed once and non-reusable. The
sealed rotation proposal `sha256:76f14ae25cff7840d0028be1ca0af87bbf325178d99a5ca2b80806aa3ddb2c73`
at commit `1845be6c852654c8396f2973981733ce64a3d2d0` was approved under authority
`v2-13-credential-rotation-normalization-20260827-095717z-76f14ae2` and completed at commit
`58ad5cd5c5bf4dbe6fa7ad99b98288b3d4f1bd9a`. The secret-free execution result is
`sha256:815258fce0b32ecd8afa6ad1dae0399615c26533c7fd1b1d60ecf4657d567ac6` and its receipt is
`sha256:35caf042a18f6f4b42f264d96e52926856bcc387890c4925f512f2bf2c6c1eab`.
The same Google project/client received one new secret, which was securely read back and
hash-bound before only the exposed old secret was disabled; only the two local R2 files were
normalized. Cloudflare R2 provider mutation, RunPod calls, GPU use, and spend were zero. No raw
credential values are retained in evidence or the receipt. Preserve the exact resources/files;
do not reuse the consumed authority or perform further credential/provider/R2 mutation, retry, or
redispatch. The full-live successor is sealed and awaits its independent proposal-record audit and
fresh exact authority.
The current provider-free full-live execution source is exact independently audited commit
`5da33e8` (default no-op).
The separate zero-cost credential-bootstrap proposal is independently audited and sealed at commit
`9106f9d` with SHA-256 `48bf5c7bb7304630eb3744b038fea7b7a11a53878bd1a18d94768c6f0e96e0ab`;
the user's exact approval was received, but the single-use attempt stopped before mutation when
Google reported that the authenticated account had reached its project quota. It remains
`BLOCKED_UNCONSUMED_NO_MUTATION`; no executable authority, credential access, OAuth client, R2
credential, protected-file write, RunPod call, GPU use, or spend occurred. One separately authorized
request for one additional Google Free Services project was submitted on 2026-08-27; Google
confirmation says review typically takes about 2 business days and follow-up goes to the submitted
email. This request does not authorize project creation or any credential action.
The repair adds the zero-cap prequalification database bootstrap after SoulX verification,
exact 36-to-45 recovery, the protected receipt CAS, and a split operator-only fresh-live bridge;
runtime/reconciler roles remain absent until guarded activation. Full-live config, migrations,
grants, roles, images, endpoints, and Workflows remain undeployed/unprovisioned and
`DISABLED_UNQUALIFIED`; only the separately recorded credential files and receipt from the
completed rotation exist. The earlier `sha256:f2d183...` approval is superseded unconsumed because the
worker, executor, and migration hashes changed. Production release and final live smoke require a
wholly fresh exact proposal/approval plus fresh Mage and SoulX image qualification; no live
acceptance is claimed. Proposal `sha256:9425de8e…899a` was approved and recorded at `67f6fb2`, then
superseded unconsumed before mutation after the exact 21-versus-22 secret scope drift and execution-
order/source-worktree blockers were found. The `sha256:2e6a605d…d2958` approval at `9eaf276` and its
authority are now superseded unconsumed with no mutation after the source-lineage repair. The
replacement proposal was resealed as `sha256:45894ac0…01aaca` against `7444ed0`; its fresh
authority `59dfe8a` is now superseded unconsumed before mutation because the Cloudflare
workers.dev origin and credential boundary proved incomplete. The successor draft is
`project-context/evidence/acceptance/VF-10-13/2026-08-27-cloudflare-credential-origin-repair-candidate/combined-live-proposal.json`
with current sealed hash `sha256:9357579a1adfa4cbb750599fc70c31d9234af6e74f1b00b3c4e7c3aa44de800a`,
explicitly superseding `45894ac0…01aaca`/`59dfe8a` as unconsumed and no-mutation. It remains
unapproved pending the exact proposal-record commit/audit and fresh exact approval. Google OAuth/R2 credential
rotation is already completed, receipt-bound, consumed, and non-reusable.
The replacement proposal binds a closed 25-operation graph, including zero-cap
`record-workflow-start-authority` after promotion and before V2-09, full bootstrap receipt/ACL/public-
revoke readback, guarded receipt verification before secret reads, durable billing, and a separate
early no-database cleanup seam. The successor additionally requires an authenticated account-bound
workers.dev origin, exact absent-route HTTP 404 `text/html; charset=UTF-8`, length `19984`, sealed
body hash before creation (not 503 JSON),
post-quarantine HTTP 200 `DISABLED_UNQUALIFIED`, post-qualification HTTP 200 `QUALIFIED_EXACT`,
protected Wrangler OAuth configuration with no API-token export, strict nested seed-hash binding,
one Google OAuth WEB client callback only at the final production origin in a user-accessible
project, and one least-privilege R2 S3 credential restricted to the existing bucket/account.
Its validator is bound to the release commit's tree entry rather than a self-referential hash.

## Historical existing-project reuse proposal sealed — 2026-08-27

- Read-only evidence shows accessible ACTIVE project `adroit-archive-329710` (project number
  `984657838923`) with no OAuth client, API key, service account, billing account, Cloud Storage
  bucket, Cloud SQL instance, BigQuery dataset, or Firestore database. The exact R2 bucket is
  `videoforge-v2-06-staging-private`; its existing staging account token remains preserved and the
  sealed graph permits exactly one distinct production bucket-only Object Read & Write credential.
- The first independent reuse-proposal audit found a P1 and the P1 repair is now reflected in the
  sealed candidate. The current proposal is
  `sha256:90d6b19d6935ded1bfebdb6df53c64ea33edeba4dce750fe3a81b93708228ed4`, committed at
  `68ea8a0`. It is
  `PASS_SEALED_AWAITING_FRESH_EXACT_APPROVAL`, with no executable authority.
- Proposal work made no credential access, credential/provider mutation, RunPod call, GPU use, or
  spend. A fresh exact approval must name this proposal and its successor commit before any
  credential or protected-file operation. The earlier exact-new-project approval remains
  `BLOCKED_UNCONSUMED_NO_MUTATION` and cannot be reused for this path.
- The unexpected Firestore API enablement incident remains preserved as separate history; no
  disable, rollback, or further API action is authorized. The separately submitted project-quota
  request also remains history and does not authorize this reuse path.

## Credential-bootstrap execution stop — 2026-08-27

- The exact approved proposal `sha256:48bf5c7b…e96e0ab` at commit `9106f9d` reached the Google
  project-create preflight only. The authenticated Google Cloud console reported the project limit
  was exhausted; no alternate project is allowed. A separate, one-use quota-increase request for
  one additional Google Free Services project was submitted on 2026-08-27 and is awaiting review.
- Google confirmation says review typically takes about 2 business days and follow-up goes to the
  submitted email. The request is recorded at
  `project-context/evidence/acceptance/VF-10-13/2026-08-27-credential-bootstrap-candidate/quota-increase-request.json`.
- The secret-free attempt record is
  `project-context/evidence/acceptance/VF-10-13/2026-08-27-credential-bootstrap-candidate/blocked-execution.json`
  with SHA-256 `sha256:84f329bdaf2b649b7b1786e774ae8d500a31f376f8746d6d673423d82b2e35c5`.
  It records `BLOCKED_UNCONSUMED_NO_MUTATION`, zero Google project/client mutations, zero R2
  credential creation, zero credential-value access, zero protected-file writes, zero RunPod/GPU/
  spend, and no temporary compute to drain.
- The next boundary is a fresh read-only project inventory after quota resolution, followed by a
  fresh exact replacement proposal and approval. Do not delete or reuse another project, create
  credentials, or write the four protected files under this blocked approval.

## Existing-project reuse audit incident — 2026-08-27

- A separate read-only audit of accessible project `adroit-archive-329710` stopped after opening
  the Google Cloud Console Firestore inventory unexpectedly enabled
  `firestore.googleapis.com` and `firebaserules.googleapis.com`. The enabled-service inventory
  changed from 13 to 15. This was outside the credential-bootstrap approval and is recorded as an
  unauthorized provider-side effect requiring independent safety review; no disable, rollback, or
  further provider action is authorized.
- Exact service-list evidence uses compact UTF-8 RFC-8785/JCS-compatible JSON array bytes with no
  trailing newline: before SHA-256 is
  `sha256:868bd0892c1df56410e064e554df7cce396ae84fab5e2906a34b75cc564897d0`, after SHA-256 is
  `sha256:3b55246afeb2fe025bb8cdc9764207f15519ce027c06e82416e610a838b30581`, newly enabled display
  names SHA-256 is `sha256:960be93b69f36dd7f9f3ed8c36c78182a64a6dad6f71b2f1d1825f7a43cd7993`, and
  the `{before,after}` transition SHA-256 is
  `sha256:3ee225606f55eaf2a16658d417aa0457a813cc4216b6313234c9a9cf8907d0c0`.
- No database or other resource was created, no billing account was associated, and no OAuth client
  or R2 credential value was accessed or created. Cloudflare R2 calls, RunPod calls, GPU use, protected
  credential-file writes, and external spend remain zero. The original exact-new-project blocked
  attempt remains unchanged and cannot authorize reuse or any partial continuation.
- Secret-free incident evidence with the complete before/after arrays is
  `project-context/evidence/acceptance/VF-10-13/2026-08-27-credential-bootstrap-reuse-adroit-archive-candidate/unexpected-firestore-api-enablement-incident.json`
  with SHA-256 `sha256:936117ccc777b37d6e6ee595c8d8feccb4fbd026e11d7705084af03230db2229`. The
  first independent reuse-proposal audit P1 was repaired; the sealed reuse proposal now carries the
  independent safety verdict and requires a fresh exact approval before any reuse mutation.

## Goal

Release the exact tenant-private, autoscaling system for 5–10 invited accounts, with two concurrent
videos, durable account/workspace-owned tenant state, caps, recovery, and no user/developer Pod
lifecycle.

## Scope

Provider-free security hardening, scans, spend/queue guards, observability, runbooks, fixture
firewalls, rollback preparation, and focused tests may proceed now. Deployment, invitations, live
smoke, provider calls, mutations, and spend remain blocked pending every release gate.

## Sealed provider-free release proposal — 2026-08-27

- Proposal `2026-08-27-cloudflare-credential-origin-repair-candidate` validates as
  `PASS_SEALED_AWAITING_FRESH_EXACT_APPROVAL` with no approval or authority. It supersedes
  proposal `sha256:45894ac0…01aaca` at `ec38849` and authority `59dfe8a` as unconsumed/no-mutation;
  the current sealed hash is `sha256:9357579a1adfa4cbb750599fc70c31d9234af6e74f1b00b3c4e7c3aa44de800a`.
- The proposal binds the exact audited source contract: protected Wrangler OAuth config
  `VIDEOFORGE_V2_13_WRANGLER_OAUTH_CONFIG_FILE` mode `0600`, no raw Cloudflare API-token file or
  `CLOUDFLARE_API_TOKEN` export, authenticated account and `/workers/subdomain` readback, derived
  production `workers.dev` origin, exact pre-creation HTTP 404 `text/html; charset=UTF-8`, length
  `19984`, and `sha256:2000e6b28a1517ba1268e1649cd3163326ef839492edfdba31e8959830580976`
  (not 503 JSON), then exact HTTP 200 `DISABLED_UNQUALIFIED` and
  `QUALIFIED_EXACT` reads. Release source and exact lightweight-tag target
  `dd5549509dcbe89d31f56517ebc96f50ee6c2a70` and all exact release component hashes are bound.
- The full-live graph binds the completed secret-free credential receipt for exactly one Google OAuth
  WEB client in `adroit-archive-329710` with one final-production callback and one least-privilege R2
  S3 credential bound to the authenticated account and existing bucket. The consumed credential
  authorities are non-reusable; no credential creation/rotation, new R2 bucket, wildcard/account-wide
  access, or second credential is authorized. The materialization seed must bind
  `materialization_seed_sha256` in outer authority and consumption state, and verify it at every
  seed read/restart. No provider calls, credentials, mutations, GPU use, or spend.

## Provider-free progress — 2026-08-25

- Source commit `e737eac` closes the audited provider-free execution path through migration 0045. It binds
  protected inputs before any operation, registers the exact DB authority, stages Mage then SoulX
  then max1 deployments, promotes only from exact DB and Cloudflare readbacks, requires DB-owned
  V2-09 terminal output/readback/Chrome/cost proof, composes V2-10 through V2-13 acceptance, and
  performs authoritative crash-safe cleanup. Exact-envelope persistence, raw HMAC worker receipts,
  encrypted one-use handoff escrow, lost-response replay, delayed cleanup, and one-authority-per-stage
  fences are executable-tested.
- Independent stable-byte audit found no P0/P1. Canonical scripts passed 154/154; focused outer
  execution passed 47/47, web passed 72/72, migration 0045 passed 9/9, and TypeScript, builds,
  contracts, production firewall, formatting, and context validation passed. All default entrypoints
  remain `NO_ACTION` with zero provider calls, mutations, GPU use, or spend.
- Superseded proposal `sha256:9425de8e…899a` bound source `e737eac`, semantic lightweight tag
  `videoforge-v2-13-release-20260826-v3`, the seven phase caps totaling `$17.50`, exact RTX4090
  EU-RO-1 LOW-or-better at no more than `$1.10/GPU-hour` with no fallback, and separate retention
  of only the same two 50 GB volumes at `$7/month`. Authority `67f6fb2` is superseded unconsumed with
  every executable flag false and no external action. A new proposal must bind the forthcoming
  repaired source, exact 22-name secret allowlist, staged future-artifact preflight, isolated source
  worktree, exact authority-record commit readback, durable hash-chained internal materialization of
  production-input/activation/promotion records from prior results, a true 30-minute end-to-end run-ID
  verification deadline, and a `curl --disable` closed-environment system-CA-verified HTTPS HEAD clock
  before consumption and every non-cleanup operation. The initial seed must omit both endpoint IDs and
  hashes; the actual max-one result alone materializes the final production secrets and deployment
  snapshot hashes, writes the exact four guarded endpoint secret files, and rebinds all 22 guarded
  secret hashes. Seed validation recursively rejects endpoint identity case variants and future
  hashes. After operator verification, pre-endpoint failures use a separate cleanup-only
  schema/runtime with only the operator database and RunPod inputs; before that verification, the
  early cleanup runtime uses only request and RunPod inputs, performs no database mutation, and never
  claims database cleanup. Neither path loads the normal production input, guarded roles, endpoint
  identities, signing keys, or key registration, and normal/acceptance work rejects it.

- Commit `bd0cede` adds a fail-closed ledger for 15 exact release gates. It binds current source,
  deployed executable/config/contracts, both immutable lane identities, and V2-09–V2-12 evidence to
  trusted evidence classes and strict freshness/lineage checks.
- Fresh bounded two-lane smoke must precede independent zero-job/zero-worker drain and settled
  billing. Missing, stale, misordered, fixture-backed, or identity-drifted evidence blocks release.
  The ledger always leaves `liveReleaseAuthorized=false` and requires separate explicit authority.
- Commit `c516726` exposes authenticated factual GPU readiness in the hosted catalog/UI: both lanes
  remain `NOT_QUALIFIED`, exact SoulX full/split visual approval is recorded, missing live gates are explicit, transport
  is `DISABLED_UNQUALIFIED`, and no dispatch control is exposed.
- Commit `614464b` adds dry-run-default production config rendering, exact binding validation, a
  separately guarded deployment entrypoint, and production bundle firewalls. Placeholder or drifted
  config fails closed; the committed runtime remains `DISABLED_UNQUALIFIED` and is not deployed.
- Commits `0f73d81`, `9ef6efa`, `120b6b8`, `941176f`, `3d1b439`, and `4239be9` complete audited
  provider-free source through migrations 0041–0044 and the guarded pair Workflow: durable lane
  batches, authenticated envelopes, atomic two-lane predispatch, fenced runtime/recovery, callback
  settlement, and durable zero-worker evidence. They neither deploy nor authorize the live path.
- The envelope verifier byte change invalidates both earlier image-bound lane qualifications. Fresh
  immutable Mage and SoulX images, sealed deployment snapshots, dual live qualification, explicit
  fresh exact paid/deployment authority are required before activation. The visual crop gate is closed.
- Focused tests pass 8/8 for the release ledger and 15/15 for readiness; both received independent
  GO audits, and the production-config slice also received independent provider-free GO. No config
  render, deployment, provider call, credential access, GPU use, invitation, or spend occurred.

- Close essential P0/P1 auth/session/origin/rate, tenant/R2/callback/SSRF/path/upload, secret/log,
  cost-amplification, and legacy production-bundle risks.
- Add per-project/global spend stops, quotas, essential alerts, backup readback, rollback pins, and
  concise incident/provider/billing/rollback runbooks.
- Remove/fail-close fixture GPU profiles, fake production transport, manual Pod/GPU controls, and
  legacy public dispatch exports from production builds.
- Run focused security/build checks, canonical provider-free aggregate once, scans, one backup/
  rollback readback, one release-current hosted control-plane restart/reconcile with no duplicate
  dispatch or foreign result, and one production Chrome smoke. Perform a disposable restore only if
  V2-09 through V2-13 changed schemas/migrations; otherwise cite the accepted V2-06 restore proof.
- Defer nonessential broad cross-browser/accessibility, exhaustive fault, and ten-job statistical
  matrices to real-sample hardening.

## Minimum proof

No P0/P1, exact production URL/commit/config/image/endpoint identities, invited private smoke,
spend/queue/drain guards, release-current restart/reconcile proof, rollback readiness, settled cost
and recurring-charge disclosure, terminal jobs, zero workers, intended retained resources, and final
independent audit.
