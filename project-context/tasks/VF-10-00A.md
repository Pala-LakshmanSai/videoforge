# VF-10-00A — V2-00 independent roadmap/context audit

Status: ready; read-only; `$0`

## Goal

Independently verify the V2-00 reset at current HEAD before any V2-01 implementation starts.

## Scope

- Read the startup files, V2-00 acceptance, roadmap, prompt pack, normative context diff, selectors,
  decisions, gates, and handoff evidence.
- Run context/schema validation, formatting/diff checks, and contradiction scans.
- Confirm active sources consistently describe tenant-private data, one active provider
  workload/account and two different accounts globally, ordinary video caps of one/account and two
  globally, lower-priority preset previews on the same locks/slots, fair admission, private
  artifacts, two scale-to-zero Serverless lanes using the existing isolated volumes, preserved
  foundations/UI/Ranga grammar, and correct non-exactly-once RunPod recovery semantics.
- Prove only the V2 brief allowlist remains and detect any active singleton-session, manual-compute,
  alternate-runtime, fallback/repair, or cross-user rule that conflicts with V2 production truth.
- Confirm active context, selectors, validators, and source indexes have no dangling reference to a
  removed checkpoint, task brief, profile, archive, or evidence path. Embedded labels inside exact
  retained artifact identities and evidence paths are provenance only.

Read-only means no edits, commit, credentials, provider calls, cloud mutation, allocation, or spend.
Return PASS/FAIL with P0/P1/P2 findings and exact evidence. Missing proof is FAIL; do not repair.
