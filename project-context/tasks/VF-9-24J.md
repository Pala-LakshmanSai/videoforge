# VF-9-24J — Isolated persistent model-lane architecture reset

Status: context/architecture task complete; no application or provider implementation

## Goal and outcome

Replace the incorrect active Serverless/ephemeral design with one coherent, implementation-ready
architecture based on the user's working ImageForge pattern.

The context now locks:

- exact ImageForge Mage-Flow INT8 ConvRot and EchoMimicV3-Flash Turbo FP8 as separate model lanes;
- one different persistent `EU-RO-1` model volume and one disposable Pod per lane;
- no shared/cross-mounted/cross-adopted volume, Pod, manifest, cache, lock, or runtime;
- manifest-derived volume capacity chosen only before separately authorized provisioning;
- independent explicit live compatible GPU selection for Mage and Echo;
- verified voiceover/input pre-Generate barrier, then concurrent Pod start with local preparation;
- offline model load, exact manifest verification, warm-up, and authoritative `model_ready`;
- durable outputs, exact Pod deletion/absence proof, and retained volumes;
- automatic deterministic final MP4 assembly with no Premiere/manual alignment.

The prior `VF-9-24I` paid/ephemeral route is historical and non-executable. No production gate was
closed. No code/schema, credential, model, Pod, template, volume, deployment, or cloud mutation was
performed. One read-only RunPod inventory check made at the user's request confirmed zero Pods,
zero endpoints/active workers, and zero volumes; it created no resource and incurred no spend.

## Authority used

- Context, architecture, decision, plan, task, template, and gate text only.
- Provider/cloud mutations: false.
- Application code changes: false.
- External spend: `$0`.

## Validation

- `project-context/scripts/validate-context.sh`
- `project-context/scripts/validate-schemas.sh`
- `git diff --check`
- Active-context contradiction audit plus independent Mage/runtime and scheduler/reference audits.

The next proposed task is `VF-9-24K`. It remains paused until the user explicitly authorizes
application implementation.
