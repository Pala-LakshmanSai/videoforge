# VF-9-24N — CP-03 production-grade word transcript

Status: active; provider-free local/container implementation only; external spend `$0`

## Identity and dependencies

- Task ID: `VF-9-24N`
- Milestone: `CP-03` production-grade word transcript
- Dependency: committed, re-audited green `CP-02` at audit-fix commit
  `5747e7b4e9c1d41564663afb1c0c0ad7272efe5b`
- Prerequisite evidence:
  `project-context/evidence/acceptance/VF-9-24M/cp02-shared-admission-queue/reaudit-after-fixes.json`
- Base commit/branch: `c9529ea` on `main`
- Decision ID: `DEC_TIMING_001`
- All production/provider/model/GPU/cost gates remain open.

## Goal

Promote the existing `whisper.cpp base.en` worker and durable timing lineage into one pinned,
container-compatible media-worker contract shared by Mac development and future Cloud Run Jobs. An
owned 30-minute-class input must produce deterministic word/phrase timing plus a durable,
restart-safe work receipt without altering original render audio.

## Context read profile

Use `cp03_word_transcript` from `MANIFEST.yaml`. Read only the CP-03 section of
`22_PROJECT_COMPLETION_CHECKPOINTS.md`, this brief, and narrow timing/worker/acceptance sections from
normative domain files when implementation requires them.

## In scope

- Audit and promote existing `workers/image-media` whisper.cpp word-timing code; preserve proven
  parser, contracts, failure taxonomy, model hash pin, and durable control-plane persistence.
- Separate media CPU ownership from the image-model lane and provide one identical Mac/container
  entrypoint with pinned executable behavior.
- Probe and hash original audio, normalize a separate analysis WAV, deterministically chunk long
  audio with overlap, reconcile duplicate boundary words, and emit monotonic bounded word and phrase
  timestamps.
- Keep original voiceover immutable and identified as final-render truth. Normalized/chunk files are
  temporary analysis/span inputs only.
- Add content-addressed local and R2-port fixture adapters, immutable result/receipt publication,
  idempotent replay, restart recovery, attempt lineage, and fail-closed conflicts.
- Test owned short, noisy, silence, malformed, and 30-minute-class fixtures; manual timing spot-check
  using the already pinned local whisper model when available.
- Add compact transcript inspection only if required for acceptance. Do not redesign UI.

## Out of scope

- Cloud Run deployment, Cloudflare/Neon/R2 mutation, credentials, provider calls, paid API, GPU,
  model download, model change/addition, or external spend.
- Scheduler/composition changes, CP-04 work, provider orchestration, render pipeline completion, or
  hosted-production proof.
- Any claim that Mac/container fixtures prove deployed Cloud Run/R2 behavior; that waits for CP-08.

## Acceptance

- Exact source probe/hash and separate normalized-analysis artifact; original bytes unchanged.
- Deterministic long-audio chunk plan and overlap reconciliation; no duplicate/backward words at
  chunk boundaries.
- Monotonic non-overlapping words, timestamps within source duration, complete phrase word coverage,
  and bounded phrase/audio timing.
- Silence and malformed inputs fail with stable contract errors; noisy/short/long owned fixtures pass.
- Same input/config/model produces same canonical transcript and receipt across Mac and container
  contract modes.
- Identical replay returns existing success; conflicting replay fails closed; interrupted attempt
  resumes from durable chunk receipts after process restart.
- Focused Python/control-plane/contract tests, container smoke, canonical `pnpm verify`, context
  validation, secret scan, dependency audit, and `git diff --check` exit zero.
- If UI changes, installed real Chrome acceptance must pass. If UI does not change, record that
  Chrome was not required.
- Evidence under `project-context/evidence/acceptance/VF-9-24N/cp03-word-transcript/`.
- Final handoff commits bounded CP-03 only and stops before CP-04.

## Safety and budget

- `provider_calls_authorized: false`
- `remote_or_cloud_mutations_authorized: false`
- `credential_access_authorized: false`
- `model_downloads_authorized: false`
- `application_code_changes_authorized: true` for CP-03 only
- Maximum external spend: `$0`
- Existing pinned local whisper model may be used. Any new or changed model requires user approval.
- No paid compute starts; no provider inventory refresh is needed.

## Context update

At handoff, update `CURRENT_STATE.yaml`, `MANIFEST.yaml`, `00_START_HERE.md`, CP-03 roadmap status,
this brief, and immutable acceptance evidence. Record exact commits, validations, fixture/manual
spot-check results, contract/container parity, remaining gates, provider/spend state, and paid-compute
shutdown state. Select no CP-04 work without new authority.
