# VF-1-05 — Postgres, outbox, cost, and workflow adapters

Status: standing provider-free authority after VF-1-02

## Goal and dependencies

Implement concrete query adapters for the committed repository contracts plus transactional outbox,
cost reservation, workflow transport, signed callbacks, execution claims, reconciliation, and
cancellation. VF-1-02 must be green; provider and cloud transports remain fakes/local emulators.

Read profile: `phase1_orchestration`.

## Scope and ownership

- Own concrete repository/workflow adapter modules and their isolated tests.
- Use the existing additive migrations as authority; any necessary additive corrective migration is
  integration-owned and must commit before adapter code depends on it.
- Atomically bind generation task, attempt, reservation, outbox, owner and idempotency records.
- Implement callback signature/replay checks, one execution claim, ambiguous acknowledgement,
  reconciliation, cancellation, accepted-result pairing, and monotonic events.
- Run every fixed canonical repository behavior body against the concrete adapter.
- Integration owner alone edits migrations, dependencies, root/app composition, context, or commits.
- Out of scope: Neon/DATABASE_URL, Cloudflare deployment, provider network, credentials, model code,
  production claims, and spend.

## Acceptance

- All 13 canonical repository scenarios execute semantically against fresh concrete adapters.
- Rollback leaves no orphan task/attempt/cost/outbox records; retries preserve exact IDs and costs.
- Unknown acknowledgement reconciles without blind redispatch; cancellation shapes remain coherent.
- Owned lint/typecheck/tests/build pass; integration runs `pnpm db:check`, full uncached verify,
  secret scan, dependency audit, and diff check.

Evidence: `evidence/acceptance/VF-1-05/durable-orchestration-adapters`.

## Rollback and handoff

Revert adapter and any additive migration commits normally; never rewrite 0001/0002. Report green
to the Wave 2 integration owner. Select VF-1-06 only after VF-1-03/04/05 are jointly integrated.
