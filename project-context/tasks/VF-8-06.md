# VF-8-06 — AvatarForcing capacity-corrected qualification

Status: exact successor selected after VF-8-05 container/capacity diagnosis

## Goal

Run one capacity-corrected, bounded qualification of the pinned AvatarForcing worker, then prove
technical output or a truthful terminal failure and leave RunPod at absolute zero.

## Context read profile

Use `phase8_avatarforcing_resume` from `MANIFEST.yaml`.

## In scope

- Immutable image `ghcr.io/pala-lakshmansai/videoforge-avatar-primary@sha256:02022fad4b9c924d329925446968aad946db7b3578f78f0b925c9779d1396db7`.
- Locked source `KlingAIResearch/AvatarForcing@63b73e6c0f7bb42180ca6d7e1bf11c1de1a80b39`
  and weights `lycui/AvatarForcing@e2448919a7b535c29f34e07892884ae1a43c6ace`.
- One Serverless endpoint, one concurrent worker, `workersMin=0`, GPU allowlist exactly
  `NVIDIA L40S` and `NVIDIA A100 80GB PCIe`, and the smallest owned five-frame input.
- Existing checksum, progress, startup-timeout, cost-stop, cancellation, queue-purge, drain, endpoint
  delete, template delete, and independent zero-inventory guards.

## Authority and limits

- Existing Keychain RunPod credential only; maximum new spend `$4.50`.
- Combined sprint provider spend must remain below `$10.00`; settled spend before this task is
  `$0.6120589383` across Runware and RunPod.
- Maximum one endpoint and one running worker; no standalone Pod or network volume.
- No model substitution, MuseTalk/SkyReels execution, Mage work, UI/schema redesign, deployment, or
  production profile promotion.
- Stop on ambiguity, more than one concurrently running pod, five minutes without running compute,
  `$4.25` observed spend, operator signal, or unconfirmed cleanup.

## Acceptance

- A valid five-frame MP4 has exact source/audio/model/image lineage, checksum, duration, FPS,
  dimensions, audio/video probe, measured cost, and final zero inventory; or evidence records one
  truthful terminal failure without blind redispatch.
- Never promote the candidate to a selectable execution profile from a failed or merely booting run.
- RunPod ends with zero Pods, endpoints, templates, volumes, and active workers.
- Focused/full verification, context/schema/secret/diff checks pass and the server is stopped.
