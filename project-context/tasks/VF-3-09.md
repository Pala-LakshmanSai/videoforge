# VF-3-09 — Runware Gemini Image Style adapter

Status: exact provider-free implementation task

## Goal and dependencies

Implement the production-shaped Runware Gemini transport boundary for explicit draft Image Style
analysis, using injected fake transports and reference-input resolvers only. Depends on
`GATE_STYLE_001` PASS and the VF-3-06 trusted style semantic/profile foundation. It does not
activate the custom-style database/API/UI lifecycle and cannot close `GATE_STYLE_002`.

Read profile: `phase3_runware_style_adapter`.

## Scope and ownership

- Own provider-free style adapter modules/tests under `packages/pipeline/**`, their package export,
  evidence, and `CURRENT_STATE.yaml`.
- Pin canonical AIR `google:gemini@3.5-flash`, `style-analyzer-v1`, `outputFormat: JSON`, the
  qualification-locked strict inlined provider schema, `thinkingLevel: low`, temperature `0.1`,
  top-p `0.9`, max tokens `6000`, and Google media resolution `medium`; omit seed and tools.
- Inject an exact reference-input resolver, task-ID source, clock, transport, and evidence sink.
  Resolved inputs must preserve ordered alias/hash identity and use bounded HTTPS image URLs; URLs,
  image bytes, provider text, and secrets never enter committed evidence.
- Build one canonical one-task Runware array request. Validate response task identity, finish
  reason, JSON shape, usage, cost, optional returned model identity, and the full VF-3-06 trusted
  schema/semantic/profile boundary.
- Preserve a valid first result. If and only if its provider output fails deterministic
  schema/semantic validation and a reserved second call stays within the exact caps, retry the same
  whole reference set once with explicit correction lineage. Do not retry ambiguous dispatch,
  timeout, transport failure, identity drift, missing/invalid usage or cost, cap exhaustion, or
  private-input resolution failure.
- Emit redacted immutable attempt evidence containing only task IDs, hashes, usage/cost, latency,
  disposition, validation category, retry lineage, and accepted profile/output hashes.

## Out of scope

- No live Runware/model-search call, credential access, provider spend, reference upload or image
  processing, database mutation, API/UI route, publication/version/archive lifecycle, Mage,
  RunPod, staging, deployment, or qualification-runner change.
- No change to the built-in `documentary_stock_v1`, accepted UI/renderer/output grammar, or
  ordinary project creation/generation. The deterministic fixture analyzer remains the default.

## Acceptance

- Tests lock exact request bytes/settings/schema/alias mapping and prove imports/build/tests perform
  zero network or credential activity.
- Cover one-attempt success, one deterministic-validation retry success, retry-reservation refusal,
  malformed/duplicate JSON, schema/content leakage/crop failure, task/model/finish/usage/cost drift,
  timeout/ambiguous transport, resolver reorder/hash/URL drift, and evidence redaction.
- Prove accepted output/profile hashes replay exactly, at most two transport calls occur, ordinary
  video creation makes zero analyzer calls, and no input URL/pixels/provider text enters evidence.
- Targeted pipeline lint/typecheck/tests, then `TURBO_FORCE=true pnpm verify`, context validation,
  secret scan, and diff check pass at `$0` in fixture mode.
- Evidence: `evidence/acceptance/VF-3-09/runware-gemini-style-adapter`.

## Safety, caps, and handoff

- `provider_calls_authorized: false`; maximum external spend `$0`.
- Runtime policy constants remain first analysis `$0.08`, retry total `$0.15`, and second-attempt
  reservation `$0.08`; these are fail-closed response checks, not call authority.
- Revert only this task's commits. Stop on a required shared-schema/migration/API decision,
  credential/network requirement, unresolved provider identity mismatch, or regression.
- On pass, select only the next dependency-ready provider-free custom-style lifecycle slice or a
  separately evidence-ready Mage task; do not activate either provider path from this adapter.
