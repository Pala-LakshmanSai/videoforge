# VF-9-09 — Compliant Mage quality confirmation

Status: authorized bounded one-image quality successor

## Goal

Generate one representative VideoForge-compliant Mage BF16 image without requested text/logo/brand,
preserve timing/cost/output evidence, and determine whether the candidate is suitable for a real
owned-video render slice.

## Context read profile

Use `phase9_mage_quality_confirmation` from `MANIFEST.yaml`.

## Authority and limits

- Existing Keychain RunPod credential, exact VF-9-07 candidate digest, one transient template, one
  scale-zero RTX 4090 endpoint, one job, public model download, evidence, Git push.
- Task cap `$0.15`, automatic cost stop `$0.12`; no Runware call; no retry after terminal failure.
- Exact prompt must require believable documentary realism and prohibit visible text, logos,
  watermarks, signage, graphics, borders, and overlays.
- Save/show output; record all VF-9-07 timing phases and exact GPU/cost; mandatory drain/delete/zero.
- No production promotion, deployment, schema/UI/Avatar change, or ImageForge volume mutation.

## Acceptance

- Valid 1280x720 PNG with exact lineage and no visible prohibited output on inspection.
- RunPod absolute zero; canonical verification green.
- Exactly one real owned-video render/Chrome successor if pass, or one bounded correction brief if fail.
