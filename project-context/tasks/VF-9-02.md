# VF-9-02 — Mage fail-closed worker boundary

Status: provider-free implementation successor after VF-9-01

## Goal

Implement and test the strict Mage job/result/bootstrap boundary while exact model access remains
unresolved, without publishing a runnable model image or starting provider compute.

## Context read profile

Use `phase9_mage_worker_boundary` from `MANIFEST.yaml`.

## In scope

- Add a Mage production module to `workers/image-media` with exact source revision, immutable model
  revision requirement, prompt/hash/batch/seed/size validation, four-step CFG-1 BF16 invocation,
  safe PNG probe/hash/lineage, cancellation, timeout, and redacted errors.
- Fail bootstrap closed with `MAGE_MODEL_REVISION_INACCESSIBLE` until an exact official revision is
  supplied by a later authenticated admission task.
- Test packed 32–64 production boundaries and one 1024x1024 inline qualification contract using
  fakes only. Keep existing ASR/render/fixture behavior unchanged.

## Authority and limits

- Local code/tests/docs and Git push only; `$0` external spend.
- No HF credential access, model download, GHCR model image, RunPod/Runware mutation, GPU, dispatch,
  app/schema/UI changes, Avatar work, model substitution, promotion, or deployment.
- RunPod remains absolute zero.

## Acceptance

- Focused and full verification pass with strict hostile-input, timeout/cancel, output, lineage,
  redaction, and unresolved-revision tests.
- Existing worker behavior remains green.
- Exactly one authenticated Mage revision-admission successor is authored; RunPod remains zero.
