# VF-10-09 — V2-09 hosted real-GPU bridge and short E2E

Status: provider-free product implementation authorized in parallel; live E2E blocked on accepted
V2-07 and V2-08 certifications; 5–6-hour best-case live timebox

Provider-free progress at `ada2541`: the durable dispatcher now accepts a provider-neutral async
transport and the existing RunPod client has a mocked fail-closed adapter. Hosted endpoint identity,
authority activation, ordinary output callback/barrier composition, and live dispatch remain open.

Hosted progress at `bcf3a1c` and `fa1a6a9`: complete sealed certification lineage now gates every
lane-scoped new-work call, expired evidence exposes restart-safe cleanup only, and the production
client is free of fixture APIs/schemas/assets with GPU truth `DISABLED_UNQUALIFIED`. Later audited
provider-free commits below add ordinary-output and render-plan groundwork, but live composition and
execution remain open.

Provider-free integration is independently audited through migrations 0037–0044 and guarded Workflow
commit `4239be9`. Commits `0f73d81`, `9ef6efa`, `120b6b8`, `941176f`, and `3d1b439` add durable lane
batches/task lineage, authenticated envelopes, atomic two-lane predispatch, fenced pair transitions,
database-backed production/recovery composition, and exact zero-worker evidence. This completes the
source path only: production remains `DISABLED_UNQUALIFIED`; migrations/grants/config are undeployed,
and no roles, secrets, provider credentials, or live authority are provisioned. The verifier change
invalidates prior Mage/SoulX image qualifications, so fresh immutable images, sealed deployment
snapshots, dual live qualification, and fresh exact paid/deployment authority remain required before
hosted live execution. The exact SoulX full/split provider-free crop profile was approved on
2026-08-26 and is hash-bound to its candidate, evidence, samples, source geometry, and transforms.

Provider-free cost admission freezes the exact canonical 40-second, 30 fps approved-SoulX manifest:
one Mage item from the split right image and two SoulX avatar items, with every segment and accepted
artifact ID/hash bound by the plan hash. The DB-aligned cold-lane wall bound is 20 minutes: historically observed
SoulX start-to-ready 672.035 seconds plus 107.965 seconds execution/upload and a conservative 420 seconds cancellation/
terminal tail. At the pinned RTX4090 Serverless Flex ceiling of $1.10/GPU-h, the two primary lanes
reserve $0.733334, possible provider duplicate liability without redispatch reserves $0.733334, and
settlement/billing-lag/cancellation uncertainty reserves $0.533332: exactly the $2.00 V2-09 hard
variable-cost ceiling. This is a conservative provider-free forecast, not measured live economics.
Fresh trusted provider rate/availability and billing observations, DB/provider time without future
timestamps, exact plan/work/cap/baseline/no-redispatch persistence, DB-aligned 20/30 deadlines,
settled cost, terminal jobs, and zero workers are enforced at the real pair-commit and Workflow
settlement boundaries. Migration 0042 unconditionally requires canonical plan
`sha256:f975e2be15db227e96c6ea06f025c3f7ead025a5f80b80e9e2b0ac1f9fd6a4ea`, derives and requires
its exact one-Mage/two-SoulX work from persisted lane artifacts, and atomically checks baseline plus
the full $2 phase cap against the $17.50 completion cap.
Migration 0044 atomically recomputes fresh provider billing and durable settled, duplicate, and
reserved exposure before guarded settlement can commit; the runtime role cannot invoke settlement
and the reconciler role receives only the cost-guarded settlement capability. Focused provider-free
repair tests pass; fresh independent audit is still required. The $17.50 completion cap allocates
V2-09 and V2-10 separately; the same volumes remain separately $7/month and excluded.

## Goal

Connect the hosted tenant-private app to the exact Mage/SoulX autoscaling lanes and complete one
owned 30–60-second Generate-to-private-MP4 journey with no fake GPU asset or manual media edit.

## Scope

Provider-free hosted composition, fixtures, bundle fencing, integration tests, and durable state
work may proceed now. Real-lane configuration, deployment, hosted live execution, provider calls,
and spend remain blocked until both lane certifications and a fresh exact authority permit them.

- Fix hosted client/Worker production-mode truth and emitted-bundle firewall coverage.
- Compose fair admission, durable account/workspace-owned tenant project/stage state, Serverless
  outbox/runtime, real lane adapters, private R2 barrier, and personal-worker render-plan
  materialization.
- Reuse the completed word transcript, scheduler-v2 voiceover splitting, three compositions,
  prompt/style path, and renderer; do not rebuild them.
- Keep fake transport fixture/test-only and remove legacy public production exports only after
  transitive-import proof, the provider-free bridge, and the short hosted E2E are green; never
  rewrite migrations.
- Pass one provider-free journey, one resume/cancel, and a focused foreign-tenant negative matrix
  over the newly composed assignment, callback, signed-R2-port, artifact-barrier, render-plan, and
  result paths; then run one exact bounded hosted live journey and Chrome play/seek/download.

## Minimum proof

Touched-package tests/typecheck/build, production bundle scan, one canonical provider-free aggregate,
one Chrome journey, exact lineage/hashes/ffprobe, the focused changed-surface tenant-negative matrix,
settled itemized cost, terminal jobs, zero workers, and an independent audit.
