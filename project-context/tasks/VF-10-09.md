# VF-10-09 — V2-09 hosted real-GPU bridge and short E2E

## Deployed render, prompt and claim repairs — 2026-09-15

Source `e68013c1` is live on Cloudflare `054e4e1e-9c12-4a16-b1f8-e29d26d21674`, QUALIFIED_EXACT. Worker0.1.21 is published, installed and ONLINE with exact bundle. The 18m11s local final MP4 is delivered. Render thread bounds and one audio-only loudness correction are verified on retained media; unchanged output gates pass. Prompt-only camera/equipment suppression and no readable/invented text apply to initial and regenerated images. Existing accepted images remain unchanged.

Shared job claiming now uses two transactions instead of four, retaining tenant/freshness/lease checks. Focused claim6, render16, prompt25, regeneration5 and release8 tests, TypeScript and production build/firewall passed. Extra ASR threads changed words; two-chunk parallelism gave only25.45s to24.80s, so neither speculative change shipped. Exact full local transcription32.65s matched production words/times; this is not a newly measured hosted speedup. Prior hosted18-minute totals remain2m12s transcription and13m34s spanning.

No GPU generation or visual rerender was used for this deployment. At05:06Z balanceUSD14.4230990852, zero active compute. Retained sealed volumes remain. The existing long project's hosted failed render record is preserved; its final video is available locally, and hosted finalization remains a separate gate. Evidence: `evidence/acceptance/VF-10-09/2026-09-15-render-prompts/acceptance.json`.


## Current long-video render and prompt repair — 2026-09-15

Stages 6/7 accepted 212 images and 63 avatar clips for project `7d101190-87f6-4b51-a870-757e858e2abd`. Bounded FFmpeg threads resolve the measured long-render resource failure. The local final MP4 was delivered: 18m11.567s, 1080p30, 233491300 bytes, SHA256 `e22c16de224aca87349ade3984bdffea6e75f22f2df51d7f1c8b7c8705c9d101`. One audio-only correction preserved every video frame and passed unchanged gates at -16.44 LUFS, -5.25 dBTP and 1ms AV drift. Worker0.1.21 automates this fallback. Hosted release/finalization remain pending.

Prompt-only fixes describe optical treatment as viewpoint rather than physical camera equipment and require plain unmarked surfaces without readable or invented lettering. Ordinary generation and per-image regeneration share these guards; immutable styles and existing media stay intact. Focused checks: 25 prompt, 5 regeneration, 16 render, 8 release compatibility tests pass. No new GPU dispatch; balanceUSD14.4328213074 and zero active compute verified at04:46:49Z. User authorized release/deployment after receiving the local file.


## Current span performance repair — 2026-09-14

User authorized focused diagnosis, repairs, tests, commit/push and production delivery for existing castle2. Audio65/65 is live-verified; source `ec4a629c`, desktop0.1.20, median cadence43.24s to10.09s. The subsequent211-image request exceeds the qualified ordinary Mage64-item list guard. Candidate4096-item envelope-bound parser passes57 tests, retaining sequential GPU execution. Publishing/qualifying a new image is the next finite-spend boundary; preserve the consumed Mage attempt and reconcile existing SoulX work. Long-video final MP4 acceptance remains open. Evidence: `../evidence/acceptance/VF-10-09/2026-09-14-span-performance/acceptance.json`.

## Short comparison: production render repaired — 2026-09-14

The 37-second castle comparison completed transcription, context, planning, seven images and two avatar clips. Exact saved-input reproduction proved assembly failed its true-peak check: AAC output was −16.46 LUFS/−0.99 dBTP. Worker 0.1.19 uses matching −2.5 dBTP targets in both normalization passes, preserving the output limit of −1.5. The production MP4 now passes at −16.93 LUFS/−1.97 dBTP, 37,000 ms, 7,916,962 bytes. Source `2671d957` also awaits finalization before closing its database pool; the existing completion retry finalized the saved MP4 without another render. Runtime is COMPLETE, generation SUCCEEDED, and actual Chrome playback to the end and seeking passed.

Deployment is `2671d957` / Worker `fea826c1-a7a7-4ec3-9fae-fa989d66f8d7`, QUALIFIED_EXACT; desktop worker 0.1.19 is installed and ONLINE. Upload renewal migration 133 is applied. Stale generation banners/timers and failed media-link refresh are repaired. Focused checks and the actual CPU/production flow passed; no full suite was added. Exact long Stage 3 requests are about 5,000 estimated input tokens and returned in 2.45 seconds; input length was not the proven blocker, so no speculative chunking was added.

The original paid attempts remain unreplayed. This verifies the short production pipeline, not an 18-minute final generation. Real Chrome shows all ten stages COMPLETE at 100%; final approval and MP4 download passed with an exact checksum match. The separately initiated image regeneration also completed and preserved the original final MP4. At 16:18:39Z all work had zero active compute, empty queues and balance USD16.4803671682. Current evidence: `evidence/acceptance/VF-10-09/2026-09-14-pipeline-audit/short-comparison-acceptance.json`.

## Confirmed provider failure repair — 2026-09-14

The separate `castle1` attempt failed at the upstream provider: exact archive records `providerUnavailable`, HTTP502, and no accepted result. Strict task-identity checks now classify that nested archive explicitly; the API reports a confirmed failure and the UI removes the misleading repeated recovery control. Source `518cd88c` is deployed as Worker `799e8d79-5a97-49c2-af1c-7be080ab35a8`, `QUALIFIED_EXACT`. Actual Chrome verifies the stopped state and corrected recovery message. 145 API/UI tests, 11 transport tests, both TypeScript targets, production build/firewall and focused lint/format pass. No inference replay or GPU job was started. Latest saved preflight at14:56:34Z shows zero active compute and empty queues. Full18-minute acceptance still needs a fresh bounded attempt; prior one-attempt authority is consumed. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-pipeline-audit/castle1-repair.json`.


## Latest pipeline follow-through — 2026-09-14

Approved deployment and one fresh 18-minute run reproduced Stage 3 failure: `aggregate_budget`, not a transport failure. The validator now retains whole normalized facts within the existing 360-character limit. Source `853851c8` is deployed and activated as Worker `cb062d87-e67f-4088-9e21-ac5f79a244e8`, `QUALIFIED_EXACT`. An additional 31 focused tests, both TypeScript targets, production build/firewall, context validation, and focused lint/format pass.

Fresh project `d7bffbed-7188-4eac-98c0-ce42085a417e` cannot recover its original context: exact-task retrieval returns a 46-character redacted placeholder instead of the original 567-character output. No inference replay or GPU job occurred. Provider-reported context cost is USD0.00034207. The approved fresh attempt is consumed; another inference requires bounded authority. Full 18-minute MVP acceptance remains open. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-pipeline-audit/fresh-run.json`.

## Pipeline audit and source repairs — 2026-09-14

The 18m11.553s castle project stopped at Stage 3 with `VOICEOVER_CONTEXT_INVALID` after preparation and ASR completed. Exact original invalid content is unproven: original-task retrieval returns a redacted archive. Preserve the UNKNOWN attempt without inference replay. Source repairs cover context parsing and task identity, truthful status/resume controls, span scheduling replay, streamed final-MP4 verification, and existing TypeScript errors. Final focused verification: 209 tests, both TypeScript targets, and Cloudflare build/firewall pass. Existing accepted 37-second MP4 was played to its end in actual Chrome without media errors; this does not establish long-video acceptance.

Audit source `aab538e5` was subsequently deployed; the latest follow-through above supersedes its initial pending live gate. No new provider job, GPU resource, deployment, or migration occurred during this audit. Read-only inventory at 13:17:33Z showed zero active compute and zero queued/running jobs; existing sealed volumes remain retained. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-pipeline-audit/audit.md`. Current authoritative status is in `CURRENT_STATE.yaml`.

## UI/UX polish — 2026-09-14

User requested a bounded full-interface audit with Luna workers, concise actionable copy, fewer
avoidable interactions, all dropdowns inspected, and the existing design preserved. Presentation
and shared-control fixes are deployed after the user approved the verified commit. Only the
Cloudflare release and exact-version activation record changed; no provider job, GPU mutation,
schema migration or retained-resource change occurred. Current release/check state is in
`CURRENT_STATE.yaml`; evidence:
`evidence/acceptance/VF-10-09/2026-09-14-ui-ux-polish/acceptance.json`.

## Style analysis validator and status repair — 2026-09-14

Production analysis reached Runware, then local profile validation attempted runtime Ajv code
generation inside Cloudflare Workers. The exact invocation dispatched at 10:58:18.361Z, logged
schema compilation failure at 10:58:34.410Z, and returned HTTP 503 after 20,081ms. Its durable attempt
became UNKNOWN while its immutable version remained ANALYZING; catalog/UI ignored the attempt
state and displayed perpetual progress. No successful provider receipt or task UUID was persisted.

Reuse existing precompiled style validators and retain semantic validation/hash behavior. Future
requests use the already persisted reservation UUID as their provider taskUUID. Migration 132 adds
a tenant-scoped read-only status projection with a narrow runtime EXECUTE grant; the UI clearly
marks UNKNOWN as stopped/unconfirmed without exposing a retry action. No paid replay is authorized.

Focused UI/API checks 140/140, pipeline checks 182/182, and a fresh subprocess regression with
runtime code generation disabled pass. Cloudflare build/firewall and changed-file lint pass.
The generic migration/grant test is blocked by the pre-existing omitted 115 in both manifest and
live ledger (130 rows through 131). Apply only 132 after exact existing manifest-prefix verification;
do not introduce unrelated 115 or rebuild unrelated runtime grants. Live deployment/recovery
readback is recorded in CURRENT_STATE.yaml and the scoped evidence directory.

## Private preset upload timeout repair — 2026-09-14

The user requested repair and production deployment after Prepare analysis timed out on six
4.1–5.1 MB original references. Real Chrome reproduced the retained failure state. The exact draft
was created at 09:08:46.899Z; private storage completed the first 4,822,562-byte original at 09:09:19Z.
Read-only storage checks found that object intact and the other eleven uploads absent, with all
assets still UPLOADING. The fixed 30-second client race ended the sequence while leaving its PUT
running. This is a transfer deadline failure, not provider analysis or draft creation failure.

Use a finite five-minute file-transfer deadline and abort the PUT when the operation ends. Keep
API deadlines, private destinations, checksums, same-draft retry identities, and explicit paid
analysis unchanged. Scope is provider-free; no GPU or analysis dispatch is authorized by this repair.
Deployed source `56994706` as Worker `c4a9bb0c-598a-46e3-ae6c-3272153d2ad7`,
transport `QUALIFIED_EXACT`. Focused upload/UI tests, lint, production build/firewall, and context
validation passed. Real Chrome recovered the existing style: all twelve assets verified and the
durable draft reaches Analyze references. One transient normalized R2 PUT HTTP500 was reconciled
with object HEADs before a same-key retry; replay reused recovery version2 and completed.
Evidence: `evidence/acceptance/VF-10-09/2026-09-14-style-upload-timeout/acceptance.json`.
No paid analysis or GPU work was started.

## Production preset creation repair — 2026-09-14

The user requested working avatar and image-style creation on videoforge.buzz. Production-only UI
and API guards still limited creation to staging despite the existing tenant-private upload,
commit, review, approval, analysis, and publication handlers. Enable those existing flows in
production independently of GPU transport; preserve origin validation, tenant ownership, immutable
versions, explicit style analysis, and all generation qualification and spend guards.

Source `10f415c173363a179535a4bcac1d6119d75ba8fd` is deployed as Worker
`2089443a-3ee2-446b-822d-358fedc594b3` with `QUALIFIED_EXACT`. Focused tests142/142,
Cloudflare production build/firewall, changed-file lint/format, and context validation pass.
Real Chrome verified both creation links/forms, avatar upload/commit/approval with a visible private
preview and New Project picker entry, and a durable three-reference style draft. Full style analysis
and publication passed in the local fixture browser; no billed live analysis was rerun. No GPU or
provider job was started. A transient post-deployment version-header mismatch was reconciled by
read-only verification before activation; the committed deployment was never repeated. Both disposable verification presets were archived
through the existing tenant-scoped function after exact identity and zero project-reference checks;
immutable versions and media remain retained.


## Current autonomous MVP repair — 2026-09-14

The 37-second project `b79fef93-11e0-4333-9af0-8cd4fddcbd01` completed seven images and two avatar clips. Mage and SoulX submitted 1.136 seconds apart. Chrome verified both galleries, both avatar clips, full final MP4 playback, approval, and native download; downloaded SHA-256 matches the successful render.

Worker `0.1.18` is published and installed. Migrations119–128 are applied. Runtime is COMPLETE, generation SUCCEEDED, exactly one final-output event exists, and the paid lease is RELEASED. Final fixes are deployed from `d27b055d2478e3d2e2d2e216b607732aae2af1c7` with `QUALIFIED_EXACT`. Chrome shows all ten stages complete and no stale review flags. At22:04Z RunPod balance was USD16.9136 with zero active Pods/workers and zero queued/running jobs. No remaining gates for this Stage6/7 MVP repair.

Preserve USD2/run, USD3 RunPod floor, exact RTX4090 EU-RO-1 lanes at min0/max1, and sealed50GB volumes. Stop the entire flow below the floor. Never redispatch accepted media or terminal/uncertain paid attempts. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-stage6-7-mvp/acceptance.json`.


## Superseding current approval — 2026-09-10

The current user approved a bounded production repair and one fresh successor Stage 6/7
qualification at clean source `a1a324b3cfaa19124e9ed8214d7b5b379caef6ab`: deploy the terminal
UUID-ingestor fix, publish/activate the corrected Mage image, verify the existing SoulX binding,
and start one new project. The finite cap is USD 2 on RTX 4090 in EU-RO-1, with the existing 50 GB
volumes, `workersMin=0`, at most one worker per lane, no fallback, no redispatch, and mandatory
provider drain/readback. The previously consumed project/pair remains frozen and non-retryable;
this approval is for a fresh successor only.

The user explicitly authorized continuing this V2-09 scope without further approval prompts:
bounded repairs, reseals, fresh source-bound authorities and deployment through9.7, the single9.8
Chrome E2E, and9.9 cleanup/readiness. Existing numeric caps, frozen Stage 6/7 and no consumed-authority
replay remain mandatory. This instruction overrides older repeated-approval requirements below.

The frozen Stage 6/7 wording in the historical continuation below refers to the consumed pair and
does not override the current bounded approval above.

**Current 9.8 continuation — 2026-09-08:** Main checkpoints1–8 finished;
9.7 VERIFIED and frozen;9.8 remains active on the existing project after successful
upload, commit and ASR. Exactly one missing-secret PUT succeeded. Strict23-secret
readback verifies resulting Worker `399e9a38-0c26-42c5-b668-33d16e7d6ad5`, unchanged
deployed source `9dad16ee5939c8633bc363d06fef65b1deea2425` and configuration.
Current effective transport is DISABLED_UNQUALIFIED: ledger88's append-only
source/config uniqueness prevents new-version activation rebinding. Two scoped
reads confirm the attempted successor did not commit. No context, prompt or GPU
request has run. Three separated reads prove zero Pods/workers/queues; completion
USD0.386630 and observed provider incrementUSD0, subject to billing lag.

The user prioritizes finishing9.8 with multiple Luna workers. Minimal forward
migration89 passes five runner checks and one actual PostgreSQL17 regression;
live guard and independent Luna audit passed. Applying89 is a
change to the explicit ledger88 boundary and remains pending that scope decision.
No secret/create/upload/commit/CPU replay, Worker rebuild, changed lane/qualification,
or Stage 6/7 rerun is permitted. Existing USD 2 incremental/USD17.50 completion caps
and separate retained-volume consent remain unchanged.9.9 still requires completed
private MP4 playback/seek/download and final closure. Current details live in
CURRENT_STATE.yaml and the tracker; stop evidence is
`evidence/acceptance/VF-10-09/2026-09-08-one-secret-rebind-stop/closure.json`.
Older attempts below are historical and non-reusable.

## Historical activation evidence

Prior attempts: CURRENT_STATE.yaml and GATES.yaml.

Earlier child-stdin and PostgreSQL17 role repairs remain accepted; exact evidence is in CURRENT_STATE.yaml and GATES.yaml.

Historical proposals `sha256:58cb3e2b…92f4a` and `sha256:cfcaccfc…e05` are consumed
`FAILED_CLEAN`, with no later deployment or GPU work reached. Exact stop conditions, cleanup and
PostgreSQL role-repair evidence remain in `CURRENT_STATE.yaml`, `GATES.yaml` and Git history.

Previous activation: proposal `sha256:7ecd9929…751f` and authority
`v2-09-7ecd9929c7cedc66` are consumed `FAILED_CLEAN`. Exact source and descriptor preflight passed,
and the global pre-mutation completion baseline was USD 0.386630. Protected-input materialization
then stopped because the exact plan's private `secrets/` parent had not been created. Role creation
was never called; authority-bound cleanup completed, all 22 later operations remained pending, and
there was no endpoint/template, media release/install, Cloudflare/R2, GPU, provider-job POST,
redispatch, Stage 6/7, or spend action. This authority is non-reusable.

Provider-free repairs `f0dac6df…d1eb`, `0117cb1c…03e7`, `0f4ed8dd…fca0`,
`dcd4c146…bdfa`, and `eb113e44…4884` validate every state, role-journal, database, RunPod, and Cloudflare-secret output
parent plus the exact clean source, content-bound 30–60-second voiceover, exact private Wrangler
home, and local media-worker pairing before the single-use state claim. Missing, unsafe,
wrong-HEAD, dirty, unsupported-media, or malformed-environment prerequisites now stop before
credential read, provider preflight, or database access; the executor never creates arbitrary
directories. The production voiceover probe derives exact duration from the stable streamed packet
timeline instead of an unavailable pipe format summary. Focused tests pass 97/97 and independent
audit is GO P0=0/P1=0. A clean
context/bundle/proposal seal, pre-staged private inputs, and fresh exact approval remain. Stage 6/7
remain frozen `QUALIFIED_PASS_CLEAN`.

Latest stop: proposal `sha256:f9d40174…554a` and authority `v2-09-f9d401740dc721ca` are consumed
`FAILED_CLEAN`. Descriptor-HEAD preflight passed, and the global pre-mutation completion baseline was
USD 0.386630 against the USD 15.50 ceiling. `materialize-v209-protected-inputs` then stopped with
`V2_09_PROTECTED_MATERIALIZATION_ROLE_INVALID`: plan role suffix `f9d40174` did not equal the exact
authority-derived suffix `5beb5d17`. The guard ran before owner URL derivation, role journal/file
write, or psql mutation. State-only cleanup closed outer state `sha256:1cefe37d…78228`; no inner
authority, database mutation, publication/install, endpoint/template, Cloudflare/R2 mutation,
provider-job POST, GPU job, redispatch, Stage 6/7 work, or spend occurred. The authority is
non-reusable; fresh exact approval is required after provider-free role-binding reseal.

Provider-free repair `6cc1dc20…d39b` validates the exact SHA-256 authority-derived role names while
loading the private materialization plan, before state claim, credential read, or provider preflight.
The same pure validator governs later materialization and cleanup, eliminating the prior drift path.
The five focused suites pass 120/120 and independent design audit is GO P0=0/P1=0. Stage 6/7 were
untouched. Final context, bundle, proposal, and independent audits remain before fresh approval.

Consumed activation materialization: proposal `sha256:f9d40174…554a` was approved at pushed clean
source `b10b63dc…02b8`. Its private mode-0600 authority and plan were source-bound to media-worker
bundle `sha256:2d51ce97…c1e1`, recorded by one authority-materialization successor, and independently
audited before their single execution. They are historical and cannot be reused. Stage 6/7 remain
frozen `QUALIFIED_PASS_CLEAN`.

Status: descriptor-HEAD proposal `sha256:ea4e2e45…e396` and authority
`v2-09-ea4e2e45c34f740d` were consumed once at pushed clean source `e5d0c452…c21a`. The exact
descriptor-HEAD preflight passed both frozen image identities and all layer checks, exact RTX 4090
EU-RO-1 LOW availability, guarded rate, zero compute, billing USD `3.5284023286949378`, and two
unchanged retained volumes. The next `read-pre-mutation-completion-baseline` step stopped locally
with `V2_09_PROTECTED_MATERIALIZATION_PG_SERVICE_INVALID` because the configured `channel_binding`
key is outside the closed pg_service allowlist. Outer state `sha256:609cf984…98c7e` is `FAILED_CLEAN`:
no inner authority, database call, mutation, media publication/install, endpoint/template, provider
job POST, GPU job, redispatch, or spend occurred, and no cleanup was required. The authority is
non-reusable. The preceding shared-abort slow-link proposal
`sha256:91892237…e161` was consumed once at pushed clean source `f226c321…fdb5` and is
`FAILED_CLEAN`. Authority `v2-09-918922376c1f5677` and its plan are consumed and non-reusable. The
one combined execution ran for approximately 539 seconds (8m59s), entered only
`run-read-only-preflight`, and stopped with exact stderr code
`V2_09_READ_ONLY_PREFLIGHT_GHCR_READ_AMBIGUOUS`. No inner authority or preflight proof was emitted;
all 25 later operations remained pending. No database/R2/Cloudflare/media-worker mutation, lane,
GPU job, provider-job POST, redispatch, or spend occurred. Closure is
`failed-clean-shared-abort-slow-link.json` (`sha256:914c6a85…e77f1`); no cleanup or replay is required,
and no active V2-09 provider authority remains. The provider-free repair removes the repeated approximately
8.9-GB layer-body transfer: it fully hashes each exact immutable manifest/config, validates every ordered
layer descriptor against the frozen full-byte proof anchor, performs one bounded anonymous HEAD per unique
layer with strict redirect/size/digest checks and no authorization forwarding, and preserves the first
failure across shared cancellation. Focused preflight tests pass 23/23 and combined focused tests pass
47/47. Repair `6b2d2e91…69a4` now requires exactly one service section and the exact five production
keys, `sslmode=require`, `channel_binding=require`, the exact matching URL query, and
`PGCHANNELBINDING=require` across every `psql` and sanitized media-worker child environment. The
five focused suites pass 118/118 and independent audit is GO with P0=0/P1=0. No provider call,
credential read, remote mutation, GPU job, Stage 6/7 action, or spend occurred during repair.
Production remains disabled pending the final clean source/bundle/proposal seal and fresh exact authority.
The preceding proposal `sha256:d6e5ff7b…a7a0` is consumed and non-reusable after its mandatory
independent successor audit stopped before provider action. Its authority carried the prior source's
media-worker bundle hash even though the canonical bundle identity includes `git_commit`; the exact
bundle for control source `1631a0c1…` is `sha256:17214dfa…a5e9d`. No credential, provider, database,
R2, Cloudflare, GPU, job, mutation, redispatch, or spend action occurred. A corrected source-bound
replacement proposal is required. Audit-stop evidence is
`evidence/acceptance/VF-10-09/2026-09-07-combined-production-authority/audit-stop-stale-media-bundle-d6e5ff7b.json`
(`sha256:3aaa3e3e…c139`). The prior unlimited-project authority `v2-09-051c6d42eec2cd69` is consumed
`FAILED_CLEAN` and non-reusable. Its independently audited execution started only the bounded
read-only preflight, which ended with native message `The operation was aborted due to timeout`.
No operation completed, no inner authority was derived, and no database, R2, Cloudflare,
media-worker, endpoint/template, GPU-job, provider-job POST, redispatch, mutation, or external spend
occurred. Provider-free inspection proved the GHCR request's 60-second signal remained attached to
the streamed multi-gigabyte body and its unguarded reader leaked the native timeout. The repaired
source separates the bounded header deadline from size-derived absolute and idle body deadlines,
maps stream failures to the exact V2-09 ambiguity code, and preserves size/digest enforcement.
Focused read-only-preflight verification passed 17/17 for that consumed authority; it is historical
and non-reusable. Production remains disabled.
The preceding consumed authority `v2-09-d9703df5651be2e5` passed its independent successor audit,
then its single bounded preflight securely read the approved
credential and issued the authorized read set but failed closed with
`V2_13_FULL_LIVE_ADAPTER_RUNPOD_MUTATION_ADMISSION_RATE_CATALOG_FLEX_MISSING`. The exact RTX 4090
EU-RO-1 match had LOW-or-better availability but no required own `price.flex` field. No raw catalog
was persisted, so no alternate price key is claimed or accepted. No database, endpoint, R2,
media-worker, Cloudflare, GPU, provider-job POST, redispatch, spend, Stage 6, or Stage 7 action
occurred. That preceding authority is non-reusable. Provider-free repair now
uses the authenticated Serverless catalog only for exact RTX 4090, EU-RO-1, LOW-or-better
availability and uses RunPod's official Serverless pricing page for the exact guarded
`$0.00031/second` / `$1.116/hour` rate. It does not infer an unavailable catalog price field or add
GPU/region/rate fallback.

The repaired provider-free production lifecycle source preserves the narrow GHCR repeated-layer,
signed-shard redirect, and same-file-descriptor credential repairs and adds one closed-world
Serverless catalog normalizer. It checks exact-GPU identity, region, and LOW-or-better availability
from authenticated catalog truth while independently parsing the exact rate from RunPod's official
pricing page and exposing only redacted diagnostic subcodes.
It securely hydrates the media-worker PostgreSQL environment from the same descriptor-bound
operator-URL snapshot used by the adapter identity, rejects secret-bearing static plan fields, and
never embeds the generated database password in the plan. Focused combined verification passes
55/55 and the independent repair audit is GO P0=0/P1=0. It also derives the exact 1,660-second
Chrome success horizon only after authenticated tenant/catalog discovery, requires the outer
authority to cover that full horizon, and preserves resumable login without extending an adopted
request. Focused Chrome/coordinator verification passes 38/38 and independent audit is GO P0=0/P1=0.
V2-07 Stage 6 and V2-08 Stage 7 are independently live-qualified,
cleaned to zero compute, consumed, and frozen; neither was modified or rerun. The current V2-09
request's initial bounded read-only provider preflight and five exact replacement authorities were
each consumed by one clean failure. Exact fifth-replacement proposal `d186ca50...c962` ran once at
source `7ff69fde...b912`, exited 1 at the generic rate gate, emitted no proof, and was cleaned without
redispatch, mutation, job POST, database or R2 call, GPU use, or spend. Live
deployment and the real browser E2E require fresh exact V2-09 deployment/spend authority. Combined
proposal `sha256:453a2763d76c5fc1a19d04507df21b46248b4dae4bcfc6ab6b948431d7cb838e`,
bound to `38a363a228f48bb7f3f866744d2dee5a3b3abb26`, was consumed only by its exact push; protected-input
admission then stopped before preflight, credential read, database, RunPod, R2, media, Cloudflare,
GPU, or spend mutation.

The implemented provider-free source connects an ordinary tenant-owned Generate request from
fair admission through database-derived two-lane admission, exact Mage and SoulX request
materialization, signed private R2 ports, SPAN audio preparation, one-shot RunPod dispatch and
recovery, terminal-output ingestion, artifact acceptance, renderer handoff, and internal diagnostics.
It retains no fixture-only project or fixed-plan dependency in the ordinary path. Additive migrations
0074–0086 carry this contract and the activation loaders require the exact ledger through 0086.
Migration 0081 guards the ordinary avatar predispatch identity; 0082 settles exact two-lane SUCCESS
GPU cost; 0083 performs reconciler-only atomic render-terminal handoff; and 0084 persists the
one-click staged-claim tombstone and cleans pre-admission, waiting, partial, terminal, and replay
lifecycle states without browser reclick or provider redispatch. Migration 0085 adds the exact
tenant-bound integer-micro-USD completion-baseline read. Migration 0086 makes hosted project cost
limits nullable/unlimited while retaining exact reservation, attribution, and settlement. Production remains
`DISABLED_UNQUALIFIED`.

The last recorded hosted database state is migration 0073. Migrations 0074–0086, their exact owner,
activation-operator, hosted-runtime, and pair-reconciler grants, media-worker 0.1.15, the Cloudflare
production configuration, and the two
production max-one endpoint/template pairs are not deployed. The qualified immutable Mage and SoulX
images and their V2-07/V2-08 acceptance artifacts are frozen predecessors. V2-09 may bind them to
fresh exact max-one deployment snapshots without image republication or Stage 6/7 rerun.

The concrete adapters now bind git push/readback, migrations and exact grants, media release,
RunPod and Cloudflare production operators, early one-click Chrome continuation, render handoff,
cost settlement, and deterministic cleanup. Owner, operator, runtime, and reconciler use four
distinct PostgreSQL roles on one exact host/port/database/TLS identity. Their protected URL files
and the complete Cloudflare secret set are mode-0600, owner-bound, snapshotted, hash-bound, and
drift-checked before mutation. The Chrome claim is durably exclusive before browser navigation and
persists exact request/project/revision/generation identity before continuing, so recovery never
reclicks Generate.

The dedicated V2-09 read-only preflight tooling is provider-free tested. Its initial bounded
execution failed closed on a provider-free GHCR duplicate-layer false positive. Exact replacement
proposal `67b08992...ba31` was consumed once at source `7697707e...e5d9`; it made exactly one
read-only RunPod account GraphQL POST, six authenticated RunPod GETs, and anonymous GHCR reads,
then failed closed with `V2_09_READ_ONLY_PREFLIGHT_GHCR_BLOB_REDIRECT` before proof emission. Its
zero-byte mode-0600 output and disposable worktree were removed. Exact second-replacement proposal
`5e8d10d8...a3c2` was then consumed once at source `263775af...f18c` with the same exact read-only
call counts. It failed closed with
`V2_13_FULL_LIVE_ADAPTER_RUNPOD_MUTATION_ADMISSION_RATE_CATALOG` before proof emission. That generic
code could represent catalog container, exact-GPU cardinality, `price.flex`, EU-RO-1 cardinality,
or availability failure; no raw catalog was persisted, so none is claimed as observed. Its
zero-byte mode-0600 output and disposable worktree were also removed. Both replacements performed
no mutation, job POST, database or R2 call, GPU use, or spend.

Source `eba705f4...7593` now admits only a top-level GPU array or wrapper `.gpus` array, exactly one
stable NVIDIA GeForce RTX 4090 identity, exactly one EU-RO-1 region, strict positive finite
`price.flex` no higher than USD 0.00031/second or USD 1.116/hour, and LOW, MEDIUM, or HIGH
availability. Shape, identity, rate, region, and below-threshold capacity fail with distinct
redacted codes; no GPU, rate, or region fallback was added. It binds a clean source and performs
only fixed account, Serverless catalog/rate, billing, inventory, retained-volume, and anonymous
frozen-image reads; it exposes no mutation, dispatch, authority-write, DB, R2, GPU, or spend
capability and emits only a redacted canonical proof. Its third-replacement execution is recorded
below and is consumed.

Exact third-replacement proposal `d36a9022...1b28` was consumed once at source
`eba705f4...7593`. It made the same exact approved read-only calls and failed closed with
`V2_13_FULL_LIVE_ADAPTER_RUNPOD_MUTATION_ADMISSION_RATE_CATALOG_RATE` before proof emission. Its
mode-0600 output remained zero bytes, and its detached worktree and temporary directory were
removed. The categorical result proves only a `price.flex` rate-shape failure. Because no raw
catalog was persisted, the fixed-scale trailing-zero explanation is the strongest provider-free
inference rather than an observed fact. No mutation, job POST, database or R2 call, GPU use, or
spend occurred.

Exact fourth-replacement proposal `38a4e06f...13d` was consumed once at source
`aa8353ef...4b61`. It made the same exact approved read-only calls and failed closed with
`V2_13_FULL_LIVE_ADAPTER_RUNPOD_MUTATION_ADMISSION_RATE_CATALOG_RATE` before proof emission. Its
mode-0600 output remained zero bytes; the clean detached worktree and temporary directory were
removed. No raw catalog was persisted, so the exact live `price` object remains unobserved. No
mutation, job POST, database or R2 call, GPU use, or spend occurred, and no redispatch occurred.

Source `7ff69fde...b912` keeps own `price.flex` mandatory and parses only that leaf while ignoring
sibling price fields. Alternate-only keys cannot act as fallback; whitespace, signs, exponents,
malformed decimals, zero, nonfinite values, and cap breaches remain rejected. Exact GPU, EU-RO-1,
availability, and no-fallback gates are unchanged. Focused tests pass 30/30 and independent repair
audit is GO P0=0/P1=0. Exact fifth-replacement proposal `d186ca50...c962` was consumed once at this
source. The execution exited 1 with
`V2_13_FULL_LIVE_ADAPTER_RUNPOD_MUTATION_ADMISSION_RATE_CATALOG_RATE`, emitted no proof, and left the
mode-0600 output at zero bytes. Its disposable worktree and temporary directory were removed. There
was no redispatch, mutation, job POST, database or R2 call, GPU use, or spend.

Provider-free diagnostic source `0fb1a927...18e6` preserves every admission constraint while moving
the availability check before rate parsing and adding only redacted shape/rate subcodes; it does not
persist or expose the provider catalog. Focused tests pass 31/31 and independent source audit is GO
P0=0/P1=0. After deterministic context materialization and independent context audit, the next gate
must be one combined authority for the bounded diagnostic preflight and, only if that preflight
passes unambiguously, the exact V2-09 deployment plus Chrome live E2E. Do not request a standalone
blind sixth preflight approval.

Combined source `c6ac4b89...1e527` removed the standalone-preflight approval loop with one
single-use outer authority. It
runs the redacted preflight first, then only on PASS journals the exact 12-operation staging prefix,
streams and seals media-worker 0.1.15, applies the then-current migrations, derives and rechecks the durable
completion baseline, creates the two exact max-one lanes, renders the real endpoint-bound config and
Wrangler bundle, and derives a distinct inner authority for the remaining deployment and one Chrome
E2E suffix. STARTED or COMPLETED inner ambiguity invokes full inner cleanup-only reconciliation
without replay. Focused V2-09 integration passes 146/146 and independent whole-source audit is GO
P0=0/P1=0.

The historical canonical 40-second plan and one-Mage/two-SoulX work set remain immutable fixture
evidence for migration 0042; they are not the ordinary browser project's plan. Migration 0074 derives
the real project's current revision, plan, task counts, artifact reservations, authority, and work
manifest from tenant-owned durable state. The exact SoulX full/split crop profile remains hash-bound
to its approved candidate, evidence, source geometry, and transforms.

Hosted projects are unlimited: project creation exposes no maximum-spend field and migration 0086
persists a `NULL` project cost limit. This does not remove cost accounting or release authority.
Any future V2-09 production authority must still carry its own finite source-bound action cap and
stop conditions. At the exact RTX 4090 Serverless Flex ceiling of $1.116/GPU-hour, the provider-free
forecast still accounts for two primary lanes, duplicate liability without redispatch, and
settlement/billing/cancellation uncertainty. Fresh official rate, exact authenticated EU-RO-1
availability, billing, inventory, deployment identities, persisted plan/work, terminal jobs,
settled cost, and zero workers must all be verified during the approved live run. The two retained
50 GB volumes continue separately at $7/month combined and are excluded from the finite release cap.

## Goal

Connect the hosted tenant-private app to the exact Mage/SoulX autoscaling lanes and complete one
owned 30–60-second Generate-to-private-MP4 journey with no fake GPU asset or manual media edit.

## Scope

Provider-free hosted composition, fixtures, bundle fencing, integration tests, durable-state source,
and lifecycle recovery build on audited source `ea5942d99dc3c515276cdd281275bea7a6dafcc2`.
The current repair removes the user project-cap input/end-to-end field, persists unlimited project
cost state through migration 0086, and separates authenticated availability proof from the official
RunPod rate source. Both lane
certifications remain satisfied and frozen. Prior authority `v2-09-4d1aa75c99393ec1` was consumed
`FAILED_CLEAN` before credential access. Replacement authority `v2-09-d9703df5651be2e5` is
consumed against pushed clean source `5442eb77`; real-lane configuration, deployment, provider
mutations, GPU use, and spend require a new independently audited source-bound authority.

- Preserve hosted client/Worker production-mode truth and emitted-bundle firewall coverage.
- Verify the composed fair admission, durable account/workspace-owned tenant project/stage state, Serverless
  outbox/runtime, real lane adapters, private R2 barrier, and personal-worker render-plan
  materialization.
- Reuse the completed word transcript, scheduler-v2 voiceover splitting, three compositions,
  prompt/style path, and renderer; do not rebuild them.
- Keep fake transport fixture/test-only, preserve the production bundle firewall, and never rewrite
  migrations.
- Preserve the green provider-free journey, resume/cancel behavior, and focused foreign-tenant
  negative matrix over assignment, signed-R2-port, artifact-barrier, render-plan, and result paths.
  Run the exact bounded hosted live journey and Chrome play/seek/download only after fresh authority.

## Minimum proof

Predecessor source proof at `ea5942d99dc3c515276cdd281275bea7a6dafcc2`: focused launch-wiring
verification passes 41/41 and independent audit is GO P0=0/P1=0. The unlimited-project and official
rate-source repair at `dc6ef647...86b9` passed focused validation and two independent audits
`GO P0=0/P1=0`. The current descriptor-HEAD repair passes 23/23 focused preflight tests and
47/47 combined focused tests and awaits independent audit completion, a clean commit, exact bundle recomputation, and one
fresh replacement proposal. No Stage 6 or Stage 7 source or qualification was modified or rerun. Exact fifth-replacement proposal
`d186ca50...c962` is consumed. The
consumed preflights made no provider mutation, database/R2 call, job POST, GPU use, or spend. Final
proposal `4d1aa75c...62bb2` failed closed at its missing preflight key path before any credential read
or external call.

Future authorized live proof: one real Chrome Generate-to-private-MP4 journey, exact
lineage/hashes/ffprobe, settled itemized cost, terminal jobs, zero active workers, and deterministic
cleanup with only the two existing retained volumes left unchanged.


## Single-image review request — 2026-09-14 (source validated; not deployed)

User requested editable saved prompts, Enter submission, and a fast Regenerate image action for every generated scene. The implementation uses the existing Mage materializer, signing, transport, receipt verifier, and durable workflow.

- Prompt projection joins exact tenant/revision/task identity; accepted replacements carry their edited prompt. Per-scene drafts, IME-safe Enter, Shift+Enter, pending/error states, and session-persisted idempotency survive lost responses and remounts. The accepted original stays visible until verified replacement and refreshed projection.
- Migration 0129 adds tenant-scoped immutable regeneration operations, real output reservations/receipts, and dedicated IMAGE_REGENERATION leases under existing account/global capacity caps. Ordinary video lease ownership and accepted units remain untouched. New attempts retain pinned style/negative prompt and receive a fresh seed/output identity.
- Runtime dispatch uses CAS, never resends SENT/unknown acknowledgements, verifies provider provenance and R2 content, atomically selects replacement, and releases capacity only after provider absence proof. Queued work expires before dispatch; expired assigned work is cancelled and observed. Existing provider startup preflight runs before new dispatch.
- Migration 0130 cost hardening: each regeneration displays a USD2 maximum; fresh read-only provider balance/catalog/billing admission protects the USD3 floor and records a conservative estimate including cancellation tail. Estimates are projected separately from settled costs; aggregate endpoint observations are not represented as a per-scene invoice. Failed predispatch checks cancel only unsent requests, then prove drain before releasing capacity.
- Existing final videos remain unchanged; image editor states this explicitly. Automatic completed-video rebuilding was asked as an optional scope question and remains unanswered. No incomplete render migration is registered. Fresh render identity, immutable manifest/final promotion, and stale approval handling are a separate unimplemented integration; do not claim a regenerated image appears in an existing final MP4.
- Verified locally: 166 focused UI/product/runtime tests; final guard additions add twelve tests; regeneration PostgreSQL 10/10 and fair admission 13/13; static grants 5/5; Cloudflare production bundle; Chrome HTTP-fixture regeneration and ordinary-flow checks. Actual signed receipt/materializer tests are provider-free. Whole-project typecheck has unrelated existing errors.
- No deployment, remote mutation, provider generation, or paid compute occurred. Deployment/activation and one bounded live UI-to-provider-to-receipt check remain required. Prior paid authority is consumed and cannot be reused. Preserve USD3 balance floor, existing qualified RTX4090 EU-RO-1 min0/max1 lanes and sealed volumes; no fallback/redispatch.

### Approved live continuation and sign-in blocker — 2026-09-14

User approved deployment and one USD2-capped live image regeneration, then requested faster regeneration and sign-in repair. Source now runs independent readiness checks concurrently and returns only id/attempt/state during status polling. Auth handler converts thrown/empty500 failures into a safe503 message and preserves successful redirects;14 focused tests pass. This does not lift provider quotas.

Read-only production proof: Google social sign-in returns500; the owner database rejects connections with the exact Neon transfer-quota-exceeded error. Thus no migration, app deployment/activation, or GPU request was attempted. Fresh RunPod baseline: USD16.8650115845, zero active Pods/workers, existing min0/max1 RTX4090 EU-RO-1 lanes and sealed50GB volumes unchanged. Catalog Serverless rate USD1.10/hour is below approved USD1.116/hour ceiling. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-image-regeneration-source/live-blocker.json` and `provider-preflight.json`.

Restore Neon access before deploying source that requires migrations129–130. An upgrade changes ongoing database billing and is outside the approved RunPod test cap; no account billing change was made. Preserve the unused single-generation authority, but refresh source/rate/balance/zero-compute preflight after quota recovery. Existing finished video remains unchanged as disclosed in the accepted image-only proposal.


## Superseding regeneration live evidence — 2026-09-14

The user upgraded Neon to Launch; production database access recovered. Migrations129–130 and runtime grants applied. Source `2c3261a7` deployed as Worker `eec81dcd-fe0d-4ebc-b8e9-f6b418e54939` and activated `QUALIFIED_EXACT`. Real Chrome Google sign-in and private queue passed. The grant rebuild omitted `selected_span_audio` SELECT; restoring that scoped grant recovered project detail, and the source grant script now includes it.

One Enter-submitted scene regeneration reached one RunPod job, then failed with `MAGE_SERVERLESS_TIMING_ISSUED_AT_INVALID`: copied original `issued_at` and changed expiry violate Mage timing contract. Original seven images, two avatars, and final MP4 remain unchanged. Provider delay66981ms/execution59912ms are failed-job timings, not successful regeneration speed. Observed balance decrease USD0.0303383611; final observed balance USD16.8346732234. Fresh inventory and both health queues proved zero compute/queued/running jobs. Failed request lease released using existing scoped release function after proof. Source cleanup must use retained-endpoint terminal scale-zero proof because exited FlashBoot records prevent strict zero-record drain. Timing and cleanup repairs and a fresh bounded live acceptance remain. Original one-attempt authority is consumed; never redispatch this failed attempt. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-image-regeneration-source/live-attempt-1.json`.


### Corrected regeneration deployment

Source `43ad91438e22a757fab252806e28fe3d7011e332` deployed as Worker `c5bf759f-0940-43d4-ae37-b5b69ea20b1c`, activated `QUALIFIED_EXACT`. Fresh signed issue time and exact3600s Mage/port validity are separate from the preserved600s operational deadline. Regeneration uses retained-endpoint terminal scale-zero cleanup, and already-released leases finish safely. Service11/11, execution5/5, grant static2/2, production build, changed-file lint/format and context validation pass. Real Chrome reloaded healthy completed project after deployment. Successful replacement and successful latency measurement still require a fresh bounded live attempt; the original one-attempt approval is consumed and no further paid submission occurred. Evidence: `repair-deployment.json` beside prior live-attempt proof.


### Image regeneration accepted in production — 2026-09-14

The user approved one fresh test capped at USD1.96. Request `ecc6327e-7fcc-402f-ba07-1bd7b34a0d7b` completed on deployed43ad9143 with one provider POST and no redispatch. Real Chrome Enter submission, original-image retention, accepted replacement, success message, and new image/prompt persistence after full reload passed. Request-to-acceptance157222ms; provider delay12144ms/execution133610ms. Signed receipt/reservation/hash checks pass, the gallery selects the new artifact, all original nine runtime units remain immutable, eight other media items and existing final MP4 are unchanged. The gallery intentionally overlays the latest verified regeneration; it does not rewrite the already-rendered video's original accepted units.

Automatic terminal cleanup released the lease. Fresh provider proof shows zero Pods/workers and empty queues. Balance16.8249510012 before,16.7686399735 at closure; observed decreaseUSD0.0563110277, withinUSD1.96 and aboveUSD3 floor. This is observed account movement, not a settled per-job invoice.

Live inspection also found migration129 persisted envelope expiry rather than the separate lineage deadline. Forward migration131 now persists exact600s request/lease deadlines while preserving3600s signed validity. All11 database regression tests pass;131 applied successfully with runtime execute grant preserved. Historical requests were not rewritten. No additional GPU test or app rebuild was needed for this SQL-only correction.

No remaining gates for requested per-image regeneration or sign-in repair. Existing final video remains unchanged by design and is disclosed in editor. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-image-regeneration-source/live-attempt-2.json`, `chrome-regenerated.png`, and `repair-deployment.json`.

## Custom domain deployment — 2026-09-14

User owns and approved `videoforge.buzz` as the production origin. Cloudflare Free DNS/SSL hosts the zone; Spaceship remains registrar. Custom domain is bound to the existing `videoforge-production-runtime` Worker. Google OAuth retains its workers.dev callback and adds `https://videoforge.buzz/api/auth/callback/google`. Domain deployment performs no GPU generation or model/provider mutation. Canonical origin promotion must preserve exact deployment qualification and verify sign-in/project/media access in Chrome. Evidence: `evidence/acceptance/VF-10-09/2026-09-14-custom-domain/setup.json`.

Custom domain accepted live: deployed `d185b032` / Worker `12be28ff-c0a1-4ad7-83aa-d86d6b14be56` remains `QUALIFIED_EXACT`. Chrome Google sign-in returns to `videoforge.buzz`; queue/API, connected computer, completed project, seven-image prompt/regeneration viewer, and final video playback to 37.166 seconds verified. HTTP redirects to HTTPS. R2 adds the new exact origin while preserving old origins; signed PUT preflight returns 204. No GPU actions or paid-plan additions. 33 deployment tests, build, changed-file lint/format, context and secret checks pass. No remaining domain gates. Final evidence: `evidence/acceptance/VF-10-09/2026-09-14-custom-domain/acceptance.json`.
