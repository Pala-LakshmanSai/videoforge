# VF-1-06 — durable mock recovery convergence

Status: complete at `dc86938972e776c8479637f2fb3ca9706999eed4`

## Completion — 2026-08-10

The initial `7028413f0ca7f69e37d72d663de93f327e075d5a` checkpoint exposed seven post-commit audit
findings. Corrective implementation `dc86938972e776c8479637f2fb3ca9706999eed4` closes terminal and
cancellation fencing, safe same-attempt redispatch, sibling suppression, exact cost conservation,
bounded recovery inspection, and UI truthfulness. Three fresh post-commit read-only audits found no
remaining high or medium issue. The full uncached gate and 36 installed-Chrome journeys pass with
zero skips. Evidence is committed at `6e0431fc3bddf3c187a8a80020ee8fd50c6282ad` under the path
below; provider calls remained disabled and external spend was `$0`.

## Goal and dependencies

Prove one durable mock job across dispatch ambiguity, restart, reconciliation, duplicate execution,
accepted-result selection, exact cost state, and cancellation using the integrated Phase 1 adapters.

Read profile: `phase1_recovery`. VF-1-03, VF-1-04, and VF-1-05 must all be green.

## Scope and ownership

- Integration owner composes the existing adapters and owns end-to-end recovery tests, evidence,
  context, and commits.
- Verification lanes may add disjoint fault fixtures only after the integration API commits.
- Use deterministic local transports and owned artifacts; restart must reconstruct from persisted
  truth rather than process memory.
- Out of scope: provider calls, staging resources, cloud deployment, credentials, models, real cost,
  UI redesign, and schema weakening.

## Acceptance

- Ambiguous dispatch enters reconciliation and never blindly creates a second paid attempt.
- Duplicate workers contend for one execution claim; duplicate attempts/cost remain visible.
- Restart recovers progress; exactly one verified output becomes accepted.
- Cancel works before dispatch, after dispatch, during reconciliation, and after terminal states.
- Installed Chrome truthfully displays pending/reconciling/cancelled/ready states.
- Targeted fault tests and full uncached verify, context validation, secret scan, dependency audit,
  Chrome regression, and diff check pass.

Evidence: `evidence/acceptance/VF-1-06/durable-mock-recovery`.

## Rollback and handoff

Revert the convergence commit; keep adapters independently green. On success, record the isolated
staging checkpoint as awaiting external authority and activate parallel local VF-1-07/VF-1-08.
Never provision staging in this task.
