# VF-10-08 — V2-08 SoulX Serverless and visual activation

Status: V2-07 predecessor accepted and frozen; V2-08 is not yet qualified. Proposal `a9bfe3d0` was
consumed after exactly one cold POST. The job remained `IN_QUEUE` for all 55 reads before an
operator interruption; no warm/fault job or redispatch occurred and no output was verified.
Cleanup deleted the disposable lane, proved all 27 planned R2 keys absent, and three reads proved
zero pods/workers/endpoints/templates, exactly two retained volumes, and stable billing
`2.9606986929138657`. Evidence is in `failed-audit-interruption-attempt.json`.

The same approved workflow `34009939213` published immutable
image `sha256:75c5999b5934b13ed6eaf6e35f28fe22b4a3ec30702efc1f44ff4407438bf036`
and fully anonymously verified its config/source labels and all 24 descriptors. It remains reusable
without republication because the worker/Dockerfile/workflow bytes are unchanged. The provider-free
repair gives only V2-08 whole-span materializations a 3,600-second port/envelope lifetime so a warm
batch cannot expire behind the 1,660-second cold horizon; the shared V2-13/default lifetime remains
900 seconds. Fresh reuse-only approval is required. Stage 6 remains unchanged and must not be rerun.
The sealed reuse-only proposal is `sha256:784f68823244adf733bcbe423f1445fdabb0e85ea7f6a37aaec076c820e6d04f`.
It is now exactly approved at pushed clean control `fe716c3b402c82e7397357aa48d50c58cbeba597`.
One deterministic authority successor is materialized and must pass an independent diff audit before
any RunPod or R2 mutation. The immutable image must be anonymously reverified without publication or
republication. The hard incremental cap is `1.8681257496355103` from baseline
`2.9606986929138657`; the cumulative stop remains `4.828824442549376`.

Historical receipt-nonce failure: Proposal `106c97f6` and authority `87968f10` at execution commit `d42deeb` are consumed
and non-reusable. Its cold and warm jobs both completed four native clips, but the worker hardcoded
`receipt_nonce: 1` for both distinct attempts; cold verification consumed nonce 1 and warm
verification correctly failed with `RECEIPT_NONCE_REPLAYED`. Exactly two jobs were posted, there was
no redispatch and no fault-job dispatch. Cleanup removed the disposable lane and all eight generated
R2 outputs; three final reads proved zero compute/disposable resources and stable posted billing
`2.828824442549376`, with USD `0.20` conservatively reserved for delayed settlement. Evidence is in
`failed-receipt-nonce-attempt.json`.

The provider-free repair derives a stable positive JavaScript-safe nonce from a domain-separated
SHA-256 of the signed `attempt_id`, so the same attempt remains deterministic while cold and warm
attempts cannot share the hardcoded value. Focused worker tests pass. The successful successor run
will also preserve local mode-0600 copies only after the complete receipt, every R2 byte/hash, and
every exact ffprobe check pass, before mandatory R2 cleanup, so the verified clips can be shown to
the user. Worker bytes changed, therefore a new immutable SoulX image publication and fresh exact
source-bound qualification authority are required. Stage 6 remains unchanged and must not be rerun.
Fresh read-only preflight at `2026-09-06T03:27:50.385Z` proves the exact account, HIGH RTX 4090
availability in EU-RO-1, unchanged `$1.116/GPU-hour` Serverless Flex rate, zero compute/disposable
resources, both retained 50 GB volumes, and three stable billing reads at `2.828824442549376`.
Proposal `sha256:ea326f65537ea0cb07dd96606645209a4a2b977d348cbf7cbedb1e838b820780`
requests one repaired image publication and the unchanged max-one five-job qualification under a
`$2.00` cap, including a `$0.20` delayed-settlement reserve. No external mutation is currently
authorized.

That proposal was consumed after its exact source push only. Before workflow dispatch, review found
the publication workflow and Dockerfile still pinned the previous worker hash, which would have
failed the build gate. No workflow dispatch, registry image, RunPod/R2 mutation, GPU job, or spend
occurred. The provider-free successor rebinds the worker hash in the Dockerfile, all workflow gates,
and the workflow test. Successor proposal
`sha256:a9bfe3d0aecf97f412ba1e1cde1a19c71fa4fe6457241e2b2bb79f1612a10bb1`
retains the same `$2.00` cap and exact max-one qualification. Fresh exact source-bound approval is
required before publication.

Historical status-horizon record follows.
Workflow `33975663940` published and anonymously
verified immutable image `sha256:cca75a1593748b43fa0f6cda96108c0fd6d7ba81f26ba1ae211e66cd1b4ab714`.
Builder-rebind proposal `sha256:6d7df208…e106` was consumed by one live attempt. The exact cold
job reached the RTX 4090 and model, then returned `SOULX_SERVERLESS_MEDIA_CONTRACT_INVALID`; the
warm job was cancelled and no fault jobs or redispatch occurred. Cleanup-only reconciliation deleted
the disposable endpoint/template and proved all five deterministic R2 key sets absent. Three stable
billing reads remained exactly `2.7231514439627063`. Evidence is in
`failed-media-contract-attempt.json`. The failure was isolated to Stage 7 post-trim media validation:
the worker allowed only 40 ms for AAC/container/A-V duration while native readback already allowed
80 ms and 16 kHz AAC uses 64 ms access units. Clean repair `3c1e810` deterministically normalizes
25 fps 512x512 H264 video and mono 16 kHz AAC with exact sample/frame trimming, aligns only the
AAC/container/A-V tolerance to 80 ms, and adds fixed redaction-safe predicate failure codes. Focused
media/publication tests pass and an independent audit reports no findings. Stage 6 is unchanged.
The consumed authority and old image are not reusable. Exact media-repair proposal
`sha256:a64a3ac76dba05c52085adb387e10a5fa210755b2ecc0a130a20207ec8de199a` was approved at clean
pushed control/image source `251752b8b390fa69008e0abd49f73a6a65ae971a`. Workflow `33982211389`
published and anonymously verified repaired immutable image
`sha256:e1dce095fbd4522fdc9c704f48609876903616de3781a3ed232a22789e4fd47d`, including exact manifest,
config/source labels, and all 24 descriptors. One allowlisted authority successor is materialized
pending independent diff audit before the exact max1 five-job qualification. No Stage 6 change or
rerun is authorized.
That successor `f34e0390` failed the mandatory provider-free audit before any RunPod or R2 access:
`build-soulx-live-request.mjs` still pinned the preceding image/source/config/proof, producing
`V208_SOULX_REQUEST_BUILDER_SOULX_BINDING_INVALID`. The authority is consumed and non-reusable;
there was no GPU job or spend. The builder and its focused test are now rebound to the exact
published repaired image/source/config/proof, and 6/6 focused builder tests pass. The exact image
remains reusable without republication. Proposal
`sha256:cbff56e5eb1ed367da7574abe7765068b38255b655b1b3251ef115755c38c5e1` is approved at clean
pushed control `f333f06a5b7d96f8151b226def501d667d543d2c`. A fresh anonymous 24-descriptor,
5,115,594,505-byte full readback reconfirmed image `e1dce095…fd47d`, config `3098152b…20d2`, and
source `251752b8` without republication. One allowlisted authority successor is materialized pending
independent diff audit before any RunPod/R2 access.
That audit passed with no P0/P1 findings. The exact live admission then consumed the authority and
failed closed before any RunPod job POST because cumulative endpoint billing had settled to
`2.7981025265471544`, while the authority required exact baseline `2.7231514439627063`. The
`0.0749510825844481` delta belongs to a provider billing row predating this authority. The durable
journal has zero materializations and terminal absent lane evidence; three independent live reads
prove zero pods, workers, endpoints, and templates, stable billing `2.7981025265471544`, and only the
two approved retained volumes. No spend is attributable to this attempt. Evidence is in
`failed-billing-baseline-drift-attempt.json`. The authority is consumed and non-reusable. A minimal
fresh-baseline proposal reuses the same immutable image without republication and retains the exact
max1, USD 1.80 incremental cap, cleanup, and Stage 6 freeze; cumulative stop is
`4.598102526547154`.
Fresh-baseline proposal `sha256:236bfadad6cfbab5b2a574de3a9d189325862c66dda79f45378f1a006e68e94f`
is approved at clean pushed control `1de1489e9eb802a0ebb7df91c32b4dd5261a0df4`. Three billing reads
remain stable at `2.7981025265471544`; provider inventory is zero compute/disposable resources.
Anonymous verification reconfirmed image `e1dce095…fd47d`, config `3098152b…20d2`, source
`251752b8`, and 24/24 descriptors without publication. One allowlisted authority successor is
materialized and independently audited. Its live run posted exactly one cold whole-span job with no
redispatch. RunPod returned 93 `IN_QUEUE` reads followed by 87 `IN_PROGRESS` reads; the local
180-read watcher then cancelled the healthy job at 360 seconds even though the signed job allowed
800 seconds. Cleanup-only reconciliation sealed all five R2 cleanup phases, proved all 27 planned
keys absent, deleted the disposable endpoint/template, and three reads prove zero compute resources.
Posted billing remains `2.7981025265471544`; USD `0.08` is reserved for possible delayed settlement.
The authority is consumed and non-reusable. The narrow provider-free repair gives successful jobs a
bounded 1,660-second watcher covering the 800-second queue/cold-start allowance, 800-second active
execution allowance, and 60-second status-propagation window; it keeps fault cancellation at 60
seconds and admits later billing only within the same finite cap. The fresh proposal therefore caps prior reserve plus the new
qualification at USD `1.88`, cumulative stop `4.678102526547154`, while reusing the same immutable
image without republication. Stage 6 remains frozen and unchanged.
Status-horizon proposal `sha256:106c97f6495d30fdd7c8c318c7e94fd8286c551b55784bd25ac119808abc24f5`
is approved at clean pushed control `099befafa378af6d1acc5bd174f27e802df48280`. Fresh preflight
proved HIGH RTX 4090 EU-RO-1 availability at `$1.116/GPU-hour`, zero compute/disposable resources,
the two exact retained volumes, and stable billing `$2.828824442549376`, which is within the approved
`$0.08` delayed-settlement reserve. Anonymous verification reconfirmed the exact immutable image,
source/config labels, and 24/24 descriptors without republication. One allowlisted authority
successor is materialized pending independent audit before RunPod/R2 mutation.
The first standalone V2-08 live attempt consumed proposal `sha256:fbd5acf1…c440` before a startup-
health classification failure. The next exact attempt consumed proposal `sha256:a4ff77b2…e7e7` and
failed before any RunPod `/run` POST: strict zero-queue startup health passed, but the fresh V2-08
client guard remained `unknown`, so the synchronous dispatch check threw `RUNPOD_DISPATCH_BLOCKED`;
the outer transport mislabeled it `V208_DISPATCH_ACK_UNKNOWN`. No GPU job was submitted and billing
remained exactly `2.7231514439627063`.

Proposal `sha256:8cf7ca34…ee29a` then dispatched exactly the cold and warm whole-span jobs. The cold
worker failed before model load with `SOULX_SERVERLESS_VOLUME_MANIFEST_MISMATCH`: the sealed-volume
helper returned the correct digest as bare hexadecimal text, while the signed Stage 7 wire contract
required `sha256:` plus that digest. The warm job was cancelled cleanup-only. The disposable
endpoint/template are absent, all five deterministic R2 cleanup phases prove absence, and three
stable reads at `15:32:21.248Z`, `15:32:25.766Z`, and `15:32:28.327Z` prove zero pods, endpoints,
templates, running pods, and active workers, unchanged billing `2.7231514439627063`, and only the two
existing 50 GB EU-RO-1 volumes. The consumed authority is non-reusable and no redispatch occurred.

The minimal worker repair normalizes only the Stage 7 worker's verified bare digest into the
canonical `sha256:` wire form. Ordinary and Stage 6 code are untouched. Proposal
`sha256:ba2c0a8a663e20edec756c992db83eba9fc56f4db71cd7f472a8c6871582335c` authorized publication
of the exact new image above, but its single successor failed provider-free pre-dispatch audit:
the approved-authority record omitted one hex character from the correct proposal digest and the
request builder retained the prior image/source/config/proof pins. Either mismatch failed closed.
There were zero RunPod/R2 mutations, GPU seconds, or spend. The authority is consumed and null.
Fresh builder-rebind proposal `sha256:6d7df20805361ddb552f438cc89327598a0db907e174024d8c0928748bf5e106`
at clean pushed control `a5e116d025b98505c1b53d80ec8f25a7b12ecaa2` is approved exactly. The
published image was anonymously reverified without republication across its manifest, config/source
labels, and all 24 descriptors. One allowlisted authority successor is materialized pending
independent diff audit before the unchanged max1 five-job qualification under the USD 1.80 cap.

Fresh completed post-cleanup read-only preflight is recorded at
`evidence/acceptance/VF-10-08/2026-09-05-live-qualification-candidate/read-only-preflight.json`
(`sha256:726b831fa6d156802b2a8239eed657aa26cd0587d56a5f5b6d5a7c668779be73`): three stable zero-
compute reads, RTX 4090 stable offering id in `EU-RO-1` at `MEDIUM`, Serverless Flex `$1.116/GPU-hour`,
fresh cumulative billing baseline `2.7231514439627063`, and only the two existing 50 GB volumes.
The replacement proposal accepts `LOW` or better only for that exact GPU, region, and rate, with no
fallback. The approved proposal is
`evidence/acceptance/VF-10-08/2026-09-05-live-qualification-candidate/combined-live-proposal.json`
and is sealed against its clean repaired-source commit, published image, and exact approval. Proposals
`sha256:fbd5acf1…c440`, `sha256:a4ff77b2…e7e7`, and `sha256:8cf7ca34…ee29a` are consumed and
non-reusable. The earlier proposal
`sha256:deb2ed27914b6541103e5581d2a323dbbc3e4197fec5c50c0038a3908c3fa1e8` was consumed before the
launch-readiness failure and grants no current executable authority. The precursor/image source is
`4f2c5cb3c602ffb48db4a1dc7143477259202896`; its immutable image contains the manifest-format bug
and cannot be reused. The clean repaired source and its new immutable image are bound above.

Prior approved publication workflow run `33961641686` failed before authentication/publication because
`TMPDIR_DIRECTORY_ABSENT`; it produced no digest or image artifact and made no RunPod mutation, GPU use,
or spend. Its approval is consumed/non-reusable and invalidated by source drift; this fresh successor
must be approved exactly and does not reuse that authority.

The next approved publication workflow run `33962783745` at source `67af7d9a` built a local image but
failed before authentication/publication because of a literal backslash in a single-quoted Docker Go
template. It produced no registry digest or image artifact and made no RunPod mutation, GPU use, or
spend. That approval is also consumed/non-reusable and invalidated by source drift; the `4f2c5cb3`
successor repairs only the workflow quoting.

Provider-free progress at `7039092`: the exact Serverless handler, per-envelope tenant authority,
48 kHz to 16 kHz preparation, trim/probe validation, resume/readback, signed receipt, exact observed
single-RTX4090 evidence, measured timing, and terminal scratch cleanup pass focused tests. Image
publication, endpoint/config activation, user visual approval, and live qualification remain open.

Image-definition progress at `84e0088`: the exact sealed Serverless overlay, pinned requirements,
source/hash checks, offline read-only volume fence, ffprobe, and entrypoint pass 22 focused worker
tests and independent audit. Image build/publication and every live/provider action remain open and
unauthorized.

Current provider-free seal: the SoulX source, Dockerfile, immutable image, and evidence hash chain
agree; the retained model volume is application-read-only; and the whole-span max1 runner uses exact
`idleTimeoutSeconds=60`. Cold-to-warm proof still requires signed `runtime_cache_hit=false` then
`true` from the same worker. Durable local phase evidence now resumes safely across lane deletion,
per-descriptor R2 cleanup, output deletion, and final-zero proof without redispatch. Focused launcher,
builder, authority, orchestration, and store tests plus web typecheck pass. Stage 6 source and live
qualification were not rerun or changed. The only provider activity during repair was the bounded
read-only preflight; it made no mutation, GPU use, R2 access, or spend.

First-party terms observation:
`evidence/acceptance/VF-10-08/2026-09-05-terms-preflight/first-party-license-observation.json` records
that the exact pinned code LICENSE and model-card metadata both declare Apache-2.0. The user accepted
the unresolved risk for this qualification only; this is not legal advice and does not close the
production legal-clearance gate.

Publication/crop groundwork at `e49b938` and `f355705`: a manual-only immutable publication
workflow is audited but untriggered. On 2026-08-26 the user explicitly approved both exact owned
full/split layouts. Approval record
`evidence/acceptance/VF-10-08/2026-08-26-soulx-crop-profile-approval.json` at
`sha256:c3aae03d…4c45` binds unchanged candidate `sha256:f6c8dd21…d442`, both exact MP4 hashes,
source geometry, and transforms. The provider-free renderer profile is active only for those exact
bindings; SoulX remains `NO_LIVE_AUTHORITY` and `NOT_QUALIFIED`.

Provider-free envelope-auth hardening changed the shared worker verifier and SoulX handler/image
definition. The prior SoulX image candidate and all image-bound qualification evidence predate
these bytes and are invalid for activation. Mage and SoulX each require a fresh immutable image
build, sealed deployment snapshot, and full qualification; this note grants no publication,
provider, or spend authority.

Provider-free pair execution is independently audited through durable lane batches (`0f73d81`),
authenticated envelopes (`9ef6efa`), atomic two-lane predispatch (`120b6b8`), runtime transitions
(`941176f`), recovery composition (`3d1b439`), and the guarded Workflow/zero-worker evidence
(`4239be9`), covering migrations 0041–0044. Production remains `DISABLED_UNQUALIFIED`; migrations,
grants, config, roles, and secrets are undeployed/unprovisioned. Fresh immutable Mage and SoulX
images plus dual live qualification are required after the verifier byte change. Fresh exact
paid/deployment authority also remains mandatory.

## Goal

Qualify the exact retained SoulX-FlashHead Pro BF16 runtime as a max1 autoscaling Serverless lane,
preserve the approved exact crop/visual profile, and close the deployability record without changing
the model.

## Scope

Product track may implement the exact handler, fixtures, private-port composition, durable resume,
receipts, scratch cleanup, tests, and integration seams now with provider transport fail-closed.
This does not authorize publication, endpoint mutation, provider calls, GPU use, or spend.

- Show the existing owned native/full/split samples before new spend.
- Record exact first-party terms and an honest user risk decision.
- Implement only the Serverless-v3 whole-span-batch handler, durable resume/receipts, private ports,
  `/runpod-volume` read-only verification, job scratch, exact pad/trim/A-V, and terminal cleanup.
- Qualify one representative 2/4/6/10-second batch on RTX 4090 in `EU-RO-1`, `workersMin=0`,
  `workersMax=1`, with deliberate `RUNPOD_INIT_TIMEOUT`, cold model-ready below the documented
  seven-minute unhealthy threshold, cold and warm proof, then drain to zero. Reuse accepted Pod
  samples for avatar/phoneme/head-motion/hair/hat/background breadth.
- Prove request/status, durable reconciliation, cancel, invalid-output, and timeout behavior for the
  exact max1 handler without a broad fault matrix.
- Move live max2/concurrent-reader proof to V2-11.

No download, volume write, alternate runtime, repair, enhancement, fallback, full-voiceover job, RTX
5090, production integration, or broad fault matrix.

## Minimum proof

Focused handler/contract checks, one synthetic batch, exact image/config readback, playable native/
full/split outputs, hashes/ffprobe/receipts, init-timeout/cold/warm/focused-fault evidence, user
approval, timings/VRAM/settled cost, unchanged volume, terminal jobs, zero total workers, and one
independent audit. A PASS permits bounded max1 V2-09/V2-10 dispatch; V2-11 owns max2 promotion.

Certification evidence is atomic and cumulative under `DEC_DELIVERY_002`; every reused gate must be
within 24 hours and match all sealed-lineage hashes. Final release still requires a fresh
two-lane smoke and independent drain.

## 2026-09-06 focused Stage 7 qualification outcome

Proposal `sha256:784f68823244adf733bcbe423f1445fdabb0e85ea7f6a37aaec076c820e6d04f`
was consumed by exactly five POSTs with no redispatch. Cold and warm produced eight verified,
preserved 2/4/6/10-second MP4s; cancel and invalid passed. RunPod reported the exact 5-second
execution timeout as `FAILED` without an application failure code or output instead of
`TIMED_OUT`, so V2-08 was not qualified. Cleanup-only resume and three independent stable reads
proved zero compute, all five R2 keysets absent, two unchanged retained volumes, and stable billing
USD 2.9606986929138657 with zero observed increment.

The provider-free repair accepts that provider-native terminal only for the deterministic V2-08
timeout descriptor, while rejecting application failures and any output-bearing failure. Focused
orchestrator tests pass 24/24 and web typecheck passes. The replacement proposal is sealed at
`sha256:b6bb008e7e321acd66a5b9e062b9ea50758911ea7d1075f7a2a19f83e2987bc2`; no executable
authority remains and fresh exact approval is required before any RunPod or R2 mutation.

Fresh exact approval was received for proposal `sha256:b6bb008e7e321acd66a5b9e062b9ea50758911ea7d1075f7a2a19f83e2987bc2`
at control source `263838300f3133ec3cc0fd6565584127e6f5eaa4`; the exact source push is
verified. One deterministic authority successor is materialized and must pass independent diff
audit before any RunPod or R2 mutation.

The successor audit passed with P0/P1/P2 `0/0/0`. Anonymous image, config, and source-label
proofs were reverified, but admission then found no exact RTX 4090 offering in `EU-RO-1`. The
single-use authority was consumed without creating any endpoint, template, R2 object, job, or POST;
there was no redispatch and no verified output was changed or removed. Three final reads proved
zero compute, the same two retained 50 GB volumes, and stable billing USD 2.9606986929138657. The
provider-free waiting proposal is sealed at
`sha256:62735aede6fbe9de22c5bd0bf48eb3e725749d248b3963f7008111c790f589bd`.
Do not request fresh approval until exact RTX 4090 `EU-RO-1` availability is LOW or better; GPU and
region fallback remain forbidden.

A fresh read-only catalog check at `2026-09-06T05:33:05.229Z` now proves the exact stable RTX 4090
offering is present in `EU-RO-1` at LOW availability. Three accompanying reconciliation reads again
prove zero compute, the same two retained volumes, and stable billing. This satisfies the capacity
condition for presenting the newly sealed proposal for fresh exact approval; it does not itself
authorize any mutation, job, R2 access, GPU use, or spend.

Fresh exact approval was received for proposal
`sha256:c7501e25f5f8ccdaffbccbdb313f76e0bf5dde0d7b49705050f3aa6f596d7752`
at clean control `f0a3a860b74f52410555b51e8af777dbe019406c`, and the exact control was
pushed to `refs/heads/codex/serverless-v2-roadmap-v4`. One deterministic authority successor is
materialized and remains blocked from all RunPod or R2 mutation until its independent diff audit
passes. Stage 6 remains frozen and fallback remains forbidden.

The authority successor `75062eb3659b7e47518c0f683d65b518e5992370` passed independent audit
with P0/P1/P2 `0/0/1` (only stale inactive-authority test assertions), but fresh pre-mutation reads
then found billing had settled from the sealed USD 2.9606986929138657 baseline to
USD 2.9793930258310866. Execution stopped before any endpoint, template, R2 object, job POST, GPU
second, redispatch, or attributable spend. Three reads prove zero compute and the same two retained
volumes; exact RTX 4090 `EU-RO-1` availability remains LOW. The authority is consumed and compiled
authority is deactivated. Replacement proposal
`sha256:e197999886a45c2d787a4b26c2b4846470a5f84b4c0fc0863e9b502a03ed5a0e`
reduces the incremental cap to USD 1.8494314167182893 under the unchanged cumulative stop
USD 4.828824442549376. Fresh exact approval is required; Stage 6 and all eight preserved MP4s remain
unchanged.

Fresh exact approval was received for reduced-cap proposal
`sha256:e197999886a45c2d787a4b26c2b4846470a5f84b4c0fc0863e9b502a03ed5a0e`
at clean control `f7a30a80e3ce2df58c8c01fa582efde4af00ef15`, and that exact control was
pushed to `refs/heads/codex/serverless-v2-roadmap-v4`. One deterministic authority successor is
materialized and remains blocked from all RunPod or R2 mutation until its independent diff audit
passes. The approved baseline is USD 2.9793930258310866, the cap is USD 1.8494314167182893, and the
absolute cumulative stop remains USD 4.828824442549376.

The reduced-cap successor `d1b14c21856de5cbd3d2b52481c4ac25c253287d` passed independent audit,
but fresh pre-mutation reads then found billing had settled again to USD 3.072259441105416. Execution
stopped before any endpoint, template, R2 object, job POST, GPU second, redispatch, or attributable
spend; three reads prove zero compute and the same two retained volumes, and exact RTX 4090
`EU-RO-1` remains LOW. Only USD 1.7565650014439598 remains under the prior cumulative stop, which is
USD 0.03678499855604023 below the USD 1.79335 conservative qualification liability. The authority
is consumed and deactivated. Wait for billing stability before sealing a revised cumulative-stop
proposal; do not request another baseline-bound approval while delayed settlement is still moving.

A five-read watch spanning 234.464 seconds now proves billing stable at USD 3.072259441105416, zero
compute, and the same retained volumes. Exact RTX 4090 `EU-RO-1` availability was intermittent but
the final read was LOW. Proposal
`sha256:76bb40a526e3f49fcd90ca41c2f507248428c1b73063aa79652dac0a1347c6f2`
preserves the previously approved USD 1.8494314167182893 incremental cap and raises only the
cumulative stop by the settled USD 0.09286641527432948 to USD 4.921690857823705. It remains
provider-free and requires fresh exact approval; no fallback or Stage 6 action is authorized.

Fresh exact approval was received for stable-billing proposal
`sha256:76bb40a526e3f49fcd90ca41c2f507248428c1b73063aa79652dac0a1347c6f2`
at clean control `8f3260a1074263d97d476ae49c44139a104a4273`, and that exact control was
pushed to `refs/heads/codex/serverless-v2-roadmap-v4`. One deterministic authority successor is
materialized and remains blocked from RunPod or R2 mutation until independent diff audit and fresh
admission pass. The approved baseline is USD 3.072259441105416, the cap is USD
1.8494314167182893, and the cumulative stop is USD 4.921690857823705. Stage 6 remains frozen.

The audited successor `8275f9cf8effce15539c1c0af54f2a03484851ca` passed fresh admission and
posted cold and warm exactly once. Cold preserved four verified MP4s. Warm completed on a different
worker after the cold worker scaled to zero, so its signed cache miss correctly failed the strict
same-worker warm gate. Cleanup-only RESUME proved the endpoint/template and all five deterministic
R2 keysets absent; three final reads proved zero compute, stable billing USD 3.072259441105416, and
the same two retained volumes. The provider-free repair prequeues warm immediately after cold under
workersMax one, retaining serial execution and all strict receipt checks; focused tests pass 25/25.
Replacement proposal `sha256:bcde60df44e6b79278bf0fff0ba530cc7c1747d0e7530895133d04ab56517b7e`
requires fresh exact approval. Stage 6 remains frozen and unchanged.
