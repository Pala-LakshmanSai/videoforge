# VF-10-08 — V2-08 SoulX Serverless and visual activation

Status: V2-07 predecessor accepted. Workflow `33963672249` published and anonymously verified
immutable image `sha256:177755b6cc6029311beb8a8891434e68433d5706e0456137d50b03c4ca503ab3`.
The approved V2-08 attempt then failed launch readiness before RunPod because its direct CLI still
depended on an expired V2-13 SQL outer authority/materialization path and had no supported standalone
durable launcher. No RunPod job, GPU use, R2 mutation, retained-volume mutation, or added spend
occurred. That single-use authority is consumed/non-reusable. The replacement standalone launcher,
proposal-bound private request builder, local crash-durable journal, source pin, unique lock claims,
and cleanup-only RESUME path are implemented and focused-green. Exact repaired proposal
`sha256:fbd5acf14dcd891b3468c9c74d9570652415d96819b915168171767b9d80c440` at control source
`7ad0cd2aeb53b4925d02cca0ff263a5d3a473591` is approved. Single-use authority
`sha256:ffe23bacc1ab05c042ea9638dd6616cdc377832fe1e0bdd08f039534cc0ec37e` is materialized;
independent diff audit and anonymous image reverification remain mandatory before RunPod mutation.

Fresh completed read-only preflight is recorded at
`evidence/acceptance/VF-10-08/2026-09-05-live-qualification-candidate/read-only-preflight.json`
(`sha256:f75a070e1dda2e8c8e630fe26233bf817101722f09993907573939c26a7a594d`): three stable zero-
compute reads, RTX 4090 stable offering id in `EU-RO-1` at `LOW`, Serverless Flex `$1.116/GPU-hour`,
fresh cumulative billing baseline `2.7231514439627063`, and only the two existing 50 GB volumes.
The replacement proposal accepts `LOW` or better only for that exact GPU, region, and rate, with no
fallback. The sealed exact proposal is
`evidence/acceptance/VF-10-08/2026-09-05-live-qualification-candidate/combined-live-proposal.json`
at `sha256:fbd5acf14dcd891b3468c9c74d9570652415d96819b915168171767b9d80c440`. The earlier proposal
`sha256:deb2ed27914b6541103e5581d2a323dbbc3e4197fec5c50c0038a3908c3fa1e8` was consumed before the
launch-readiness failure and grants no current executable authority. The precursor/image source is
`4f2c5cb3c602ffb48db4a1dc7143477259202896`; the verified immutable image may be referenced by a
fresh repaired-source proposal without republication.

Prior approved publication workflow run `33961641686` failed before authentication/publication because
`TMPDIR_DIRECTORY_ABSENT`; it produced no digest or image artifact and made no RunPod mutation, GPU use,
or spend. Its approval is consumed/non-reusable and invalidated by source drift; this fresh successor
must be approved exactly and does not reuse that authority.

The next approved publication workflow run `33962783745` at source `67af7d9a` built a local image but
failed before authentication/publication because of a literal backslash in a single-quoted Docker Go
template. It produced no registry digest or image artifact and made no RunPod mutation, GPU use, or
spend. That approval is also consumed/non-reusable and invalidated by source drift; the `4f2c5cb3`
successor repairs only the workflow quoting.

Provider-free progress at `7039092`: the exact Serverless handler, per-envelope tenant authority,
48 kHz to 16 kHz preparation, trim/probe validation, resume/readback, signed receipt, exact observed
single-RTX4090 evidence, measured timing, and terminal scratch cleanup pass focused tests. Image
publication, endpoint/config activation, user visual approval, and live qualification remain open.

Image-definition progress at `84e0088`: the exact sealed Serverless overlay, pinned requirements,
source/hash checks, offline read-only volume fence, ffprobe, and entrypoint pass 22 focused worker
tests and independent audit. Image build/publication and every live/provider action remain open and
unauthorized.

Current provider-free seal: the SoulX source, Dockerfile, immutable image, and evidence hash chain
agree; the retained model volume is application-read-only; and the whole-span max1 runner uses exact
`idleTimeoutSeconds=60`. Cold-to-warm proof still requires signed `runtime_cache_hit=false` then
`true` from the same worker. Durable local phase evidence now resumes safely across lane deletion,
per-descriptor R2 cleanup, output deletion, and final-zero proof without redispatch. Focused launcher,
builder, authority, orchestration, and store tests plus web typecheck pass. Stage 6 source and live
qualification were not rerun or changed. The only provider activity during repair was the bounded
read-only preflight; it made no mutation, GPU use, R2 access, or spend.

First-party terms observation:
`evidence/acceptance/VF-10-08/2026-09-05-terms-preflight/first-party-license-observation.json` records
that the exact pinned code LICENSE and model-card metadata both declare Apache-2.0. The user accepted
the unresolved risk for this qualification only; this is not legal advice and does not close the
production legal-clearance gate.

Publication/crop groundwork at `e49b938` and `f355705`: a manual-only immutable publication
workflow is audited but untriggered. On 2026-08-26 the user explicitly approved both exact owned
full/split layouts. Approval record
`evidence/acceptance/VF-10-08/2026-08-26-soulx-crop-profile-approval.json` at
`sha256:c3aae03d…4c45` binds unchanged candidate `sha256:f6c8dd21…d442`, both exact MP4 hashes,
source geometry, and transforms. The provider-free renderer profile is active only for those exact
bindings; SoulX remains `NO_LIVE_AUTHORITY` and `NOT_QUALIFIED`.

Provider-free envelope-auth hardening changed the shared worker verifier and SoulX handler/image
definition. The prior SoulX image candidate and all image-bound qualification evidence predate
these bytes and are invalid for activation. Mage and SoulX each require a fresh immutable image
build, sealed deployment snapshot, and full qualification; this note grants no publication,
provider, or spend authority.

Provider-free pair execution is independently audited through durable lane batches (`0f73d81`),
authenticated envelopes (`9ef6efa`), atomic two-lane predispatch (`120b6b8`), runtime transitions
(`941176f`), recovery composition (`3d1b439`), and the guarded Workflow/zero-worker evidence
(`4239be9`), covering migrations 0041–0044. Production remains `DISABLED_UNQUALIFIED`; migrations,
grants, config, roles, and secrets are undeployed/unprovisioned. Fresh immutable Mage and SoulX
images plus dual live qualification are required after the verifier byte change. Fresh exact
paid/deployment authority also remains mandatory.

## Goal

Qualify the exact retained SoulX-FlashHead Pro BF16 runtime as a max1 autoscaling Serverless lane,
preserve the approved exact crop/visual profile, and close the deployability record without changing
the model.

## Scope

Product track may implement the exact handler, fixtures, private-port composition, durable resume,
receipts, scratch cleanup, tests, and integration seams now with provider transport fail-closed.
This does not authorize publication, endpoint mutation, provider calls, GPU use, or spend.

- Show the existing owned native/full/split samples before new spend.
- Record exact first-party terms and an honest user risk decision.
- Implement only the Serverless-v3 whole-span-batch handler, durable resume/receipts, private ports,
  `/runpod-volume` read-only verification, job scratch, exact pad/trim/A-V, and terminal cleanup.
- Qualify one representative 2/4/6/10-second batch on RTX 4090 in `EU-RO-1`, `workersMin=0`,
  `workersMax=1`, with deliberate `RUNPOD_INIT_TIMEOUT`, cold model-ready below the documented
  seven-minute unhealthy threshold, cold and warm proof, then drain to zero. Reuse accepted Pod
  samples for avatar/phoneme/head-motion/hair/hat/background breadth.
- Prove request/status, durable reconciliation, cancel, invalid-output, and timeout behavior for the
  exact max1 handler without a broad fault matrix.
- Move live max2/concurrent-reader proof to V2-11.

No download, volume write, alternate runtime, repair, enhancement, fallback, full-voiceover job, RTX
5090, production integration, or broad fault matrix.

## Minimum proof

Focused handler/contract checks, one synthetic batch, exact image/config readback, playable native/
full/split outputs, hashes/ffprobe/receipts, init-timeout/cold/warm/focused-fault evidence, user
approval, timings/VRAM/settled cost, unchanged volume, terminal jobs, zero total workers, and one
independent audit. A PASS permits bounded max1 V2-09/V2-10 dispatch; V2-11 owns max2 promotion.

Certification evidence is atomic and cumulative under `DEC_DELIVERY_002`; every reused gate must be
within 24 hours and match all sealed-lineage hashes. Final release still requires a fresh
two-lane smoke and independent drain.
