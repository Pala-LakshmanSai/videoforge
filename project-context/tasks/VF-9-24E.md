# VF-9-24E — A100 80 GB Elias EchoMimic sample

Status: selected; one higher-memory paid attempt authorized within cumulative ceiling

## Goal

Run the unchanged exact Elias sample on one A100 80 GB PCIe worker, return its private native MP4
here, and prove RunPod absolute zero immediately afterward.

## Exact authority

- User-authorized cumulative ceiling across corrected-sample work: `$2.00`.
- Live balance delta across prior attempts: `$0.0649517111`; maximum remaining authority:
  `$1.9350482889`.
- Operational breaker remains `$0.50` hard / `$0.45` active. One attempt and one job only.
- One `NVIDIA A100 80GB PCIe` worker, temporary template/endpoint, 100 GB ephemeral disk, no
  network volume or GPU fallback.
- Worker:
  `ghcr.io/pala-lakshmansai/videoforge-avatar-primary@sha256:d0e487d13bf19b74d09af5c7bb3b800eb2faa75dcce6eca9e155a48b0403ffe9`.
- Canonical private source SHA-256:
  `sha256:bcc1b20cf51c0a1f5d8aae834dced1dcecf62e78434470f014f1391939fe31f7`.
- Canonical private audio SHA-256:
  `sha256:ee4f007c5c5770a404a1fe7eb008ed7c9964acb47c34677374131ab509f26076`.

## Exact inference

- Unchanged BF16, 8 steps, 253 frames, 25 fps, `Flow_Unipc`, TeaCache `0.1`, seed `43`.
- Prompt: `A man talks naturally to the camera with subtle head and upper-body movement. Static camera, stable background, realistic motion.`
- Empty negative prompt. No retry, fallback, tuning, crop, interpolation, repair, enhancement, or
  model/GPU substitution.

## Acceptance and cleanup

- Output: `.videoforge/review/VF-9-24E/echomimic-v3-flash-elias-10.12s-native.mp4`.
- Evidence: `evidence/acceptance/VF-9-24E/elias-echomimic-v3-flash-a100-sample/`.
- Preserve model-ready/inference progress, exact config/hashes/revisions, timings, GPU/rate/cost,
  FFprobe, and local playback evidence.
- Cleanup in `finally`; if runner cleanup is ambiguous, perform exact scoped API deletion and three
  independent zero/balance reads.
- Show the playable MP4 to the user. End `READY_FOR_USER_REVIEW`; never self-approve.
- Do not retry, promote, integrate, create a successor, or continue roadmap work.
