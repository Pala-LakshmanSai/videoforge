# VF-10-00 — Serverless v2 production-roadmap reset

Status: in progress; provider-free planning and context reconciliation only

## Goal

Replace the active manual-Pod/global-shared checkpoint roadmap and copy-ready prompt pack with one
dependency-safe plan from the repository's exact current state to a production-ready, tenant-private
VideoForge for 5–10 invited users. The selected production direction is two independent
scale-to-zero RunPod Serverless model lanes, durable fair admission, whole-video lane batching where
practical, private object storage, and deterministic Ranga-close composition.

## Scope

- Audit current code, evidence, gates, and decisions before planning.
- Preserve proven Mage, SoulX, transcription, scheduler, renderer, UI, and fixture work.
- Supersede active global-shared data, one-global-session, manual Pod lifecycle, and Pod-bound
  dispatch assumptions.
- Replace `22_PROJECT_COMPLETION_CHECKPOINTS.md` and
  `templates/CHECKPOINT_CHAT_PROMPTS.md`; reconcile every directly affected normative context file,
  selector, gate, cost statement, and current-state handoff.
- Provide one implementation prompt and one independent-audit prompt for every new checkpoint.

## Authority and stop conditions

This task authorizes local context/planning edits, repository inspection, provider-free validation,
and read-only public research. It authorizes no application implementation, credentials, provider
mutation, image publication, endpoint creation, model download, GPU allocation, or spend. Preserve
historical task briefs and immutable evidence even though their old active roadmap is superseded.

Stop if the proposed sequence would silently discard a proven runtime, violate the user's cost or
visual-quality requirements, or depend on an unverified provider capability without an explicit
qualification gate.

## Required handoff

- Exact reusable, incomplete, replaced, and deferred state.
- Entirely new ordered checkpoint sequence through production release.
- Copy-ready implementation and audit prompts for each checkpoint.
- Updated selectors and context validation with a clean committed handoff.
