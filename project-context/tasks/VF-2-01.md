# VF-2-01 — durable timing contract lock

Status: standing provider-free Phase 2 serial task

## Goal and dependencies

Lock the additive persistence/repository/API contract for transcripts, words, sentences, phrases,
selected span audio, timeline plans, invalidation, and deterministic lineage. VF-1-07 and VF-1-08
must be green.

Read profile: `phase2_contract_lock`.

## Scope and ownership

- Integration owner exclusively edits schemas/types, additive migration, repository interfaces,
  generated copies, metadata/lockfiles, context, evidence, and commits.
- Preserve local ASR/timeline wire contracts and scheduler semantics; add durable identity and
  lineage rather than a second format.
- Define optimistic concurrency, idempotency, invalidation, exact source/model/config hashes, frame
  coverage, and selected span-audio ownership.
- Out of scope: provider prompts/images/avatar, scheduler retuning, UI redesign, cloud, credentials,
  model downloads, and spend.

## Acceptance

- Fresh/upgrade/idempotent migration tests pass; cross-workspace and stale revision bindings fail.
- Same source/config/seed can resolve the exact persisted plan; changed input invalidates dependent
  timing/timeline without mutating locked history.
- Contract sync/parity, control-plane tests, full uncached verify, context validation, secret scan,
  audit, and diff check pass.

Evidence: `evidence/acceptance/VF-2-01/durable-timing-contract-lock`.

## Rollback and handoff

Revert the additive migration/contract commit normally; never rewrite earlier migrations. Once
committed, activate disjoint VF-2-02, VF-2-03, and VF-2-04 lanes.
