# VF-8-12 — Pinned SkyReels V3 worker build

Status: exact provider-free successor after VF-8-11 preflight

## Goal

Replace the avatar-quality fixture skeleton with one fail-closed SkyReels V3 Serverless worker,
publish an immutable GHCR image, and keep RunPod at absolute zero.

## Context read profile

Use `phase8_skyreels_worker_build` from `MANIFEST.yaml`.

## In scope

- Pin `SkyworkAI/SkyReels-V3@28c771e8456341be6a213e3d1133ed1fd19bf75d` and
  `Skywork/SkyReels-V3-A2V-19B@fdad4053f492aba389b5a8c3c6982118c6a1ecf3`.
- Implement strict checksum-only input resolution, original-source/audio enforcement, bounded command
  construction, timeout/cancel, MP4 probe, immutable lineage, safe error codes, and secret redaction.
- Container smoke must prove linux/amd64 entrypoint, handler registration, pinned dependency/source
  integrity, and model-not-loaded health without downloading the 55.97 GB weights.

## Authority and limits

- `$0` external provider spend. GitHub/GHCR build/publish is allowed.
- No RunPod credential read/mutation, model-weight download, GPU start, provider dispatch, deploy,
  promotion, Mage work, or MuseTalk execution.
- No failed AvatarForcing derivative may enter the worker. RunPod must remain absolute zero.

## Acceptance

- Focused worker tests cover valid command, hostile input, hash drift, timeout/cancel, safe failures,
  output probe/lineage, and zero hidden activity.
- Immutable GHCR digest passes exact container smokes and full local verification stays green.
- Exactly one capped A100-80GB qualification successor is authored; no paid action occurs here.
