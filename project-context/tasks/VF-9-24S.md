# VF-9-24S — SoulX-FlashHead Pro replacement qualification

Status: active; paid replacement authority recorded, no provider mutation performed yet

## Identity and dependency

- Task ID: `VF-9-24S`.
- Milestone: replace the active Echo avatar runtime with exact SoulX-FlashHead Pro, while leaving
  CP-08 hosted staging unactivated.
- Dependency: completed CP-07 evidence remains immutable history; current live preflight proves
  zero Pods/endpoints/private templates/workers and exactly the retained 50 GB Mage and 50 GB Echo
  volumes in Sujal `EU-RO-1`.
- Decisions/gates: `DEC_AVATAR_010`, `GATE_AVATAR_005`, `GATE_GPU_001`, `GATE_RUNPOD_001`,
  `GATE_COST_001`.
- Branch: `codex/soulx-flashhead-pro-vf924s` from the completed CP-07 handoff.

## User-authorized outcome

Remove the operational Echo runtime and model volume, install the latest exact official
SoulX-FlashHead Pro model on a clean replacement 50 GB avatar-only `EU-RO-1` volume, publish an
immutable VideoForge worker, run the owned Elias 10.12-second image/audio sample on one exact RTX
4090, return the native playable MP4, and leave no Pod or temporary template running. Historical
Echo evidence remains truthful immutable history and cannot dispatch.

## Exact pinned lineage and settings

- Source: `Soul-AILab/SoulX-FlashHead@9bc03de06bb0de82cd6bc477804512ae06144bf2`.
- Weights: `Soul-AILab/SoulX-FlashHead-1_3B@59119b6c681230c3eeee157e224ae1941746711e`,
  exact `Model_Pro` safetensors plus `VAE_Wan` only.
- Audio encoder: `facebook/wav2vec2-base-960h@22aad52d435eb6dbaf354bdad9b0da84ce7d6156`,
  safetensors only.
- License: source and official weight repositories are labeled Apache-2.0; dependency notices and
  exact pinned hashes remain part of the runtime manifest.
- Runtime profile: `videoforge_soulx_flashhead_pro_bf16_v1`; BF16, native 512x512, 25 fps, four
  distilled denoising steps, timestep shift 5, color correction 1.0, streaming audio encoding,
  deterministic seed 42, FlashAttention 2, one real compile/inference warm-up before readiness.
- Official Wan VAE `.pth` is loaded only with `torch.load(..., weights_only=True)` through the
  VideoForge patch; no uncarded pickle, repair, fallback, upscaler, face restoration, Lite model,
  model substitution, or runtime download is allowed.
- Selected volume bytes are exactly `6,916,084,703` before source-code/container dependencies and
  small manifest files. A 50 GB volume leaves more than 43 GB decimal headroom.

## Recorded paid authority

The user authorized this replacement in chat on 2026-08-15 with a maximum cumulative finite
external spend of `$4.00`. The exact allowed operations are:

1. Push the bounded VF-9-24S source/context commits and publish the smoke-tested immutable
   `ghcr.io/pala-lakshmansai/videoforge-soulx-flashhead-pro-vf924s` image.
2. Revalidate Sujal identity, zero compute, exact current RTX 4090 Secure `EU-RO-1` offering at no
   more than `$0.74/hour`, the exact retained Mage volume hash, and the exact retained Echo volume
   hash `sha256:cc4160b3ade65a0d715eb993e0a05c330703013adf10f1e50ff270d6b917440f`.
3. Delete only that exact Echo volume, prove absence, and create one clean 50 GB
   `videoforge-soulx-flashhead-pro-volume-eu-ro-1-50gb` volume. Continuing retained-volume billing
   remains the already accepted `$3.50/month`; no additional retained volume is introduced.
4. Create one temporary private template and one preparation RTX 4090 Pod; download and verify only
   the pinned Pro/VAE/audio files; seal the manifest; delete the Pod and prove absence.
5. Create one fresh offline RTX 4090 runtime Pod, perform real warm-up, generate the owned
   10.12-second sample, retrieve the private MP4/evidence, delete the Pod, delete the template, and
   prove zero compute plus exactly the retained Mage and SoulX volumes.

The `$4.00` cap covers finite publication/preparation/sample actions through handoff and excludes
the unchanged continuing `$3.50/month` retained avatar-volume charge. The user authorized no other
provider, account, volume, GPU, model, repair, fallback, or output publication.

## Stop conditions

Stop and clean the exact owned Pod/template on account, region, GPU, rate, volume, image, manifest,
hash, license, settings, VRAM, or output mismatch; projected or observed `$4.00` cap risk; any Mage
mount/cross-mount; runtime registry/network access; unsafe serialization; generation/upload failure;
or uncertain cleanup. RTX 5090 is not an automatic fallback for this activation.

## Required evidence and handoff

- Immutable image digest and source commit.
- Exact source/model/audio revisions, sizes, SHA-256 manifest, and volume identity.
- Actual GPU/rate/VRAM, Pod start-to-container, manifest verification, model load, compile warm-up,
  model-ready, inference, encode/upload, and delete timings.
- Playable native MP4, SHA-256, byte size, FFprobe streams, frame count, and A/V duration delta.
- Finite conservative and settled cost under `$4.00`; unchanged `$3.50/month` retained-volume rate.
- Independent zero-Pod/zero-template/zero-endpoint/zero-worker and two-volume proof.
- User visual review before any SoulX-specific full/split renderer crop becomes active.

