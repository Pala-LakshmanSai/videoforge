# VF-2-03 — durable deterministic timeline

Status: standing provider-free authority after VF-2-01

## Goal and dependencies

Persist and resolve the proven deterministic scheduler output with exact invalidation, coverage and
selected-span lineage. VF-2-01 must be green.

Read profile: `phase2_timeline`.

## Scope and ownership

- Own pipeline timeline persistence/resolution services and isolated deterministic/property tests.
- Reuse the accepted composition, phrase-boundary, 21–22% avatar, alternating layout, crop/rate and
  frame-rounding rules without retuning.
- Persist exact scheduler/version/config/seed/input hash and byte-equivalent canonical plan.
- Integration owner alone edits schemas, migrations, shared interfaces, app composition, context,
  or commits.
- Out of scope: LLM decisions, provider assets, visual grammar changes, cloud, and spend.

## Acceptance

- Same input/seed/version produces byte-identical plans before and after restart/restore.
- Frame union covers exactly once with no gap/overlap; split/full requirements remain legal.
- Short/silent/fast/slow/unpunctuated/30-minute property fixtures pass.
- Focused pipeline/control-plane tests and full uncached verify, secret scan, audit, and diff check
  pass.

Evidence: `evidence/acceptance/VF-2-03/durable-deterministic-timeline`.

## Rollback and handoff

Revert the timeline service commit; never mutate accepted prior plans. Report green to the Phase 2
integration owner; VF-2-05 waits for all three parallel lanes.
