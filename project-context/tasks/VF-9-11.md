# VF-9-11 — Hardened compliant Mage quality resume

Status: authorized bounded one-image resume

## Goal

Generate one representative VideoForge-compliant Mage BF16 image without visible text or branding,
using the hardened exact-job read reconciliation, then preserve quality/timing/cost evidence and
return RunPod to absolute zero.

## Context read profile

Use `phase9_mage_quality_resume` from `MANIFEST.yaml`.

## Authority and limits

- Existing Keychain RunPod credential; exact image digest
  `sha256:9f3dc9d886b309e74adac3d7d101ee546d8d3a31d123dd1c203852d22709334b`;
  one transient template, one scale-zero RTX 4090 endpoint, one job, public model downloads,
  evidence, and Git push.
- Task cap `$0.15`; automatic cost stop `$0.12`; no Runware call and no redispatch.
- Exact prompt and seed from VF-9-09. Save/show any output and record exact GPU, all boot/download/
  generation phases, network-volume state, hash, and cost.
- Mandatory cancel on bounded failure, drain, delete endpoint/template, and independent zero proof.
- No production promotion/deployment, ImageForge volume mutation, schema/UI change, or Avatar work.

## Acceptance

- One valid 1280x720 PNG with exact pinned lineage and no visible prohibited output on inspection.
- Canonical verification green and RunPod absolute zero.
- Exactly one real owned-video render/Chrome successor if accepted, or one bounded correction brief
  if rejected.
