# VF-7-03 — Claimed Image Style analysis composition

Status: exact provider-free implementation task

## Identity and dependencies

- Milestone: `phase_7_style_analysis_composition`.
- Depends on VF-7-01 lifecycle/reservation, VF-7-02 durable reference bindings, VF-3-06 trusted
  style semantics, VF-3-09 injected Gemini adapter, and the Phase 1 dispatch/claim foundation.
- `GATE_STYLE_001` is closed. `GATE_STYLE_002` remains open and is not changed by this task.
- Base: clean `main` descending from VF-7-02 implementation
  `802a42b4171463cc22a504149047623c3b574a91` and evidence
  `640a93aaaa855013e1fa10691bc12917cb6c0dc1`.

## Goal

Compose one exact durable Image Style reference set with an already injected `StyleAnalyzerPort`
only after its version-owned general attempt has a valid single-use execution claim. Produce a
canonical, hash-bound completion candidate without accepting or settling it durably yet.

## Context read profile

Use `phase7_style_analysis_composition` only.

## In scope

- Add a narrow exported control-plane Image Style analysis-composition module; do not add a route,
  background scheduler, or runtime provider selector.
- Add workspace-scoped resolution of one specialized Image Style analysis attempt by exact
  style/version/attempt identity. Keep this a read operation; migration `0008` remains the schema
  head for this task.
- Resolve the VF-7-02 analysis reference set and map it one-for-one into
  `style-analyzer-v1`: same order, aliases, normalized hashes, MIME, dimensions, and bytes. Keep
  authoritative reference/asset IDs in the application fingerprint, never in provider output.
- Define one canonical RFC 8785 input fingerprint envelope binding workspace, style version,
  specialized/general task and attempt IDs, analyzer provider/model/revision, analyzer version, and
  the exact ordered durable reference facts. Preparation occurs before `beginAnalysis`; the same
  hash must be used for the specialized `requestHash` and general attempt `inputHash`.
- Before invoking the analyzer, re-resolve all durable state and fail closed unless the version is
  `ANALYZING`, both stored hashes equal the recomputed fingerprint, the specialized attempt owns
  the exact task/general attempt, and that general attempt is unfinished, `CLAIMED`, and belongs to
  the same version-owned task. Reference drift must fail before analyzer invocation.
- Invoke only an injected `StyleAnalyzerPort`. Run deterministic semantic validation again and
  return an immutable completion candidate containing the canonical `image-style-profile/v1`
  document/hash, analyzer-output hash, input fingerprint, exact attempt lineage, model snapshot,
  and reference-set hash. Never trust model-supplied lifecycle, identity, hashes, or provenance.
- Add focused tests with the deterministic fixture analyzer and the VF-3-09 Gemini adapter behind
  fake reference resolver/transport/evidence seams. Prove one exact call only after claim, exact
  durable mapping, canonical replay, workspace/reopen behavior, and pre-call rejection for missing
  claim, state/owner/hash/reference drift, malformed output, or adapter failure.

## Out of scope

- No success/failure database completion, canonical artifact write, specialized/general attempt
  terminal mutation, cost settlement, callback/reconciliation change, result acceptance, review,
  publication, route, UI, upload/normalization, signed-URL implementation, byte read, EXIF work,
  retention/deletion mutation, preview, Mage, or ordinary-video integration.
- No live Runware/model call, credential access, network transport, provider spend, model download,
  RunPod/GPU action, staging/cloud mutation, deployment, or provider-mode activation.
- Do not change the VF-3-09 qualified model/settings/caps, built-in `documentary_stock_v1`, accepted
  UI/renderer/output grammar, or ordinary-project zero-analysis behavior.

## Files and ownership

- Own the new control-plane Image Style composition module/exports, the minimal repository read,
  focused tests, evidence, and `CURRENT_STATE.yaml`.
- Shared repository types/adapter, package exports, evidence, and context remain serial
  integration-owner files. Maximum lanes including integration: one.

## Acceptance

- Focused tests pass against fresh and reopened PGlite state and prove exact preparation/replay,
  claim fencing, workspace isolation, drift rejection, trusted profile/hash output, fake Gemini
  composition, and zero calls on every invalid precondition.
- Tests and source prove no reference byte read, ambient credential access, real network transport,
  provider call, or external object-store operation.
- Targeted control-plane/pipeline lint, typecheck, build, and tests pass.
- `TURBO_FORCE=true pnpm verify`, `pnpm context:validate`, `pnpm secret:scan`, and
  `git diff --check` pass in fixture mode at `$0`; installed-Chrome baseline remains green.
- Small implementation commit, separate evidence commit at
  `evidence/acceptance/VF-7-03/style-analysis-composition`, then one clean context handoff.
- After success, select only an exact provider-free durable analysis-result acceptance slice. Do
  not select routes/UI or live provider transport.

## Safety and budget

- `provider_calls_authorized: false`; credential access and model downloads are forbidden.
- Maximum external spend: `$0`.
- Provider mode remains `fixture`; no GPU or RunPod lifecycle action is allowed.
- No signed URL, reference bytes, EXIF, provider output, secrets, or private paths may enter logs,
  evidence, Git, or returned completion candidates.

## Rollback

Revert only this task's implementation/evidence/context commits. Do not reset or rewrite accepted
Phase 0–2, provider qualification, adapter, public-remote, UI, VF-7-01, or VF-7-02 history.

## Context update

On a green handoff, record exact commits, hashes, and test counts; select one exact provider-free
result-acceptance brief; leave provider mode, spend, and every open image/avatar/style-adherence/
staging/production gate truthful.
