# VF-10-06 — V2-06 hosted staging and CPU jobs

Status: provider-free local repair green; full hosted acceptance blocked; no active external or credential authority

## Goal

Run the private app independently of the Mac with production auth, Neon, private R2, durable
orchestration, and scale-to-zero pinned ASR/render jobs; GPU transport stays fake.

## Scope

- Finish local IaC/config/tests and read-only inventory first.
- Activate Cloudflare, Better Auth, Neon, R2, and Cloud Run only after one exact combined proposal
  and fresh numeric user cap.
- Prove two invited accounts, tenant-private objects, hosted transcription/render, restart/replay,
  secret isolation, real Chrome, and measured fixed/variable cost.

No RunPod endpoint/image/GPU action belongs here.

## Provider-free preflight — 2026-08-17

- Better Auth, Neon/PostgreSQL, private R2, Cloudflare Workflow, and Cloud Run CPU adapters and
  fail-closed deployment manifests are implemented locally.
- Migration `0029_v2_06_hosted_foundation.sql`, least-privilege runtime grants, backup/restore,
  retention, observability, callback/poll reconciliation, and pinned CPU media containers are green.
- `CI=1 TURBO_FORCE=true pnpm verify` passes with no provider mutation or spend.
- Cloudflare and Neon require dashboard sign-in before exact account/plan/region/domain facts can be
  read. The email provider/sender is unselected, and the visible Google project still needs target
  confirmation. No combined mutation proposal or numeric finite spend cap exists yet.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-provider-free-preflight/acceptance.json`.

## Local audit repair and same-chat re-audit — 2026-08-17

- Hosted-auth expiry fixtures are clock-relative and no longer become invalid merely because the
  calendar advances.
- Cloud Run ASR/render workers fail closed unless the declared primary output is a regular,
  non-symlink file whose exact bytes and SHA-256 match the result contract.
- R2 upload authorization signs the declared SHA-256 checksum and returns exact required length,
  type, and checksum headers; durable completion remains responsible for independently verifying
  the exact length, type, and bytes.
- The repository secret scanner no longer mistakes a PEM delimiter parser for a committed private
  key block.
- This closes the provider-free code findings only. The staging app is still an activation and
  auth/status shell, and none of the live deployment, tenant, CPU parity, recovery, Chrome, cost,
  rollback, or current zero-worker claims required by V2-06 have been proven.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-local-repair-reaudit/acceptance.json`.
