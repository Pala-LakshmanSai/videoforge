# VF-10-06 — V2-06 hosted staging and personal media workers

Status: local personal-worker conversion, hosted-router hardening, and ImageForge-style beta release path implemented; full hosted/device acceptance blocked

## Goal

Run the private app with production auth, Neon, private R2, durable orchestration, and zero-
configuration account-owned Windows/macOS ASR/render workers; GPU transport stays fake.

## Scope

- Finish local IaC/config/tests and read-only inventory first.
- Activate Cloudflare, Better Auth, Neon, R2, installer publication, and signing only after one exact combined proposal
  and fresh numeric user cap.
- Prove two invited accounts, tenant-private objects/devices/leases, immutable Windows/macOS beta
  packages with exact ImageForge-style trust metadata and native clean-install evidence,
  installation, hosted orchestration with local transcription/render, offline/restart/replay/cancel,
  secret isolation, real Chrome, and measured fixed/variable/user-device cost.

No RunPod endpoint/image/GPU action belongs here.

## Superseding personal-worker decision — 2026-08-17

- The user selected free local CPU compute. `DEC_PERSONAL_WORKER_001` supersedes the Cloud Run
  production clauses of `DEC_TIMING_001`, `DEC_RENDER_001`, and `DEC_HOSTING_001` and the former
  blanket production-device-independence clause of `DEC_BUILD_001`.
- The app offers the detected OS first and always exposes both Windows and Mac downloads. After a
  normal installer launch, the user confirms `Connect this computer` once in the signed-in browser.
  There is no provider credential, database URL, R2 key, copied pairing code, port, model, or path
  configuration.
- The background worker starts at login, uses outbound HTTPS only, and may claim only the paired
  account's work. Offline truth is `Waiting for your computer`; another tenant's device is never a
  fallback.
- Successful final videos are not deleted automatically. Only an explicit authenticated user Delete
  removes the durable R2 object. Job scratch is removed on completion, cancellation, or failure.
- CPU provider spend is `$0`, but the product must still report user electricity/device time, R2,
  Runware, and RunPod costs separately. Immutable installer publication and live acceptance
  remain external gates and are not authorized by local implementation work.

## Provider-free foundation history — 2026-08-17

- Migrations `0029`–`0034` establish hosted tenant/R2/Workflow contracts, exact output authority,
  personal-worker leases, stale-result fencing, cancellation, and exact Create/Review lineage.
- Provider-free verification and installed-Chrome fixtures are green but do not prove hosted or
  native behavior; historical repair evidence remains under the V2-06 acceptance directory.

## Hosted submission and personal-worker conversion history — 2026-08-17

- Tenant-scoped CPU submission, checksum-bound output authority, durable outbox/Workflow launch,
  personal-worker pairing, leases, cancellation, stale-result fencing, streaming R2, and explicit
  deletion are implemented and locally tested. Cloud Run is removed from the active path.
- The complete hosted product router and ImageForge-style beta release path were subsequently
  composed; local Chrome fixtures remain provider-free and do not prove hosted/device behavior.

## Live inventory and unrelated Cloudflare cleanup — 2026-08-17

- Authenticated read-only inventory proves Cloudflare Workers Free with no payment method, no
  Workflows, and no active R2 subscription. R2 displayed a `$0` base, 10 GB-month Standard free,
  and `$0.015` per additional GB-month.
- The user explicitly confirmed permanent deletion of three one-year-old Workers after repository
  and dashboard checks showed no ImageForge match, bindings, custom domains, or routes. Cloudflare
  now reports `No projects found`; no other Cloudflare resource was changed.
- Neon is Free at `$0/month`. Its one dormant unrelated project remains untouched. A separate
  `videoforge-v2-06-staging` project in AWS Singapore, Postgres 17, with Neon Auth off is proposed
  but not authorized or created.
- The signed-in Google account lacks `resourcemanager.projects.get` on
  `gen-lang-client-0893746131`. Six other projects are accessible, but none is dedicated to
  VideoForge; no OAuth project was created.
- The first RunPod attempt redirected to sign-up. After the user signed the existing Chrome session
  into the Sujal account, a second read-only inventory proved zero Pods, owned Serverless endpoints,
  Active/Flex workers, private templates, and clusters. Exactly the two approved 50 GB EU-RO-1
  VideoForge volumes remain at `$3.50/month` each; current-day Cloud GPU, Cloud CPU, and Serverless
  billing are `$0`, while the displayed rounded storage burn is `$0.010/hour`. No RunPod resource or
  billing setting was changed.
- Windows x64 and Mac test devices are available. Apple Developer Program membership is absent;
  a warning-free notarized macOS distribution is unavailable, but the later explicit ImageForge-style
  beta decision removes notarization as a checkpoint blocker. No subscription, project, bucket,
  deployment, installer, secret, model download, or spend occurred.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-read-only-inventory-and-unrelated-worker-cleanup/acceptance.json`.

## RunPod read-only inventory — 2026-08-17

- The authenticated dashboard identity matched the user-authorized Sujal account.
- `My Pods` redirected to the new deployment page, the Serverless surface showed only its get-started
  page/public Hub, `My Templates` had no rows, and Clusters showed only its create landing page.
  Together with current-day `$0` Cloud GPU/Cloud CPU/Serverless billing, this is live dashboard proof
  of zero RunPod compute resources. It is not a provider-signed export.
- Storage contains exactly the existing Mage and SoulX 50 GB volumes in `EU-RO-1`. Their short IDs
  hash to the already-pinned identities, and the Home page displays `$3.50/month` for each.
- Billing shows a rounded current rate of `$0.010/hour`, a complete-day network-storage charge of
  `$0.233`, Auto-Pay disabled, and a one-hour billing lag. The continuing effective storage charge is
  `$7/month`; this read-only inventory caused `$0` new spend.
- No Pod, endpoint, worker, template, cluster, volume, credential, payment setting, or spend authority
  was created, started, stopped, edited, or deleted. GPU transport remains fake/disabled and V2-07
  remains blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-runpod-read-only-inventory/acceptance.json`.

## Hosted-router hardening and same-chat re-audit — 2026-08-17

- The authenticated staging shell now mounts the real product router. Hosted Queue, Library, and
  Settings read only tenant-scoped data; Library issues short-lived private-R2 downloads and an
  explicit confirmed Delete permanently removes only the exact owned output objects.
- CPU submission, cancellation, and deletion require the configured browser origin. Migration 0033
  serializes the one-active-project/account and two-active-project/global personal-CPU ceiling while
  permitting multiple stages or recovery attempts for the same project.
- Submission failure compensation, Workflow expiry invariants, one-hour R2 download authority,
  transaction-local tenant principals, least-privilege workspace reads, and final-DMG
  signing/notarization/stapling validation are locally covered at commit
  `ee508d0e620286b4a693f5a158e4b921bac53712`.
- `CI=1 TURBO_FORCE=true pnpm verify` passes, including 334 web tests plus one intentional skip and
  44 installed-Chrome cases. The additional hosted-router Chrome case passes against mocked local
  APIs and is not deployment proof.
- Full V2-06 remains FAIL. Create Project, preset, project/review, usage/cost, and the complete
  generation-to-playback hosted APIs are not composed. No published/installed worker release, live two-tenant/device
  isolation, Neon/R2/Workflow run, recovery, backup/restore, rollback, real-device parity, or hosted
  Chrome evidence exists. V2-07 remains blocked.
- No credential, live provider call, mutation, deployment, publication, paid request, or new spend
  occurred. RunPod remains zero-compute with the two approved retained volumes costing USD 7/month.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-hosted-router-hardening-reaudit/acceptance.json`.

## ImageForge-style beta installer decision — 2026-08-17

- The installed `/Applications/ImageForge.app` was inspected read-only. It has a valid deep/strict
  ad-hoc seal (`Signature=adhoc`, no TeamIdentifier) and `spctl` rejects it because it is not
  notarized, matching ImageForge's source/release metadata. The user nevertheless downloads and
  installs it without trouble on the target Mac and explicitly requested the same worker model.
- `DEC_PERSONAL_WORKER_001` now makes Windows unsigned beta and macOS ad-hoc sealed/non-notarized
  beta the default distribution. Authenticode and Developer ID/notarization are optional future
  upgrades, not V2-06/V2-13 blockers. No claim is made that every OS/device will suppress warnings.
- The release workflow now ad-hoc seals the Mac app, verifies the DMG and mounted app, records
  `AD_HOC_BETA` or `UNSIGNED_BETA`, and permits immutable publication without signing credentials.
  Optional signed/notarized lanes remain available and are separately labeled.
- Hosted release manifests and UI expose the exact trust mode alongside immutable URL, size,
  SHA-256, protocol, and execution-bundle identity. Native clean-download install/launch,
  autostart, update, and removal proof remains required before checkpoint acceptance.
- Apple Developer Program membership and a Windows signing certificate are no longer required for
  the selected beta release. No installer was built or published and no external mutation or spend
  occurred in this local decision update.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-imageforge-beta-distribution-reaudit/acceptance.json`.

## Hosted product journey completion and same-chat re-audit — 2026-08-17

- The authenticated hosted router now composes the tenant-private Create, Project, Review, Library,
  Usage, Queue, Settings, and worker-pairing journey instead of stopping at a staging status shell.
  Create performs a browser SHA-256 upload through exact private-R2 authority, commits verified
  bytes, locks the revision, and launches the personal-worker ASR attempt.
- Migration `0034_v2_06_hosted_product_journey.sql` makes project creation idempotent and admits
  READY assets only after an exact committed reservation/receipt/locked-revision chain. Review
  approval is accepted only for the retained primary output of the exact successful render attempt.
- The personal-worker release contract now bundles and verifies the pinned whisper model as part of
  the immutable execution identity. Exact-origin R2 CORS is validated for checksum-bound browser
  upload; there is no first-run model download or user configuration.
- `CI=1 TURBO_FORCE=true pnpm verify` passes at implementation commit
  `d552ee21d5675afcfe515ea41aef25c676a17055`, including all package/worker suites and 44/44
  installed-Chrome fixture cases. These remain provider-free local proofs, not deployment or native
  release proofs.
- Full V2-06 remains FAIL. No installer was built/published or clean-installed; no live
  Cloudflare/Better Auth/Neon/R2/Workflow, two-account isolation, personal-device ASR/render parity,
  recovery/cancel/reconnect, backup/restore/rollback, or hosted-Chrome journey has been exercised.
  Preset Hubs still require exact tenant-owned ready activation fixtures; GPU-driven preset creation
  remains V2-07/V2-08 work.
- No provider call, credential access, mutation, deployment, publication, paid request, model
  download, or new spend occurred. RunPod stays disabled with zero compute workers; the two approved
  retained 50 GB volumes continue at `$7/month`. V2-07 remains blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-hosted-product-journey-reaudit/acceptance.json`.

## Bounded live activation authority and current live state — 2026-08-17

- This latest section supersedes earlier same-day statements that mutation, publication, or spend
  authority was absent. The user explicitly issued `APPROVE V2-06 LIVE ACTIVATION`, confirmed the
  exact combined activation proposal, and later issued `ROTATE ALL AND CLEANUP` after the exact
  exposed credentials and disposable restore database were identified.
- Authority is non-transferable and capped at `$3` cumulative finite V2-06 test spend with a separate
  `$2/month` R2 recurring ceiling. It covers only the exact V2-06 Cloudflare Worker/Workflow/private
  bucket, Neon project/runtime/migrations/backup/restore, Google-only OAuth client and two test users,
  immutable Windows/macOS beta release, owned fixtures, and hosted Chrome acceptance. Email delivery,
  RunPod mutation, GPU work, V2-07, automatic final-video deletion, and cap/ceiling overruns remain
  forbidden.
- The immutable `media-worker-v0.1.0` release is published. R2 is active with the exact private bucket;
  the exposed first bucket key was replaced, read-tested, and deleted. The dedicated Neon Singapore
  project is live at migration head 35; its exposed owner password was rotated and verified, the
  runtime role can read but not write immutable render plans, and the completed restore-drill database
  was dropped. The dedicated Google OAuth project/client is live; the exposed old secret is disabled
  and the replacement remains enabled. Secret values are not retained in evidence.
- New external spend remains `$0`; the `$3` finite cap is unused. Cloudflare R2 and Neon remain on
  `$0` base plans, while the pre-existing two RunPod model volumes continue separately at `$7/month`.
  RunPod still has zero Pods, endpoints, active/flex workers, templates, or clusters and received no
  mutation authority.
- V2-06 remains FAIL while activation hardening, exact Worker/Workflow deployment, two-account hosted
  Chrome/device ASR/render/recovery/delete proof, native clean-device lifecycle proof, and independent
  acceptance are unfinished. V2-07 remains blocked.
- Authority evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-live-activation-authority/acceptance.json`.

## Current deployed staging and post-repair handoff — 2026-08-17

- At commit `0855028794a902946cd7f9feb3d7ada38a6f949b`, the exact Worker
  `videoforge-v2-06-staging` was deployed as version
  `553cbbd2-6907-48c6-a57d-2cd576e097bd` from rendered-config SHA-256
  `f398d7264419bed929ffab3e40dde1040f3ce440d73e891f4022856abcd3069d`.
- The deployed status endpoint returned HTTP 200 with the recorded commit, staging environment,
  `DISABLED_FAKE_ONLY` GPU transport, private R2 requirement, Cloudflare Workflow requirement,
  personal-worker CPU jobs, Google-only auth, and `$0` provider CPU spend. The exact
  `videoforge-v2-06-staging-video` / `HostedVideoWorkflow` is listed live.
- Live read-only checks now prove the exact eight-name secret allowlist, private R2 Standard state
  with public dev URL disabled and no final-object expiry, Neon migration 35/35 with FORCE RLS and
  exact runtime grants, and `hosted_render_plans` SELECT-only for `videoforge_v2_06_runtime`.
- These deployment/grant facts do not close V2-06. Real Google callback/two-account Chrome proof,
  paired Windows/macOS lifecycle and media parity, durable Workflow recovery, hosted end-to-end
  output/delete, rollback exercise, and post-acceptance billing observation remain required.
- Evidence: `evidence/acceptance/VF-10-06/2026-08-17-live-activation-authority/deployment-proof.json`.
- Post-repair independent audit: `evidence/acceptance/VF-10-06/2026-08-17-post-repair-live-audit/acceptance.json`.
  It is FAIL for full checkpoint acceptance with no P0; the remaining P1 is live hosted/device
  proof, not a local code/test failure. The handoff records are committed after this audit.
