# VF-10-06 — V2-06 hosted staging and personal media workers

Status: local personal-worker conversion, hosted-router hardening, and ImageForge-style beta release path implemented; full hosted/device acceptance blocked

## Goal

Run the private app with production auth, Neon, private R2, durable orchestration, and zero-
configuration account-owned Windows/macOS ASR/render workers; GPU transport stays fake.

## Scope

- Finish local IaC/config/tests and read-only inventory first.
- Activate Cloudflare, Better Auth, Neon, R2, installer publication, and signing only after one exact combined proposal
  and fresh numeric user cap.
- Prove two invited accounts, tenant-private objects/devices/leases, immutable Windows/macOS beta
  packages with exact ImageForge-style trust metadata and native clean-install evidence,
  installation, hosted orchestration with local transcription/render, offline/restart/replay/cancel,
  secret isolation, real Chrome, and measured fixed/variable/user-device cost.

No RunPod endpoint/image/GPU action belongs here.

## Superseding personal-worker decision — 2026-08-17

- The user selected free local CPU compute. `DEC_PERSONAL_WORKER_001` supersedes the Cloud Run
  production clauses of `DEC_TIMING_001`, `DEC_RENDER_001`, and `DEC_HOSTING_001` and the former
  blanket production-device-independence clause of `DEC_BUILD_001`.
- The app offers the detected OS first and always exposes both Windows and Mac downloads. After a
  normal installer launch, the user confirms `Connect this computer` once in the signed-in browser.
  There is no provider credential, database URL, R2 key, copied pairing code, port, model, or path
  configuration.
- The background worker starts at login, uses outbound HTTPS only, and may claim only the paired
  account's work. Offline truth is `Waiting for your computer`; another tenant's device is never a
  fallback.
- Successful final videos are not deleted automatically. Only an explicit authenticated user Delete
  removes the durable R2 object. Job scratch is removed on completion, cancellation, or failure.
- CPU provider spend is `$0`, but the product must still report user electricity/device time, R2,
  Runware, and RunPod costs separately. Immutable installer publication and live acceptance
  remain external gates and are not authorized by local implementation work.

## RunPod read-only inventory — 2026-08-17

- The authenticated dashboard identity matched the user-authorized Sujal account.
- `My Pods` redirected to the new deployment page, the Serverless surface showed only its get-started
  page/public Hub, `My Templates` had no rows, and Clusters showed only its create landing page.
  Together with current-day `$0` Cloud GPU/Cloud CPU/Serverless billing, this is live dashboard proof
  of zero RunPod compute resources. It is not a provider-signed export.
- Storage contains exactly the existing Mage and SoulX 50 GB volumes in `EU-RO-1`. Their short IDs
  hash to the already-pinned identities, and the Home page displays `$3.50/month` for each.
- Billing shows a rounded current rate of `$0.010/hour`, a complete-day network-storage charge of
  `$0.233`, Auto-Pay disabled, and a one-hour billing lag. The continuing effective storage charge is
  `$7/month`; this read-only inventory caused `$0` new spend.
- No Pod, endpoint, worker, template, cluster, volume, credential, payment setting, or spend authority
  was created, started, stopped, edited, or deleted. GPU transport remains fake/disabled and V2-07
  remains blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-runpod-read-only-inventory/acceptance.json`.

## Bounded live activation authority and current live state — 2026-08-17

- This latest section supersedes earlier same-day statements that mutation, publication, or spend
  authority was absent. The user explicitly issued `APPROVE V2-06 LIVE ACTIVATION`, confirmed the
  exact combined activation proposal, and later issued `ROTATE ALL AND CLEANUP` after the exact
  exposed credentials and disposable restore database were identified.
- Authority is non-transferable and capped at `$3` cumulative finite V2-06 test spend with a separate
  `$2/month` R2 recurring ceiling. It covers only the exact V2-06 Cloudflare Worker/Workflow/private
  bucket, Neon project/runtime/migrations/backup/restore, Google-only OAuth client and two test users,
  immutable Windows/macOS beta release, owned fixtures, and hosted Chrome acceptance. Email delivery,
  RunPod mutation, GPU work, V2-07, automatic final-video deletion, and cap/ceiling overruns remain
  forbidden.
- The immutable `media-worker-v0.1.0` release is published. R2 is active with the exact private bucket;
  the exposed first bucket key was replaced, read-tested, and deleted. The dedicated Neon Singapore
  project is live at migration head 35; its exposed owner password was rotated and verified, the
  runtime role can read but not write immutable render plans, and the completed restore-drill database
  was dropped. The dedicated Google OAuth project/client is live; the exposed old secret is disabled
  and the replacement remains enabled. Secret values are not retained in evidence.
- New external spend remains `$0`; the `$3` finite cap is unused. Cloudflare R2 and Neon remain on
  `$0` base plans, while the pre-existing two RunPod model volumes continue separately at `$7/month`.
  RunPod still has zero Pods, endpoints, active/flex workers, templates, or clusters and received no
  mutation authority.
- V2-06 remains FAIL while activation hardening, exact Worker/Workflow deployment, two-account hosted
  Chrome/device ASR/render/recovery/delete proof, native clean-device lifecycle proof, and independent
  acceptance are unfinished. V2-07 remains blocked.
- Authority evidence:
  `evidence/acceptance/VF-10-06/2026-08-17-live-activation-authority/acceptance.json`.

## Deployed staging handoff — 2026-08-17

- At commit `0245c100e5e20ee1f0ebf262e046f7f3c32bbb13`, the exact Worker
  `videoforge-v2-06-staging` was deployed as version
  `054910bd-00a2-4503-bc2f-1c2d0e364f59` from rendered-config SHA-256
  `ece4752ad98c927d3d55459327617ade074fcfdf1e8542e075d68d4e4297fcc2`.
- The deployed status endpoint returned HTTP 200 with the recorded commit, staging environment,
  `DISABLED_FAKE_ONLY` GPU transport, private R2 requirement, Cloudflare Workflow requirement,
  personal-worker CPU jobs, Google-only auth, and `$0` provider CPU spend. The
  `videoforge-v2-06-staging-video` / `HostedVideoWorkflow` is listed live.
- Live read-only checks prove the exact eight-name secret allowlist, private R2 Standard state
  with public dev URL disabled and no final-object expiry, Neon migration 35/35 with FORCE RLS and
  exact runtime grants, and `hosted_render_plans` SELECT-only for `videoforge_v2_06_runtime`.
- These deployment/grant facts do not close V2-06. Real Google callback/two-account Chrome proof,
  paired Windows/macOS lifecycle and media parity, durable Workflow recovery, hosted end-to-end
  output/delete, rollback exercise, and post-acceptance billing observation remain required.
- Proof: `evidence/acceptance/VF-10-06/2026-08-17-live-activation-authority/deployment-proof.json`.
- Audit: `evidence/acceptance/VF-10-06/2026-08-17-post-repair-live-audit/acceptance.json` (FAIL;
  no P0; hosted/device proof remains P1). Handoff is committed.

## Bounded completion attempt — 2026-08-18

- Migration 0036 and exact least-privilege runtime grants are live on the approved Neon project.
  The exact V2-06 Worker/Workflow/private R2 deployment is restored after a successful rollback drill
  and now runs application commit `8e965758247f4cf222e0446f723b007af80297a7` as Worker version
  `8e036f86-b79d-4ab6-9de4-97b9293747ec`. Status is HTTP 200, GPU is disabled, provider CPU is `$0`,
  auth-negative routes return 401, and the eight-name secret/CORS/private-R2 checks pass.
- `media-worker-v0.1.8` is published from its exact release commit with checksum-bound Windows x64
  unsigned-beta and macOS ad-hoc universal2 artifacts. The Mac updated from 0.1.6 to 0.1.8, retained
  its OS credential without value access, repaired its LaunchAgent, and passed a native restart cycle.
  Windows native execution remains for the available device on 2026-08-19.
- Full V2-06 remains FAIL. Ordinary browser project commit does not materialize the activation-owner-
  only `hosted_render_plans` row, so real render handoff cannot succeed under the migration-0036/runtime
  authority. Fresh encrypted backup/restore could not run without the required protected mode-0600
  service/password/passphrase files. Real two-account hosted Chrome ASR/render/isolation/recovery/
  cancel/delete, Mac remove/offline UI, Windows lifecycle/parity, and independent audit remain open.
- Current read-only RunPod truth is zero Pods, endpoints, workers, private templates, and clusters;
  the two pre-existing 50 GB volumes remain untouched at `$7/month` total. New V2-06 external spend
  is `$0`, R2 remains within its free allowance, and V2-07 is blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-18-bounded-completion-8e96575/acceptance.json`.

## Autonomous readiness closure — 2026-08-19

- Commit `2e1d1d0d1bf01da35ebdfb9abd95689ee6a04934` finishes every provider-free
  preparation that does not require a user secret, authenticated browser action, or physical
  Windows device. It adds a fail-closed backup/restore preflight with safe empty mode-`0600`
  placeholder bootstrap and a bounded Windows x64 native acceptance harness. Canonical
  `CI=1 TURBO_FORCE=true pnpm verify` passes, including 62 script tests and 44/44 installed-Chrome
  fixture cases.
- The local operator machine resolves `pg_dump`, `pg_restore`, and `psql` from Homebrew libpq.
  `/Users/lakshmansai/.config/videoforge/v2-06/postgres-inputs` is mode `0700` and contains
  exactly three empty mode-`0600` placeholders. No credential or passphrase value was read,
  generated, logged, or retained.
- The checkpoint requires activation-owner owned-fixture ASR/render proof. The existing
  `provision-owned-render-fixture.mjs` passed its canonical local path and dry-run plan. A proposed
  runtime/browser render-plan materializer and migration 0037 were rejected because they would
  unnecessarily widen authority; migration head remains 36 and the runtime remains SELECT-only on
  `hosted_render_plans`.
- Fresh Mac verification proves the public 0.1.8 DMG SHA/size, valid HFS image, deep/strict ad-hoc
  seal, universal2 executable, and exact installed-app code identity. LaunchAgent bootout/bootstrap
  recovery passed and credential metadata remained present after restart without value access.
  Hosted ONLINE/offline/reconnect UI plus revoke/uninstall/re-pair still need controllable
  authenticated Chrome and user confirmation.
- Windows is reduced to one prepared script on the clean x64 profile:
  `apps/media-worker-desktop/windows-native-acceptance.ps1`. SmartScreen, one browser confirmation,
  sleep/reconnect, reboot/autostart, update/remove, and authenticated revoke remain physical
  device/user observations.
- Full V2-06 remains FAIL only on user-dependent gates: protected owner/R2/backup inputs; two-account
  authenticated Chrome and destructive-action confirmations; the Windows device run; explicit
  approval for GitHub's repository-wide native immutable-release setting; and a fresh independent
  audit. The current Worker remains deployed from `8e96575`, GPU transport is
  `DISABLED_FAKE_ONLY`, new V2-06 spend is `$0`, RunPod received zero mutation, rollback/restore is
  proven, and V2-07 remains blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-19-autonomous-readiness-2e1d1d0/acceptance.json`.

## Live encrypted backup and restore closure — 2026-08-19

- The user populated the three protected local inputs without sending values to chat. Metadata-only
  preflight confirmed a mode-`0700` directory and three non-empty regular mode-`0600` files; no
  credential or passphrase value was printed, recorded, or committed.
- Commit `804f7d35504852f33e696904c628ee760276dcdc` repairs two failures found only by the approved live
  drill: authenticated envelope writers now recreate their random reserved paths with no-clobber
  semantics, and the Neon grant script pins `public, pg_catalog` because disposable databases may
  inherit an empty `search_path`. Focused tests pass `11/11`.
- The approved `neondb` source passed exact `36/36` migration-ledger verification and produced an
  authenticated encrypted mode-`0600` backup. SHA-256 is
  `84cfe137a49b4b1b3cd44034f37b9cc343fead42b7f8cc3b1a82439488b22a7a`.
- The exact disposable database `videoforge_v2_06_disposable_drill` restored all source relations,
  then passed exact runtime-role/table/function grants, FORCE RLS, and `hosted_render_plans`
  SELECT-only verification. It was deleted; the final exact-name database count is zero.
- Installed-Chrome fixtures pass `44/44`. This is local fixture proof, not hosted two-account proof.
  GitHub repository-wide immutable releases are enabled, but pre-existing release `0.1.8` remains
  `isImmutable=false`; a new post-setting release is required for native immutable proof.
- Windows native acceptance is running in the user's separate Windows Codex workspace. Remaining
  V2-06 gates are the authenticated two-account hosted journey and destructive lifecycle checks,
  Windows evidence reconciliation, Mac hosted lifecycle/remove observations, a new natively
  immutable release, and a fresh independent audit. New external spend remains `$0`; RunPod received
  zero mutation; V2-07 remains blocked.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-19-live-backup-restore-804f7d3/acceptance.json`.

## Mac live completion and immutable 0.1.11 — 2026-08-19

- `media-worker-v0.1.11` is a repository-wide immutable GitHub Release from commit
  `2740dd13c955a6d8705ee341f7a6d4a522d50862`. Its exact manifest, Windows unsigned-beta setup,
  and Mac ad-hoc-beta DMG hashes and sizes are recorded in the acceptance evidence. The Mac DMG
  passed HFS verification and deep/strict code-sign verification and is installed as 0.1.11.
- Cloudflare Worker/Workflow version `5ffa68ef-8a4e-4784-a3cb-d02320ce5cc2` reports exact source
  commit `1ad68ea46b642949591c1090cecddc28d7ec428e`, private R2, Google-only auth, account-owned
  personal workers, GPU `DISABLED_FAKE_ONLY`, and provider CPU `$0`. Neon re-verification passes
  migration 36/36, FORCE RLS, exact least-privilege grants/functions, and render-plan SELECT-only.
- One tenant-owned v4 fixture passed real signed-in Chrome plus native Mac 0.1.11 ASR and render.
  Both durable attempts succeeded, the final private MP4 reached Review, and the user-owned result
  was approved. A separate v5 fixture was submitted while the worker was offline and cancelled from
  `OUTBOXED`; cancellation and terminal timestamps are durable, and the same Mac device returned
  RUNNING/ONLINE after bootstrap with no duplicate installation device.
- Current R2 truth is 16 Standard/APAC objects and 32.1 MB, inside the free 10 GB allowance and the
  approved `$2/month` ceiling. New external V2-06 spend remains `$0`. Current authenticated RunPod
  read-only truth is zero Pods, endpoints, Active/Flex workers, private templates, and clusters;
  only the two approved 50 GB EU-RO-1 volumes remain at `$7/month` total, with current-day GPU,
  CPU, endpoint, and Serverless billing all `$0`.
- V2-06 remains FAIL only on Windows native reconciliation, a controllable signed-in session for
  the second admitted Google identity, action-time confirmation for destructive output delete/device
  revoke/Mac uninstall, and a fresh independent audit. V2-07 remains blocked and unauthorized.
- Evidence:
  `evidence/acceptance/VF-10-06/2026-08-19-mac-live-completion-1ad68ea/acceptance.json`.
