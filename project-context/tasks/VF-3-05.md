# VF-3-05 — Private GitHub remote and hosted CI bootstrap

Status: exact private remote/CI task

## Goal and dependencies

Create the authorized private GitHub source-of-truth backup for the clean committed `main` history,
run the existing pinned hosted CI workflow, and preserve local `main` as the integration branch.
Depends on green VF-3-04 and authenticated GitHub CLI access to `Pala-LakshmanSai`.

Read profile: `phase3_private_remote_ci`.

## Scope and ownership

- Create `Pala-LakshmanSai/videoforge` as a private GitHub repository with no generated README,
  license, gitignore, or history rewrite.
- Add only that repository as `origin`; push the exact current local `main` history and set upstream.
- Confirm visibility, default branch, remote URL, and exact local/remote commit equality.
- Observe the existing `.github/workflows/ci.yml` run triggered by the push through terminal state.
- If CI fails, inspect exact hosted logs and fix only a repository-owned deterministic defect under a
  small green commit; do not weaken, skip, or delete a gate.
- Record remote and CI URLs/IDs without credentials or private source excerpts.

## Out of scope

- Public visibility, release/tag/package publishing, PR creation, branch-history rewrite, force push,
  branch deletion, secret creation, production deployment, cloud staging, provider calls, or spend.
- Do not alter accepted UI, renderer, output grammar, architecture, models, or product decisions.

## Acceptance

- GitHub repository visibility is `PRIVATE`; `origin` is the sole remote.
- Local `main`, `origin/main`, and GitHub default branch resolve to the same commit.
- Existing hosted `verify` workflow completes successfully, including install, full `pnpm verify`,
  installed Chrome, gitleaks, and dependency audit.
- Local `pnpm context:validate`, `pnpm secret:scan`, `git diff --check`, and clean handoff pass.
- Evidence: `evidence/acceptance/VF-3-05/private-github-ci`.

## Safety, rollback, handoff

- Provider calls remain disabled; external spend is `$0`.
- No force push or history rewrite. On creation/push ambiguity, inspect remote state before retry.
- Stop on wrong owner/repository, non-private visibility, auth scope failure, unexplained CI failure,
  secret exposure, or any required destructive Git operation.
