# VF-7-09 — Provider-free Image Style Hub completion

Status: exact provider-free continuation selected after VF-7-08 completion

## Goal

Complete the accepted Image Styles Hub workflow in fixture mode: normalized reference intake,
draft review/edit/publication, and exact published-version selection. Reuse the durable VF-7-01–08
services and shared contracts without provider activation or UI redesign.

## Context read profile

Use `phase7_style_hub_completion` from `MANIFEST.yaml`.

## In scope

- Add browser normalization for 3–8 user-owned JPEG/PNG/WebP references: honor orientation,
  produce bounded sRGB analysis derivatives, strip metadata, and compute exact bytes/checksums.
- Independently validate server-side magic bytes, raster metadata, dimensions, limits, checksum,
  decompression bounds, rights attestation, retention choice, order, and original/normalized pairing.
- Expose authenticated versioned lifecycle routes for style/draft creation, reference registration,
  deterministic fixture analysis, safe review, VF-7-08 full-candidate edit, exact publication, and
  published-version selection. Preserve optimistic concurrency and idempotency at every mutation.
- Connect the existing accepted Image Styles Hub UI to the shared DTOs and provider-free routes.
  Support upload disclosure/progress, deterministic review/edit/publish, active-version truth,
  archive visibility, and exact project-draft selection without changing the approved shell.
- Keep ordinary project creation on exact immutable published versions and prove it performs zero
  style analysis. Keep `documentary_stock_v1` as the built-in default.
- Add server/unit and fixture-backed installed-Chrome coverage for normalization, invalid media,
  auth/isolation, lifecycle/replay/stale authority, review invalidation, publish/select round-trip,
  accessibility/responsive behavior, reopen/restore, and zero outbound calls.

## Out of scope

- No live Gemini or Mage call/preview, provider credential, model/weight download, GPU/RunPod,
  cloud resource, deployment, push, spend, production asset, or provider-gate change.
- No motion graphics, text/captions, decorative output, renderer change, ordinary-video analysis,
  project-local Avatar upload, shell/dock redesign, migration, or unrelated refactor unless a
  reproduced additive schema defect is first reconciled in context.

## Ownership

- One integration owner owns shared routes/contracts/client/UI/tests/evidence/context. Changes to
  routes, contracts, lockfiles, context, and final integration remain serial.

## Acceptance

- Focused contract/server/browser tests prove exact normalized bytes and the complete provider-free
  upload → analyze → review/edit → publish → select path, including adversarial rejection cases.
- Forced full `pnpm verify` passes with zero skips in fixture mode at `$0`; installed Chrome proves
  the accepted responsive Hub workflow without provider or network activity.
- Evidence: `evidence/acceptance/VF-7-09/style-hub-completion`.
- On completion author exactly one provider-gate successor brief from refreshed current authority;
  do not author or implement later provider/staging/release briefs early.

## Safety and rollback

- Reference and profile artifacts remain immutable and workspace-scoped. Publication pins exact
  current bytes; project revisions never resolve mutable latest state.
- Roll back local changes with normal Git revert. No external cleanup exists.
