# VF-9-04 — Restore authenticated Mage access

Status: user-access blocker successor

## Goal

Restore the existing Chrome connection and authenticated Hugging Face page needed to resolve the
exact official `microsoft/Mage-Flow-Turbo` revision without exposing credentials.

## Required user action

1. In Codex, ensure the ChatGPT Chrome extension is connected under **Settings → Computer use**.
2. In that Chrome, sign in to Hugging Face and open
   `https://huggingface.co/microsoft/Mage-Flow-Turbo`.
3. Tell Codex the page is ready. Do not paste a token.

## Agent scope after user action

- Read the visible authenticated first-party page/API metadata only.
- Do not create, reveal, copy, or persist a token.
- No model download, provider mutation, GPU, dispatch, or spend. RunPod stays absolute zero.

## Acceptance

- Chrome connection and authenticated official model page are visibly available.
- Resume the exact revision-admission workflow from VF-9-03.
