# VF-1-01A — relational foundation audit hardening

Status: complete at implementation commit
`36bf1ae0b66524f8d1c158b301eaa77ebea04a92`; acceptance evidence commit
`e2e0ea2e1f0e613578b08457380d08984aedac22`

Milestone: Phase 1 durable control-plane foundation
Owner: integration owner
Read profile: `phase1_foundation_hardening`

## Goal

Correct every high-priority defect found by the post-VF-1-01 adversarial audit without changing
visible application behavior, contacting a provider/database service, or discarding the committed
VF-1-01 foundation. Finish with additive PostgreSQL hardening, canonical repository behavior
scenarios, coherent execution contracts, regression proofs, corrected evidence, and a clean commit.

## Dependencies and authority

- The user explicitly authorized fixing all high-priority audit findings on 2026-08-10.
- Preserve VF-1-01 commits and add a corrective migration; never rewrite an already committed SQL
  migration.
- `10_DATA_AND_API_CONTRACTS.md`, `18_IMAGE_STYLES_HUB.md`, `20_AVATAR_HUB.md`, and
  `evidence/project_revision_config.schema.json` are normative for the affected semantics.
- Providers, Neon, Cloudflare, deployment, credentials, model downloads, and external spend remain
  unauthorized.

## In scope

1. Make migration installation deterministic in `public`, schema-qualify invariant functions, and
   prove alternate `search_path` values cannot redirect their reads or writes.
2. Hash exact migration SQL bytes in the production executor, compare every stored metadata field,
   require the applied chain to be an exact manifest prefix, and serialize concurrent runners.
3. Add an append-only corrective migration for canonical state/snapshot constraints: assessment
   absence means `UNTESTED`; `STALE` is representable; avatar/style lifecycles match the normative
   vocabulary; prompt keywords preserve nullable/max-500 semantics; spend cap and seed bounds are
   enforced.
4. Make active-pointer, locked-revision, accepted-result, cost-owner, and event-sequence invariants
   safe under real PostgreSQL concurrency and hostile session `search_path`.
5. Replace caller-authored repository “behavior” placeholders with exported canonical scenario
   implementations that later PGlite and Neon adapters must run unchanged.
6. Repair task-only versus attempt cancellation typing and SQL mapping, accepted-result provenance,
   and billed Avatar/Image Style action linkage to the atomic task + attempt + budget + outbox
   boundary. Apply only the closely coupled type refinements needed to prevent contradictory
   authorization or terminal-state results.
7. Replace overstated VF-1-01 acceptance claims with exact hardening evidence and refreshed counts.

## Out of scope

- A production repository implementation, Neon driver/connection, ORM, API route, auth, R2,
  Cloudflare binding, provider transport, UI/render change, or deployment.
- Destructive migration/down migration or rewriting `0001_durable_control_plane_foundation.sql`.
- Closing model/provider gates or making production-readiness claims.

## Files and ownership

Maximum three lanes including integration; only the integration owner commits.

1. **Integration/database lane** owns migrations, `src/database/**`, package/root metadata if needed,
   shared context, evidence, and final integration.
2. **Repository lane** owns `src/repositories/**` and
   `tests/repository-contract-suite.test.mjs`; it must not edit migrations, other tests, metadata,
   lockfiles, or context.
3. **Constraint lane** owns `packages/control-plane/tests/**` except
   `repository-contract-suite.test.mjs`; it must not edit production code, migrations, metadata,
   lockfiles, or context.

The migration/executor contract lands before constraint tests finalize. Agents preserve each
other's files, report blockers immediately, and do not create independent handoffs.

## Acceptance

- Tampered SQL with an unchanged declared digest is rejected before execution.
- Stored filename/name/digest drift and non-prefix applied chains are rejected.
- Two concurrent migration runners both finish successfully with one application/no duplicate DDL.
- Alternate `search_path` cannot bypass active-pointer, revision snapshot, accepted-result, or event
  invariants and cannot relocate VideoForge tables/ledger outside `public`.
- Canonical `STALE`, nullable 500-character keywords, $0.10–$2.00 cap, and uint32 seed cases pass;
  invalid boundary/vocabulary cases fail.
- Concurrent workflow/cost inserts preserve strictly monotonic per-owner ordering.
- Canonical repository scenarios contain the assertions; adapters provide only implementation and
  fixture setup, not replacement behavior bodies.
- Task-only cancellation, attempt cancellation, billed preset work, and accepted result each have
  one coherent typed/database mapping.
- Package lint/typecheck/test/build, `pnpm db:check`, context/schema validation, secret scan,
  dependency audit, forced `pnpm verify`, and `git diff --check` pass with providers disabled.

Expected evidence:
`project-context/evidence/acceptance/VF-1-01A/2026-08-10-relational-audit-hardening/`.

## Safety and rollback

`provider_calls_authorized: false`; maximum external spend `$0`. Use only owned synthetic data in
ephemeral PGlite. Roll back an unsuccessful local increment with a normal Git revert of its narrow
commit; do not reset or delete the prior VF-1-01 history.

## Required handoff

Update `CURRENT_STATE.yaml`, `00_START_HERE.md`, this brief, and acceptance evidence with exact
commits/counts/commands. Restore `VF-1-02` as recommended only after this corrective task is
committed and fully green; it remains blocked until its own exact brief exists.

## Outcome

Complete. The baseline `0001` migration remains unchanged; additive `0002`, the hardened executor,
repository contracts, and 49 control-plane regressions close the authorized audit findings. The
forced whole-repository verification passed with 34 installed-Chrome journeys and 8 intentional
compact-project skips. Exact inventory, limitations, authorization, and command evidence live at
`evidence/acceptance/VF-1-01A/2026-08-10-relational-audit-hardening`. `VF-1-02` is next but blocked
before implementation because its exact brief and local authority do not exist.
