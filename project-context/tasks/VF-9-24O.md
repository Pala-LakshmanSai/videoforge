# VF-9-24O — CP-04 three-composition scheduler and complete work plan

Status: complete; independently re-audited provider-free after fixes; external spend `$0`

## Identity and dependencies

- Task ID: `VF-9-24O`
- Milestone: `CP-04` three-composition scheduler and complete work plan
- Dependency: committed, re-audited green `CP-03` at audit-fix commit
  `a6a924856a58c233eadd8af402fbf78c6c821b97`
- Prerequisite evidence:
  `project-context/evidence/acceptance/VF-9-24N/cp03-word-transcript/reaudit-after-fixes.json`
- Base commit/branch: `e77ecdb` on `main`
- Activation commit: `8d0e49e`
- Implementation commit: `ca9b816f1bd196654e03633264560050729b020a`
- Audit-fix commit: `e857cfa1d8bce6ecfdd51f600378790aeedd28f2`
- Local-render binding fix: `cf7a843fea8535bbc4fb1dc6b516ac2dbe5e9690`
- Current re-audit evidence:
  `project-context/evidence/acceptance/VF-9-24O/cp04-three-composition-scheduler/reaudit-after-fixes.json`
- Original acceptance evidence (immutable history):
  `project-context/evidence/acceptance/VF-9-24O/cp04-three-composition-scheduler/acceptance.json`
- Decision IDs: `DEC_SCHEDULER_001`, `DEC_SCHEDULER_002`, `DEC_OUTPUT_001`,
  `DEC_OUTPUT_002`, and `DEC_OUTPUT_003`
- All production/provider/model/GPU/cost gates remain open.

## Goal

Finish and extend the existing seeded scheduler so a durable CP-03 transcript deterministically
produces an inspectable, immutable, complete work and render plan for all three approved timeline
compositions without provider work.

## Context read profile

Use `cp04_three_composition_scheduler` from `MANIFEST.yaml`. Read only the CP-04 section of
`22_PROJECT_COMPLETION_CHECKPOINTS.md`, this brief, the scheduler domain file, and locked Ranga
reference metrics selected by that profile.

## In scope

- Audit and extend existing deterministic scheduler foundations; do not replace working code.
- Produce exact contiguous 30 fps frame coverage plus retained source-time and word boundaries for
  `IMAGE_FULL`, `AVATAR_FULL`, and `AVATAR_SPLIT_IMAGE`.
- Enforce no gaps, overlaps, or word cuts; 21–22% avatar time; near-even cumulative full/split
  shares; normal 2–6-second avatar spans; a seven-second opening maximum; alternating avatar
  layouts; hard cuts; and required slow centered image zoom semantics.
- Assign deterministic varied literal image shot roles and build complete image slots, prompt
  batches, Echo span tasks, artifact IDs, exact cost counts, and immutable work/render manifests.
- Materialize playable 16 kHz mono PCM selected-span WAVs with bounded coarticulation padding and
  exact trim metadata. Never expose or dispatch the full voiceover as Echo work.
- Add long and boundary-heavy owned fixtures, deterministic replay, exact coverage and cardinality
  checks, timeline visual inspection, focused/canonical checks, and real Chrome acceptance when UI
  changes are visible.

## Out of scope

- CP-05 or successor orchestration, legacy cutover, final provider-backed render, or production
  promotion.
- Provider calls, credentials, cloud mutation, model downloads or changes, GPU use, paid APIs, or
  external spend.
- LLM selection of timing/layout, full-voiceover Echo work, model substitution, repair, fallback,
  or any claim that local evidence proves hosted production.

## Acceptance

- Every integer frame in `[0, total_frames)` appears exactly once with exclusive end frames;
  source-time and word-boundary fields remain exact and separately validated.
- All three compositions appear on owned long fixtures. Avatar share is 21–22%; full/split
  cumulative shares are near-even; normal spans are 2–6 seconds; opener is at most seven seconds.
- Image roles vary deterministically. Every full-image and split-image slot has exactly one prompt
  work item and required artifact ID. Every avatar span has exactly one padded WAV/task reused by
  both avatar layouts; no missing or duplicate work exists.
- Padded selected-span WAVs decode as playable 16 kHz mono PCM and trim exactly to scheduled source
  spans without changing timeline duration.
- Work/render manifests are canonical, content-addressed, immutable, replay-identical, and reject
  conflicting or incomplete input.
- Visible timeline inspector exposes composition, frame/source bounds, shot role, span trim, work
  counts, and locked grammar without adding prohibited graphics to output video.
- Focused scheduler/media/control-plane tests, long/boundary fixtures, installed Chrome when UI is
  changed, canonical `pnpm verify`, context/schema validation, secret scan, dependency audit, and
  `git diff --check` exit zero.
- Evidence lives under
  `project-context/evidence/acceptance/VF-9-24O/cp04-three-composition-scheduler/`.
- Final handoff commits bounded CP-04 only and stops before CP-05.

## Safety and budget

- `provider_calls_authorized: false`
- `remote_or_cloud_mutations_authorized: false`
- `credential_access_authorized: false`
- `model_downloads_authorized: false`
- `application_code_changes_authorized: true` for CP-04 only
- Maximum external spend: `$0`
- No paid compute starts. No live provider inventory refresh is authorized or required.

## Context update

At handoff, update `CURRENT_STATE.yaml`, `MANIFEST.yaml`, `00_START_HERE.md`, CP-04 roadmap status,
this brief, and immutable acceptance evidence. Record exact commits, validations, fixture/timeline
inspection results, remaining gates, provider/spend state, and paid-compute shutdown state. Select
no CP-05 work without new authority.

## Completion result

The owned 30-minute CP-03 voiceover produced 54,000 exactly covered 30 fps frames across 394
segments. Avatar coverage is 21.05%; full/split differ by 81 frames (2.7 seconds); all 103 padded
selected-span WAVs probe as playable 16 kHz mono PCM. The plan contains 342 image slots in seven
balanced prompt batches, six deterministic shot roles, no missing/duplicate work, no full-voiceover
Echo dispatch, hard cuts only, and slow centered zoom for every image-bearing segment. Installed
Chrome accepted the real-audio local render/download/playback path, the fixture suite passed 40/40,
and canonical `CI=1 TURBO_FORCE=true pnpm verify` passed. Independent re-audit fixes now bind the
work compiler to the exact scheduler configuration and transcript semantics, reject forged word
cuts and duplicate task keys, and keep the active local render path on an explicit provider-free
fixture profile. CP-05 was not started.
