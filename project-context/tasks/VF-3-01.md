# VF-3-01 — Bounded Runware DeepSeek qualification

Status: completed 2026-08-11 — gate passed

Implementation commit: `e157935`
Evidence: `evidence/gates/GATE_LLM_001/2026-08-10-runware-deepseek-qualification`

The accepted run pinned canonical AIR `deepseek:v4@flash`, covered 40 scenes across five styles,
passed every criterion, and cost `$0.00085053`. Total task spend including earlier recorded
attempts was `$0.00243598`; provider mode remained fixture outside the qualification runner.

## Identity and dependencies

- Milestone: Phase 3 evidence-first provider qualification.
- Dependencies: green Phase 2 checkpoint; completed provider-sandbox harness; official Runware
  DeepSeek facts refreshed 2026-08-10; user authorization recorded 2026-08-10.
- Decision IDs: `DEC_LLM_001`, `DEC_LLM_002`.
- Open gate ID: `GATE_LLM_001`.
- Base: clean `main` descending from context handoff `a8912b3` and the activation commit that
  selects this brief.

## Goal

Close or truthfully fail `GATE_LLM_001` with reproducible live Runware evidence while preserving
all prompt, timeline, output, UI, privacy, and cost rules.

## Context read profile

`phase3_runware_deepseek_qualification`; additionally inspect only the existing
`packages/provider-sandbox` boundary and canonical prompt/style schemas needed by the fixtures.

## In scope

- Create or use the user-authorized Runware account without exposing credentials in logs, Git,
  evidence, browser handoff, process arguments, or tool output.
- Add the smallest fail-closed live qualification transport/runner around the existing sandbox
  safety, identity, cap, reconciliation, and evidence model. Provider mode remains fixture outside
  this explicit runner.
- Exercise at least 40 representative narration scenes using the built-in documentary profile and
  four distinct synthetic/owned saved style-guidance blocks.
- Pin exact request settings, model IDs returned by Runware, provider usage/cost, response hashes,
  schema results, complete scene-ID coverage, latency, retry/reconciliation outcomes, and redacted
  evidence.
- Update the gate, decisions/facts if evidence requires it, and select `VF-3-02` only after a green
  handoff. A provider identity ambiguity is a recorded gate failure, never a guessed pass.

## Out of scope

- Application prompt integration, UI/renderer changes, Mage, Avatar, RunPod, cloud staging,
  production enablement, full voiceover disclosure, user-private assets, or unrelated refactors.

## Files and ownership

- One integration owner: `packages/provider-sandbox/**`, the exact qualification fixture/runner,
  `evidence/gates/GATE_LLM_001/**`, root script only if required, and context handoff files.
- No parallel shared-schema, migration, route, lockfile, root-script, evidence, or CURRENT_STATE
  editor.

## Acceptance

- Meet every criterion under `14_TESTING_AND_ACCEPTANCE.md#runwaredeepseek-gate`, including at least
  40 scenes, exact-once IDs, five styles, strict schema, unchanged deterministic shot roles, no
  forbidden visual requests, and provider-reported 30-minute-equivalent cost below `$0.02`.
- The live response or provider evidence must establish the approved 0731 identity; if Runware
  exposes only the mutable alias, record the unresolved fact and leave the gate open.
- Targeted provider-sandbox tests pass, then `TURBO_FORCE=true pnpm verify`,
  `pnpm context:validate`, `pnpm secret:scan`, and `git diff --check` pass.
- Commit implementation and redacted evidence separately in small green commits. Expected evidence:
  `evidence/gates/GATE_LLM_001/2026-08-10-runware-deepseek-qualification`.

## Safety and budget

- Provider calls and credential access are authorized only for this exact Runware DeepSeek task.
- Non-transferable task ceiling: `$1.00`; aggregate ceiling remains `$25.00`. Reserve/measure before
  dispatch and stop before any call that could exceed the remaining task cap.
- Do not purchase a balance, accept a paid subscription, reveal an API key, use tools/search/code
  execution, or send a full voiceover/private user asset. Owned/synthetic fixtures only.
- Any email verification, CAPTCHA, MFA, payment, unclear charge, missing model identity, credential
  handling ambiguity, or cap uncertainty is an immediate stop for this lane.

## Rollback and handoff

Revert only this task's commits; the Phase 2 baseline remains intact. On pass, select `VF-3-02`.
On failure/blocker, preserve evidence, keep provider mode `fixture`, spend only the measured amount,
and leave the exact gate open with no application integration.
