# VF-3-03 — Deterministic prompt compiler and fixture adapter

Status: exact provider-free implementation task

## Goal and dependencies

Implement deterministic, provider-neutral prompt batching/validation/compilation so Wave 5 can
advance while `GATE_LLM_001` waits on Runware's `$5` account prerequisite. Depends on green Phase 2
and the 2026-08-10 Runware preflight; does not depend on live DeepSeek output.

Read profile: `phase3_prompt_compiler`.

## Scope and ownership

- One integration owner for `packages/pipeline` prompt modules/tests, any package export, evidence,
  root script only if essential, and `CURRENT_STATE.yaml`.
- Add typed prompt-writer input/output ports plus deterministic fixture adapter. Fixture writer must
  echo stable scene IDs and code-assigned shot roles; it never selects composition.
- Build 25–50-scene batches with sanitized title/style guidance once per batch and compact carried
  continuity. Validate exact-once IDs, roles, bounds, enums, and hostile/unknown output before use.
- Compile exact positive/negative Mage strings in normative order with version, components, UTF-8
  bytes, and hashes. Enforce crop guidance, permanent guardrails, extra-keyword toggle/normalization,
  hard conflicts, and exact-once insertion.
- Preserve all accepted output/UI/renderer/Hub/version rules.

## Out of scope

- Network, credentials, provider calls/spend, Runware application integration, Mage/Avatar work,
  schema/migration/UI/route changes, cloud staging, or production.

## Acceptance

- Cover full/split images, six shot roles, five styles, 25/40/50-scene batching, continuity, Unicode,
  duplicate/missing/unknown IDs, role changes, blank/oversize/control text, enabled/disabled extras,
  negative refinements, hard-rule conflicts, crop rules, deterministic replay, and hash tampering.
- Meet compiler matrix under
  `14_TESTING_AND_ACCEPTANCE.md#image-style-analyzer-and-compiler-gate` without calling analyzer.
- Targeted pipeline tests, then `TURBO_FORCE=true pnpm verify`, context validation, secret scan, and
  diff check pass. Evidence: `evidence/acceptance/VF-3-03/deterministic-prompt-compiler`.

## Safety, rollback, handoff

- `provider_calls_authorized: false`; maximum external spend `$0`; provider mode `fixture`.
- Revert only this task's commits. On pass, select next dependency-ready gate/adapter task; do not
  activate DeepSeek until `GATE_LLM_001` closes.

