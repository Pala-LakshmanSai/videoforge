# VF-8-11 — SkyReels V3 whole-frame worker preflight

Status: exact provider-free successor after VF-8-10 whole-frame quality failure

## Goal

Pin the approved SkyReels V3 whole-frame fallback source/model contract and design the smallest
fail-closed worker qualification path without starting paid compute.

## Context read profile

Use `phase8_skyreels_worker_preflight` from `MANIFEST.yaml`.

## In scope

- Preserve VF-8-10 as a technically valid but rejected-quality AvatarForcing candidate.
- Pin official SkyReels V3 source/model revisions and exact low-VRAM/offload command surface.
- Define original pinned avatar source plus selected audio input, native 1280x720/25 output,
  immutable lineage, checksum/probe, failure codes, and scale-zero lifecycle.
- Decide exact container/GPU/disk/time/cost limits and author only the next executable brief.

## Authority and limits

- `$0` external spend; read-only first-party source/model review and local docs/code only.
- No RunPod credential read/mutation, model download, GPU start, provider call, deployment, or promotion.
- Never use failed AvatarForcing bytes as SkyReels input. Do not run MuseTalk: VF-8-10 shows
  whole-frame identity/detail blur, not an otherwise-good lip-only defect.

## Acceptance

- Exact source/model revisions and launch contract are evidence-backed or recorded as blockers.
- Worker contract follows locked SkyReels renderer profile and zero-idle cleanup policy.
- One exact successor brief exists; RunPod remains absolute zero.
