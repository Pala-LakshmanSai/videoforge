# VF-4-02 — Deterministic Mage-shaped fixture image result acceptance

Status: exact provider-free continuation selected after VF-7-07 completion

## Goal

Compose deterministic Mage-shaped fixture worker results into durable control-plane image acceptance
without weights, provider calls, or runtime provider activation. Preserve completed prompt authority
`1fba04c` and style artifact persistence `20fd592`.

## Context read profile

Use `phase4_fixture_image_result_acceptance` from `MANIFEST.yaml`.

## In scope

- Define or reuse the exact fixture image worker input/result boundary and deterministic image bytes.
- Bind the accepted result to the exact project revision, current timeline, published style version
  and profile artifact/hash, prompt execution/scene, task, claimed attempt, acknowledged outbox,
  reservation/cost, callback receipt, and telemetry lineage.
- Persist technical media validation and one immutable accepted-image asset atomically with task,
  attempt, cost, and callback convergence.
- Make exact replay return the original acceptance; reject conflicting replay, malformed media,
  checksum/profile/style drift, stale claim, cancellation, partial output, cost drift, and
  cross-workspace access.
- Prove fresh, migration upgrade, reopen, metadata export/restore, rollback, and zero outbound calls.

## Out of scope

- No Mage weights/checkpoint, model download, provider transport, GPU/RunPod, credential, cloud,
  route/UI, preview/regeneration UX, deployment, push, or spend.
- No Avatar acceptance, Style API/Hub, renderer, or production-profile work.

## Ownership

- One integration owner owns any additive migration, image acceptance repository/UoW, fixture worker
  composition, metadata restore, tests, evidence, and context. Shared surfaces remain serial.

## Acceptance

- Focused PGlite tests cover valid acceptance, exact/conflicting replay, malformed media, checksum
  and pinned-style drift, stale/cancelled claims, partial output, cost drift, isolation, rollback,
  reopen, upgrade, and restore.
- Forced full `pnpm verify` passes with zero skips in fixture mode at `$0`.
- Evidence: `evidence/acceptance/VF-4-02/fixture-image-result-acceptance`.
- On completion author exactly one successor brief: `VF-5-02`. Do not author later briefs.

## Safety and rollback

- Accepted prompt/style/artifact rows are immutable; never reinterpret old hashes or accepted bytes.
- Roll back additive local changes with normal Git revert; no external cleanup exists.
