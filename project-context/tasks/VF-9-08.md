# VF-9-08 — Durable Mage result integration

Status: authorized provider-free integration successor

## Goal

Integrate the qualified Mage BF16 worker/result boundary into VideoForge's durable image lane behind
fail-closed candidate configuration, without another provider call or production promotion.

## Context read profile

Use `phase9_mage_result_integration` from `MANIFEST.yaml`.

## In scope

- Add a server-side Mage RunPod transport/configuration seam using the existing scale-zero control and
  durable image-task acceptance contracts; keep secrets/network outside pipeline packages.
- Bind exact candidate image/model/source/GPU/result/timing/cost/output lineage.
- Test fake transport success, malformed/oversized/wrong-hash output, timeout, cancellation,
  ambiguous dispatch, replay/idempotency, cost overrun, secret redaction, and cleanup uncertainty.
- Keep fixture mode default and production profiles nonselectable.

## Authority and limits

- Local app code/tests/docs, Git push, hosted CI, and `$0` only.
- No RunPod/Runware mutation, credential access, provider call, download, GPU, deployment, production
  promotion, schema/UI/Avatar change, or new external service.
- The Costco qualification proves technical execution only; its malformed requested logo cannot close
  style/semantic acceptance and must not enter normal VideoForge output.

## Acceptance

- Exact candidate configuration composes only under explicit non-production mode and fails closed on
  every drift/replay/cost/cleanup boundary.
- Canonical local and hosted verification green.
- Exactly one next real-video acceptance or image-quality bakeoff successor is authored from evidence.
