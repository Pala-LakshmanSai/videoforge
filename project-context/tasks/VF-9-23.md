# VF-9-23 — EchoMimicV3-Flash worker and sample runner

Status: selected; implementation authorized at `$0` provider spend

## Goal

Replace AvatarForcing internals in the generic `workers/avatar-primary` lane with one fail-closed,
production-shaped EchoMimicV3-Flash worker and bounded RTX 4090 sample runner.

## Context read profile

Use `phase9_echomimic_v3_flash_worker` from `MANIFEST.yaml`.

## Exact authority

- Source `antgroup/echomimic_v3@7e89489ca51c0d008fc1963ec6c03fc5bd0b9397`.
- Flash weights `BadToBest/EchoMimicV3@311e176905a8c4c24b240b530488fe636ce4d249`.
- Base `alibaba-pai/Wan2.1-Fun-V1.1-1.3B-InP@fc913c34361f4ec879e2f9c78b4f11ae50a937d1`.
- Audio encoder `TencentGameMate/chinese-wav2vec2-base@3991242c806928916fff4a8c0e4f76acf661b743`.
- Exact runtime manifest: `evidence/gates/GATE_AVATAR_004/2026-08-12-echomimic-v3-flash-preflight/source-manifest.json`.

## In scope

- Preserve `AvatarPrimaryJob`, `AvatarPrimaryResult`, signed transfers, checksum validation,
  cancellation, deadlines, unique output, redaction, cost, and cleanup boundaries.
- Replace container/bootstrap/inference internals with minimal pinned Echo Flash source/dependencies;
  no model weights in image.
- Ephemeral `/models`, immutable complete-cache marker, exact manifest verification, 100 GB container
  disk, no network volume.
- RTX 4090 24 GB only, `workersMin=0`, `workersMax=1`, one job, no redispatch after ambiguity.
- Strict result lineage for all four revisions, config hash, GPU/VRAM/stages, inputs, output probe/hash,
  and cost. Raise private output ceiling for one native 10.12-second MP4.
- Focused tests, one GHCR build after local green, pinned digest, full verification, Git push, hosted CI.

## Out of scope

- Model download or RunPod inference during this task; `VF-9-24` owns one paid sample.
- AvatarForcing, MuseTalk, SkyReels, LongCat, fallback/repair, tuning, application integration,
  profile promotion, migration, deployment, or unrelated cloud/account mutation.

## Acceptance

- Exact revision/manifest, cache hit/incomplete cache, output bounds, ambiguity, OOM, timeout,
  dependency failure, checksum/probe/audio, redaction, inventory/cap/deadline/cleanup tests pass.
- Container import, pinned CLI parser, handler registration, and compile smoke pass.
- `CI=1 TURBO_FORCE=true pnpm verify`, context validation, secret scan, format, dependency audit, and
  `git diff --check` pass; hosted build/CI pass and digest is pinned.
- Refresh `CURRENT_STATE.yaml` to exactly `VF-9-24`, maximum `$0.50`, only after all above pass.

## Safety and budget

- `provider_calls_authorized: false`.
- Maximum external spend: `$0`.
- Git/GHCR/hosted CI publication is authorized. Credential use is limited to existing GitHub auth.
- Private inputs, outputs, credentials, model weights, and downloaded caches never enter Git/image.
- Fixture remains default. Stop on manifest mismatch, dirty unexplained state, test/build failure, or
  any nonzero RunPod inventory.
