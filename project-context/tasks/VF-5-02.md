# VF-5-02 — Durable Avatar fixture result acceptance

Status: exact provider-free continuation selected after VF-4-02 completion

## Goal

Compose deterministic VF-5-01 Avatar fixture results into durable control-plane acceptance without
provider activation. Preserve one native clip for both full and split layouts, with exact pinned
Avatar version/source/span lineage.

## Context read profile

Use `phase5_avatar_result_acceptance` from `MANIFEST.yaml`.

## In scope

- Consume the existing `avatar-fixture-job-input/v1`, result, callback, and validated native clip.
- Bind exact workspace/project/revision/current timeline, pinned Avatar Profile/version/hash,
  canonical runtime source asset/checksum, selected padded span-audio asset/checksum/trim, task,
  claimed attempt, acknowledged outbox, callback receipt, reservation/cost, and telemetry.
- Persist one immutable accepted native clip, technical QA, and both deterministic renderer layout
  bindings without generating or storing a second clip.
- Preserve explicit subjective review as `UNREVIEWED | ACCEPTED | LIP_ONLY | WHOLE_FRAME`; never
  infer subjective identity/body/background/lip quality from deterministic fixture checks.
- Enforce retry, cancellation, callback replay, stale-claim, cross-workspace, media/checksum,
  Avatar/span/profile/crop/rate drift, partial output, cost, and atomic rollback fences.
- Prove fresh, migration upgrade, reopen, metadata export/restore, and zero outbound calls.

## Out of scope

- No AvatarForcing/MuseTalk/SkyReels code or weights, provider call, credential, GPU/RunPod, cloud,
  route/UI, fallback activation, deployment, push, or spend.
- No image lane, Style API/Hub, renderer, or production-profile work.

## Ownership

- One integration owner owns any additive migration, Avatar acceptance repository/UoW, fixture
  composition, metadata restore, tests, evidence, and context. Shared surfaces remain serial.

## Acceptance

- Focused PGlite tests prove single-clip/two-layout binding, valid acceptance, exact/conflicting
  replay, technical-versus-subjective authority, drift/fencing/isolation, rollback, reopen, upgrade,
  and restore.
- Forced full `pnpm verify` passes with zero skips in fixture mode at `$0`.
- Evidence: `evidence/acceptance/VF-5-02/avatar-result-acceptance`.
- On completion author exactly one successor brief: `VF-7-08`. Do not author later briefs.

## Safety and rollback

- Accepted Avatar/source/span/artifact rows are immutable; never reinterpret old hashes or clips.
- Roll back additive local changes with normal Git revert; no external cleanup exists.
