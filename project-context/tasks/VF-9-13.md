# VF-9-13 — Corrected Mage negative-prompt quality resume

Status: authorized bounded one-image resume

## Goal

Generate and inspect one documentary Mage BF16 image through the exact negative-prompt contract,
preserve full timing/cost evidence, and return RunPod to absolute zero.

## Context read profile

Use `phase9_mage_corrected_quality_resume` from `MANIFEST.yaml`.

## Authority and limits

- Existing Keychain RunPod credential; exact VF-9-12 image digest; one transient template, one
  scale-zero RTX 4090 endpoint, one job, public model downloads, evidence, and Git push.
- Task cap `$0.15`; automatic cost stop `$0.12`; no Runware call and no redispatch.
- Exact positive/negative prompt bytes and seed from the VF-9-12 runner. Save/show output; record GPU,
  boot/download/generation phases, volume state, hashes, and cost.
- Mandatory bounded cancel, drain, endpoint/template deletion, and independent absolute-zero proof.
- No production promotion/deployment, ImageForge volume mutation, schema/UI change, or Avatar work.

## Acceptance

- Valid 1280x720 PNG with exact positive and negative prompt lineage; no visible text/logo, duplicated
  people, or material anatomy/object/vehicle defects on inspection.
- Canonical verification green and RunPod absolute zero.
- Exactly one real-video integration successor if accepted, or one model-lane decision if rejected.
