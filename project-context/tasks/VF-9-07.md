# VF-9-07 — Mage BF16 RTX 4090 qualification

Status: authorized bounded one-image qualification

## Goal

Run exactly one clean VideoForge Mage-Flow-Turbo BF16 qualification on one RTX 4090, preserve the
output and complete timing/cost evidence, then delete/drain every transient RunPod resource.

## Context read profile

Use `phase9_mage_qualification` from `MANIFEST.yaml`.

## Exact candidate

- Image: `ghcr.io/pala-lakshmansai/videoforge-mage@sha256:9f3dc9d886b309e74adac3d7d101ee546d8d3a31d123dd1c203852d22709334b`.
- Timing-evidence correction commit: `75d43acbda215f9ed2d38acf8c8155ac96726064`;
  hosted image build `31572443030` passed.
- GPU allowlist: exactly `NVIDIA GeForce RTX 4090`, one GPU, one worker maximum, minimum zero.
- Model: `Comfy-Org/Mage-Flow` BF16 at
  `d8c99241f6fa80fbd453014234af2bf337ea21e6`.
- Output: one 1280x720 PNG, four steps, CFG 1, Euler/simple, seed `1234`.
- Exact user prompt: `a wideshot of costco store at night.`

## Authority and limits

- Local qualification runner/tests/docs, RunPod API-key retrieval from the existing macOS Keychain,
  one transient template, one scale-zero endpoint, one job, public model download, Git push, and
  evidence commits are authorized.
- RunPod task cap `$1.00`; automatic cost stop at `$0.90`; combined session provider spend remains
  below the user's `$10` ceiling. No Runware call.
- Do not attach or mutate the existing ImageForge network volume. Record network-volume attachment
  as not applicable for this isolated first run; measure dispatch-to-bootstrap as the combined
  provisioning/image-pull/container-start interval.
- No retry after a terminal model/output failure. An ambiguous dispatch may only be reconciled, never
  blindly repeated.
- Candidate-only: no production profile promotion, deployment, app/schema/UI/Avatar change.

## Required evidence

- Exact commit/image/model/weight hashes, GPU, task/job identity hashes, input prompt hash, seed.
- Timestamp/duration for endpoint creation, dispatch, provisioning to bootstrap start, bootstrap
  download/cache, ComfyUI start-to-ready, generation, total job, cleanup, and overall wall time.
- Explicit network-volume state, initial/final balance delta, RunPod reported execution cost when
  available, and final independent inventory.
- Save PNG locally, verify byte count/SHA-256/1280x720 profile, and remove inline base64 from evidence.
- Cancel if necessary; drain queue; delete endpoint and template; prove zero pods, workers, endpoints,
  private templates, and VideoForge-created volumes before finishing.

## Acceptance

- One valid saved PNG with exact lineage and complete timings/cost evidence.
- Full canonical verification green and RunPod absolute zero.
- Exactly one next integration/reliability successor authored from measured facts.
