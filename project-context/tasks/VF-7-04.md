# VF-7-04 — Durable Image Style analysis-result acceptance

Status: exact provider-free implementation task

## Identity and dependencies

- Milestone: `phase_7_style_analysis_result_acceptance`.
- Depends on VF-7-01 lifecycle/reservation, VF-7-02 durable reference bindings, VF-7-03 claimed
  analysis composition, and the Phase 1 artifact/result/cost/unit-of-work foundation.
- `GATE_STYLE_001` is closed. `GATE_STYLE_002` remains open and is not changed by this task.
- Base: clean `main` descending from VF-7-03 implementation
  `72d5cff2ba1ecf9049115801a159365097eb1324` and evidence
  `85144d3c2fc17e2c9c41c4023ac55e4b34ba30ab`.

## Goal

Accept one already trusted VF-7-03 completion candidate as an immutable canonical profile artifact,
one cost-finalized accepted general attempt, one successful specialized analysis attempt, and one
`NEEDS_REVIEW` Image Style version in a single replay-safe transaction.

## Context read profile

Use `phase7_style_analysis_result_acceptance` only.

## In scope

- Add one narrow exported control-plane success-acceptance service. It receives the in-memory
  VF-7-03 completion candidate, a bounded exact usage summary, exact append-only cost mutations,
  and a completion timestamp; it does not choose or invoke an analyzer.
- Revalidate the candidate as plain exact-shape data. Re-run canonical
  `image-style-profile/v1` schema/hash validation and require exact workspace, style/version,
  specialized/general task and attempt lineage, request fingerprint, reference-set hash,
  analyzer-output hash, model snapshot, disclosure actor, and `VISION_ANALYSIS` provenance.
- Encode only the canonical profile payload as UTF-8 RFC 8785 bytes. Derive stable artifact identity,
  object key, and idempotency keys from durable lineage/hashes; write through an injected
  `CanonicalDocumentObjectStore` and fail if returned immutable facts differ. A failed database
  transaction may leave only harmless content-addressed bytes.
- Register and bind one `CANONICAL_DOCUMENT` artifact sourced by the exact general attempt. Keep
  only hashes, IDs, model snapshot, and bounded aggregate usage in metadata; never persist raw
  provider output, signed URLs, reference bytes, provider prose, or secrets.
- Add one Image Style repository success mutation that re-resolves the exact version,
  specialized attempt, completed accepted general task/attempt, output artifact, stored profile,
  request/model/reference/output hashes, and usage/cost facts. It may transition only
  `ANALYZING -> NEEDS_REVIEW` and `CREATED -> SUCCEEDED`; exact replay returns the same result.
- Within one existing repository unit of work: register/bind the canonical artifact, record the
  general attempt success, append the exact caller-owned cost events, require complete per-attempt
  reservation conservation with explicit reported and settled cost, accept the general result,
  then accept the specialized result/version. Any typed failure rolls every metadata, task,
  attempt, cost, and lifecycle mutation back.
- The usage summary has one exact versioned numeric shape and permits one or two internally bounded
  analyzer calls. Its totals must be safe nonnegative integers and internally consistent. The
  specialized reported micro-USD amount must equal the finalized per-attempt reported and settled
  ledger totals.
- Add focused fresh/reopened PGlite and fake-object-store tests for exact success/replay, canonical
  bytes, cost conservation, workspace isolation, candidate/hash/lineage/artifact drift, stale or
  unclaimed state, malformed usage/profile, object mismatch, transaction rollback, and absence of
  raw/signed/private data.

## Out of scope

- No analyzer invocation, live Runware/model call, credential access, signed-URL resolver,
  reference-byte read, upload/normalization, failure/cancellation/retry convergence, pricing
  lookup, callback/reconciliation change, route, UI, user review edit, publication, preview, Mage,
  ordinary-video integration, cloud/staging mutation, deployment, GPU, or spend.
- Do not invent provider usage from fixture data. Tests use explicit synthetic zero-cost or bounded
  cost events through existing ledger contracts; production evidence-to-settlement mapping remains
  a later exact task.
- Do not change the VF-3-09 model/settings/caps, migration head `0008`, built-in
  `documentary_stock_v1`, accepted UI/renderer/output grammar, or ordinary-project zero-analysis
  behavior.

## Files and ownership

- Own the new Image Style result-acceptance module/exports, the minimal Image Style repository
  success mutation, focused tests, evidence, and `CURRENT_STATE.yaml`.
- Reuse existing artifact, execution, event, unit-of-work, canonical-object-store, and schema
  contracts. Shared repository types/adapter, package exports, evidence, and context remain serial
  integration-owner files. Maximum lanes including integration: one.

## Acceptance

- Focused tests pass against fresh and reopened PGlite state and prove one atomic successful
  transition, exact replay, full rollback, canonical artifact bytes/hash, general/specialized
  attempt convergence, finalized cost conservation, and `NEEDS_REVIEW` profile truth.
- Every invalid candidate, usage, cost, state, owner, hash, lineage, artifact, or workspace case
  fails without partial durable mutation; object-store mismatch fails before database mutation.
- Tests and source prove no analyzer, reference-byte, credential, ambient environment, real network,
  provider, signed-URL, or cloud activity.
- Targeted control-plane lint, typecheck, build, and tests pass.
- `TURBO_FORCE=true pnpm verify`, `pnpm context:validate`, `pnpm secret:scan`, and
  `git diff --check` pass in fixture mode at `$0`; installed-Chrome baseline remains green.
- Small implementation commit, separate evidence commit at
  `evidence/acceptance/VF-7-04/style-analysis-result-acceptance`, then one clean context handoff.
- After success, select only an exact provider-free Image Style review/publication application
  service slice. Do not select routes/UI, uploads, previews, or live provider transport.

## Safety and budget

- `provider_calls_authorized: false`; credential access and model downloads are forbidden.
- Maximum external spend: `$0`.
- Provider mode remains `fixture`; no GPU or RunPod lifecycle action is allowed.
- No signed URL, reference bytes, EXIF, raw provider output, provider prose, secrets, or private
  paths may enter logs, evidence, artifact metadata, mutation receipts, or returned records.

## Rollback

Revert only this task's implementation/evidence/context commits. Do not reset or rewrite accepted
Phase 0–2, provider qualification, adapters, public remote, UI, VF-7-01, VF-7-02, or VF-7-03
history. Content-addressed fake-store bytes created before a typed database rollback are harmless
and may be garbage-collected later.

## Context update

On a green handoff, record exact commits, hashes, and test counts; select one exact provider-free
review/publication application-service brief; leave provider mode, spend, and every open
image/avatar/style-adherence/staging/production gate truthful.
