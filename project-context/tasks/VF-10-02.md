# VF-10-02 — V2-02 tenant-private artifacts and scratch

Status: blocked on accepted V2-01; provider-free

## Goal

Promote the existing workspace-prefixed private-artifact foundation into the V2 contract for every
voiceover, portrait, generated asset, receipt, preview, and final output.

## Scope

- Add immutable object ownership/hash/metadata/retention rows and opaque key rules.
- Define short-lived method/key/type/size/checksum-scoped transfer URLs without persisting secrets.
- Prove two-account negatives, forged keys, expiry, multipart interruption, hash mismatch, and
  orphan cleanup through a fake storage adapter.
- Define job-local scratch/cache variables and cleanup on every terminal path; model volumes never
  contain tenant bytes.

Real R2, credentials, cloud mutation, GPU work, and spend belong to later checkpoints.

