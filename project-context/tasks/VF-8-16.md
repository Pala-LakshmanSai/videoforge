# VF-8-16 — SkyReels timeout observability hardening

Status: exact provider-free successor after VF-8-15 bounded timeout

## Goal

Make long SkyReels inference stage-visible and fail closed before the platform limit, then publish one
immutable worker image without starting paid compute.

## Context read profile

Use `phase8_skyreels_timeout_hardening` from `MANIFEST.yaml`.

## In scope

- Preserve VF-8-15 as one bounded timeout with no accepted MP4 and absolute-zero RunPod inventory.
- Emit safe bootstrap/inference/heartbeat/output stages through RunPod progress updates.
- Keep the pinned official single-GPU `--offload` command; do not guess a faster launch mode.
- Give worker inference timeout enough margin to return its safe diagnostic before the endpoint limit.
- Extend tests and container smoke, publish one immutable GHCR image, and author only one bounded resume.

## Authority and limits

- `$0` external provider spend; local code/tests and GitHub/GHCR build/publish only.
- No RunPod credential read/mutation, model download, GPU, dispatch, alternate GPU count, low-VRAM,
  USP, model substitution, Mage/MuseTalk work, app/schema/UI change, promotion, or deployment.
- RunPod remains absolute zero.

## Acceptance

- Focused/full checks prove safe ordered progress, timeout margin, cancellation, and redaction.
- One immutable image digest is retained.
- Exactly one capped paid-resume successor is authored; RunPod stays zero.
