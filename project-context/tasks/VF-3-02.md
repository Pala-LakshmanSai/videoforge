# VF-3-02 — Bounded Runware Gemini style qualification

Status: exact authorized provider-qualification task; dependency-ready after VF-3-01 handoff

## Goal and dependencies

Qualify or truthfully fail Runware Gemini 3.5 Flash as the one-time Image Style analyzer. Requires
VF-3-01's account/credential path to be safe and committed; ordinary video creation remains a
zero-style-analysis-call path.

Read profile: `phase3_runware_style_qualification`.

## Scope and ownership

- One integration owner adds only the bounded style qualification runner/fixtures, redacted gate
  evidence, and handoff context.
- Use owned/synthetic normalized derivatives for all seven analyzer fixtures in the acceptance
  suite. Exercise strict schema, style/content separation, outlier/uncertainty behavior, reference
  alias binding, semantic validation, latency, provider usage/cost, retry reconciliation, and the
  standard-service privacy disclosure boundary.
- Preserve immutable published style versions, `documentary_stock_v1`, no ordinary-production
  vision call, no reference-image input to Mage, and all accepted output/UI rules.
- Out of scope: application/UI integration, user-private or Ranga references, Mage generation,
  direct Google fallback, cloud staging, production, or broad refactors.

## Acceptance

- Meet the analyzer portions of
  `14_TESTING_AND_ACCEPTANCE.md#image-style-analyzer-and-compiler-gate`; evidence distinguishes
  provider facts from VideoForge inference and records exact model/settings/request/response hashes,
  usage, cost, latency, and redacted outputs.
- First analysis remains below `$0.08`; any reconciled retry path remains below `$0.15`.
- Targeted tests, `TURBO_FORCE=true pnpm verify`, context validation, secret scan, and diff check
  pass. Evidence path:
  `evidence/gates/GATE_STYLE_001/2026-08-10-runware-gemini-style-qualification`.

## Safety and budget

- Provider calls and credential access are authorized only for this exact Runware Gemini style
  qualification.
- Non-transferable task ceiling: `$3.00`; aggregate ceiling remains `$25.00`.
- Runware's standard service is not ZDR/confidential. Use only owned/synthetic references, strip
  metadata, minimize payloads, and do not accept a paid subscription or purchase balance.
- CAPTCHA, MFA, payment, unclear charge, credential ambiguity, or cap uncertainty stops this lane.

## Rollback and handoff

Revert only this task's commits. Pass/fail evidence must keep normal provider mode `fixture` and
must not imply application integration. Select the next dependency-ready brief only after context
reconciliation.

