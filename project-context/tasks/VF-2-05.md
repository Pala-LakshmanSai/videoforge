# VF-2-05 — Phase 2 timing/timeline convergence

Status: complete at implementation `907e0e4`; evidence `d16c2a9`

## Goal and dependencies

Integrate durable transcription, selected span audio, deterministic timeline persistence and Chrome
inspection into one restart/restore-safe provider-free flow. VF-2-02, VF-2-03, and VF-2-04 must be
green.

Read profile: `phase2_convergence`.

## Scope and ownership

- Integration owner composes adapters/routes/UI, resolves only committed interface mismatches,
  records evidence/context, and commits.
- Run owned real-audio local acceptance plus restart and metadata restore.
- Preserve accepted renderer/output and all product decisions.
- Out of scope: provider calls, prompt/image/avatar production lanes, staging/cloud, credentials,
  model downloads, and spend.

## Acceptance

- Owned audio persists exact transcript → phrases → byte-equivalent timeline → selected span audio
  across restart and restore.
- Coverage has no gaps/overlaps; cancellation, stale input and recompute are truthful.
- Installed Chrome inspects the real-audio timeline with no console/network/accessibility regression.
- `TURBO_FORCE=true pnpm verify`, local-slice acceptance, context validation, secret scan,
  dependency audit, diff check, and clean worktree pass.

Evidence: `evidence/acceptance/VF-2-05/phase2-timing-convergence`.

## Rollback and handoff

Revert implementation commit `907e0e4` while keeping each earlier lane green. The standing
provider-free authority ended at this green handoff. `VF-3-00` owns the next planning-only
gate/authorization review; no Phase 3 implementation brief is selected.
