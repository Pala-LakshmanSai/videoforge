# VF-9-06 — Build pinned Mage candidate image

Status: bounded candidate worker-image successor

## Goal

Build and publish one immutable linux/amd64 Mage candidate image using the exact proven headless
ComfyUI BF16 graph, pinned public weights, and a strict RunPod handler.

## Context read profile

Use `phase9_mage_worker_image_build` from `MANIFEST.yaml`.

## In scope

- Pin `Comfy-Org/ComfyUI` at `1108f2ac5e412b27accb0e5d51c90ef2ba39784d` and
  `Comfy-Org/Mage-Flow` at `d8c99241f6fa80fbd453014234af2bf337ea21e6`.
- Build a small worker image without model bytes. At runtime, download/cache only the exact BF16
  transformer, Qwen text encoder, and VAE, and verify all three sizes/SHA-256 values before serving.
- Use `CLIPLoader.type=mage`, `TextEncodeMageFlowEdit` latent output, 1280x720, four steps, CFG 1,
  Euler/simple. Do not use diffusers, FlashAttention, Microsoft source patches, or empty SD3 latent.
- Verify pinned source/import/handler/graph/bootstrap boundaries and immutable image digest.
- Publish candidate image to existing GHCR only after all build checks pass.

## Authority and limits

- Local code/tests/docs, Git push, GitHub Actions, official GitHub/Hugging Face reads/download, and
  existing GHCR publication are authorized.
- Maximum external spend `$0`; no RunPod/Runware mutation, GPU, dispatch, inference, qualification,
  promotion, deployment, new credentials, or app/schema/UI/Avatar changes.
- Candidate-only: do not change production execution profiles. RunPod remains absolute zero.

## Acceptance

- Hosted build passes from exact source identity; immutable linux/amd64 image digest is recorded with
  handler/import/graph/bootstrap evidence. Exact weight bytes are verified on runtime bootstrap.
- Canonical local and hosted verification remain green.
- Exactly one capped one-image RunPod qualification successor is authored; its total task cap must
  preserve the user's combined provider ceiling and mandatory delete/drain/zero proof.
