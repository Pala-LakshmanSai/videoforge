# VF-9-06 — Build pinned Mage candidate image

Status: bounded candidate worker-image successor

## Goal

Build and publish one immutable linux/amd64 Mage candidate image containing exact source revision,
exact model revision, the mandatory no-watermark/refusal patch, and a strict RunPod handler.

## Context read profile

Use `phase9_mage_worker_image_build` from `MANIFEST.yaml`.

## In scope

- Add a dedicated Mage worker image/handler and build workflow using exact source/model revisions.
- Download only `microsoft/Mage-Flow-Turbo` at
  `395402ba3ef110c96e70d01abe4d178dbe4e01a5` during the controlled image build.
- Verify repository size ceiling, transformer SHA-256, applied source patch, import path, handler
  registration, offline runtime, and immutable image digest.
- Publish candidate image to existing GHCR only after all build checks pass.

## Authority and limits

- Local code/tests/docs, Git push, GitHub Actions, official GitHub/Hugging Face reads/download, and
  existing GHCR publication are authorized.
- Maximum external spend `$0`; no RunPod/Runware mutation, GPU, dispatch, inference, qualification,
  promotion, deployment, new credentials, or app/schema/UI/Avatar changes.
- Candidate-only: do not change production execution profiles. RunPod remains absolute zero.

## Acceptance

- Hosted build passes from exact source and weight identities; immutable linux/amd64 image digest is
  recorded with handler/import/patch/size evidence.
- Canonical local and hosted verification remain green.
- Exactly one capped one-image RunPod qualification successor is authored; its total task cap must
  preserve the user's combined provider ceiling and mandatory delete/drain/zero proof.
