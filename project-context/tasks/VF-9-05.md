# VF-9-05 — Admit pinned Mage revision

Status: provider-free implementation successor

## Goal

Admit exact official `microsoft/Mage-Flow-Turbo` revision
`395402ba3ef110c96e70d01abe4d178dbe4e01a5` into the strict worker boundary without downloading
weights or publishing/running an image.

## Context read profile

Use `phase9_mage_revision_code_admission` from `MANIFEST.yaml`.

## In scope

- Replace the unresolved model constant with the exact 40-hex revision.
- Pin the known official transformer LFS identity and expected repository byte ceiling.
- Test exact revision match/mismatch, command pinning, and unchanged hostile-input/output behavior.
- Retain the source watermark/refusal patch and candidate-only promotion policy.

## Authority and limits

- Local worker code/tests/docs and Git push only; `$0` external spend.
- No model download, GHCR image, RunPod/Runware mutation, GPU, dispatch, qualification, promotion,
  deployment, credential/token/session access, or Avatar/app/schema/UI change.
- RunPod remains absolute zero.

## Acceptance

- Boundary admits only the exact pinned revision and remains offline/fail-closed for mismatch.
- Focused and canonical verification pass.
- Exactly one capped worker-image build successor is authored.
