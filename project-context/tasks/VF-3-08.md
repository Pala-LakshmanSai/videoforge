# VF-3-08 — Mage checkpoint access and license preflight

Status: exact read-only access/license task

## Goal and dependencies

Resolve or truthfully preserve `GATE_IMAGE_002` before any Mage weight download, GPU activation, or
paid benchmark. Depends on the official `microsoft/Mage-Flow-Turbo` model repository and the
provider-free prompt/compiler foundation. This task cannot close speed/quality gate
`GATE_IMAGE_001` or GPU gate `GATE_GPU_001`.

Read profile: `phase3_mage_access_license_preflight`.

## Scope and authority

- Inspect only current official Microsoft/Hugging Face repository metadata and small text/config
  artifacts needed to identify the checkpoint and governing license.
- Record direct official URLs, checked timestamps, HTTP/access state, exact repository revision,
  gating state, file inventory/size or LFS object identity, and hashes of any retrieved small
  license/config text.
- An existing signed-in Chrome session may be used to inspect access. Do not retrieve, print,
  create, rotate, or store credentials. Do not accept new legal terms or create an account when the
  exact checkpoint/license would otherwise remain ambiguous.
- `GATE_IMAGE_002` closes only if the exact official checkpoint revision and an authoritative,
  unambiguous license artifact governing the downloadable weights are both accessible and recorded.
  Marketing metadata or an unofficial mirror is supporting evidence only.

## Out of scope

- No model weights, safetensors, large LFS objects, code installation, container build, provider
  inference, Runware generation, RunPod Pod/endpoint/volume activation, GPU use, or spend.
- No application code, schema, migration, UI, renderer, output grammar, provider profile, or
  production decision change.

## Acceptance and handoff

- Evidence distinguishes public metadata from authenticated repository facts and inference.
- No secret, browser cookie, signed URL, private reference, or weight bytes enter Git or logs.
- `pnpm context:validate`, `pnpm secret:scan`, and `git diff --check` pass at `$0`.
- Evidence path:
  `evidence/gates/GATE_IMAGE_002/2026-08-11-huggingface-mage-access-license`.
- On pass, author one exact capped Mage/RunPod benchmark brief under the non-transferable `$8`
  Mage/GPU cap and `$5` RunPod lifecycle cap. On blocked/open result, record the exact blocker and
  select a dependency-ready provider-free custom-style task; do not guess or download around it.

## Safety and rollback

- `provider_calls_authorized: false`; maximum external spend `$0`; RunPod stays off.
- Stop on credential exposure, required legal acceptance, ambiguous official ownership/license,
  large-file transfer, external mutation, or any charge.
