# VF-8-05 — AvatarForcing cold-start diagnostic

Status: exact successor selected after bounded VF-8-04 failure

## Goal

Resolve the container-start/capacity boundary that prevented the pinned AvatarForcing worker from
registering reliably, without another paid qualification attempt until a concrete correction is proven.

## Context read profile

Use `phase8_avatarforcing_diagnostic` from `MANIFEST.yaml`.

## In scope

- Preserve the locked AvatarForcing source/weight revisions and immutable GHCR image history.
- Prove the exact entrypoint imports and registers one RunPod handler in the built linux/amd64 image.
- Record compressed image/model byte budgets, safe startup signals, and a five-minute no-running-pod
  timeout that purges/drains/deletes before handoff.
- Obtain redacted worker lifecycle/log evidence through the signed-in RunPod console when available;
  never store endpoint secrets, API keys, raw worker IDs, or provider environment values.
- Author one capped qualification-resume brief only after the failure has a concrete code, image,
  capacity, or configuration correction.

## Authority and limits

- `$0` external spend; read-only RunPod inventory only.
- No endpoint/template/Pod/volume creation, job dispatch, model download, or provider mutation.
- No model substitution, MuseTalk/SkyReels execution, schema/UI redesign, production profile exposure,
  or Mage work.
- Server stopped and RunPod inventory must remain zero.

## Acceptance

- A pinned GHCR digest passes dependency import, exact handler-registration, and source compile smoke.
- The retained VF-8-04 evidence truthfully shows every attempt, cost, and final zero inventory.
- The next paid retry has a specific evidenced correction; otherwise leave `GATE_AVATAR_003` open.
- Focused verification, context/schema/secret/diff checks pass; worktree clean and RunPod remains zero.
