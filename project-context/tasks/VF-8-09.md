# VF-8-09 — AvatarForcing memory-failure diagnostic correction

Status: exact provider-free successor after VF-8-08 classified process failure

## Goal

Close the remaining CUDA OOM classifier gap, constrain the next run to A100 80GB, publish a pinned
worker image, and keep RunPod at absolute zero.

## Context read profile

Use `phase8_avatarforcing_memory_diagnostic` from `MANIFEST.yaml`.

## In scope

- Classify Python `OutOfMemoryError` spellings and CUDA allocation markers without exposing logs.
- Remove L40S from qualification allowlist. AvatarForcing's pinned model files are 43.5 GB before
  runtime allocations, so a 48 GB worker lacks reliable headroom.
- Focused tests and pinned GHCR dependency/handler/source-compile smoke.

## Authority and limits

- `$0` external spend; no RunPod credential read, mutation, dispatch, model download, or GPU start.
- No blind redispatch, model substitution, Mage/MuseTalk/SkyReels execution, promotion, or deploy.
- RunPod inventory must remain absolute zero.

## Acceptance

- OOM variants map to `AVATAR_INFERENCE_CUDA_OOM`; no raw diagnostic crosses boundary.
- Next-run allowlist is exactly A100 80GB and still permits only one scale-zero worker.
- Pinned image passes smokes; focused/full checks pass; RunPod remains zero.
