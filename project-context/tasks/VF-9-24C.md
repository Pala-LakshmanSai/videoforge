# VF-9-24C — EchoMimic tokenizer digest correction

Status: selected; provider-free correction authorized

## Goal

Correct the one-character tokenizer digest truncation exposed by `VF-9-24B`, publish one corrected
immutable worker image, then stop before another paid attempt.

## Root cause

The pinned SHA-256 for `base/google/umt5-xxl/tokenizer.json` omitted its final `b`. First-party
revision metadata reports
`6e197b4d3dbd71da14b4eb255f4fa91c9c1f2068b20a2de2472967ca3d22602b`; every other selected
runtime hash and size matched. The worker therefore correctly returned
`AVATAR_BOOTSTRAP_CACHE_MUTATED` before model-ready or inference.

## Authority

- Worker/source/tests/context/Git/GHCR changes and one corrected image build are authorized.
- RunPod calls, model downloads, GPUs, jobs, retries, and external spend are not authorized.
- Preserve historical `VF-9-22` evidence; record the correction as new evidence.

## Acceptance

- Every required SHA-256 is exactly 64 lowercase hex characters and the tokenizer pin matches the
  immutable first-party revision metadata.
- Worker tests, container entrypoint smoke, full local verification, image build, and hosted verify
  pass.
- Pin the corrected digest, leave RunPod absolute zero, and request explicit authority for a second
  sample attempt.
