# VF-1-03 — authentication and workspace authorization adapter

Status: standing provider-free authority after VF-1-02

## Goal and dependencies

Implement the auth/session port, Better Auth Google-OAuth configuration boundary, admin allowlist,
workspace membership authorization, and session-derived reviewer identity. VF-1-02 must be green;
the production Google flow waits for staging authority.

Read profile: `phase1_auth`.

## Scope and ownership

- Own auth adapter/domain modules and isolated auth tests only.
- Use a deterministic local identity provider in fixture/local tests; production credentials are
  bindings and fail closed when absent.
- Enforce invitation, active membership, workspace scope, authorization status, session expiry,
  and reviewer derivation. Never accept reviewer identity from request data.
- Integration owner alone edits dependencies, root/app composition, migrations, context, or commits.
- Out of scope: live Google OAuth, account mutation, email invites, public signup, storage,
  orchestration, UI redesign, and provider calls.

## Acceptance

- Invited active membership succeeds; uninvited, archived, wrong-workspace, expired, and spoofed
  reviewer cases fail without existence leakage.
- Fixture behavior and accepted access screens remain unchanged.
- Owned lint/typecheck/tests pass; integration runs full uncached verify, Chrome access regressions,
  secret scan, dependency audit, and diff check.

Evidence: `evidence/acceptance/VF-1-03/auth-workspace-adapter`.

## Rollback and handoff

Revert the auth adapter/integration commits; fixture auth remains available. This lane reports green
to the Wave 2 integration owner and never edits `CURRENT_STATE.yaml`. VF-1-06 becomes eligible only
after VF-1-03, VF-1-04, and VF-1-05 are all integrated.
