# VF-8-17 — SkyReels stage-visible A100 resume

Status: exact paid successor after VF-8-16 timeout hardening

## Goal

Run one stage-visible five-second SkyReels qualification with enough bounded runtime for the pinned
official single-GPU path, retain exact evidence, and return RunPod to absolute zero.

## Context read profile

Use `phase8_skyreels_stage_visible_resume` from `MANIFEST.yaml`.

## In scope

- Immutable image
  `ghcr.io/pala-lakshmansai/videoforge-avatar-quality@sha256:8f94062164794c19036605282968a96dd098a09aa91d066b9eaafbb5577f48c9`.
- Same pinned source/model/input hashes as VF-8-15, one scale-zero endpoint, one A100 80GB worker,
  120 GB ephemeral container disk, one five-second inline dispatch.
- Worker timeout `2,550` seconds; endpoint timeout `2,700` seconds. Persist safe progress stages.

## Authority and limits

- Existing secure RunPod credential and exact pinned model download only; maximum new spend `$2.10`,
  hard stop at `$2.00` observed. Combined sprint provider spend remains below `$10.00`.
- No second dispatch, alternate GPU/count, Pod/volume, low-VRAM/USP, command/model substitution,
  MuseTalk, Mage work, app/schema/UI change, promotion, or deployment.
- Cancel/drain/delete on terminal result, cap risk, or ambiguity. Zero idle cost is mandatory.

## Acceptance

- One valid 960x960/25 MP4 has exact hash/probe/lineage/timing/progress/cost evidence, or one safe
  terminal failure is retained without redispatch.
- Quality review remains separate; no promotion from technical evidence alone.
- Final and independent RunPod inventories are absolute zero.
