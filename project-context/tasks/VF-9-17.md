# VF-9-17 — Real-provider render composition boundary

Status: in progress at checkpoint `95ff125`; authorized `$0` local completion

## Goal

Compose durable real-provider Mage and Avatar acceptances into the existing FFmpeg v3 render manifest
and review/download path using exact fake provider artifacts, while blocking rejected or missing QA.

## Context read profile

Use `phase9_real_provider_render_composition` from `MANIFEST.yaml`.

## In scope

- Resolve only accepted exact Mage/Avatar assets and their immutable provider/model/prompt/runtime/cost
  lineage into real-shaped 30–120-second render inputs.
- Preserve hard cuts, continuous slow image zoom, fixed layouts, no text/graphics/overlays, and exact
  avatar crop bindings.
- Fake-real artifact composition, restart/replay, cancellation, missing/rejected QA, checksum drift,
  FFmpeg v3, review/download/hash, and installed-Chrome tests.
- Fixture remains default; no profile promotion.

## Authority and limits

- Local application/control-plane/renderer/tests/docs, Git push, hosted CI, `$0` only.
- No provider call, credential, GPU, model download, RunPod/Runware mutation, deployment, profile
  promotion, UI redesign, or spend.

## Acceptance

- One 30–120-second real-shaped candidate renders/replays/downloads only from accepted provider
  artifacts; rejected VF-9-13 and VF-8-10 evidence cannot enter an accepted render.
- Canonical verification and installed-Chrome playback/download/hash proof pass.

## Resume checkpoint

Completed at `95ff125`: pure provider-acceptance asset barrier plus 116/116 focused pipeline tests.
Evidence: `evidence/acceptance/VF-9-17/real-provider-render-composition/README.md`.

Resume in this order:

1. Compose durable Mage and Avatar acceptance records into the barrier without changing fixture
   defaults or stripping immutable acceptance lineage.
2. Add one fake-real 30–120-second local run using accepted exact artifacts; test rejected/missing
   QA, checksum drift, cancellation, and restart/replay.
3. Run FFmpeg v3 and installed Chrome play/seek/approve/download/hash verification.
4. Run full canonical verification, update context, and close VF-9-17 only when all evidence exists.

Do not use the rejected `VF-9-13` Mage PNG or `VF-8-10` AvatarForcing MP4 as accepted inputs.
