# VF-9-10 — RunPod status-read reconciliation

Status: authorized `$0` reliability correction

## Goal

Prevent one transient same-job status-read failure from cancelling an acknowledged RunPod job while
preserving no-blind-redispatch, caps, cancellation, and cleanup guarantees.

## Context read profile

Use `phase9_runpod_status_reconciliation` from `MANIFEST.yaml`.

## In scope

- Distinguish idempotent GET/read ambiguity from POST mutation ambiguity.
- Add bounded same-job status/health reconciliation with backoff and exact terminal failure codes.
- Test transient recovery, exhaustion, abort, no second dispatch, cost polling, and cleanup behavior.
- Apply the hardened poller to qualification runners without provider activity.

## Authority and limits

- Local code/tests/docs, Git push, hosted CI, `$0` only.
- No RunPod/Runware call, credential, GPU, download, dispatch, deployment, promotion, schema/UI/Avatar
  change, or spend.

## Acceptance

- Transient GET failures recover against the exact acknowledged job ID; POST dispatch remains
  non-retriable/ambiguous.
- Canonical verification green and RunPod independently zero.
- Exactly one capped quality-confirmation resume authored if successful.
