# VF-9-24F — A100 guard repair and Elias sample

Status: selected; repair plus one corrected paid A100 attempt authorized

## Goal

Publish the narrowly corrected worker, run the unchanged exact Elias BF16 sample once on A100 80 GB
PCIe, return the native MP4, and prove RunPod absolute zero.

## Exact authority

- Cumulative ceiling remains `$2.00`; prior live balance delta is `$0.0694373407`; maximum remaining
  authority is `$1.9305626593`.
- Operational breaker remains `$0.50` hard / `$0.45` active. One corrected job only.
- Permit only RTX 4090 24 GB and A100 80 GB runtime identities; reject RTX 5090 and A100 40 GB.
- Map input-download and GPU-query failures to safe exact codes instead of `AVATAR_PRIMARY_FAILED`.
- Publish one immutable corrected worker image, then use one temporary A100 endpoint with 100 GB
  ephemeral disk. No network volume, fallback, tuning, or alternate model.
- Inputs and exact BF16 inference parameters remain byte/config identical to `VF-9-24E`.

## Acceptance and cleanup

- Output: `.videoforge/review/VF-9-24F/echomimic-v3-flash-elias-10.12s-native.mp4`.
- Evidence: `evidence/acceptance/VF-9-24F/elias-echomimic-v3-flash-a100-guard-repair/`.
- Record immutable image digest/build, queue/worker activation, model bootstrap/download/verification,
  model load plus 10.12-second generation/encode, total wall time, peak VRAM, output hash/probe, and
  cost as separate measurements.
- Cleanup in `finally`, then three independent zero-resource reads.
- Show playable MP4 here. End `READY_FOR_USER_REVIEW`; never self-approve.
