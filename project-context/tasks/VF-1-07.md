# VF-1-07 — account isolation, large upload, and avatar reuse

Status: standing provider-free authority after VF-1-06

## Goal and dependencies

Use two invited local accounts to prove workspace isolation, direct large-audio transfer, and exact
reuse of one stored Avatar Profile without project-local upload. VF-1-06 must be green and the
migration/API contract frozen for this wave.

Read profile: `phase1_isolation`.

## Scope and ownership

- Own account-isolation/large-upload/avatar-reuse integration tests and necessary bounded route or
  UI wiring; preserve established presentation.
- Prove every object, hash lookup, signed transfer, project revision and reviewer action is scoped.
- Integration owner alone edits shared bindings, migrations, dependencies, context, or commits.
- Out of scope: live Google/R2, staging, providers, new avatar model tests, and spend.

## Acceptance

- Neither account can read, bind, infer, approve, download, rename, archive, or hash-probe the
  other's records.
- Large audio uses signed multipart/direct bytes; application bodies remain bounded.
- Create Project submits only the exact ready Avatar Profile version and preserves draft fields
  through Hub round trips.
- Focused integration/Chrome tests and full uncached verify, secret scan, audit, and diff check pass.

Evidence: `evidence/acceptance/VF-1-07/isolation-upload-avatar-reuse`.

## Rollback and handoff

Revert its narrow integration commit. Report green without editing context. VF-2-01 becomes eligible
only after both VF-1-07 and VF-1-08 are integrated.
