# VF-9-12 — Mage negative-prompt contract correction

Status: authorized `$0` worker-boundary correction

## Goal

Stop discarding the production prompt compiler's negative prompt. Bind and hash its exact bytes in
the Mage job contract and feed them to `TextEncodeMageFlowEdit.negative_prompt`.

## Context read profile

Use `phase9_mage_negative_prompt_correction` from `MANIFEST.yaml`.

## In scope

- Add an exact bounded `negative_prompt` and SHA-256 to each Mage item.
- Reject missing, blank, oversized, hash-drifted, accessor, and extra-field inputs.
- Feed the admitted negative prompt into the stock ComfyUI Mage graph; update runner/tests/evidence.
- Build/publish one immutable corrected image only after full local verification.

## Authority and limits

- Local worker/runner/tests/docs, GHCR build, Git push, hosted CI, `$0` only.
- No RunPod/Runware call, credential read, GPU, model download, dispatch, promotion, deployment,
  application schema/UI/Avatar change, or spend.

## Acceptance

- Exact compiled negative bytes reach the graph and are lineage-bound; no silent empty fallback.
- Hostile contract tests, canonical verification, immutable image build, and secret scan pass.
- Exactly one capped quality-resume successor authored if successful.
