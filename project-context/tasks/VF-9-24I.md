# VF-9-24I — Historical FP8 full-video RTX sample attempt

Status: superseded; non-executable; former paid authority revoked by `DEC_RUNPOD_002`

## Historical goal and authority

This task previously attempted the exact 10.12-second Elias EchoMimicV3-Flash native MP4 with a
runtime-created TorchAO FP8 transformer on RTX 4090/5090. The user formerly raised its cumulative
ceiling to `$8`. That authority applied only to the old Serverless/ephemeral or temporary-cache
architecture and is no longer active or transferable.

Do not resume this task, run its private scripts, create/delete any provider resource, apply the old
global-zero/volume-deletion cleanup, spend against its former cap, or infer production approval.
Current authority lives only in `CURRENT_STATE.yaml` and the selected task.

## Preserved facts

- Exact source/audio: 253 frames at 25 fps; private bytes never entered Git.
- FP8 method: pinned TorchAO dynamic activation-and-weight `float8_e4m3fn` for compatible
  transformer linears; remaining tensors BF16; no uncarded third-party pickle.
- The pinned upstream pipeline did not support the attempted Long Video CFG keywords. Active
  `DEC_AVATAR_008` now forbids Long Video CFG and uses short scheduler-selected spans.
- Historical bounded attempts reached RTX 5090/model execution and exposed device-transfer and
  unsupported-call issues. The preserved evidence contains exact digests, timings, costs, and
  cleanup observations.
- No reviewable MP4 was produced.
- Evidence: `evidence/acceptance/VF-9-24I/elias-echomimic-v3-flash-fp8-rtx/`.

The current architecture prepares the exact Echo FP8 runtime once on its own persistent
`EU-RO-1` model volume, creates a disposable Echo Pod for scheduled short spans, then deletes only
the Pod while retaining that volume. Any future provider run requires a new explicit brief and cap.
