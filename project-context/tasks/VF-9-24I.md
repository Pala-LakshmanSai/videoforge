# VF-9-24I — FP8 full-video RTX sample

Status: pinned-call repair in progress; paid retry authorized under cumulative $8 ceiling

## Goal

Produce and return the same 10.12-second Elias EchoMimicV3-Flash native MP4 after the BF16 A100
attempt remained unavailable beyond twenty minutes. Use a pinned TorchAO FP8 dynamic
activation-and-weight transformer on RTX 4090 or RTX 5090 for the full exact sample.

## Exact authority and limits

- User explicitly authorized an FP8 EchoMimicV3-Flash installation and one same-input run on RTX
  4090 or RTX 5090 after the BF16 attempt exceeded twenty minutes.
- RTX 4090 community allocation was unavailable twice. RTX 5090 is the selected allowed fallback;
  its pinned CUDA 12.8/PyTorch 2.7.1/TorchAO 0.11 runtime passed hosted image smoke.
- Keep exact source image and audio bytes, 253 output frames, 25 fps, 768 square, 8 steps,
  `Flow_Unipc`, TeaCache `0.1`, seed `43`, prompt, and private output transport.
- Quantize compatible transformer linear weights and activations to `float8_e4m3fn`; keep remaining
  tensors BF16. The pinned upstream pipeline does not accept Long Video CFG arguments, so invoke its
  documented full-video interface without unsupported keywords.
- Do not use the uncarded third-party pickle checkpoint. Quantize the pinned Apache-2.0 upstream
  safetensors at runtime with pinned BSD-3-Clause TorchAO.
- User authorized three additional `$2` increases after the earlier `$2` ceiling. Treat the combined ceiling as `$8` from the
  original baseline balance; enforce the cumulative balance breaker and retain no paid capacity.
- After one authorized run, delete every endpoint/template/volume/Pod, prove global zero three
  times, and do not retain paid capacity.

## Evidence and output

- Output: `.videoforge/review/VF-9-24I/echomimic-v3-flash-fp8-elias-10.12s-native.mp4`.
- Evidence: `evidence/acceptance/VF-9-24I/elias-echomimic-v3-flash-fp8-rtx/`.
- Initial published image:
  `ghcr.io/pala-lakshmansai/videoforge-avatar-primary@sha256:833ea5cd3406b8cc82562edba8a5a701a18eb813f7cee22e40f245f7c08fceed`;
  hosted build/smoke run `31636493440` passed at `$0`.
- Bounded attempts reached RTX 5090 boot in 31.385 seconds and model execution, exposing FP8
  quantization before upstream device transfer. Commit `add8985` defers quantization until after
  both pipeline transfers. Repaired image
  `sha256:2d4384bed4dbe2aa479e11f0efaba8b8b18c8ebdad46871e0b3fd816e004d247` passed hosted run
  `31640266063`. No MP4 exists yet; every attempt ended with three global-zero reads.
- The async repaired worker reached 462 FP8 linear layers, then failed before denoising because the
  wrapper supplied unsupported Long Video CFG keywords to the pinned pipeline. The exact repair removes
  only those keywords and preserves the full sample. All paid resources were deleted after failure.
- Record image digest, GPU SKU/rate, queue/activation, cache/bootstrap, FP8 quantization/model load,
  generation/encode, total execution, peak VRAM, exact output SHA-256/probe, measured cost, and
  three zero-resource inventory reads.
- Show the MP4 here and end `READY_FOR_USER_REVIEW`; user alone approves visual quality.
