# VF-9-24K — CP-01 global-session contracts and dispatch firewall

Status: reopened for independent-audit fixes at base
`96f53a7a10f0936c3e4140a14f2d5dc80abbd9fe`; `$0`; no production gate closed

## Goal

Implement `CP-01` from `22_PROJECT_COMPLETION_CHECKPOINTS.md`: version the global session/queue and
two isolated model-lane lifecycle before any new paid dispatch, then make the legacy Serverless path
impossible to import from the vNext production composition.

## Read profile and dependencies

- Read `cp01_global_session_contracts` from `MANIFEST.yaml` only after the mandatory files.
- Base: clean `main` containing completed `VF-9-24L`/`CP-00` context handoff.
- Decisions: `DEC_AUTH_001`, `DEC_TENANCY_001`, `DEC_QUEUE_002`, `DEC_RUNPOD_002`,
  `DEC_COST_001`, `DEC_IMAGE_001`, `DEC_AVATAR_007`, `DEC_AVATAR_008`.
- All production model/GPU/RunPod/cost gates stay open.

## In scope

- Add vNext schemas, generated types, additive migrations, repositories, and valid/invalid fixtures
  for admitted identity, singleton generation session, immutable two-GPU pair/rate receipts, global
  waiting queue/version, active-project-only execution plan, isolated Mage/Echo volume manifests,
  Pod create/readiness/delete/absence attempts, durable results, and cost.
- Enforce exactly one active video. Waiting rows create no ASR, prompt, render, GPU task, attempt, or
  outbox. A waiter may retain an already-running Pod warm but cannot create/recreate one. Only the
  next-project activation may recreate a missing lane on the same session GPU after revalidation.
- Reject cross-volume binding, stale inventory, selected/actual GPU mismatch, false `model_ready`,
  ambiguous duplicate create/delete, false stop, routine volume deletion, and concurrent idle-start
  losers.
- Add a production import/config firewall against Serverless `/run`, endpoints,
  `workersMin`/`workersMax`, `Auto`/priority GPU routing, old Mage BF16,
  AvatarForcing/MuseTalk/SkyReels, repair/fallback, and legacy worker registries.
- Preserve every old migration, v1 byte, fixture, task, and provider evidence unchanged.

## Acceptance

- Schema/type parity, coherent positive/negative fixtures, fresh/upgrade/restore migrations, and
  focused concurrency/failure tests pass.
- One synthetic session proves one idle-start winner, one active project, inert/reorderable waiting
  rows, independent synthetic Pod drain, next-activation-only missing-lane recreation, both Pods
  absent, and both volumes retained.
- Legacy imports/config cannot enter the vNext production graph.
- `pnpm verify:fast`, canonical `CI=1 TURBO_FORCE=true pnpm verify`, context/schema validators,
  secret scan, dependency audit, and `git diff --check` pass. Use real Chrome only if visible state
  changes.
- End with one small green commit and refreshed `CURRENT_STATE.yaml`. Do not start `CP-02`.
- Bind every dispatch input/output path to the exact session, queue entry, run plan, lane, and Pod
  attempt; foreign lineage must fail before the dispatch port.
- Scan the complete production composition graph, not only one source file, and require one
  canonical non-legacy port composition.
- Lifecycle mutation methods must reject zero-row readiness/delete/stop updates.

## Authority and safety

- `application_code_changes_authorized: true` for this CP-01 scope only.
- `provider_calls_authorized: false`.
- `remote_or_cloud_mutations_authorized: false`.
- `credential_access_authorized: false`.
- `model_downloads_authorized: false`.
- Maximum external spend: `$0`.
- Preserve private local audio/avatar files; never stage them.

## Rollback

Revert only this task's implementation/context commits. Never reset or rewrite historical
evidence, accepted UI/render work, or the `CP-00` architecture decision.

## Handoff

- Exact proof: `evidence/acceptance/VF-9-24K/cp01-global-session-contracts/acceptance.json`.
- Green gates: TypeScript 66, Python 50, control-plane 217, web unit 219, Cloudflare 1, installed
  Chrome 38; `verify:fast` and canonical `CI=1 TURBO_FORCE=true pnpm verify` exited zero.
- Fresh/full-legacy-upgrade/restore migrations and schema parity passed. Migrations `0001`–`0013`,
  v1 worker-envelope bytes, and historical evidence were unchanged.
- Provider calls, credentials, cloud mutations, resources, downloads, and external spend: zero.
  Local server stopped. No live provider inventory refresh was performed.
- CP-02 was not started and requires a fresh narrow brief plus explicit user authority.
