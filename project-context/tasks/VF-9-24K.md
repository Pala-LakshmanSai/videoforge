# VF-9-24K — Isolated model-lane contracts and fixtures

Status: proposed provider-free implementation task; `$0`; awaiting explicit user authorization

## Identity and dependencies

- Milestone: `isolated_model_lane_contracts`.
- Base: clean `main` descending from the `VF-9-24J` context handoff.
- Decisions: `DEC_IMAGE_001`, `DEC_AVATAR_007`, `DEC_AVATAR_008`, `DEC_RUNPOD_002`.
- Open gates remain open: `GATE_IMAGE_001`, `GATE_AVATAR_001`, `GATE_AVATAR_004`,
  `GATE_GPU_001`, `GATE_RUNPOD_001`, and `GATE_COST_001`.

## Goal

After explicit implementation authorization, add a versioned, provider-free contract and fixture
state machine for the two isolated persistent-model lanes without making a RunPod call or claiming
production readiness.

## Context read profile

Use `phase9_isolated_model_lane_contracts` from `MANIFEST.yaml` only.

## In scope after authorization

- Add vNext schemas/types/valid and invalid fixtures for:
  - one exact Mage-only model-volume binding and immutable manifest;
  - one different Echo-only model-volume binding and immutable manifest;
  - expiring per-lane live compatible GPU inventory receipts;
  - independent exact Mage and Echo offering selections plus final revalidation identity;
  - Pod create/reconcile/readiness/work/delete/absence attempts and timings;
  - authoritative `model_ready` only after container readiness, exact volume verification, GPU
    load, and warm-up;
  - durable output barriers and Pod-deleted/volume-retained terminal state.
- Reject shared/swapped/cross-mounted volume IDs, stale receipts, selected-versus-actual GPU
  mismatch, wrong region/model/image/manifest, container-ready-as-model-ready, duplicate ambiguous
  create, false stopped state, and routine volume deletion.
- Add fixture adapter/UI states showing one Generate starting both lanes concurrently after verified
  input preflight while local preparation advances, then independent synthetic Pod deletion with
  both synthetic volumes retained.
- Keep existing v1 bytes and historical fixtures replayable. Version rather than reinterpret them.
- Update generated bindings and provider-free tests required by the new contracts.

## Out of scope

- No RunPod API/live inventory call, credential access, GPU use, Pod/template/volume creation, model
  download, worker image publication, cloud/account mutation, deployment, live database migration,
  real model sample, or external spend.
- No Mage/Echo inference implementation, ImageForge resource/secret/volume reuse, volume-capacity
  choice, GPU compatibility approval, gate closure, production profile promotion, repair/fallback,
  or model substitution.
- No claim that fixture/local tests prove the real provider lifecycle.

## Files and ownership

- One integration owner serializes shared schemas, generated bindings, state vocabulary, root
  fixture composition, context, and commits.
- After schema shape locks, disjoint lanes may own contract generation/tests and fixture UI/tests;
  maximum three implementation lanes including integration.
- Preserve the user's untracked private avatar/audio inputs; never stage them.

## Acceptance

- Every vNext schema has coherent valid/invalid fixtures and TypeScript/Python validation parity.
- Cross-model adoption and every listed false-readiness/false-stop case fail closed.
- One provider-free Generate fixture visibly runs both lane lifecycles concurrently, overlaps local
  preparation, reaches durable outputs, deletes both synthetic Pods independently, retains both
  synthetic volumes, and reaches final MP4 review without provider success claims.
- Focused tests, `pnpm verify:fast`, `CI=1 TURBO_FORCE=true pnpm verify`, context/schema validators,
  secret scan, format, dependency audit, and `git diff --check` pass.
- Real Chrome baseline and after-change journey use the stable dev URL, show no new unexplained
  console/network failures, and retain truthful `$0` fixture labeling.
- End with one small green commit and a refreshed provider-free handoff. A successor may adapt
  offline workers, but may not provision volumes or use RunPod without new explicit authority.

## Safety and budget

- `provider_calls_authorized: false`.
- `remote_or_cloud_mutations_authorized: false`.
- `credential_access_authorized: false`.
- `model_downloads_authorized: false`.
- `application_code_changes_authorized: false` until the user explicitly authorizes this task.
- Maximum external spend: `$0`.
- Server defaults to fixture mode and is stopped at handoff unless the user is actively reviewing.

## Rollback

Revert only this future task's implementation/context commit. Do not reset or rewrite prior
provider evidence, paid-attempt history, accepted UI/render evidence, or the architecture decision.

## Context update

After authorization and completion, record exact commits/green commands/Chrome route and successor
in `CURRENT_STATE.yaml`. Do not close any production gate from provider-free evidence.
