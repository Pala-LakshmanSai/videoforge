# VF-9-19 — Avatar and whole-frame fallback qualification

Status: authorized bounded provider qualification; maximum `$2.00`

## Goal

Resume the locked SkyReels whole-frame route first after the rejected VF-8-10 AvatarForcing clip,
retain one exact bounded result, apply the locked fallback ladder only when its trigger is proven, and
return RunPod to absolute zero.

## Context read profile

Use `phase9_avatar_fallback_qualification` from `MANIFEST.yaml`.

## Exact authority

- First prove zero pods, workers, endpoints, templates, and volumes. Use exactly one transient
  scale-zero endpoint, one A100-80GB worker, and one job at a time.
- SkyReels image:
  `ghcr.io/pala-lakshmansai/videoforge-avatar-quality@sha256:8f94062164794c19036605282968a96dd098a09aa91d066b9eaafbb5577f48c9`.
- SkyReels source revision `28c771e8456341be6a213e3d1133ed1fd19bf75d`; model revision
  `fdad4053f492aba389b5a8c3c6982118c6a1ecf3`.
- Canonical Avatar Profile runtime source SHA-256
  `sha256:b4214d8f586608dae08ca2a56716f340d87c7bbbd8817383f4cbeaa3e188fb34` and selected
  span-audio SHA-256 `sha256:b7f6585b5775b53de774ce6a7b3b5d059bb4ec3c6e558d2deb44296f80dc4041`.
  Never use a failed derivative as SkyReels input.
- Task cap `$2.00`; combined Tasks 2-4 cap remains `$5.00`. Unused budget cannot raise a task cap.
- Never use LongCat or substitute a model, image, revision, GPU count, source, or audio.

## Execution

- Do not promote or reuse VF-8-10 as accepted evidence. Its whole-face blur is a whole-frame
  failure and selects SkyReels first.
- Use one five-second 960x960/25 SkyReels job, fixed deadline, exact same-job reconciliation, and no
  blind redispatch. Record boot, model load, inference stages, GPU/VRAM, delay, execution, cost,
  output probe, and checksum.
- Stop on cap, deadline, provider ambiguity, capacity failure, or technical/quality rejection.
- Only if a new AvatarForcing candidate is otherwise accepted with an isolated lip defect may one
  MuseTalk repair run. MuseTalk output must never feed SkyReels.

## Acceptance and cleanup

- SkyReels must pass identity, lips, full and split crops, body/background stability, cadence,
  technical probe, and checksum validation. Technical success remains `READY_FOR_USER_REVIEW`.
- No execution profile or Avatar Profile is promoted. `GATE_AVATAR_003` remains open for
  clear-permission claims.
- In `finally`, cancel, drain, and delete every temporary resource. Independently prove absolute
  zero pods, workers, endpoints, templates, and volumes.
- Refresh context, commit/push, and verify hosted CI. Author exactly one successor only after this
  task reaches a terminal evidence-backed outcome.
