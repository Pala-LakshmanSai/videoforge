# VF-9-24U — SoulX Pro new-avatar timed sample in both compositions

Status: provider-free and read-only preflight complete; awaiting one exact paid authorization with a user-supplied numeric cap

## Goal

Use the retained exact SoulX-FlashHead Pro runtime to generate one fresh 10.00-second sample from the
new owned avatar and a previously unused, natural-pause-bounded voiceover passage. Persist exact
provider Pod-start to model-ready and generation timings before cleanup, render the corrected
Ranga-style full and split compositions, project 30-minute avatar cost, delete all temporary
compute, and return playable MP4s.

## Exact inputs

- New avatar: private `new-avatar-sample.png`, 1672x941, SHA-256
  `37f07580badf2c459db496e0a74a15e524534b91432478d5e84e8f084e6b1e83`.
- Owned voiceover: `sampe voiceover.mp3`, SHA-256
  `4b5c404ea6a042ca5e82ef64f238d7866f2fb770967b953e593abdadcf6c7fc4`.
- New passage: “You are fighting a root's savings. And it explains the other thing that puzzles
  people, which is why the weeds sometimes come back thicker than they were.”
- Source interval: `223.430–233.100` seconds, followed by exactly `0.330` seconds of zero padding.
- Exact 10.000-second 16 kHz mono PCM WAV SHA-256
  `51765f504d1a241af1aa05040cd06bbf377768bc3b2806000191f23855e577cb`.
- Split companion: owned CP-06 soil/weed image `cp06-owned-04.png`, SHA-256
  `844a98770bf2772becebc3dc71f3dd609fa0392295ef96d745cbdcb0ecb70f97`.

## Exact runtime and compositions

- Runtime/model/image/manifest/volume identities remain exactly those qualified by VF-9-24S. No
  download, preparation, volume write, image publication, model change, repair, enhancement, or
  fallback is permitted.
- Full composition: scale the nearly exact 16:9 source to 1920x1080; scale the native 512x512
  animation to 1080x1080, align at `x=420,y=0`, and alpha-feather 32 pixels at both vertical edges.
- Split composition: native `crop=448:504:32:4` to a 960x1080 left panel; owned soil image on the
  right with restrained centered slow zoom; clean seam and no border, label, or decoration.
- Both outputs are H.264 `yuv420p`, AAC 48 kHz stereo, 1920x1080, 30 fps, 300 frames, and exactly
  10.000 seconds with ordinary nearest-timestamp frame conversion.

## Paid proposal

- Current exact offering: Secure RTX 4090, EU-RO-1, 24 GB, HIGH availability, `$0.74/hour` as of
  `2026-08-15T09:31:33.996Z`.
- Retained exact 50 GB SoulX-only volume remains `$3.50/month` under existing retention consent.
- Create one temporary private template from the existing immutable image digest; create one fresh
  Pod mounting only the SoulX volume; verify the sealed manifest; run the existing real compile
  warm-up; generate/retrieve one private native sample; write runtime timing evidence before local
  post-processing; delete and prove absence of the Pod; render both local compositions; delete and
  prove absence of the template; read exact billing when available; prove zero compute and exactly
  the two intended retained volumes.
- Conservative maximum new-Pod estimate is `$0.641334`; the user must supply the numeric cumulative
  finite-action cap. Continuing retained-volume billing is excluded from that cap.
- Stop on account/region/GPU/rate/image/manifest/volume/input hash drift, cap risk, output mismatch,
  ambiguous provider mutation, timing-evidence failure, or uncertain cleanup.

## Required handoff

- Native, full, and split MP4s with hashes, bytes, ffprobe, frame count, and A/V duration.
- Provider Pod-start timestamp/source; exact start-to-ready, manifest verify, load, compile warm-up,
  inference, encode/mux, retrieval, deletion, and lifecycle timings; GPU/rate/VRAM.
- Settled or explicitly pending finite cost, measured 30-minute avatar projection, ongoing retained
  volume rate, and zero-Pod/template/endpoint/worker plus two-volume proof.
- User visual review before crop/profile activation.
