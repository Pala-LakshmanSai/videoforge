# VF-3-10 — AvatarForcing access and commercial-license preflight

Status: exact read-only provider-gate successor selected after VF-7-09 completion

## Goal

Resolve or truthfully preserve `GATE_AVATAR_003` before any AvatarForcing weight download, paid
qualification, GPU activation, or commercial profile. Reconcile the official code repository,
committed license files, public weights repository, model card, and any authoritative owner notice.

## Context read profile

Use `phase3_avatar_access_license_preflight` from `MANIFEST.yaml`.

## In scope

- Inspect current first-party KlingAIResearch/Kuaishou and official weights-host metadata plus only
  small text/config/license artifacts needed to identify code, weights, governing terms, and exact
  revisions.
- Record official URLs, checked timestamps, access/gating state, repository revisions, relevant
  file hashes, and the exact contradiction or resolution. Distinguish facts from inference.
- Existing public or signed-in read-only browser state may be inspected. Do not retrieve, print,
  create, rotate, or store credentials; accept terms; create accounts; or contact third parties.
- Close `GATE_AVATAR_003` only when authoritative terms unambiguously permit the intended commercial
  code-and-weights use. Otherwise preserve it open and recommend a separate replacement-model
  decision as the sole successor.

## Out of scope

- No weights, LFS objects, model/code installation, container build, inference, provider call,
  RunPod/GPU/resource mutation, credential use, spend, deployment, push, or application code.
- No model replacement selection, paid benchmark, production profile, architecture change, or
  reinterpretation of ambiguous licensing.

## Acceptance and handoff

- Evidence is reproducible, first-party, timestamped, hash/revision-pinned where possible, and
  contains no secrets, cookies, signed URLs, weight bytes, or copyrighted bulk content.
- Update `GATES.yaml`, decision/provider/source summaries, `CURRENT_STATE.yaml`, and exactly one
  successor brief based on the result: capped Avatar qualification only on a clear pass;
  replacement-model decision on ambiguity/block.
- Run context/schema validation, secret scan, Prettier, dependency audit, and `git diff --check` at
  `$0`. Keep the server stopped.
- Evidence path: `evidence/gates/GATE_AVATAR_003/<run_id>`.

## Safety and stop conditions

- Provider calls are unauthorized; maximum external spend is `$0`.
- Stop on credential exposure, legal acceptance, owner ambiguity, large-file transfer, external
  mutation, charge, context contradiction, or any request for legal judgment beyond recorded facts.
