# Runtime mode, fixture, URL, and LAN exposure are command/route choices, not secrets.
# Use `pnpm dev` for loopback fixture mode, `pnpm dev:lan` for explicit fixture-only
# LAN review, and `pnpm dev:local` for loopback local-media mode.

# Future server-only integration names; leave blank until an explicitly capped provider task.
RUNPOD_API_KEY=
RUNWARE_API_KEY=
DATABASE_URL=
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
BETTER_AUTH_SECRET=
